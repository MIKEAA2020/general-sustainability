# Verified environment (exact versions used for the committed results and
# byte-identical figure regeneration):
numpy==2.3.5
matplotlib==3.10.9
Pillow==12.3.0
# Python 3.9+ required (developed and verified on CPython 3.x).

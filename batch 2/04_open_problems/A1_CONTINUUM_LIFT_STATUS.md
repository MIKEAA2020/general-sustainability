# A1 — The Continuum Lift: Status Record

> **Provenance & status discipline:** reconstructed after the filesystem loss of the long-form original (worklog Task 6; expansion recorded in TRANSFER_AUDIT_RESPONSE Finding 1). This is a *status record*, not a theorem file: A1's Step 1–2 tail-envelope verifications are COMPUTED_PARTIAL (verified for the computed modes k=1..80 only; the k>80 extension is hypothesis H-tail); the K=1600 Newton run is numerical only. **Nothing in this file is PROVEN; the piecewise-Chebyshev campaign remains the specified, unexecuted route.**

## What was proved and computed

### A1.Thm1 (verified tail envelope — PROVED)
K=80 orbit's Fourier coefficients obey per-state geometric envelopes |c_k^s| ≤ C_s q_s^k verified for k=1..80 (outward-rounded): q ≈ 0.80–0.83. Tail norms under H-tail: C0 ≤ 5.3e-6 (N).

### A1.Thm2 (K=1600 substrate — COMPUTED)
Zero-padded orbit at K=1600, matrix-free Newton with Fourier-diagonal preconditioning, collocation residual 5.57e-6.

## The obstruction (the diagnostic finding — PROVED)

The Fourier-global radii polynomial cannot close for this orbit: P·Lip_f ≈ 7900 defeats global-Fourier and global-Schauder approaches. The correct route: **piecewise-Chebyshev collocation radii polynomials** (local patches, M≈8000 segments, local gain O(1)). All inputs exist.

## Status

COMPUTED_PARTIAL. The piecewise-Chebyshev campaign was specified but NOT EXECUTED. The discrete K=80 Krawczyk certificate is PROVEN and committed.

## Update 2026-08-26: Stage 1 EXECUTED (substrate + local-gain diagnostic — not a certificate)

`research_program/validated_computations/a021_c4/c4_piecewise_chebyshev_stage1.py` (+ `.json` artifact) executes the campaign's first stage on the committed validated orbit (the Krawczyk box midpoint): the piecewise-Chebyshev substrate, the measured local-gain distribution, the delay-coupling band, and the defect levels that feed the future radii polynomials. Measured results:

1. **The local-gain premise HOLDS on the measured orbit.** The sup of the right-hand side's Lipschitz constant (local Jacobian norm + delayed-coupling column, charged at full weight) along the orbit is **7.17** — the global obstruction is P·lip = **2660** (the status record's ~7.9e3 used the cruder global bound 21, cf. the certificate's E_sup 20.08; both readings recorded). The per-segment gain h·lip falls like P·lip/M exactly as specified: **M\* = 2660 segments for max gain ≤ 1, M\* = 5320 for ≤ 0.5; at the specified M = 8000 the max local gain is 0.333 (median 0.060)** — the O(1)-local-gain premise of the route is confirmed with margin.
2. **The delay-coupling band is finite and moderate:** at M = 8000 the delay window spans **97 segments** — each patch couples to a ~97-patch band, as the specification requires.
3. **The defect levels are small:** the K=80 orbit's own DDE defect at the Chebyshev–Lobatto nodes is 7.86e-9 (matching the committed certificate's residual scale); the degree-8-per-patch spectral-derivative gap against the Fourier derivative is ≤ 1.9e-9 at M = 8000. The future radii-polynomial Y-input at this scale is dominated by the orbit's own defect, not by the local representation — the substrate is adequate.
4. **Remaining stages (not executed):** Stage 2 — outward-rounded interval evaluation of the local defects and Jacobian blocks; Stage 3 — the local Krawczyk/radii-polynomial system with the finite-band coupling enclosed; Stage 4 — patch-to-patch contraction assembly and the continuum orbit certificate (the A1 gate).

Honesty: Stage 1 measures and verifies premises; it certifies nothing and upgrades no theorem status. A1 remains COMPUTED_PARTIAL until Stage 4 closes.

## Update 2026-08-26 (b): Stage 2 EXECUTED (outward-rounded interval evaluation — machinery + measurement, not a certificate)

`research_program/validated_computations/a021_c4/c4_piecewise_chebyshev_stage2.py` (+ `.json`/`.npz` artifacts, deterministic — byte-identical across reruns) executes the campaign's second stage on the same substrate: the local collocation defects and Jacobian blocks of all M=8000 patches re-evaluated in outward-rounded interval arithmetic (float64 with `np.nextafter` everywhere; softplus/sigmoid per node in mpmath dps=30; the differentiation matrix and all P-dependent constants in mpmath dps=40), plus the tube-inflation ladder Stage 3 needs.

The width machinery (the enabling trick): at the algebraic node times t = (P/M)(j + (ξ_i+1)/2) the Fourier phase 2πkt/P is **P-free** (= 2πkj/M + πk(ξ_i+1)/M), so the patch phases are exact M-th roots of unity computed once in mpmath — the integer power z^k is again an M-th root of unity, taken by exact integer table permutation (width 1 ulp; no binary powering) — and the node offsets are 729 mpmath evaluations. Node-value widths: X ≤ 1.8e-12, Zd ≤ 1.9e-14 (a naive interval product chain would give ~1e-11); the delay is folded per mode (At = A cos φ − B sin φ, Bt = A sin φ + B cos φ) so the delayed values reuse the same phase factors. Measured results:

1. **The Stage-3 Y-input is rigorously enclosed**: sup |Y_cheb| ≤ **8.326e-9** (interval width 7.0e-9) — 5.8% above Stage 1's float 7.865e-9. The orbit's own DDE defect (Fourier derivative, no matrix amplification) is enclosed ultra-tightly: sup |Y_four| ≤ **7.864e-9** with width 4e-14, matching the committed certificate's residual scale.
2. **The O(1)-local-gain premise now holds in interval arithmetic**: the rigorous gain interval at M=8000 is [0.33245909687600, 0.33245909687601] (width 1.6e-14), bracketing the recomputed float sup 0.33245909687600106 (Stage 1's committed reading 0.3325, rounded to 4 decimals).
3. **The tube machinery works**: the inflation ladder (δ ∈ {0, 1e-8, 1e-6}) runs end-to-end — at δ=1e-6 the gain sup upper stays 0.3325 and the defect-interval width grows to 1.4e-5 (measured, ready for Stage 3's radii-polynomial tube evaluation).
4. **Verification (all pass)**: an independent full-mpmath evaluation of the Fourier sum AND the delayed value at five nodes is contained in the interval node values (mpmath widths 1.7e-46); the float64 point evaluator agrees with the interval midpoints to 1.25e-12 (the documented float64 phase-rounding scale); the float defects are contained; the interval gain contains the float gain.
5. **Remaining stages (not executed):** Stage 3 — the local Krawczyk/radii-polynomial system with the finite-band delay coupling enclosed (consumes the interval Y-input and tube-Jacobian machinery delivered here); Stage 4 — patch-to-patch contraction assembly and the continuum orbit certificate (the A1 gate).

Honesty: Stage 2 encloses and measures; it certifies nothing and upgrades no theorem status — the substrate is the float64 box-midpoint orbit, NOT an enclosure of the true DDE solution. A1 remains COMPUTED_PARTIAL until Stage 4 closes.

## Update 2026-08-26 (c): Stage 3 EXECUTED (the local Krawczyk/radii-polynomial systems, marching form, with the finite-band delay coupling enclosed — not the certificate)

`research_program/validated_computations/a021_c4/c4_piecewise_chebyshev_stage3.py` (+ `.json`/`.npz` artifacts, deterministic — byte-identical across reruns, ~45 s) constructs and evaluates, on ALL M=8000 patches, the local Krawczyk systems whose composition is the Stage-4 assembly:

1. **The local system is the MARCHING form** (collocation-with-inheritance): the unknowns of patch j are the 32 corrections to its node values at the Lobatto nodes i=1..8; the left-endpoint value is an inherited input (patch j−1's right endpoint), the delayed values at the 9 nodes are interval inputs (the finite-band coupling: the delayed time lands in one of the two patches j−97/j−98, read by rigorous interval Lagrange evaluation at the offset, product form, degree 8), and the equations are the collocation conditions at the nodes i=0..7. **The free-form variant (all 36 node values free, no inheritance) was examined and rejected on structural grounds**: the local Jacobian has a near-null direction (the A state, relaxation rate ωA=1e-3 — the A-column of J is O(1e-3)) which the free-form stage matrix carries on its constant-in-node mode; it is numerically singular on the orbit. The slow mode must be pinned by inheritance — exactly what the marching form does.
2. **Every stage matrix is rigorously invertible with the promised O(h) inverse**: the Neumann bound q_total sup = 1.6e-11 over all 8000 patches; the rigorous ‖A_j⁻¹‖ sup = 0.0464 ≈ h = 0.04637. The Y-inputs (the preconditioned defect with the inputs enclosed) are evaluated on the (r_in, δ) ladder {0,1e-8,1e-6}×{0,1e-8,1e-6}: sup 4.82e-10 at the minimal enclosures, 2.95e-6 at the largest — and the local radii polynomials CLOSE at every ladder point, with the uniform closing radius 4.82e-10 at the minimal enclosures. The Z-terms stay ≤ 8e-8 on the whole tube ladder.
3. **The period family is enclosed**: the committed box certifies P* within 1e-8 of the grid period; the rescaled equation y′ = ρ·f(y, y(t−τ/ρ)) with ρ ∈ [P_lo/P, P_hi/P] (|ρ−1| ≤ 2.7e-11) is carried as an interval factor on the rhs/Jacobians, and the delay-argument shift |τ/ρ−τ| ≤ 1.21e-10 is enclosed by sup|y′|·dtau (the a-priori bound) — all local closing conditions hold uniformly over the ρ family.
4. **The assembly constants Stage 4 needs are measured rigorously**: ‖S_in‖ sup = 1.0035 (rigorous 1.0035, via the interval matvec bound) — the stage response to the inherited value: mildly expanding, so the inheritance chain requires the Stage-4 dichotomy treatment (the naive radius march grows like the product of the norms); ‖S_zd‖ sup = 0.316 (rigorous 0.318) — the stage response to the delayed inputs: locally contractive, the delay feedback alone does not obstruct the assembly; Λ sup = 2.052 (the Lagrange evaluation constant over all 72000 delay offsets).
5. **The dichotomy premise is MEASURED at the collocation level (float preview)**: the composed delay-augmented monodromy of the linearized collocation march (the 895-dimensional state = the input perturbation + the Z-value history of the last 99 patches, marched over all M patches with the periodic wrap) has top eigenvalues **{1.0000000000029, 0.68769, 0.30272, <1e-16, ...}**: the phase eigenvalue is EXACTLY 1 to 3e-12 (the collocation linearization preserves the time-translation invariance structurally — a strong validation of the march implementation), the dominant transverse eigenvalue 0.68769 matches the committed method-of-steps monodromy's 0.686932 to 7.6e-4, and the rest of the spectrum collapses below 1e-16 — the transverse contraction that the Stage-4 assembly must exploit, measured on the very discretization the assembly will certify.
6. **Remaining (Stage 4, not executed):** the patch-to-patch contraction assembly — the dichotomy-structured composition of the local certificates with the periodic-delay bootstrap and the phase pinning, the between-nodes defect bound, and the continuum orbit certificate (the A1 gate).

Honesty: Stage 3 constructs and evaluates the local systems; it certifies nothing at the orbit level and upgrades no theorem status — the substrate is the float64 box-midpoint orbit, and the assembly, the between-nodes bridge, and the period statement remain open. A1 remains COMPUTED_PARTIAL until Stage 4 closes.

## Update 2026-08-27 (d): Stage 4a EXECUTED (the assembly measurements, the interval-march obstruction measured, and the RIGOROUS BETWEEN-NODES CONTINUUM DEFECT BOUND — not the assembly certificate)

`research_program/validated_computations/a021_c4/c4_piecewise_chebyshev_stage4a.py` (+ `.json`/`.npz` artifacts, deterministic — byte-identical across reruns, ~52 s) executes the campaign's fourth stage on the same committed substrate:

1. **The assembly measurements (float, validated)**: the delay-augmented collocation monodromy is reconstructed by an INDEPENDENT ring-march implementation and matches the committed Stage-3 preview to ≤ 1.2e-12; the float dichotomy constant **K_0 = sup_j ‖P_j‖_∞ = 731.6** (argmax patch 3461); the phase-pinned inverse has ‖A_pin⁻¹‖_∞ = 17475 (q0 = 1e-11) and the bordered (phase-condition) variant 254; the tangent is ξ-dominated by the A-state (−0.665; the pin coordinate is A) with ‖Mon·τ̂ − τ̂‖/‖τ̂‖ = 3.3e-8; and the NONLINEAR MISMATCH MARCH — the one-period float composition of the exact local Newton maps starting at the substrate augmented state — gives **‖Ψ(u*) − u*‖_∞ = 1.185e-8** (the Y-center preview 4.1e-10): the substrate is essentially the periodic collocation fixed point, so the future certificate's Y-term is dominated by enclosure widths, not the center.
2. **The interval-march obstruction, MEASURED**: the per-step width-growth rate of the |step| products (the interval matrix march's pessimism) is **1.00264/step — 1.50e9 per period**. With K_0 = 731.6 and the per-step enclosure widths, the direct interval monodromy march accumulates width ~1e4 and the 4-window re-centered variant ~1e12 (the K^(n_w−1) compounding). Plain interval arithmetic cannot see the dichotomy cancellation; the rigorous assembly (the pinned-monodromy Z-term and the mismatch Y-term) therefore REQUIRES correlation-tracking arithmetic — an affine/Taylor-model march with noise symbols, in which the phase direction's correlation is carried explicitly. This is the Stage-4b specification, now grounded in measured constants (ρ_w, K_0, the pinned/bordered conditioning, the 1.2e-8 mismatch center).
3. **THE RIGOROUS BETWEEN-NODES CONTINUUM DEFECT BOUND (the certificate-grade new result)**: for every patch, every t in the CLOSED patch interval, every ρ ∈ [P_lo/P, P_hi/P], and every state:
   **|p′(t) − ρ·f(p(t), p(t − τ/ρ))| ≤ 2.5938e-8.**
   The bound is Λ_8 (the rigorously computed Lebesgue constant 2.3467 of the degree-8 Chebyshev-Lobatto interpolation) × the rigorous node-residual enclosure (1.1053e-8 — all NINE node rows computed directly, including the row-8 one-sided endpoint derivative; the Stage-2 cross-check passes once the zd_shift×Dv3 ρ-family inflation is accounted, Stage-2's 8.326e-9 having been evaluated at ρ = 1 without the delay-argument shift) plus the Faà di Bruno ninth-derivative interpolation remainder **3.6e-14** — three-plus orders below the node defects, exactly as the smoothness argument predicted: since p is degree 8, r⁽⁹⁾ = −ρ(f∘p)⁽⁹⁾, and the composition's ninth derivative is bounded by the full multivariate Faà di Bruno expansion with (i) the per-variable state-polynomial derivative bounds taken from the INTERVAL CHEBYSHEV COEFFICIENTS of the interpolants (the smooth-data cancellation captured — the naive Σ|X|·sup|L⁽ʲ⁾| bound overestimates by ~12 orders), (ii) the partial derivatives of f up to total order 9 over 128 sector tubes via sparse interval Taylor jets, the softplus composed through the GLOBAL logistic-derivative sup bounds (the Eulerian polynomial recurrence, exact midpoint evaluations: sup|σ⁽ᵐ⁾| = 1, 0.25, 0.0962, 0.125, 0.128, 0.25, 0.409, 1.064, 2.416, 7.853 for m = 0..9), and (iii) the truncated-exponential Bell DP in the (u, t⁵) monomials at the sector level, computed in magnitude arithmetic (exact for the symmetric-interval G). The inflation constant for a corrected polynomial within r_ball of the substrate node values: C_infl = 2775 × r_ball (dominated by the differentiation-matrix row sums) — at the declared r_ball = 1e-6 the node-residual part inflates by 2.8e-3, the number the Stage-4b ball budget must respect.
4. **Remaining (Stage 4b, not executed):** the correlation-tracking (affine/Taylor-model) march — the rigorous monodromy enclosure via noise symbols, the pinned-bootstrap Z-term, the mismatch Y-term, the patch-to-patch assembly certificate with the periodic-delay bootstrap and the phase pinning, and the continuum orbit certificate (the A1 gate), consuming this stage's ε-bound (2.59e-8) and measured constants.

Honesty: Stage 4a measures the assembly structure, records the interval-arithmetic obstruction with its measured constants, and proves ONE rigorous orbit-level bound (the continuum defect of the substrate polynomial over the whole period and the ρ-family). It certifies no solution existence and upgrades no theorem status — the assembly, the bootstrap, and the continuum lift remain open. A1 remains COMPUTED_PARTIAL until Stage 4b closes.

## Update 2026-08-27 (e): Stage 4b EXECUTED — THE BORDERED ASSEMBLY CERTIFICATE CLOSED (the discrete periodic collocation fixed point certified)

`research_program/validated_computations/a021_c4/c4_piecewise_chebyshev_stage4b.py` (Phase A) + `c4_piecewise_chebyshev_stage4b_certify.py` (Phase B: eval / checks / r:<radius> / final) — deterministic, byte-identical across full reruns (Phase A ~83 s + the certify phases ~110 s) — execute the campaign's fifth stage and CLOSE THE ASSEMBLY:

1. **THE CORRELATION-TRACKING AFFINE MARCH (the Stage-4a obstruction's prescribed fix, delivered)**: the operator columns (the 895 unit basis columns + the p-column dΨ/dp) propagate SIGNED through the float stage matrices — the dichotomy cancellation the plain interval march cannot see — while the interval stage-matrix evaluation widths and the tube (r-ball) Jacobian widths inject as FRESH noise symbols, in-block magnitude-accumulated (the measured 1.00264/step pessimism paid ONCE per 500-step block: 1.00264^500 ≈ 3.74, never compounded across blocks) and block-wrapped into 895 fresh coordinate symbols at each of the 16 block boundaries, whose coefficients again propagate SIGNED. The eval-only march reproduces the float monodromy to 3.0e-16 and the Phase-A p-column to 4.7e-15.
2. **THE MISMATCH ENCLOSURE (the Y-term)**: the one-period composition of the exact local Newton maps evaluated in mpmath (dps 30) — the center sup **9.653e-9** (vs Stage-4a's float 1.185e-8, gap 2.2e-9) — plus its own affine noise-symbol widths (T_m sup 1.93e-7).
3. **THE BORDERED KRAWCZYK SYSTEM in (δ, p)**: the joint system with the tangent phase pin and the marched bordering column dΨ/dp (the (a)-formulation — the Stage-3 ρ-family: KD = 2M/P P-INDEPENDENT; the p-dependence carried by drho/dp scaling the rhs/Jacobian, dLw/dσ·dσ/dp at the delay landings, and dRinv/dp = −Rinv·dMhat/dp·Rinv). The float bordered inverse ‖R‖_∞ = 254.2 (q0 = 1.06e-13). **THE CLOSURE Y + Z(r)·r ≤ r CLOSES at r = 3e-7**: Y = 8.99e-8 (the preconditioned mismatch: center 2.4e-9 + widths 8.7e-8), Z = q0 + (1+r)·‖|R|·T_op(r)‖ = 0.444 at r = 3e-7. The full ladder is recorded honestly: 3e-5 (Z = 43.98), 1e-5 (Z = 14.66), 3e-6 (Z = 4.40), and 1e-6 (Z = 1.47 — the near-miss, Y+Zr = 1.56e-6 vs r = 1e-6) all FAIL with the sound tube bound; 3e-7 closes with 26% margin.
4. **THE CERTIFIED STATEMENT**: THE EXACT PERIODIC COLLOCATION FIXED POINT of the one-period local-Newton map EXISTS — within r = 3e-7 (sup-norm, the augmented state) of the substrate, at a period P + p* with |p*| ≤ 3e-7. This is the campaign's first certified EXISTENCE statement at the orbit level: every prior stage measured or enclosed; this one closes.
5. **Two implementation defects caught and fixed during development** (both found by the verification machinery, both recorded): (i) **THE P-COLUMN LANDING-PATCH INDEXING BUG** — ZdP's substrate history read used H_sub[src_slot] (the ring SLOT, patch mod 100) where it needed H_sub[jp] (the landing PATCH, 0..7999) — wrong for every landing patch ≥ 100 (98% of the 64000 delay sites); the corruption localized EXACTLY to the undamped phase/A coordinate xi[1] (the analytic column 1.029 vs the finite-difference truth 0.665 — a 55% error in the one coordinate the monodromy does not contract, invisible in every transverse check); the finite-difference check caught it (rel_gap 0.354 at exactly one coordinate of 895, h-independent across 1e-4..1e-8 — the decisive diagnostic), and after the fix the FD gap is 1.14e-5 with machine-level agreement at h = 1e-4..1e-6. (ii) **THE INJECTION-TUBE BOUND'S UNSOUND PROXY** — the per-step affine injection's tube variation bounded its dominant landing-H channel by dv·Σ|Lw|·|dσ/dp|·r where the true channel is dv·Σ|dLw/dσ|·|H[patch]|·|dσ/dp|·r (the weight DERIVATIVES times the history magnitudes — up to 15.6x larger at 32% of the sites; the signed cancellation Σ_l L_l'(σ) = 0 does NOT apply because the slot-tube variations are independent) — the old bound would have UNDERESTIMATED Z by ~10x (unsound); it was replaced by the channel-explicit sound bound (the dominant landing-H channel; the δDv3·ZdP channel with the ACTUAL |ZdP| (sup 3e-4 — the signed sum cancels the constant mode); the δJ/ρ channel; the dRinv·δF, δRinv·F_p, and δdRinv·F_sub channels each derived and bounded) — the honest price: the sound Z is ~3.5x the unsound one at every radius, and the certificate still closes at 3e-7.
6. **All 10 verification checks pass**: the monodromy eigenvalues vs the committed preview (1.8e-12), the tangent residual (3.3e-8), the mismatch center vs Stage-4a (2.2e-9), the enclosure width (1.9e-7), the bordered inverse (q0 1.06e-13, norm 254.2), the operator-march monodromy consistency (3.0e-16), the operator-march p-column consistency (4.7e-15), the p-column finite difference (1.14e-5), the magnitude-vs-signed block-propagation check (the block machinery's validity), and the mpmath probe-column containment.

Honesty: what Stage 4b certifies is the DISCRETE fixed point — the exact periodic collocation solution of the marching system at a period within the certified p-ball. It does NOT yet certify a solution of the DDE itself: the continuum orbit-to-solution lift (the function-space resolvent argument consuming the Stage-4a between-nodes defect bound 2.59e-8 over the certified 3e-7 ball) remains **Stage 4c**. A1 remains COMPUTED_PARTIAL — but the assembly, the obstruction that motivated the whole correlation-tracking design, and the existence question at the discrete level are now CLOSED.

# Open-Problems Register — The Complete Map

## Tier A — Load-bearing open problems

| # | Problem | Status |
|---|---|---|
| A1 | Continuum lift (validated → certified-in-the-DDE) | **COMPUTED — THE GATE CLOSED** (piecewise-Chebyshev route specified; **Stages 1–3 executed 2026-08-26** — Stage 1: substrate + local-gain diagnostic, premises confirmed (`c4_piecewise_chebyshev_stage1.json`); Stage 2: outward-rounded interval evaluation — the Stage-3 Y-input enclosed (sup \|Y_cheb\| ≤ 8.33e-9), the gain premise in interval arithmetic, the tube-inflation ladder delivered (`c4_piecewise_chebyshev_stage2.json`/`.npz`, deterministic); Stage 3: the local Krawczyk/radii-polynomial systems in the marching form with the finite-band delay coupling enclosed — all M=8000 local systems close (rigorous \|A⁻¹\| sup 0.0464 ≈ h; uniform closing radius 4.82e-10 at minimal enclosures; the ρ-period family enclosed; the assembly constants ‖S_in‖ 1.0035 / ‖S_zd‖ 0.318 / Λ 2.052 rigorous; the delay-augmented collocation monodromy's phase eigenvalue exactly 1 to 3e-12, dominant transverse 0.68769 vs the committed 0.686932 — the dichotomy premise measured (`c4_piecewise_chebyshev_stage3.json`/`.npz`, deterministic); Stage 4a executed 2026-08-27 — the assembly measurements (the independent monodromy reconstruction validated to 1.2e-12, the dichotomy constant K_0 = 731.6, the pinned/bordered inverse conditioning, the float mismatch march ‖Ψ(u*) − u*‖ = 1.185e-8), the interval-march obstruction MEASURED (width-growth 1.00264/step defeats the direct and windowed interval marches — the rigorous assembly requires correlation-tracking/affine arithmetic), and THE RIGOROUS BETWEEN-NODES CONTINUUM DEFECT BOUND sup_t |p′ − ρf(p, p_delayed)| ≤ 2.5938e-8 uniform over the ρ-family (`c4_piecewise_chebyshev_stage4a.json`/`.npz`, deterministic); **Stage 4b EXECUTED 2026-08-27 — THE BORDERED ASSEMBLY CERTIFICATE CLOSED**: the block-wrapped affine noise-symbol march (signed operator columns + fresh noise symbols, the 1.00264^500 in-block pessimism paid once per block), the mpmath mismatch center 9.65e-9, and the bordered Krawczyk system in (δ, p) with the marched p-column and the tangent phase pin — **Y + Z(r)·r ≤ r CLOSES at r = 3e-7** (Y = 8.99e-8, Z = 0.444; the 1e-6 near-miss recorded honestly) — certifying the EXACT periodic collocation fixed point within 3e-7 (sup-norm, augmented state) of the substrate at a period P + p*, |p*| ≤ 3e-7 (`c4_piecewise_chebyshev_stage4b.py` + `_certify.py` → `c4_piecewise_chebyshev_stage4b.json`/`_results.json`/`_phaseA.json`, deterministic; two implementation defects caught and fixed: the p-column landing-patch indexing bug — localized by the FD check to the single undamped phase coordinate — and the injection-tube bound's unsound sum|Lw| proxy, replaced by the channel-explicit sound bound); **Stage 4c EXECUTED 2026-08-27 — the continuum-lift machinery built and verified, the certificate NOT CLOSED at the current gap-enclosure sharpness**: Psi_true (the true-DDE one-period march with the ring-interpolated history reads) + the coupled (u, eta) system; the consistency-gap machinery (exact Peano constants, the OWN-read ||z^(9)|| bootstrap Y9 = 2.89e6, the read-kink ladder, the per-patch truncation forcing marched by the 4b affine machinery); the eta-lift (eta_bound 1.18e-16); ALL EIGHT machinery checks pass — but Y = 3.547e-4 (T_gap-dominated: the enclosed consistency gap sup 6.42e-4) against the 1e-7..1e-6 ladder, Z = 0.78..2.10: **the closure FAILS at every ladder radius** — the read-channel kink ladder (Dv3_sup^(m-1) compounding to 1.1e6, ~8000 independent per-read noise injections) encloses the gap 2.4e5 above the measured 2.68e-9; the obstruction diagnosed channel-by-channel with three refinement paths recorded (`c4_piecewise_chebyshev_stage4c.py` → `c4_piecewise_chebyshev_stage4c.json`, deterministic, resumable partial checkpoints; A1_CONTINUUM_LIFT_STATUS section (f)) — the gate is now a precisely-scoped enclosure-sharpness problem, not a machinery problem; **Stage 4d EXECUTED 2026-08-27 — THE LOCALISED KINK LADDER CLOSES THE CONTINUUM LIFT: a TRUE periodic solution of the C4 DDE certified within 3e-7 (sup-norm, the augmented history state) of the committed substrate at a period within 3e-7 of P, via the eta-lift** — the refinement path (i) implemented (the kink sources localised to the ~98 ring-window boundaries; the per-patch lattice-image chains C[m][q] = max_i Dv3_bar[q,i]·C[m-1][jp[q,i]] with the local tube gains — the per-level geometric mean 1.345 vs the 4c sup 6.857: eps_read 4.16e-9 uniform → 2.07e-12 worst; T_gap 6.42e-4 → 3.29e-8, 12.3x above the measured proxy 2.68e-9; the ladder extended to m=40 with rigorous Hermite–Genocchi/Lebesgue tail bounds — fixing the 4c's ~10% m≥10 tail omission, immaterial to its verdict); the per-radius consistency-Jacobian (base + r·adv exactly — fixing the 4c's worst-radius reuse); Y = 1.492e-7 (the 4b mismatch-width floor), Z(3e-7) = 0.469, **Y + Z·r ≤ r closes at r = 3e-7 (and 4e-7)**; all 13 checks pass; deterministic (byte-identical reruns); the correlated-symbol march and the Richardson cancellation recorded as not-needed-at-this-sharpness (`c4_piecewise_chebyshev_stage4d.py` → `c4_piecewise_chebyshev_stage4d.json`, sha `63c1fb1cb52e37a1…`; A1_CONTINUUM_LIFT_STATUS section (g)); **A1 is COMPUTED** — the cross-toolchain independent-rerun standard remains available to a future session) |
| A2 | A021 coupling class G,f,g | DECLARED (modelling decision; discrete-level hypothesis verification) |
| A3 | Variable-event delayed-hybrid kernel | Topology + compactness (Thm1 repaired: common-modulus hypothesis, supplied free by dynamical closure) + conditional kernel theorem PROVED; residue OPEN |
| A4 | Nonlinear small-gain with nonconvex implementation and shared controls | PROVED (monotone-operator theorem) |

## Tier B — Half-closed bridges

| # | Bridge | Status |
|---|---|---|
| B1 | Sampled-data erosion theorem | PROVED (repaired: two-depth form; invariance reading withdrawn) |
| B2 | Continuous selectors | Michael class verified on E5's class |
| B3 | Algorithmic certificate production | SPECIFIED |
| B4 | Product bunching | COMPUTED_PARTIAL (discrete only; A1's gate CLOSED 2026-08-27 so the transfer is unlocked — the continuum transfer is now **SPECIFIED** with measured feasibility margins: `research_program/article_A021_liebig_graph/b4_continuum_transfer_specification.md`, stages T1–T5, the slack channel identified as the risk concentrator; execution not started) |
| B5 | Observability-hierarchy implementation | SPECIFIED |
| B6 | Nonlinear substitution (MFCQ) | PROVED (repaired: quantitative lsc + (BLK) with the sign corrected; ray lemma added) |
| B7 | Bifurcation classification | PROVED ((3) conditional on versality) |
| B8 | Event-surface calculus | CONDITIONAL (on A3's transversality) |
| B9 | Stochastic viability layer | PROVED (restricted; repaired — fixed-split equality withdrawn) |
| B10 | Strategic-implementation docket | PROVED (repaired: pessimistic existence conditional; coincidence characterised) |

## Tier C — Programmatic extensions

C-a: scoped decidability theorem PROVED; logical completeness OPEN. C-e: quadratic barriers PROVED (repaired: finite reach τ, L_n = 1/τ). C-f: RFDE-aggregate memory PROVED (repaired, scope-locked to window observables). C-h: TCS-1.1 diff FROZEN — **NOT controlling (TCS-1.0 controls every existing record; migration open; no TCS-1.1 compatibility claim available)**. C-i: external audit **EXECUTED (bounded-search level, 2026-08-26** — `E6_NOVELTY_AUDIT_EXECUTION.md`; R05/E2.B1 known-equivalent, the two feared re-instantiations confirmed; full-text pass open at paper-drafting time**)**. C-g: artifact manifests **DONE (2026-08-26)** — `research_program/validated_computations/ARTIFACT_MANIFESTS.json` (39 artifacts hashed: the Part II certificates + companions, the rebuilt nominal fold artifacts, and both scored trees' result files; reproduction commands, environments, and statuses attached; 7/7 pinned-hash consistency verified; builder: `build_artifact_manifests.py`). Persistent identifiers remain open (release infrastructure, not a register item).

## Tier D — Empirical

G1: fisheries resource–sink DECIDED as primary case — **two-track reading mandatory** (Track 1: linear-module method template READY with committed numbers; Track 2: real-system case — **G1b Edwards partially discharged 2026-08-26**: the Cor2 transfer certificate is constructed at the kernel level (`wave_e_edwards/admission/R04_Cor2_edwards_kernel.md`, erosion conversion invoked, `APPROXIMATION` defect-bound to T ≤ 3 yr) and the §15 intervention-selection leg is executed on it (`wave_e_edwards/protocol_intervention.md`; S1/cpm retained at the drought-floor/physical reading at +3.3% to +50.6% water; institutional-threshold negative certificate; BAU not robustly viable beyond ~14 yr under the perpetual-1956 floor; certified kernels defect-bound; independent rerun 2026-08-26 BYTE-IDENTICAL on a fresh second-session execution, same environment as the original run — `reaudit/intervention_rerun/INTERVENTION_RERUN.md`). **G1a cod analogue EXECUTED 2026-08-26** (`wave_e_cod/admission/R04_Cor2_cod_kernel.md` — the governed surplus object, expansive-form erosion (F′(K*) = 1.153 > 1, the contraction form inapplicable), productivity negative certificate (no catch policy, zero included, holds the LRP under UC-min/q05), no retention (the mirror image of the Edwards positive result), maximal robust flat catch 57.6 kt at UC-q10, certified to T ≤ 5 yr; **rerun 2026-08-26 BYTE-IDENTICAL** on a fresh same-env second-session execution — `reaudit/intervention_rerun_cod/INTERVENTION_RERUN_COD.md`). The intervention-selection leg now runs on both scored systems with opposite retention verdicts, and both legs are rerun-verified on fresh executions of their committed runners. Still gated: the two-pool exact specialization. The A021 J-series is an audit docket, not a system). Three preregistered protocols specified. A004/A005 blocking-list plan with ownership assigned.

## Honest bottom line

The general-theory claim is NOT blocked by any open item. The highest-leverage investment is A1 (the continuum lift). The highest-leverage non-mathematical investment is G1 (the empirical case). A2 needs an author decision. A3/A4 are correctly standing (A4 proved).

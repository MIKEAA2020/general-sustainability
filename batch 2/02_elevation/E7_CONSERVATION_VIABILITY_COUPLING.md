# E7 — Conservation–Viability Coupling (F1 + F3)

## E7.Thm1 — Moiety-barrier production rules — PROVEN

(a) Balanced-budget inner: `{q_L ≥ D_T} ⊆ Viab_T({q_L ≥ 0})`.
(b) Obligatory-outflow emptying: uniform drain ⟹ empty kernel + adversarial-exit certificate (exit within q_L(0)/γ).
(c) Best-case outer: `Viab_T ⊆ {q_L ≥ −F⁺_T}`.
(d) Mixed regime: the honesty boundary.
The sandwich `{q_L ≥ D_T} ⊆ Viab_T ⊆ {q_L ≥ −F⁺_T}` is computed from flux data alone.

## E7.Thm2 — Multi-moiety noncompensatory — PROVEN

Product inclusion per moiety; no cross-moiety transfer without declared pathway.

## E7.Cor3 — Erosion-calculus degeneracy

For affine moiety barriers: L_G = 0; the inner rule IS the erosion condition at its exactly-solvable point.

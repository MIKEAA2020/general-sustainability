# Task 111 — The Off-Diagonal Wave's Five Transcription Items: Adjudication and Implementation (v56)

**Owner directive executed:** *"the wave's five transcription items if merited"* — the off-diagonal wave's five owner-gated transcription items (Task 110's `OFF_DIAGONAL_WAVE.md` §8, recorded in its commit as "five owner-gated transcription items for a future round"), each adjudicated on the merits this round, with the merit call delegated to this round, together with the standing push rule (PAT supplied, identity MIKEAA2020).

**Deliverable:** `latex/paper1_assessment_separation_v56.tex` (+pdf) — Paper 1 (*Ecological Indicators*), v55 → **v56**: 2,816 → 3,010 lines (+194), 47 → 49 pp. Standing rules honored: new version only (never overwrite); **purely additive** — no content removed or condensed (`SUPERSEDED` is empty; the inverse-reconstruction gate proves the diff is exactly the one enumerated edit — the closure block appended at the end of §5.8, before the §5.9 heading); fail-loud build; every number transcribed from the committed wave record and its run log; the four closed forms hand re-derived during transcription.

---

## 1. The adjudication: all five items MERITED

The bar applied: the owner's "if merited" — each item judged on the merits, with counters weighed and overruled recorded.

| # | Item (wave record §8) | Verdict | Grounds |
|---|---|---|---|
| (i) | The exact off-diagonal criterion (A·D ≤ B·C with the derivation) and the r-function/ρ-curve with its properties (involution, the (√2,√2) pivot, the limits) | **MERITED** | §5.8's own honest-limits paragraph flags the off-diagonal comparison as its one undecided surface; the closure is new mathematics (a criterion with a derived boundary curve), not a re-statement. Leaving the flagged residual standing next to an existing exact closure understates the subsection. |
| (ii) | The residual wedge witnesses ((5/4,3/2) rejects, (5/4,7/4) accepts — the two rational tests' gap is genuine) and the ten-cell completion | **MERITED** | The paper's text says "nothing load-bearing rests on the residual undecided cells"; the reader is owed the fact that the residual has since been decided — and that it was *genuine* (the two rational conditions' gap is real, with certified witnesses on both sides). This is the direct completion of a sentence already in the paper. |
| (iii) | The completed 2-D σ\*-landscape sentence (the off-diagonal bands (1,2)/(1/2,1)/(1/3,1/2)/(1/4,1/3) mirroring the diagonal ladder) | **MERITED** | The subsection's advertised object is σ\*(z) on gap states, but its landscape table (Table 9) is diagonal-only; the completed landscape (13 rungs × 64 states, zero undecided, up-closure, nesting machine-complete) is the natural completion of the subsection's own object, and the up-closure monotonicity is a new structural fact. |
| (iv) | The four-regime fixed-sum tolerance structure as a labelled remark — the distribution-vs-total finding, with the 707/250 report/action pair as the anchor and the plan-design (FAST/SLOW trough-report) flip | **MERITED** | The four-part relevance test is the subsection's own standard for content-worthiness; the anchor decision passes all four parts (named decision, report flip, new exact datum, action flip) and adds a dimension the paper's relevance chains do not yet carry: the false certification is *visibly two-dimensional* — blind to the distribution, not only the total. The strongest merit case after (i). |
| (v) | A pointer to the wave's verifier as the datum's companion extension — a fifth check list under the family's non-pooling policy | **MERITED** | The family's non-pooling policy is declared three times in the paper (grid 25 / software companion 24 / σ-extension 57 / common-shock 77); without the fifth declaration the closure's verification status would be unstated, breaking the paper's own verification discipline. The numbering is settled by this round: fifth. |

**Counters weighed and overruled:** *length* (+194 lines, +2 pp on a 47-pp paper — the honest cost of transcribing a completed exact wave; the never-condense rule forbids buying the space back); *transcendental brackets are not closed forms* (stated honestly in the addendum — brackets with certified endpoints, not equalities); *the slice structure is scan-certified on grids* (stated honestly — the continuum slice-minimum recorded as an open residual); *an itemized regime list vs. prose* (the four regimes are genuinely enumerable — R1–R4 with exemplar totals — and the paper already uses `\tightlist` itemize in §5.8's master-equation list); *a §1.3 contributions item for this closure* (NOT added — out of this round's five-item scope; recorded as an option for a future owner-gated round, since (xi)/(xii) already advertise the programme and item (xii) covers §5.9 only).

## 2. The edit (one anchored, position-recorded insertion)

The closure block — 194 lines, eight labelled paragraphs — appended after §5.8's "Verification and honest limits" paragraph, before the §5.9 heading (the wave record's first-listed placement: "an additive extension of §5.8"):

1. **"The off-diagonal closure."** — the framing (a third dedicated exact computation in the programme, cross-referenced to §5.9 by label; same datum, exactness standard, delimitations) and the method (certified rational enclosures: the all-positive-term artanh series for ln with geometrically dominated tail; integer k-th-root brackets; the multilinear vertex principle; fail-loud, never triggered).
2. **Theorem G (the exact off-diagonal criterion)** — two displays (cover ⟺ A·D ≤ B·C; r(s₁)r(s₂) ≤ 1 with r(s) = ln(1/(s−1))/ln(s+1)); the ρ-involution with the (√2,√2) fixed point and the two limits; the pivot's two sides (min ≥ √2 sufficient, cover ⟹ max ≥ √2); the sᵢ ≥ 2 boundary argument; the interior-rung multilinear form F reducing on the diagonal to the master equation (Table 8); with a machine-anchored proof sketch (U = B/(B−A), L = −D/(C−D), the cross-multiplication, r's monotonicity, the involution by symmetry, vertex ranges, witness weights cross-validated both directions).
3. **"The residual cells decided."** — the wedge witnesses (reject ≈ 0.218 / accept ≈ 0.422 margins, witness weights) and the ten-cell completion (six by the boundary argument).
4. **"The completed two-dimensional landscape."** — 13 rungs × 64 states, zero undecided; prefix nesting machine-complete off the diagonal; up-closure (σ\* nonincreasing in each coordinate); the nine-band core {5/4, 3/2, 7/4}² incl. the (5/4,5/4) = 2 rung-equality state; the off-grid specials; the 13 only-linear boundary states.
5. **"Remark (the fixed-sum tolerance structure)."** — the elementary anchors; the four regimes R1–R4 as a tightlist; the slice-flip bracket (141/50, 353/125] strictly below 2√2; the ecological content of the interior window (moderate concentration more certifiable than balance).
6. **"The anchor decision: distribution versus total."** — the 707/250 licensing choice (total biomass 1707/250 kt at the §6.3 floor B_lim = 2 kt per stock); balanced (707/500, 707/500) rejected by the diagonal closed form ((707/500)² = 499849/250000 < 2 exactly, excess 151/250000) vs concentrated (607/500, 807/500) accepted (r-product ≈ 0.9847, margin ≈ 1.17×10⁻²); the linear dashboard's literally-identical number (707/500 ≥ 1) on every trough; the FAST/SLOW trough-report flip (√(139849/250000) < 1 vs √(339849/250000) > 1); the action flip (closure/rebuilding with κ\* = 1 − x = 1/2 financing the staged plan, §5.7, vs licensed with no mid-transition response).
7. **"Verification addendum (a fifth check list)."** — the 50-check verifier declaration under the non-pooling policy (25 + 24 + 57 + 77 + 50); the closure's honest limits (ρ, the slice-flip total, and interior σ\* transcendental — brackets only; the scans certify scanned points, the continuum slice-minimum open; the window interval-ness machine-evidenced; the LPI-identification caveats carried over).

## 3. Claim-by-claim source check

Every number traces to the committed wave record (`OFF_DIAGONAL_WAVE.md`) and its run log (`off_diagonal_run_log.txt`, 50/50 checks, exit 0, byte-reproducible); the margins ≈ 0.218 / ≈ 0.422 re-computed from the run log's exact Fractions this round; the r-product 0.9847 and the 1.17×10⁻² margin re-computed independently in floats as a sanity check (0.98469…, 0.011690…). Hand re-derivations performed during transcription: **(a)** the criterion — from U = B/(B−A), L = −D/(C−D), L ≤ U ⟺ A·D ≤ B·C (the two mixed terms cancel); **(b)** r(s₁)·r(s₂) ≤ 1 ⟺ A·D ≤ B·C via r(s₁) = −A/C, r(s₂) = −D/B; **(c)** r(s) ≤ 1 ⟺ s² ≥ 2 (1/(s−1) ≤ s+1); **(d)** the F-form: L ≤ U ⟺ C(B−1) + A(1−D) − (B−D) ≥ 0, and its diagonal reduction to (B−A)(A+B−2) ≥ 0, the master equation; **(e)** the 707/250 arithmetic: (707/500)² = 499849/250000, excess 151/250000; the trough products 207·1207 = 249849, 107·1307 = 139849, 1107·307 = 339849 (each against 500² = 250000); the equal-weight linear report 1414/1000 = 707/500 identical at all troughs; the total biomass 4 + 707/250 = 1707/250 kt under the §6.3 convention B = B_lim + s (B(0) = 2 + 6/5 = 16/5 kt, §6.3's own anchor). The regime exemplars and the slice-flip bracket are the run log's checks 6.2–6.7; the ten cells and margins are checks 2.2–2.3; the landscape completion is Part 5; the relevance chain is Part 7.

## 4. Build and verification battery (all green)

Build: `make_v56_p1.py` (this folder) — 1 anchored edit; **inverse-reconstruction gate**: reverting the one edit reproduces v55 byte-identically (the diff is exactly the closure block); **math-span multiset preservation**: v55's 1,249 spans all intact, 113 new spans added, **zero alterations** (the purely-additive proof at the span level); **frontmatter gate**: title/abstract/highlights byte-untouched (the Task-108 σ-abstract DECLINED ruling stands); destination-exists (idempotence).

1. **Marker gates:** 43 pinned markers, every count the audited v55 value + the block's contribution — incl. "Theorem G" ×1, "involution" ×1, "fifth check list" ×2, "off-diagonal" ×4, "undecided" ×5, "707/500" ×4, "499849/250000"/"151/250000"/"139849/250000"/"339849/250000" ×1 each, "707/250" ×3 (incl. the 1707/250 substring), "141/50" ×4, "353/125" ×2, "71/25" ×1, `\ref{tab:sigmaladder}` ×3, `\ref{tab:sigmastar}` ×2, `\ref{common-shock-variant}` ×2, "Section 5.7" ×8, "Section 6.3" ×16, hardcoded "Section 5.9" ×8 and "Section 5.8" ×1 unchanged, "closure and rebuilding" ×2, "staged plan" ×4, "mid-transition" ×5, "only-linear" ×3, "Theorem S1" ×4, "13 rungs" ×1, "log-transcendental" ×2, "up-closed"/"machine-bracketed" ×1, "0.218"/"0.422"/"0.9847" ×1. Structure gates: §5.8's closing sentence unchanged and unique; the §5.9 heading and label exactly once; itemize environments balanced (+1); the contributions paragraph untouched ((xii) ×1).
2. **Compile:** tectonic clean — **49 pp** (v55: 47; +2 for the block), exit 0, **114 warnings — exactly v55's 114** (zero new; zero warnings from the new block's 194 lines, verified by line-range); the only overfulls remain the two pre-existing 2.43091pt header-rule boxes (v53/v54/v55-identical); zero unresolved references in the final PDF (`??` count 0; the new refs resolve — "master equation of Table 8", "both already in Table 9").
3. **Flat compile clean** (tex + figs_p1 alone in a fresh directory — exit 0, 49 pp, 114 warnings, same file size).
4. **PDF text layer:** all markers render with the expected counts; 10 rendered "Section 5.9" strings (8 hardcoded + 2 label-rendered); one known pdftotext artifact (the √ glyph of "2√2" extracts out of position in the R2 bullet) — **visually verified correct** (see 5).
5. **Visual verification (VLM raster check, pp. 31–33):** both Theorem-G displays render cleanly (the criterion and the r(s) fraction); the R1–R4 bullets render with the proper radical, "moderate asymmetry accepts" intact, no garbled/overlapping/clipped text; the italic ecological-content phrase clean; no overfull lines or layout defects; all large fractions render cleanly.
6. **Never-overwrite audit:** `git status` shows only new files (v56.tex, v56.pdf, this folder); v55 and every other existing file untouched.

## 5. Standing items not in this round's scope (recorded)

- **No §1.3 contributions item** for the closure (out of the five-item gate; the contributions list (i)–(xii) is untouched). Recorded as an option for a future owner-gated round.
- Abstract/highlights untouched (the Task-108 σ-abstract DECLINED ruling stands).
- The Zenodo deposit refresh remains owner-side.
- No other family manuscript modified (the closure is P1-internal).
- The wave's own honest residuals (the transcendental brackets; the continuum slice-minimum; the window interval-ness) are transcribed *as* honest limits — not closed by the manuscript.

## 6. Reproduction

```
python3 "batch 8/off_diagonal_wave/off_diagonal_verify.py"   # 50/50, exit 0
python3 "batch 8/v56_implementation_wave/make_v56_p1.py"    # v55 -> v56 (1 edit, gated)
cd "arena agent 1/paper rewrites/latex" && tectonic paper1_assessment_separation_v56.tex
```

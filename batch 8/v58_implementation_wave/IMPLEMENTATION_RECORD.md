# Task 113 — the earlier waves' in-place limits + the continuum slice-minimum itself (PAPER 1 v57 → v58)

Owner directives (two, English-only rule standing):

1. **"the earlier waves' in-place limits (5.8 wave-1's, 5.9's)"** — the
   two open items recorded by the v57 round's record section 5, first
   bullet ("the earlier waves' in-place honest limits (5.8 wave-1's two
   limits; 5.9's limits paragraph) remain in place but are NOT in the
   Limitations registry — adding the others is an option for a future
   round"), now directed.
2. **"the continuum slice-minimum itself"** — the v57 round's second
   open bullet ("the continuum slice-minimum itself remains the
   programme's one honestly open mathematical residual (now advertised
   as such in the registry)"), given its own registry item.

Plus the standing push rule (PAT re-supplied, suffix-free).

## 1. Adjudication (both MERITED)

**Item 1 — the earlier waves' in-place limits into the registry.
MERITED**, on four grounds:

- The recorded option: the v57 round's own counters section records
  exactly this transcription as the pending option, owner-gated.
- Locus parity: the Limitations registry is the paper-level limits
  locus; after Task 112 it carries the third wave's residuals (item
  (x)) but none of the two earlier waves' — an asymmetry this round
  removes.
- Granularity: one registry item per wave's limits matches the
  registry's established pattern (item (x) = one wave's honest-limits
  paragraph), giving items (xi) (the σ-extension's two) and (xii)
  (the common-shock extension's).
- Faithfulness: both paragraphs are clause-by-clause transcribable
  from the in-place text (v57 lines 1748–1762 and 2243–2267), the
  first wave-1 limit's standing honestly recorded (closed since by
  the off-diagonal closure — the closure block's own opening, v57
  lines 1764–1766).

  Counters weighed and overruled: partial overlap with the global
  items (iii)/(vii) (the transcription states the extension-specific
  form; the registry items function as pointers; never-remove keeps
  both — the same ruling as Task 112); registry length (additive-only
  discipline; the supreme rule forbids condensing).

**Item 2 — the continuum slice-minimum itself as its own item.
MERITED**, on three grounds:

- Item (x) names the residual in a single clause ("the continuum
  slice-minimum remains an open residual"); the programme's one
  honestly open mathematical residual — the v57 round's committed
  characterization — merits its own statement of what is certified,
  what is open, and what is machine-evidenced only.
- Every clause is sourced: the closure's in-place Remark (the
  machine-bracketed slice-flip total (141/50, 353/125] strictly below
  2√2; the interior window's ecological content), the closure's
  honest-limits paragraph (the scans certify scanned points; the
  interval character machine-evidenced, not proved), and the wave
  record's own honest-limits item 2 (the continuum slice minimum "a
  transcendental inequality in general"; the R3/R4 boundary at total 3
  "elementary at the edges; its interior is scan-certified").
- It ties the residual to the ecological finding explicitly (certified
  at the scanned points, not on the continuum) — the honest standing
  of the interior window's headline content.

  Counter overruled: redundancy with item (x)'s clause (pointer
  function; never-remove keeps both — Task 112's ruling).

## 2. The edit (one anchored insertion, three items)

ONE insertion inside the Limitations registry's enumerate, after item
(x) (anchor: `machine-evidenced, not proved.\n\end{enumerate}`, unique
in v57); position-recorded; purely additive; SUPERSEDED empty.
v57 3,031 → v58 3,080 lines (+49).

**Item (xi) — the σ-extension's two in-place limits** (source: v57
lines 1748–1762, the wave-1 "Verification and honest limits"
paragraph; the closure note from v57 lines 1764–1780 and item (x)):

- "Off the diagonal, the geometric member's cover comparison is
  log-transcendental, and the verifier decides it only on proven
  sufficient and necessary rational conditions --- the headline
  results are diagonal, so nothing load-bearing rests on the residual
  undecided cells" — verbatim from the in-place first limit.
- "a limit the off-diagonal closure has since closed, every cell now
  decided by finite rational proof" — the honest standing, sourced
  from the closure block's opening ("The first limit recorded above
  is no longer open ... has decided every off-diagonal cell that the
  rational conditions left undecided") and item (x) ("decides every
  cell of the 64-state grid by finite rational proof").
- "And interior-θ values of σ*(z) are bracketed between tested rungs
  --- the brackets are exact and strictness-refined; the interior
  value is the master root, transcendental in general" — verbatim
  from the in-place second limit.

**Item (xii) — the common-shock extension's in-place limits** (source:
v57 lines 2243–2267, §5.9's "Verification and honest limits"
paragraph, clause-by-clause): the witness datum's decomposition (tent
events, exact tubes, the specific STAGED mechanics, the Section 6.3
arithmetic); no claim about other data beyond the δ-family verified;
the two regime-(iii) readings (additive/replace) bracketing the
coupled truth, Theorem CT reading-conditional, both stated rather
than choosing; grid verification fail-loud but finite, the closed
forms proved (affine interval arithmetic; the kink device), the two
cross-checked at every grid point --- the standard the σ-extension
set; κ* under the common class on the 1/40 reserve grid plus
closed-form confirmation on the survivors, the {min(s) < 3/8}
infinity verdict proved structurally, not sampled; no stochastic,
partial-observation, or infinite-horizon claim, the disturbance
quantifier innermost throughout (Section 2.3).

**Item (xiii) — the continuum slice-minimum itself** (sources: the
closure's Remark v57 lines 1897–1908; the closure's honest-limits
paragraph v57 lines 1946–1955; OFF_DIAGONAL_WAVE.md §7 item 2; the
v57 round's characterization): the programme's one honestly open
mathematical residual; in the fixed-sum tolerance structure of
Section 5.8, the slice scans certify the scanned grid points only;
the slice-flip total machine-bracketed (141/50, 353/125] strictly
below 2√2; the slice-minimum of σ* over the continuum --- where a
slice's best point lies --- a transcendental inequality in general,
open; the interior window's ecological content (moderate
concentration of a deficit more certifiable than balance) certified
at the scanned points, not on the continuum; the accepting windows'
interval character machine-evidenced, not proved; the total-3 regime
boundary elementary at the edges, scan-certified in the interior.

## 3. The gates (make_v58_p1.py, all green)

- Frontmatter (title/abstract/highlights) untouched.
- Math-span multiset: v57's 1,367 spans intact, 12 added, ZERO
  alterations.
- Inverse-reconstruction: reverting the one enumerated edit at its
  recorded position reproduces v57 byte-identically (the diff is
  exactly the three items).
- 67 content markers pinned (every expectation = the audited v57
  count + the item's contribution; phrases the v57 source wraps
  across lines pinned only where this wave's text keeps them
  contiguous).
- Structure gates: the registry enumerate unchanged at 4/4 (the items
  went INSIDE the existing list); the (x)→(xi)→(xii)→(xiii)→\end
  flow markers; the contributions paragraph (xii)/(xiii) untouched;
  itemize 7/7; the 5.9 heading/label intact; \ref counts 4+2=6
  (substitutability-spectrum) and 2+1=3 (common-shock-variant).
- **Six gate corrections across seven runs, every failure BEFORE any
  write**: four line-wrap traps in my own first drafts (the (xi)
  closure clause "a / limit"; the (xii) "specific STAGED / mechanics"
  and "partial-observation, / or infinite-horizon" clauses; the
  (xiii) opening "one / honestly") and two stale v57-contiguity count
  assumptions ("the brackets are exact and strictness-refined" is
  already contiguous in v57 — 1+1, not 0+1; "transcendental" gains
  three, not two — log-transcendental contains it — 8+3=11, not 10).

## 4. The verification battery

- **Diff review:** full hunk-by-hunk — exactly ONE insertion hunk (49
  lines, the three items after item (x)), nothing else moved.
- **Tectonic (in place):** exit 0; **51 pp** (v57: 50 — the +49 lines
  cross a page boundary in the Limitations/figure neighbourhood); the
  only overfull the pre-existing 2.43091pt header box (line 104);
  **zero box warnings attributable to the inserted lines (2314–2362)**;
  the three new refs resolve.
- **Flat compile (tex + figs_p1 alone, clean dir):** exit 0; 51 pp;
  **warning lines 115 = v57's 115** (controlled flat comparison of
  v57 in an isolated directory — the shipped v57.pdf never touched,
  no restore needed this round); Underfull 220 = 220; Overfull 4 = 4;
  LaTeX Warning 33 vs 30 — the +3 are exactly the first-pass
  unresolved-reference warnings of the three new \ref instances
  (2× substitutability-spectrum, 1× common-shock-variant), resolved
  in tectonic's later passes (converged, exit 0). Flat artifacts:
  /home/z/flat_v58, /home/z/flat_v57_cmp (outside the repo).
- **PDF text layer:** items render as **(xi)/(xii)/(xiii)** in order
  on pages 39–40 (folio 38–39); the three bodies complete (the
  text layer's curly apostrophes and radical glyph-ordering noted as
  extraction artifacts); **Section 5.8 ×7, Section 5.9 ×11** (all
  refs resolved); item (ix), item (x), the contributions (xiii),
  the Scope sentence, and the Figure 4 benchmark + §6.3 heading all
  intact; no stale markers.
- **Visual (VLM raster, pages 39 and 40):** PASS both — no garbled,
  clipped, or overlapping text; the radical in 2√2, the
  (141/50, 353/125] bracket, σ*, θ, κ*, δ render cleanly; justified
  body text, proper list indentation; the figure and caption intact.
- **Never-overwrite honored:** the git tree shows ONLY new files
  (v58.tex, v58.pdf, this wave folder) plus the append-only worklog;
  every existing file untouched.

## 5. Open items recorded (no action this round, per scope discipline)

- The manuscript-side transcription queue recorded by the v56/v57
  rounds is now **empty**: the registry carries all three waves'
  in-place limits (items (x)–(xii)) and the continuum slice-minimum
  as its own item (xiii).
- The continuum slice-minimum remains the programme's one honestly
  open mathematical residual — now advertised as such in the
  registry; closing it would need a certified continuum enclosure
  beyond the present scans (no closed form exists in general).
- The Zenodo deposit refresh (owner-side).

## 6. Reproduction

```
python3 "batch 8/v58_implementation_wave/make_v58_p1.py"   # exit 0
cd "arena agent 1/paper rewrites/latex" && tectonic -X compile paper1_assessment_separation_v58.tex
```

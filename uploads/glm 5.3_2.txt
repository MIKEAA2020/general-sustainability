Part A — robust strong-invariance lemma

Exact strong-invariance theorem and bibliographic clause.
The relevant theorem: Clarke, Ledyaev, Stern, Wolenski (1995), "Nonsmooth Analysis and Control Theory," Theorem 4.3.8 (strong invariance). Let me recall the precise statement:

Theorem (CLSW Thm 4.3.8, also Theorem 4.3.5 in some printings — the "Strong Invariance Theorem"): Let F be a set-valued map on ℝⁿ with nonempty compact convex values. If F is locally Lipschitz (Hausdorff) and satisfies the proximal normal inequality
sup 
v∈F(x)
​
 ⟨ζ,v⟩≤0∀x∈K, ∀ζ∈N 
K
P
​
 (x),

then for any x₀ ∈ K, every solution of the differential inclusion ẋ ∈ F(x) on [0,∞) that starts at x₀ remains in K. I.e., K is strongly invariant for F.

Actually, the classical CLSW statement: "If F is locally Lipschitz and h_F(x) := sup_{v∈F(x)} ⟨ζ, v⟩ ≤ 0 for all ζ ∈ N_K^P(x), x ∈ K, then K is strongly invariant with respect to F." The weak-invariance counterpart (Thm 4.2.10 or so) requires only upper semicontinuity/Marchaud and gives existence of one trajectory. The key distinction: weak invariance = ∃ trajectory staying in K; strong invariance = all trajectories stay in K. Weak invariance requires the inequality with min over v (i.e., ∃v ∈ F(x) with ⟨ζ,v⟩ ≤ 0 — actually weak invariance is equivalent to h_F^≤(x) = min_{v∈F(x)} ⟨ζ,v⟩ ≤ 0, that is, sup over ζ of inf over v). Strong invariance requires sup over v ≤ 0 and Lipschitzness of F.

Also, alternatively there's the version via "wedge" or via the Clarke normal and the "strong invariance" theorem in Clarke's "Optimization and Nonsmooth Analysis" or Ledyaev–Treiman. Also there's Donchev–Farkhi's survey "Semi-smoothness of...". The canonical citation: F.H. Clarke, Yu.S. Ledyaev, R.J. Stern, P.R. Wolenski, Nonsmooth Analysis and Control Theory, GTM 178, Springer, 1998 — Chapter 4, Theorem 4.3.8 (Strong Invariance). Some printings: Theorem 4.3.8 is "strong invariance theorem": Let K be closed, F locally Lipschitz compact convex. Then K strongly invariant iff proximal inequality holds.

Actually let me be careful: In CLSW, Theorem 4.3.8: "Let F satisfy the Standing Hypotheses, and let K be closed. Then the following are equivalent: (a) K is strongly invariant; (b) h_F(x) ≤ 0 for all x ∈ K where h_F(x) = sup{⟨ζ, v⟩ : ζ ∈ N_K^P(x), v ∈ F(x)}..." Hmm — I recall the equivalence for strong invariance needs Lipschitz F; the sufficiency of the proximal condition for strong invariance under Lipschitzness is the core. There's also Theorem 3.8 in Ledyaev–Treiman "Proximal normal technique for ...". The user asked for "the exact strong-invariance theorem and bibliographic clause supporting the all-solutions conclusion" — so I should state: CLSW, Nonsmooth Analysis and Control Theory, GTM 178 (Springer, 1998), Theorem 4.3.8 (see also §4.3, "strong invariance"; the proximal normal criterion). And note the caveat about edition/numbering, which I flagged earlier (D4(iii)).

Also relevant: the theorem applies to the inclusion ẋ ∈ G_k(x) with x-dependent disturbance sets D(x) — need G_k to have compact convex values, nonempty, measurable in x? For Carathéodory solutions with F Lipschitz, existence is fine (ACL continuity). The all-solutions conclusion: strong invariance means every trajectory of the inclusion starting in K remains in K. Disturbance trajectories: for each measurable d(·), the closed-loop ODE ẋ = f(x, k(x), d(t)) has solutions (Carathéodory if f measurable in t, Lipschitz in x... but k measurable could destroy Lipschitz in x). This is the measurable-vs-Lipschitz issue: with measurable k, the closed loop need not have unique solutions; all solutions of the closed-loop ODE are solutions of the inclusion ẋ ∈ G_k(x) provided f(x, k(x), d) ∈ G_k(x) — yes by construction since d(t) ∈ D(x(t)). So all closed-loop solutions are trajectories of G_k, and strong invariance of G_k gives all remain in K. Good — this is the route (b) from my previous turn.

Minimal regularity assumptions; proximal vs Clarke vs limiting normals.
Standing Hypotheses (CLSW): F nonempty, compact, convex values; F locally Lipschitz (Hausdorff metric); linear growth (for existence on [0,∞)). K closed (nonempty). The proximal normal inequality uses proximal normals N_K^P. Note:

Proximal normals suffice for the criterion. Since N^P ⊆ N^L (limiting/Mordukhovich) ⊆ N^C (Clarke convexified), an inequality holding for all Clarke normals implies it for proximal; the theorem is stated with proximal normals, which is the weakest requirement in the sense that the condition "for all ζ ∈ N^P" is the weakest to check? Wait: the condition must hold for all proximal normals; since proximal normals are a subset of Clarke normals, checking for all Clarke normals is stronger (sufficient). Checking only for proximal is necessary and sufficient under Lipschitz F. So the minimal requirement: proximal normals. But one can equivalently use limiting normals? Not exactly equivalent in general: the condition sup_{v∈F}⟨ζ,v⟩ ≤ 0 for all ζ ∈ N^P(x) is equivalent for Lipschitz F to strong invariance. For limiting normals, since N^P is dense-ish in N^L (limits of proximal), and F compact convex, the inequality extends by closure: if it holds for proximal normals at all points, then for a limiting normal ζ = lim ζ_k (ζ_k proximal at x_k → x), and by Lipschitzness of F, sup_{v∈F(x)}⟨ζ,v⟩ ≤ liminf sup_{v∈F(x_k)} ⟨ζ_k, v⟩ + L|x−x_k|... something like that; so under Lipschitz F the condition extends to limiting and Clarke normals. So: proximal is what's needed and is the weakest; Clarke-version also sufficient; limiting equivalent under Lipschitz F. Good.
Also note the important subtlety: weak invariance theorem (CLSW Thm 4.2.10 or Aubin's viability theorem) requires only upper semicontinuity (Marchaud conditions) and the "weak" inequality min_{v∈F(x)} ⟨ζ,v⟩ ≤ 0 (or existence of v with ⟨ζ,v⟩ ≤ 0), and yields existence of at least one viable trajectory. This cannot give all-disturbance/all-solutions conclusions. The source conflated these.

Why the source proof is insufficient.
Viability theorem (weak invariance) yields ∃ trajectory; the claim needs ∀ trajectories (all disturbances, all solutions). Existence of one viable trajectory for the inclusion F_k does not control solutions corresponding to other disturbance realizations: each d(·) generates its own vector field selection within F_k; viability of one selection says nothing about others.
Moreover, F_k ⊆ T_K(x) pointwise with T_K the contingent/Bouligand cone: for a Lipschitz F, contingent containment F(x) ⊆ T_K(x) for all x ∈ K... does that imply strong invariance? Actually there's a subtlety: F(x) ⊆ T_K(x) at points of K, with F Lipschitz, does imply strong invariance? Hmm. Let me think. The contingent cone condition sup over v of ... no. Strong invariance criterion is via proximal normals: ⟨ζ, v⟩ ≤ 0 for all v ∈ F(x), ζ ∈ N^P(x). F(x) ⊆ T_K(x) implies ⟨ζ, v⟩ ≤ 0 for all v ∈ F(x) and ζ ∈ N^P(x)? Yes: v ∈ T_K(x) implies ⟨ζ, v⟩ ≤ 0 for every proximal normal ζ at x (by definition of proximal normal: σ satisfies ⟨ζ, y − x⟩ ≤ σ|y−x|²... let me recall: ζ ∈ N^P_K(x) iff ∃σ ≥ 0, r > 0 with ⟨ζ, y − x⟩ ≤ σ|y−x|² for all y ∈ K ∩ B(x; r). Then for v ∈ T_K(x), there exist h_k → 0⁺, v_k → v with x + h_k v_k ∈ K, so ⟨ζ, h_k v_k⟩ ≤ σ h_k² |v_k|², divide by h_k: ⟨ζ, v_k⟩ ≤ σ h_k |v_k|² → 0, so ⟨ζ, v⟩ ≤ 0.) So F ⊆ T_K does imply the proximal inequality. But the point is the source's regulation map has the quantifier ∀d inside — i.e., R_K(x) = {u : f(x,u,d) ∈ T_K(x) ∀d}. Then k(x) ∈ R_K(x) means ∀d, f(x,k(x),d) ∈ T_K(x), i.e., F_k(x) ⊆ T_K(x). If F_k were Lipschitz with compact convex values, that would actually give strong invariance via CLSW. So why is the source proof insufficient? The insufficiencies:
(a) It applies a viability (weak invariance) theorem, which only asserts existence of a trajectory for the inclusion staying in K — it does not imply that every solution stays, and a fortiori does not imply that trajectories of the closed-loop ODE under each disturbance stay. Actually if F_k satisfied the hypotheses of the strong invariance theorem, the conclusion would follow. The typical failure: (i) F_k with measurable k is not upper semicontinuous/Lipschitz, not convex-valued, so even the viability theorem doesn't apply (or applies only after relaxation); (ii) the viability theorem gives existence for the convexified relaxed system, not the original; (iii) with k merely measurable, the closed-loop ODE may have no classical solutions, and "solutions in Filippov sense" involve convex hulls of limits from nearby points, which can violate the tangent condition at nonsmooth boundary points (reentrant corners); (iv) the quantifier order: even one viable trajectory of the inclusion corresponds to one particular measurable selection d(·) — no uniformity over disturbances. In effect, the source proved: ∃ a viable trajectory (for one disturbance realization / one selection), and concluded: ∀d(·) ∀ solutions remain in K. That's the invalid inference "existence → universality."

Also (v): the tangent cone at points of K vs the fact that solutions may leave K and return... no, invariance concerns staying in K. Also the source ignores that T_K(x) ⊇ ... the viability theorem requires the condition at points of K and tangential regularity or convex-valued F for the existence result (Aubin's viability theorem: F usc compact convex values, ∃v ∈ F(x) ∩ T_K(x) — weak condition; here they have F ⊆ T_K which is stronger, so existence of viable trajectory is fine if F_k is Marchaud). But the conclusion "K is robustly controlled invariant for every disturbance" = for all measurable d(·), all solutions of ẋ = f(x,k(x),d(t)) stay in K. The gap: (i) existence of solutions for closed loop with measurable k; (ii) all solutions vs one; (iii) possibly non-unique solutions under each d — need all of them.

So the precise reason: weak/strong invariance distinction + regularity of F_k (measurable k destroys Lipschitzness of F_k; also F_k not convex-valued) + quantifier order.

Additionally, the proximal inequality must be verified for G_k = clco F_k; convexification can violate the inequality? If F_k(x) ⊆ T_K(x) and T_K(x) is... contingent cone is not convex in general; convex hull of vectors in T_K(x) may exit T_K(x) (reentrant corner example: K = ℝ² \ (0,∞)², i.e., the complement of the open positive orthant; at origin, T_K(0) = {v : v₁ ≤ 0 or v₂ ≤ 0}... let me check: K = {(x₁,x₂): x₁ ≤ 0 or x₂ ≤ 0} — closed set, union of two half-planes. T_K(0): directions v such that ∃ sequences: (1,0) ∈ T (along negative x₂=0 axis? (h, 0): is (h,0) ∈ K for h>0? x₂=0 ≤ 0 so yes, (h,0) ∈ K). (0,1) ∈ T_K(0) similarly. But (½,½) ∉ T_K(0) since points (ε,ε) ∉ K for ε>0. So T_K(0) not convex. Convex hull of {(1,0),(0,1)} contains (½,½) ∉ T_K(0). And proximal normals at 0: N^P_K(0) — vectors ζ with ⟨ζ, y−0⟩ ≤ σ|y|² for y ∈ K nearby: y = (−ε, 5) ∈ K (x₁ ≤ 0) etc. For ζ = (ζ₁, ζ₂): take y = (0, ε) with ε>0 small: (0, ε): x₁=0 ≤ 0 so ∈ K. ⟨ζ, y⟩ = εζ₂ ≤ σ ε² → ζ₂ ≤ 0 in the limit. Similarly y=(ε,0) gives ζ₁ ≤ 0. And y = (−ε₁, −ε₂): ⟨ζ,y⟩ ≥ −... gives nothing (sign). y = (ε, 0) with x₂ = 0 ≤ 0 OK. So N^P_K(0) ⊆ {ζ₁ ≤ 0, ζ₂ ≤ 0} = −ℝ₊²; conversely is −ℝ₊² ⊆ N^P? ζ = (−1,0): ⟨ζ, y⟩ = −y₁ ≤ 0 for all y ∈ K? y ∈ K means y₁ ≤ 0 or y₂ ≤ 0. If y₁ ≤ 0, −y₁ ≥ 0 — wait that's wrong sign. Hmm, ⟨ζ,y⟩ = −y₁; need ≤ σ|y|². If y = (−ε, 100): −y₁ = ε > 0 but σ|y|² = σ(ε²+10⁴) ≥ ε for suitable... σ|y|² with |y| large: σ·10⁴ ≥ ε fine. Need it for all y in some neighborhood ball of radius r: y ∈ K ∩ B(0,r): −y₁ ≤ ... if y₁ < 0 then −y₁ > 0. Hmm so ⟨(−1,0), y⟩ = −y₁ = |y₁| when y₁ ≤ 0, which is ≤ |y| ≤ σ|y|²? No: |y₁| ≤ |y| and we need |y₁| ≤ σ|y|², i.e., 1 ≤ σ|y| for |y| > 0 — fails for small |y|. So (−1,0) ∉ N^P_K(0). Let me reconsider: N^P_K(x) = {ζ : x is the projection of x + tζ onto K for some t > 0}. At origin with K = {x₁ ≤ 0} ∪ {x₂ ≤ 0}: project (tζ₁, tζ₂) onto K. For ζ = (−1, 0): point (−t, 0); its projection onto K: is (−t,0) ∈ K? x₂ = 0 ≤ 0 yes! So (−t,0) ∈ K and its own projection is itself, so 0 is a projection of (−t,0)?? For 0 to be the projection of (−t, 0) onto K we need |(−t,0) − 0| = t ≤ |(−t,0) − y| for all y ∈ K. Take y = (−t, −s): |(−t,0)−(−t,−s)| = s < t. So no. Actually proximal normal: ζ ∈ N^P_K(x) iff ∃t>0: p_K(x + tζ) = x. x + tζ = (−t, 0). Projection of (−t,0) onto K: the closest point in K. Points (−t, −s) for s ≥ 0 are in K at distance s; inf over s → 0, closest point is (−t, 0) itself... wait (−t, 0) ∈ K so its projection is itself, not 0. So ζ=(−1,0) ∉ N^P. What about ζ = (−1, −1)? x + tζ = (−t, −t) ∈ K (both ≤ 0), projection is itself, not origin. Hmm, so which ζ have origin as projection? Points outside K project onto K: points in the open positive orthant project to origin? p_K((t₁,t₂)) for t₁,t₂ > 0: closest point in K = {x₁≤0 or x₂≤0}. Distance from (t₁,t₂) to the quadrant-complement: candidates on axes: (0, t₂) with distance t₁; (t₁, 0) with distance t₂; origin distance √(t₁²+t₂²). If both t₁,t₂ > 0, the closest are (0,t₂) or (t₁,0), not the origin. So N^P_K(0) = {0}?? Hmm wait, but that can't be right for strong invariance characterization. Let me recheck: the proximal normal cone to K at x is the set of ζ such that x is the nearest point of K to x + tζ. For the closed quadrant-complement K = ℝ²∖(0,∞)². At the corner origin, the "outward" direction is (1,1). Points along (1,1): (t,t), t>0, project to... distance to {(x₁≤0 or x₂≤0)}: (0,t) is at distance t; (t,0) at distance t; (0,0) at distance t√2. So nearest points are (0,t) and (t,0), not origin. So indeed no nonzero proximal normals at the reentrant corner? Hmm, that seems off — I'd expect N^P to be large at reentrant corners. Wait, reentrant corner: K is the "outside" of a quadrant — at origin, K has an inward corner (from K's perspective the origin is a reflex vertex: K contains a wedge of angle 270°). The normal cone should point into the missing quadrant. Let me redo: proximal normal at x=0: ζ ∈ N^P iff ∃t>0 with 0 = argmin_{y∈K} |(0 + tζ) − y| = argmin |tζ − y|. Take ζ = (1,1). Then tζ = (t,t) ∉ K. min over y ∈ K of |(t,t) − y|: y = (0,t): distance t. y = (t,0): distance t. y = (0,0): distance t√2. So the minimum t is achieved at (0,t) and (t,0), not origin. So (1,1) ∉ N^P(0). Take ζ = (−1, 0): tζ = (−t, 0) ∈ K, min distance 0 at y = (−t,0) itself. Not origin. Hmm, so is N^P_K(0) = {0}? Let's take ζ = (−1, 5)... tζ = (−t, 5t) ∈ K. Projection is itself. Seems any point with a nonpositive coordinate is in K. Points with both coordinates positive project to the two axes at the same coordinate, or origin if... no. So indeed N^P_K(0) = {0}?? That doesn't seem right either. Hold on: points (t₁, t₂) with t₁, t₂ > 0: nearest points of K: (0, t₂) distance t₁; (t₁, 0) distance t₂; also (0, s) for s near t₂... the face {x₁ = 0, x₂ ≤ 0}? (0, s) for s ∈ (0, t₂] has x₁ = 0 ≤ 0, so (0, s) ∈ K. Distance from (t₁, t₂) to (0, s) = √(t₁² + (t₂−s)²), minimized at s = t₂ giving t₁. Similarly face (s, 0): distance t₂. And origin: √(t₁²+t₂²) ≥ max ≥ min distance... The projection is (0,t₂) if t₁ < t₂, etc. So the origin is never the unique projection; but for proximal normal we need 0 to be a nearest point (the definition: x is the projection, i.e., the nearest point; if ties... typically requires |x + tζ − x| ≤ |x + tζ − y| ∀y ∈ K, i.e., t|ζ| ≤ |tζ − y|). For origin: need t|ζ| ≤ |tζ − y| ∀ y ∈ K. With ζ = (1,1): |tζ − (0,t)| = |(t, 0)| = t = t|ζ|/√2 < t|ζ|. Fails. So indeed (1,1) ∉ N^P. Hmm, wait — that contradicts my geometric intuition that at a reflex (reentrant from outside) corner the proximal normal cone should be the missing quadrant... but no! The proximal normal cone at a convex corner (like origin for the negative quadrant K = ℝ²₋) is the whole missing quadrant... let me test: K = ℝ²₋ = {x₁ ≤ 0, x₂ ≤ 0} (convex, closed). ζ = (1,1) at origin: tζ = (t,t) ∉ K; nearest point of ℝ²₋ to (t,t) is (0,0) — yes! Because ℝ²₋ is a cone, projection of (t,t) is 0. So N^P_{ℝ²₋}(0) ∋ (1,1). Good. But for the complement set K = {x₁ ≤ 0 or x₂ ≤ 0} (nonconvex, "L-shaped" complement), the origin is a reentrant corner from inside K — K occupies 270° around origin, and the missing wedge is the open positive quadrant. The normal should be... proximal normals exist only where there's an "outward corner" of K visible from outside — i.e., points outside K whose projection is 0. As computed, points in the open positive quadrant project onto the two half-axes, not the corner. So N^P_K(0) = {0}. Interesting. But the Clarke normal cone N^C_K(0): contains limits of proximal normals from nearby smooth points: near the origin on the face {x₁ = 0, x₂ ≤ 0}, normals are (1, 0) direction (pointing right, into the missing region relative to that face... wait for K = {x₁ ≤ 0 or x₂ ≤ 0}, at a boundary point (0, a) with a < 0: locally K is the half-space {x₁ ≤ 0}, proximal normal (1,0). At (b, 0), b<0: normal (0,1). Limiting/Clarke normal at origin: cone generated by (1,0) and (0,1): ℝ²₊. So N^C_K(0) = ℝ²₊ ≠ N^P_K(0) = {0}. Great — this is the standard example where proximal and Clarke differ, and it matters: strong invariance criterion uses proximal normals; but the *convexification* issue: Filippov/relaxed velocities are convex hulls, and the proximal condition with N^P = {0} at the corner is vacuous there, so strong invariance could hold "at the corner" trivially but fail because trajectories pass near the corner... Hmm wait, actually let me reconsider: is K strongly invariant under some Lipschitz F with F(x) ⊆ T_K(x)? Strong invariance criterion: sup_{v∈F(x)} ⟨ζ,v⟩ ≤ 0 ∀ζ ∈ N^P_K(x), ∀x∈K. Since N^P_K(0) = {0}, the condition at origin is vacuous! But consider F constant (Lipschitz) with F(x) = co{(1,0),(0,1)} — is F(x) ⊆ T_K(x) at origin? (1,0) ∈ T_K(0) yes; (0,1) ∈ T_K(0) yes; but (½,½) ∉ T_K(0). If F has convex values containing (½,½)... then F ⊄ T_K(0). If F(x) = {(1,0)} or {(0,1)} constant: trajectories from origin: (t, 0) ∈ K (x₂=0≤0). OK stays. Constant F = {(1,0)}: from any x ∈ K, x + t(1,0): if x₂ ≤ 0 stays in K; if x₂ > 0 then x₁ ≤ 0 required and moving right exits K! E.g., x = (0, 5): x + t(1,0) = (t, 5) ∉ K for t > 0. Is F ⊆ T_K((0,5))? T_K((0,5)): boundary point with x₁=0, x₂=5>0: locally K = {x₁ ≤ 0}; T_K = {v₁ ≤ 0}. (1,0) ∉ T_K. So condition fails there. OK. The real point of the example in my previous turn was: convex hulls of tangent vectors can exit the contingent cone at nonconvex-cone points, hence Filippov regularization can destroy the tangent condition — and correspondingly the proximal criterion is the right one to check, and it can be vacuous at reentrant corners while still being the correct criterion overall (strong invariance is characterized by it under Lipschitz F — the theorem is an iff for Lipschitz F? I believe for Lipschitz F, strong invariance ⟺ proximal inequality. Let me sanity check with the reentrant corner: F(x) = co{(1,0),(0,1)} constant near origin, F Lipschitz, compact convex. Proximal inequality: at origin vacuous (N^P={0}); at face points (0,a), a<0: N^P = {λ(1,0): λ≥0}; sup_{v∈F}⟨(1,0),v⟩ = sup v₁ = 1 > 0 — fails. So the criterion correctly detects non-invariance (trajectory from (0,−1) with velocity (1,0) exits... x=(0,−1), x+(t,0) = (t,−1) ∈ K since x₂=−1≤0! Hmm, that stays in K! Wait, K = {x₁ ≤ 0 or x₂ ≤ 0}: (t, −1) has x₂ = −1 ≤ 0 so ∈ K. Hmm! Then does the inequality need to fail? Trajectories under constant F = co{(1,0),(0,1)} from (0,−1): ẋ ∈ co{(1,0),(0,1)}: any convex combination (α, 1−α)·... velocities are (a, b) with a,b ≥ 0, a+b = 1. From (0,−1): x(t) = (at, −1+bt): x₂(t) = −1+bt < 0 for t < 1/b; at t = 1/b, x₂ = 0, x₁ = a/b ≥ 0... then x = (a/b, 0): on boundary; for t > 1/b, x₂ > 0 and x₁ > 0: ∉ K. So the trajectory exits K at time 1/b unless a = 0 (velocity exactly (0,1)? from (0,−1) with velocity (0,1): x(t) = (0, −1+t) ∈ K since x₁ = 0 ≤ 0. Stays!). So with F = co{(1,0),(0,1)}: the trajectory choosing v = (0,1) stays on the face x₁=0; trajectories with a>0 eventually exit. So K is NOT strongly invariant, and the proximal criterion at (0,−1): N^P at (0,−1) is {λ(1,0)}: sup_{v∈F} ⟨(1,0), v⟩ = max a over simplex = 1 > 0. Fails. Consistent.

And weak invariance holds: ∃v ∈ F with ⟨ζ,v⟩ ≤ 0: v = (0,1): ⟨(1,0),(0,1)⟩ = 0 ≤ 0. ✓. So the example cleanly separates weak from strong.

So the reentrant-corner example shows: even if a measurable feedback k(x) picks k = "use (0,1)" on the face x₁=0 and "use (1,0)" on the face x₂=0 (each individually tangent), the Filippov regularization at the corner mixes (1,0) and (0,1) (since k is discontinuous at the corner, Filippov takes convex hull of limit values), giving velocities like (½,½) which exit. This is exactly the "discontinuous feedback + Filippov destroys tangent condition" phenomenon. Good, consistent with my previous turn's D2.

OK so now I have all the ingredients. Let me also handle:

Corrected theorem and complete proof with quantifier order ∃k ∀d(·) ∀ admitted solutions.
Theorem (Robust strong invariance via regulation map and relaxed envelope). Data: f: ℝⁿ × U × D × ... hmm, the setup in Part A: f(x,u,d), D(x) set-valued disturbance, R_K(x) = {u ∈ U : f(x,u,d) ∈ T_K(x) ∀d ∈ D(x)}. Candidate repair: G_k(x) = cl co{f(x,k(x),d) : d ∈ D(x)} with assumptions: nonempty compact convex values, locally Hausdorff-Lipschitz, linear growth, proximal inequality.

Note subtlety: the proximal inequality for G_k follows from R_K membership: k(x) ∈ R_K(x) means ∀d, f(x,k(x),d) ∈ T_K(x) ⟹ ∀d, ∀ζ ∈ N^P_K(x): ⟨ζ, f(x,k(x),d)⟩ ≤ 0 ⟹ sup over the set ≤ 0 ⟹ sup over cl co ≤ 0 (linear functional's sup over convex hull = sup over set). ✓. So the proximal inequality is automatic given k(x) ∈ R_K(x) on K and the T_K→proximal implication. Good — no need to assume it separately, but need D(x) nonempty compact, f continuous.

Corrected theorem statement:

Let K ⊂ ℝⁿ be nonempty closed; U compact; D: ℝⁿ ⇉ D compact-valued, nonempty; f: ℝⁿ × U × ℝᵈ → ℝⁿ continuous (say), locally Lipschitz in x uniformly in (u,d) [not strictly needed for the inclusion route]. Suppose:

(H1) R_K(x) ≠ ∅ for all x ∈ K (with R defined with T_K and ∀d ∈ D(x));
(H2) there exists a selection k: K → U with k(x) ∈ R_K(x) such that the envelope G_k(x) = cl co f(x, k(x), D(x)) is locally Hausdorff-Lipschitz on a neighborhood of K... hmm, but with measurable k this fails; that's the crux. Two consistent routes as in my previous turn:

Route (b) (recommended): don't put k in the regularity hypothesis at the level of f∘k; instead hypothesize the joint envelope is Lipschitz OR require k locally Lipschitz.

Actually for Part A, the required output says: "Provide a corrected theorem and complete proof with quantifier order ∃k ∀d(·) ∀ admitted solutions" and "Address measurable versus Lipschitz feedback, graph measurability, convexification, and physical implementability."

So I'll give the theorem in a form with two variants:

Variant A (Lipschitz feedback): k: ℝⁿ → U locally Lipschitz with k(x) ∈ R_K(x) on K (extension off K arbitrary, e.g., constant, or Kirszbraun). Assume f locally Lipschitz in (x,u) jointly, uniformly in d; D(x) locally Lipschitz (Hausdorff) compact-valued. Then G_k is locally Lipschitz compact convex. Then define admitted solutions: absolutely continuous arcs satisfying ẋ(t) = f(x(t), k(x(t)), d(t)) a.e., d measurable, d(t) ∈ D(x(t)). Then every such solution, started in K, remains in K for all t ≥ 0 (given forward completeness). Proof: each such solution satisfies ẋ(t) ∈ f(x,k(x),D(x)) ⊆ G_k(x); proximal inequality holds for G_k; CLSW strong invariance ⇒ x(t) ∈ K ∀t. Quantifier order: the single k is chosen first (exists by hypothesis/selection), then ∀d(·), ∀ solutions.

Wait, but there's a subtlety: CLSW strong invariance gives that all trajectories of the inclusion ẋ ∈ G_k(x) stay in K. Closed-loop solutions under any d(·) are trajectories of this inclusion. Hence done. Note: existence of closed-loop solutions with Lipschitz k: RHS is measurable in t (d measurable, f,k continuous) and Lipschitz in x → Carathéodory/Picard–Lindelöf existence and (local) uniqueness per d(·). Forward completeness needs growth. Good.

Variant B (measurable feedback, inclusion-level regularity): k merely measurable with k(x) ∈ R_K(x) a.e. on K (or everywhere), graph-measurable; hypothesize directly that G_k is locally Lipschitz with compact convex values and linear growth (this is a hypothesis on the pair (f,k,D), not automatic). Then the same conclusion holds for all Filippov/Carathéodory solutions that are trajectories of ẋ ∈ G_k. But caution: with measurable k, the pointwise ODE ẋ = f(x,k(x),d(t)) may fail to have solutions; admitted solutions should be defined as trajectories of the inclusion; if one insists on Filippov solutions of the discontinuous closed loop, the Filippov set is F_Fil(x) = ⋂_{δ>0} co cl f(x, k(B_δ(x)∖N), D(x)) over measure-zero N — this can be larger than G_k(x) (mixes nearby k-values), so G_k-level invariance does NOT control Filippov solutions unless k is continuous (or a.e. continuous with the discontinuity set negligible in a suitable sense). This is the implementability caveat: physically, a measurable feedback is fine if the actual implemented closed loop is the inclusion G_k (i.e., the controller can be realized with the disturbance channel acting adversarially within D(x)); but Filippov regularization of a discontinuous k is a different system. Honest statement: for measurable k, guarantee holds for solutions of ẋ ∈ G_k; if the plant truly runs ẋ = f(x,k(x),d(t)) and solutions exist (they do if f∘(id,k) is Lipschitz in x a.e. — e.g., k piecewise constant on a locally finite partition), then those solutions are G_k-trajectories and are covered.

Hmm — one more subtlety with measurable k and the proximal inequality: k(x) ∈ R_K(x) needs to hold at every x ∈ K (all points), because the invariance proof needs the inequality at every point of K that trajectories visit. If k ∈ R_K only a.e. (Lebesgue), that's insufficient: trajectories can spend positive time on measure-zero sets (e.g., the boundary). So require k(x) ∈ R_K(x) for all x ∈ K. Good point to state.

Also: graph measurability of R_K for KRN selection: under f continuous and T_K outer... R_K(x) = {u : ∀d∈D(x), f(x,u,d) ∈ T_K(x)}. T_K(x) is a closed set (contingent cone is closed). The set {(x,u) : f(x,u,d) ∈ T_K(x) ∀d ∈ D(x)} = ⋂_{d ∈ D(x)}... with D(x) varying. If D is constant compact and f continuous, the condition ∀d ∈ D: f(x,u,d) ∈ T_K(x); since T_K(x) closed and f continuous in d, it suffices to check d in a countable dense subset of D; then the set {(x,u)} is Borel (intersection over countable family of closed conditions, provided (x,u) ↦ T_K(x) has closed graph-ish structure... T_K has closed graph? The contingent cone map has closed graph on sequences? T_K: for x_k → x, v_k ∈ T_K(x_k), v_k → v: is v ∈ T_K(x)? Contingent cone: v ∈ T_K(x) iff liminf_{h→0⁺} dist(x + hv, K)/h = 0. Hmm, the map x ↦ T_K(x) is not closed-graph in general? Actually I recall the contingent cone has closed graph when restricted to... Let me think: x_k → x, v_k → v, v_k ∈ T_K(x_k): ∃h_k → 0, w_k with |w_k − v_k|/h_k → 0... The definition: v ∈ T_K(x) iff ∃ sequences h_n→0⁺ and v_n → v with x + h_n v_n ∈ K. Graph-closedness: given x_k → x, v_k ∈ T_K(x_k), want v ∈ T_K(x): for each k there are h_{k,j} → 0, v_{k,j} → v_k with x_k + h_{k,j} v_{k,j} ∈ K. Diagonal: choose j(k) large so h_{k,j(k)} < 1/k and |v_{k,j(k)} − v_k| < 1/k; then x + h_{k,j(k)} v_{k,j(k)} is within |x − x_k| + h·(1/k) of K-member x_k + h v_{k,j(k)}; and dist(x + h_k' ṽ_k, K)/h_k' → 0? We need x + h v' ∈ K exactly for the definition, but there's the equivalent: v ∈ T_K(x) iff liminf_{h→0+} dist(x+hv, K)/h = 0. Using this: dist(x + h_k ṽ, K) ≤ |x − x_k| + h_k |ṽ − v_{k,j(k)}|, wait let me just: dist(x + h_{k,j(k)} v_{k,j(k)}, K) ≤ |x − x_k| (since x_k + h v_{k,j(k)} ∈ K, and |(x + hv) − (x_k + hv)| = |x − x_k|). Then dist(x + h_k v, K)/h_k ≤ [|x−x_k| + h_k|v − v_{k,j(k)}|]/h_k = |x−x_k|/h_k + |v − v_{k,j(k)}|. Need |x−x_k| = o(h_k): not guaranteed. So graph is not closed in general — the contingent cone's graph is not closed. Right — known: the contingent cone map is lower semicontinuous-ish? No... Known facts: x ↦ T_K(x) is not usc nor has closed graph in general (for closed K). Hmm, but the *Clarke* tangent cone C_K(x) has closed graph? I recall the Clarke tangent cone is usc? The map x ↦ C_K(x) is... For the purposes of graph measurability of R_K, there's a cleaner route: R_K can be written via the proximal-normal/polar characterization only under regularity. Alternatively define R with the *Clarke* cone or require K tangentially regular. Hmm. But actually, for KRN selection we need the graph of R_K to be measurable: {(x,u) : u ∈ R_K(x)}. Write the condition as: ∀d ∈ D: liminf_{h→0⁺} dist(x + h f(x,u,d), K)/h = 0. For fixed (x,u,d), this is a Borel condition in (x,u) (liminf of Borel functions of (x,u) — dist(·,K) continuous, f continuous → the map (x,u,h) ↦ dist(x + hf(x,u,d), K)/h is continuous in (x,u,h) for h>0; liminf_{h→0} of a jointly continuous function is upper semianalytic/Borel? liminf over h→0 of g(x,u,h): = sup_n inf_{h ∈ (0,1/n)∩ℚ} g(x,u,h) — if we restrict h to rationals, inf over countable family of continuous functions is usc, sup over n of usc is... sup of usc functions is not necessarily usc but is Borel (usC functions are Borel; sup of countably many Borel is Borel). Yes: Borel. Then intersect over countable dense subset of D (using continuity in d and closedness of the limiting condition — need care but plausible). So graph(R_K) is Borel provided D constant or D(x) with measurable graph. With D(x) set-valued: condition ∀d ∈ D(x): need joint measurability — manageable if D has measurable graph: {(x,u): ∀d ∈ D(x), φ(x,u,d)} = complement of projection of {(x,u,d): d ∈ D(x), ¬φ} — projection of Borel is analytic, so the set is co-analytic — universally measurable. KRN applies to universally measurable graphs? Kuratowski–Ryll-Nardzewski measurable selection theorem requires the graph... the standard KRN: a set-valued map with nonempty closed values in a Polish space and weakly measurable graph (i.e., preimages of open sets are measurable) admits a measurable selection; closed values + weak measurability suffice. So we can get a measurable selection under these conditions. But then the Lipschitz-ness of G_k is not automatic. Fine — I'll present this honestly.

Also "physical implementability": a measurable (even discontinuous) feedback is implementable if the sampler/zero-order-hold realizes approximately; but the invariance guarantee then degrades — connects to Part B (erosion under implementation error). Nice thread: Part B is exactly about what survives when k is implemented with error. I'll link them.

Forward-completeness: linear growth: ∃M: sup_{v∈G_k(x)} |v| ≤ M(1+|x|) on a neighborhood of K (or globally). Then solutions of the inclusion exist on [0,∞) (standard: ACL local existence + growth prevents finite escape). Also if K bounded, automatically complete while in K (but need to be careful: forward completeness is needed for the conclusion "∀t ∈ [0,∞)"; strong invariance is often stated on intervals of existence). State: either (i) linear growth of G_k, or (ii) K bounded + ... hmm even in bounded K the solution exists locally and can't leave K; but could it fail to be extendable? Solutions of Lipschitz inclusion with compact values from a point in a bounded invariant set: maximal solution either exists on [0,∞) or leaves every compact set; since it stays in K bounded, it's complete. Yes: for Lipschitz F with compact convex values, maximal Carathéodory solutions are extendable until they leave compact sets; staying in bounded K ⇒ complete. So: linear growth OR K bounded. Good.
Also address "why the source proof is insufficient" — enumerate: (i) wrong theorem class (viability = weak invariance = existence, needs only ∃v ∈ F(x)∩T_K(x), usc F); the desired conclusion is strong invariance (∀ trajectories), which needs sup over F and Lipschitz F; (ii) quantifier error: one viable trajectory (which corresponds to one relaxed selection = one disturbance realization) ≠ all disturbance realizations; disturbances enumerate different vector fields within F_k; (iii) regularity gap: measurable k makes F_k non-Lipschitz, non-usc even; viability theorem applied to non-Marchaud map is invalid; (iv) convexification: applying viability to F_k (nonconvex values) actually gives the relaxed system; a viable relaxed trajectory need not be a trajectory of the original closed loop; (v) solution concept: with measurable k, closed-loop ODE may lack solutions; Filippov solutions are not controlled by F_k ⊆ T_K.

Then the corrected theorem + proof, quantifier order.

Part B — quantitative erosion

Claim to audit: K robustly invariant under full-state feedback k with strict inward margin on ∂K; observation/implementation error ≤ ε; then K ⊖ cε (erosion: {x ∈ K : dist(x, K^c) ≥ cε}) is invariant for some c > 0. Wait, the erosion defined: K^{−cε} = {x ∈ K : dist(x, K^c) ≥ cε}. Note dist(x, K^c) = distance to complement = "inradius at x" — for x interior. So K^{−cε} is the inner cε-parallel set (Minkowski erosion). Claim: this eroded set is invariant under the imperfect-information closed loop.

Is it true for arbitrary closed K? Counterexample needed. The rejection criteria say: don't claim metric erosion preserves smooth normal margins for arbitrary closed sets. So the answer: FALSE for arbitrary closed K. Counterexample ideas:

The failure mechanism: strict inward margin on ∂K is a condition on boundary normals; erosion moves the boundary inward along normals — but for nonsmooth K, the eroded set's boundary points have normals that can be arbitrary limits (Clarke normals), and the inward margin constant can degenerate. Classic phenomenon: erosion of a nonconvex/non-prox-regular set creates new boundary geometry; also inward margin measured w.r.t. a specific normal at each boundary point doesn't control the margin at nearby points if k is discontinuous... but we assume margin of the feedback, i.e., sup over the closed-loop velocity set of ⟨ζ, v⟩ ≤ −m < 0? "Strict inward margin on boundary K": say ⟨ζ, f(x,k(x),d)⟩ ≤ −m for all unit proximal normals ζ, all d ∈ D(x), all x ∈ ∂K (or all x ∈ K, ζ ∈ N^P ∩ unit sphere).

With observation error: implemented control is k(x̂) where |x̂ − x| ≤ ε (or k(x + e), |e| ≤ ε). The realized velocity is f(x, k(x+e), d). Question: is the eroded set invariant for the perturbed closed loop?

For smooth K (C² or prox-regular with reach r), the standard argument: let ρ(x) = dist(x, K^c) (or use −dist(x, K^c) as a "barrier-like" function; for prox-regular sets, dist(x, K^c) is... hmm). Actually cleaner: for a C^{1,1} set, the signed distance function is C^{1,1} in a tubular neighborhood, and D_signed b(x)·F ≤ −m on ∂K extends to D b(x)·F ≤ −m/2 in a cε-neighborhood if F is Lipschitz with constant L and |∇b| bounded variation... The margin degrades by L·(distance) via Lipschitzness: ⟨∇b(x), v⟩ ≤ ⟨∇b(x̄), v⟩ + |∇b(x) − ∇b(x̄)| |v| ≤ −m + Lip(∇b)·|x − x̄|·|v| ≤ −m + β cε M where β = Lip(∇b) (i.e., C^{1,1} constant), M = sup|v|. Plus the feedback error: velocity f(x, k(x+e), d) vs f(x, k(x), d): difference bounded by L_u |k(x+e) − k(x)| ≤ L_u L_k ε (if k Lipschitz) or directly L_x ε if we compare f(x+e, k(x+e), d) (nominal at observed point) with f(x, k(x+e), d): |f(x, k(x+e), d) − f(x+e, k(x+e), d)| ≤ L_x ε. And the margin at x+e... So we need the margin condition to hold at nearby points with a uniformity: margin function m(x) = inf over unit normals... For C^{1,1} boundary, normals vary Lipschitzly along the boundary AND the nearest-point projection is well-defined within reach. Then:

Define b(x) = dist(x, K^c) = signed... for x ∈ K, b(x) = dist(x, ℝⁿ∖K) ≥ 0, b = 0 on ∂K. Invariance of {b ≥ cε} = K^{−cε}. Compute Dini derivative of b along trajectories: at points where b is C¹ (a.e. in tubular neighborhood for prox-regular sets — b is Lipschitz; for C^{1,1} sets b is C^{1,1} off a measure-zero set? For sets of positive reach, dist-to-complement is semiconcave... Let me set up with signed distance s(x) = dist(x, K) − dist(x, K^c)? Hmm. Alternatively work with the smooth case first: K = {h ≥ 0}, h ∈ C^{1,1}... no wait h needs C²-ish for ∇h Lipschitz; C^{1,1} suffices: |∇h(x) − ∇h(y)| ≤ β|x−y|.

Margin condition: ∇h(x)·f(x, k(x), d) ≥ γ > 0 for all x ∈ K with h(x) = 0 (boundary), all d — strict inward margin, where |∇h| ≥ μ > 0 near boundary (CQ: |∇h| ≥ μ on {h ≤ cε} say). Also margin on all of {h ∈ [0, cε]}? We only know margin on ∂K; but by Lipschitz continuation we get it nearby: ∇h(x)·f(x,k(x),d) ≥ γ − (β·δ + L_f·stuff)...

Let me do the accounting on the strip S = {0 ≤ h ≤ cε}. For x in strip, let x̄ = projection onto ∂K along... for general h-level sets, take x̄ the closest boundary point: |x − x̄| ≤ ... h(x) ≤ cε and |∇h| ≥ μ, h(x̄) = 0: by mean-value-ish for C^{1,1}: h(x) ≤ h(x̄) + ∇h(x̄)·(x−x̄) + β/2 |x−x̄|²; not directly giving |x−x̄|. Better: since |∇h| ≥ μ on strip (assume), the fastest h decreases per unit distance is μ: h(x) ≤ cε ⟹ dist(x, ∂K) ≤ cε/μ·? Actually |h(x) − h(x̄)| ≤ sup|∇h|·|x−x̄| ≤ M_∇h |x−x̄|, so |x − x̄| ≥ h(x)/M_∇h — that's a lower bound; upper: moving along −∇h direction decreases h at rate ≥ μ, so dist(x, {h=0}) ≤ h(x)/μ. So |x − x̄| ≤ cε/μ. Good: nearest boundary point within cε/μ.

Now margin at x: ∇h(x)·f(x, k(x), d) = ∇h(x̄)·f(x̄,k(x̄),d) + [∇h(x)−∇h(x̄)]·f(x̄,k(x̄),d) + ∇h(x)·[f(x,k(x),d) − f(x̄,k(x̄),d)]. First term ≥ γ. Second: ≤ β|x−x̄|·F_max ≤ β (cε/μ) F_max. Third: ≤ |∇h|·(L_x|x−x̄| + L_u|k(x)−k(x̄)|) ≤ M_∇h (L_x + L_u L_k)(cε/μ) — requires k Lipschitz on the strip (assumption) and f Lipschitz in (x,u) jointly with constants L_x, L_u, F_max = sup|f| over relevant compact set.

Feedback error: implemented k(x+e): velocity f(x, k(x+e), d). Difference from nominal: |f(x,k(x+e),d) − f(x,k(x),d)| ≤ L_u L_k |e| ≤ L_u L_k ε. Contribution to margin: ≤ M_∇h L_u L_k ε.

Total: ∇h(x)·f(x,k(x+e),d) ≥ γ − ε·[ (β F_max + M_∇h(L_x + L_u L_k))·(c/μ) + M_∇h L_u L_k ].

Require this ≥ some η > 0 on the strip, and ≤ 0 needed only at the inner boundary {h = cε}? No wait — we want invariance of {h ≥ cε}, i.e., trajectories starting in {h ≥ cε} stay there. It suffices that on {h = cε} (the new boundary), the vector field points strictly inward: ∇h·f ≥ η > 0 at h = cε. But wait: ∇h points "outward" if K = {h ≥ 0}? h decreases outward; inward = increasing h. So condition: D⁺h(x(t)) = ∇h·f ≥ η > 0 on {h = cε}. From the bound above: need γ > ε[β F_max c/μ + M_∇h (L_x + L_u L_k)c/μ + M_∇h L_u L_k]. Solve for c: choose η = γ/2, then c ≤ (μ/ε)·[γ/2 − M_∇h L_u L_k ε]/(β F_max + M_∇h(L_x + L_u L_k)). So explicit:

c = (μ/ε) · max{0, γ/2 − M_∇h L_u L_k ε} / (β F_max + M_∇h(L_x + L_u L_k))

Hmm wait — dimensions: γ has units of h-rate (h-units/time). β F_max cε/μ: β [1/length·... h-units? if h is a distance-like function]. Let me not worry; define c so that cε has distance units... Actually since the erosion is defined metrically (dist(x,K^c) ≥ cε), but my proof uses h-level sets, I should either (a) restrict to sets where h can be taken as the distance-to-complement (K^{−r} = {dist(·,K^c) ≥ r}), which holds when h = dist(·, K^c) is C^{1,1}-like — true for positive-reach sets (semiconcave signed distance)... For K with C² boundary (or positive reach ≥ r₀), the function b(x) = dist(x, K^c) is Lipschitz and, on K ∩ {b < r₀}, is semiconcave/semismooth with |∇b| = 1 a.e. and ∇b Lipschitz along... For C^{1,1} boundary: b is C^{1,1} on {0 < b < r₀}? For a C^{1,1} domain, distance-to-boundary is C^{1,1} in a tubular neighborhood? Hmm: for K with C² boundary, signed distance is C² in tubular neighborhood; for C^{1,1} boundary, signed distance is C^{1,1} in a (smaller) tubular neighborhood — I believe this is true (Gilbarg–Trudinger / Krantz–Parks). For positive reach (prox-regular), dist(·, ∂K)... the projection is single-valued within reach; distance function is Lipschitz and directionally differentiable; ∇b exists a.e. and equals unit normal at projection; the "curvature" bound = 1/reach. There's a clean classical route for reach: for K with reach ≥ r₀ > 0, and x with b(x) = s < r₀, the projection p(x) ∈ ∂K is unique, and the boundary of K ⊖ s = {b ≥ s} is exactly {b = s} = p^{-1}... and the outward normal at y ∈ ∂(K⊖s) relates to normal at p(y) ∈ ∂K; curvature radii of eroded set ≥ r₀ − s. This is Federer's structure of reach sets (Federer 1959, "Curvature measures"): reach(K ⊖ s) ≥ reach(K) − s. And normals: N^P_{K⊖s}(y) ∋ ζ iff ζ ∈ N^P_K(p(y))-direction... For prox-regular: y = p + s·ν with ν unit proximal normal at p ∈ ∂K; and ζ is a proximal normal of K⊖s at y iff ζ ∥ ν (for s < reach). So margin transfers: if ⟨ν, F(p,k(p),d)⟩ ≤ −m (inward: with ν outward normal; inward margin m: ⟨ν, v⟩ ≤ −m < 0) for all unit proximal normals ν at boundary points p, then at y = p + sν ∈ ∂(K⊖s): need ⟨ν, f(y, k(y+e), d)⟩ ≤ −η. Account: ⟨ν, f(y,k(y+e),d)⟩ = ⟨ν, f(p,k(p),d)⟩ + ⟨ν, f(y,k(y+e),d) − f(p,k(p),d)⟩ ≤ −m + |f(y,k(y+e),d) − f(p,k(p),d)| ≤ −m + L_x|y−p| + L_u|k(y+e) − k(p)| ≤ −m + L_x s + L_u(L_k(|y−p| + |e|)) = −m + (L_x + L_u L_k) s + L_u L_k ε.

Need ≤ −η < 0: s < (m − η − L_u L_k ε)/(L_x + L_u L_k). With s = cε: c < [m − η − L_u L_k ε]/[ε(L_x + L_u L_k)]. Take η = m/2: c = (m/2 − L_u L_k ε)/... need L_u L_k ε < m/2. Also need s = cε < r₀ (stay within reach so that ∂(K⊖s) points have unique projection and proximal normal structure — and actually we need the erosion to be exactly the parallel set and the normal correspondence; Federer). Also need F bounded on the relevant tube (to keep the solution in the region where projections are unique? Not needed for the normal argument, but needed for completeness: tube K ∩ {b ≤ r₀} might be unbounded if K is; then need linear growth; or assume the tube bounded).

Wait — but for the proximal-normal argument I need the strong invariance criterion for the eroded set K_s = K ⊖ s: sup_{v ∈ G(x)} ⟨ζ, v⟩ ≤ 0 ∀ζ ∈ N^P_{K_s}(x), plus G Lipschitz compact convex. The margin gives strictly negative on unit normals; N^P_{K_s}(x) is a cone: ζ = λν, λ ≥ 0: ⟨ζ, v⟩ = λ⟨ν,v⟩ ≤ −λη → fine, ≤ 0 for all λ ≥ 0 if it's ≤ −η for unit ν. And N^P_{K_s}(x) for x ∈ ∂K_s: for prox-regular sets with s < reach: N^P_{K_s}(x) = ℝ₊·ν(x) where ν(x) = (x − p(x))/|x − p(x)|... (plus at interior points, N^P = {0}). Is that right? For a C²-ish/prox-regular set, at boundary points the proximal normal cone is exactly the ray generated by the outward unit normal — for prox-regular (positive reach) sets, yes: prox-regularity ⟺ existence of r > 0 such that ⟨ζ, y − x⟩ ≤ ... equivalently all boundary points have unique... hmm, prox-regular sets can have corners? No — prox-regular sets are "positively reached": every point outside within distance < r has unique projection; boundary can still have edges? Consider a square [0,1]²: reach = 0 (points outside near the corner, on the bisector, have two projections... points outside the corner diagonal have unique projection to the corner point; but points just outside the corner along the diagonal have the corner as unique projection? Take point (1+δ, 1+δ): projections onto the square: closest point is (1,1), unique! Hmm: distance to (1,1) is δ√2; distance to (1, 1) vs (1,1)... any other point (1, y), y<1: distance √(δ² + (1+δ−y)²) > δ√2. So unique. What about point (1+δ, 1)? Projection (1,1) unique. Point (1 + δ, 1 + δ/2): closest (1,1)? distance √(δ² + δ²/4) = δ√(5)/2 ≈ 1.118δ; vs (1, 1)... vs point (1, 1−t): √(δ² + (δ/2 + t)²) minimized t=0. So (1,1). Hmm, so square has positive reach? No! Known: convex sets have infinite reach? NO — reach of a convex set: any closed convex set has reach = ∞? Projection onto closed convex set is always unique (everywhere!). Yes! Every closed convex set has unique projection from every exterior point: reach = +∞. Right — convexity ⟹ infinite reach. The square is convex: reach ∞. But the square has corners, and at a corner the proximal normal cone is the full quadrant (not a ray): N^P at corner (1,1) of [0,1]² = ℝ₊² (outward quadrant). So positive reach does NOT imply smooth boundary; it implies no reentrant (concave) corners; convex corners fine. And N^P at boundary points of prox-regular sets is a convex cone, not necessarily a ray. OK so my "N^P = ray" claim is wrong for convex sets with corners. Adjust: the normal correspondence for erosion of prox-regular sets: N^P_{K⊖s}(p + sζ̂) for unit ζ̂ ∈ N^P_K(p)... For convex K: K⊖s (Minkowski erosion = (K^c ⊕ s)^c... careful: K⊖s = {x : dist(x,K^c) ≥ s} = (K^c ⊖ ... ) — for convex K, K⊖s = K ⊖ sB = {x : x + sB ⊆ K} (Minkowski erosion), which is convex, and N^P_{K⊖s}(x) ⊆ N^P_K(x)? For convex sets: K⊖sB = K ⊖ sB; x ∈ ∂(K⊖sB): N^P_{K⊖sB}(x) vs normals of K at x: hmm, at a corner of K⊖s near original corner: the corner of K⊖s is (1−s, 1−s); normals there = ℝ₊²; corresponding boundary point of K... the erosion's corner normal quadrant ℝ₊² corresponds to K's corner (1,1) with normal quadrant ℝ₊² — same cone. Along edges: (1−s, y): normal (1,0) — matches K's edge normal. So for convex K: N^P_{K⊖s}(x) ⊆ N^P_K(p) where p = x + s·ν direction... The clean statement (Federer / Thibault-ish; also "erosion of prox-regular sets is prox-regular with reach ≥ r − s", and the normal cones correspond via translation along normals): if K is r-prox-regular (reach r) and 0 ≤ s < r, then K_s := K ⊖ s = {x ∈ K : dist(x,K^c) ≥ s} satisfies: for x ∈ ∂K_s, letting p = p_{K}(x)... hmm is p_K(x) ∈ ∂K the projection? x ∈ K, dist(x, K^c) = s: the projection of x onto K^c is at distance s; equivalently x = q − sν for q ∈ ∂K, ν ∈ N^P_K(q)... I think for s < reach: ∂K_s = {p − sν : p ∈ ∂K... sign conventions. Let me define outward normal ν (pointing out of K): x = p − sν? If K = [0,1], p = 1, ν = +1 (outward = increasing x): interior points near p: x = 1 − s: x = p − sν ✓. And K_s = [s, 1−s]: ∂K_s ∋ 1−s = p − sν ✓. Eroded normal at 1−s: outward normal of K_s is +1 = ν ✓. Corner: K = [0,1]², p=(1,1), ν ∈ ℝ₊² unit: p − sν ranges over the quarter-circle arc? No: p − sν for unit ν ∈ quadrant: that's an arc of radius s around the corner: {(1−s cosθ...)}: but K_s = [s,1−s]² has corner at (1−s,1−s) — and its boundary near the corner: the eroded square's corner (1−s,1−s) = p − s(1,1)/√2·√2... p − s(1,1) = (1−s, 1−s): with ν = (1,1)/√2 unit and displacement sν·√2? Hmm: (1,1) − s(1,1) = (1−s,1−s): that's p − s(1,1) where (1,1) = √2·ν̂. So the corner of K_s is at distance s√2 from p — but dist((1−s,1−s), K^c): distance to the line x=1 is s; to x₂=1 is s; the complement is the union of half-planes {x₁>1}∪{x₂>1}∪...: dist = s ✓. And the projection of (1−s,1−s) onto K^c: nearest complement point: on the edge x₁=1: (1, 1−s): distance s ✓ (or symmetric). So ∂K_s points near the corner: the eroded corner is sharp at (1−s,1−s), and its normal cone is ℝ₊². Normal correspondence: N^P_{K_s}((1−s,1−s)) = ℝ₊² = N^P_K((1,1)) ✓ — same cone, "translated". For points on the eroded edge (1−s, y), y ∈ (s, 1−s): normal (1,0) = normal of K at (1, y) ✓. So general fact (true for convex; for prox-regular with s < reach it's Federer's theory): x ∈ ∂K_s ⟹ ∃ p ∈ ∂K and unit ν ∈ N^P_K(p) with x = p − sν, and N^P_{K_s}(x) ⊆ N^P_K(p) (indeed equals the "translated" cone). Actually is x = p − sν with the SAME normal: for prox-regular sets, translating along a proximal normal by less than reach keeps you... the point x = p − sν has p as its unique projection onto K? No wait, x ∈ K (erosion ⊆ K). Hmm, x = p − sν ∈ K for s small (prox-regularity ⟹ normals point "outward" in a proximal sense; moving inward along a proximal normal keeps you in K for distance < reach? For prox-regular sets: yes, there's r such that p − sν ∈ K for s < r? Hmm, I recall prox-regular sets: "ξ ∈ N^P_K(p) ⟹ ⟨ξ, x − p⟩ ≤ (‖ξ‖/2r)|x−p|² for all x ∈ K" — normals are "exterior" in a quadratic sense. Moving inward along ν: is p − sν ∈ K? For the square: p=(1,1), ν = (1,0) (edge normal): p − sν = (1−s, 1) ∈ K ✓. So yes for these. In general prox-regular: I believe p − sν ∈ int K for 0 < s < reach... not exactly, but there's the standard result: for r-prox-regular K, the sets K ⊖ s for s < r are r−s prox-regular, and every x ∈ ∂(K⊖s) has unique projection onto... Let me not go too deep; I'll state the needed normal-transfer lemma as an assumption-level fact with citation to Federer (1959) §4 (Curvature measures; "reach" and parallel sets) and Colombo–Goncharov–Thibault / or Poliquin–Rockafellar–Thibault (prox-regular). Actually, the cleanest rigorous route for Part B that avoids heavy geometric measure theory: use a C^{1,1} defining function h and prove invariance of {h ≥ cε} — but the claim as stated uses metric erosion dist(x, K^c) ≥ cε. For C^{1,1}-represented sets {h ≥ 0} with |∇h| ≥ μ > 0 near boundary, the metric erosion is sandwiched between h-level sets: {h ≥ cεM_∇h} ⊆ K⊖cε ⊆ {h ≥ cεμ/M...}: hmm: dist(x, K^c) ≤ h(x)/μ? If |∇h| ≥ μ on {0 ≤ h ≤ h₀}, then moving from x along −∇h/|∇h| reduces h at rate ≥ μ, so we reach h = 0 within distance h(x)/μ: dist(x, K^c) ≤ h(x)/μ. Conversely dist(x,K^c) ≥ h(x)/M_∇h... wait that needs h to decrease at most at rate M_∇h along any path: h(x) ≤ M_∇h · dist(x, {h<0}... points with h < 0 are in K^c: h(x) ≤ M_∇h dist(x, {h ≤ 0}) ≤ M_∇h dist(x,K^c). So h(x)/M_∇h ≤ dist(x,K^c) ≤ h(x)/μ on the strip. Then invariance of {h ≥ cε M_∇h} implies invariance of the smaller set... we want K⊖cε ⊇ {h ≥ cεM_∇h}: if x has dist(x,K^c) ≥ cε then h(x) ≥ μ cε... hmm I want: the eroded set contains a level set: {h ≥ cε M_∇h} ⊆ {dist ≥ cε} (since dist ≥ h/M ≥ cε). ✓. So if I prove {h ≥ c'ε} invariant with c' = cM_∇h, then K ⊖ cε ⊇ {h ≥ cM_∇h ε} is invariant (superset of invariant set is invariant ✓ — wait, invariance of a subset S means trajectories from S stay in S ⊆ K⊖cε: that gives "trajectories starting in the level set stay in the eroded set" but NOT "trajectories starting anywhere in K⊖cε stay in K⊖cε". Hmm. So the level-set route gives a slightly different (weaker in start-set, but honest) statement, OR prove directly invariance of the metric erosion via the distance function. For the metric erosion with h = C^{1,1}: b(x) := dist(x, K^c) — is b C^{1,1} near ∂K for C^{1,1} sets? If K = {h ≥ 0}, h ∈ C^{1,1}, |∇h| ≥ μ: then b is comparable to h but b itself: b = h∘? Not exactly. However, b is Lipschitz-1 and semiconcave near ∂K? Known: for K with positive reach, b is semiconcave in K∩{b < reach}? dist(·, K^c): K^c is open; dist to complement = dist to ∂K effectively (for x ∈ K, dist(x, K^c) = dist(x, ∂K)). And dist(·, ∂K) for ∂K of positive reach... For our purposes the cleanest fully-rigorous statement avoiding delicate geometry: prove the theorem for the barrier-function erosion (level sets of a C^{1,1} h, or of a smooth barrier B with B=0 on ∂K), and then state the metric version under the stronger assumption that K is convex or C², where b is C^{1,1}/C² in the tube and ∇b = outward unit normal, |D²b| ≤ 1/reach.

Given the required output asks: "Decide whether the claim is true for arbitrary closed K; provide counterexample if false. Identify minimal regular set class... Derive explicit constant c... Distinguish erosion of the set from erosion of a barrier superlevel set." — the expected answer: FALSE for arbitrary closed K; counterexample via non-prox-regular (reentrant corner) or via normal blow-up / infinitely crinkly boundary where the margin doesn't extend inward (e.g., K with a cusp). Minimal class: positive reach / prox-regular (convex ⊂), or C^{1,1} inequality sets. Explicit c: from margin m, field Lipschitz L, feedback Lipschitz L_k, growth bound, reach r₀: c = (m − L_u L_k ε... )/((L_x + L_u L_k) ε) etc. with constraint cε < r₀ and constants requiring ε < ε*.

Let me now construct the counterexample for arbitrary closed K. Mechanism options:

(1) Reentrant corner + Lipschitz feedback with margin on each face: K = {x₁ ≤ 0} ∪ {x₂ ≤ 0} as before (complement of open positive orthant). Wait — but here's a subtlety: N^P at the corner is {0}, so the "strict inward margin on ∂K w.r.t. proximal normals" can hold vacuously at the corner while the dynamics exit. Let's design: want: full-state feedback k, K robustly invariant with strict inward margin on ∂K (i.e., ⟨ζ, f(x,k(x),d)⟩ ≤ −m < 0 for all unit ζ ∈ N^P_K(x), x ∈ ∂K, d), no disturbance needed (D singleton). But then... if the margin + Lipschitz conditions hold, K IS strongly invariant (that's Part A's theorem) — so the counterexample must exploit that the source's margin hypothesis is about ∂K with normals, and the erosion claim fails because the eroded set's boundary has different normals. Since at the reentrant corner the erosion K ⊖ s has boundary that cuts the corner: K⊖s for K = {x₁≤0}∪{x₂≤0}: {dist(x,K^c) ≥ s} = {x₁ ≤ −s} ∪ {x₂ ≤ −s}. This is the same shape translated... its corner (−s,−s) has N^P = {0} again (reentrant). Hmm, erosion of this K is just K shifted/scaled — the two half-planes' union: {x₁ ≤ −s} ∪ {x₂ ≤ −s}. Same geometry. Margins: on face {x₁ = 0, x₂ ≤ 0}: normal (1,0); suppose k gives velocity with ⟨(1,0), f⟩ ≤ −m there, i.e., f₁ ≤ −m; on face {x₂=0}: f₂ ≤ −m. At eroded faces {x₁ = −s}: same normals: need f₁ ≤ −m at x₁ = −s: the feedback with margin only guaranteed ON ∂K, not inside. If f₁(x) = −m + (something that grows inward)... e.g., k(x) = (x₁, x₂)-dependent: f(x) = (−m − x₁·0?...). Let's design failure: f(x, k(x)) = (x₁ + s?, ...) — simplest: dynamics ẋ₁ = x₁ + a, ẋ₂ = x₂ + a with... on ∂K face x₁ = 0: ẋ₁ = a ≤ −m needs a < 0. Then erosion face x₁ = −s: ẋ₁ = −s + a = a − s < a < 0 — even more inward. Not a failure. Make the inward drift *reverse*: ẋ₁ = −x₁ − m·? hmm we want on x₁ = 0: ẋ₁ ≤ −m, but at x₁ = −s (interior of K, boundary of erosion): ẋ₁ > 0. Take ẋ₁ = λx₁ − m with λ large: at x₁=0: −m ✓ margin m. At x₁ = −s: −λs − m < 0 still inward. Wrong direction. Take ẋ₁ = −λ x₁ − m? at 0: −m ✓; at −s: λs − m > 0 if s > m/λ ✓! So dynamics ẋ₁ = −λx₁ − m: at the outer face x₁ = 0 (with x₂ ≤ 0) velocity x₁-component = −m < 0: strictly inward ✓. At x₁ = −s: ẋ₁ = λs − m > 0 for s > m/λ: points outward of the eroded half-plane {x₁ ≤ −s}. But wait — the eroded set is the UNION {x₁ ≤ −s} ∪ {x₂ ≤ −s}; a point (−s, y) with y < −s: is it on ∂(K⊖s)? dist((−s,y), K^c): K^c = {x₁>0 and x₂>0}: dist = min over... distance from (−s, y) to the open quadrant: if y ≤ −s < 0: distance to quadrant = |−s| = s (to the face x₁=0)? Closest point of closure of quadrant: (0, max(y,0)) = (0,0): distance = √(s²+y²) if y<0... hmm wait: dist to K^c where K^c = {x₁ > 0, x₂ > 0} (open quadrant): points (−s, y), y ≤ −s: nearest point of closed quadrant ℝ²₊... no: K^c = (0,∞)²: nearest point of cl(K^c) = [0,∞)² from (−s, y) with y < 0: (0, 0): distance √(s² + y²) > s. Hmm! Then dist((−s,y), K^c) > s: so (−s, y) with y < −s is INTERIOR to K⊖s?? Let me recompute: K⊖s = {x : dist(x, K^c) ≥ s}. x = (−s, −s): dist to (0,∞)²: nearest closure point (0,0): dist = s√2 ≥ s ✓; but also is there a closer boundary: (0, t), t > 0 tiny: dist = √(s² + (s+t)²) > s√2. So dist = s√2 (to the corner (0,0), which is in ∂K, not in K^c — K^c is OPEN: (0,0) ∉ K^c!). dist(x, open set) = inf over points in the open set; inf is achieved in closure: dist((−s,−s), (0,∞)²) = s√2 (infimum, approached but the infimum is over the open set: points (t,t): dist = √((s+t)²·2) → s√2 as t→0⁺. So dist = s√2 ≥ s ✓. So the erosion of the L-shaped... wait I had K = {x₁ ≤ 0} ∪ {x₂ ≤ 0}: hmm, K^c = {x₁ > 0 AND x₂ > 0} = open positive quadrant ✓ (complement of union = intersection of complements). So K⊖s = {dist(x, (0,∞)²) ≥ s}: this is the complement of the s-neighborhood of the open quadrant = complement of {√(max(x₁,0)² + max(x₂,0)²) < s} = {x₁ ≤ 0} ∪ {x₂ ≤ 0} ∪ {(x₁>0, x₂>0, x₁²+x₂² ≥ s²)}... so the erosion ROUNDS the reentrant corner: near origin, the eroded boundary is the quarter-circle of radius s around origin, PLUS keeps the two half-planes {x₁ ≤ 0} (all of it? points (x₁, y) with x₁ ≤ 0: dist to open quadrant = √(max(x₁,0)² + max(y,0)²) = max(y,0) if x₁ ≤ 0: so dist ≥ s iff y ≥ s?? wait: x = (x₁, y), x₁ ≤ 0: nearest closure-quadrant point: (0, max(y, 0)): if y > 0: (0, y): dist = |x₁| = −x₁. Hmm: dist((x₁,y), (0,∞)²) for x₁ ≤ 0 < y: nearest point (0⁺, y): distance → |x₁|. So dist = |x₁| for x₁ ≤ 0, y > 0. And for x₁ ≤ 0, y ≤ 0: nearest (0,0): dist = √(x₁²+y²)... no wait: (t₁,t₂) ∈ (0,∞)², minimize (t₁−x₁)² + (t₂−y)²: both t minimized → 0⁺: inf = x₁² + y²: dist = √(x₁²+y²)?? That's wrong: dist from (−1, −1) to open quadrant: nearest quadrant points approach (0,0): distance √2 ✓. Hmm but that's the distance to the CORNER. OK so: dist(x, K^c) for our K: K^c = (0,∞)²: dist(x, (0,∞)²) = √(max(x₁,0)² + max(x₂,0)²)? Check (−1, −1): √(0+0) = 0?? No! max(−1, 0) = 0: gives 0 — wrong, since (−1,−1) ∉ cl K^c = [0,∞)²: dist should be √2. The formula √(max(x₁,0)²+max(x₂,0)²) is dist to the CLOSED quadrant [0,∞)². But K^c is open = (0,∞)²: dist to open set = dist to closure (since open ⊆ closure and closure = open ∪ boundary; dist(x, open) = dist(x, cl(open)) when x ∉ open... yes dist to a set = dist to its closure). So dist(x, (0,∞)²) = dist(x, [0,∞)²) = √(max(x₁,0)² + max(x₂,0)²). Recheck (−1,−1): max = 0,0: dist = 0?? dist((−1,−1), [0,∞)²): nearest point of closed quadrant: (0,0): distance √2. But formula gives √(0² + 0²) = 0. The formula is WRONG: dist(x, [0,∞)²) = √(max(−x₁,0)... no: dist((x₁,x₂), [0,∞)²) = √(max(−x₁, 0)² + max(−x₂, 0)²) — distance to the quadrant is the amount you must move in negative directions to enter it: for (−1,−1): need to move to (0,0): √2 ✓: max(−(−1),0) = 1: √(1+1) = √2 ✓. For (−1, 5): already x₂ ≥ 0; need x₁ ≥ 0: dist = 1: √(max(1,0)² + max(−5,0)²) = √(1+0) = 1 ✓.

So dist(x, K^c) = √(max(−x₁,0)² + max(−x₂,0)²)... wait sign: K^c = (0,∞)²: x ∈ K^c iff x₁ > 0 and x₂ > 0. dist(x, K^c) = √(max(−x₁,0)² + max(−x₂,0)²)?? For x = (−1, 5): max(1, 0) = 1, max(−5,0) = 0: dist = 1 ✓. For x = (3,5) ∈ K^c: dist = 0 ✓. For x = (−1,−1): √2 ✓. Good: dist(x, K^c) = |x⁻| where x⁻ = (min(x₁,0), min(x₂,0))... = norm of the negative part. So K ⊖ s = {|x⁻| ≥ s} = {√(min(x₁,0)² + min(x₂,0)²) ≥ s}: points with both coordinates ≥ 0 are excluded (dist 0 unless...). So near the origin: the erosion boundary is the quarter circle {x₁ ≤ 0, x₂ ≤ 0, x₁²+x₂² = s²} — the erosion ROUNDS the reentrant corner of K. And the eroded set also includes the far parts of the faces. Interesting: so the eroded set near the corner is smooth (circular arc), with outward... wait "outward" of the eroded set: the eroded set near origin = {x₁²+x₂² ≥ s², x₁ ≤ 0, x₂ ≤ 0} plus arms {x₁ ≥ 0? no...}. Hmm wait: which side? {|x⁻| ≥ s}: x = (−2, 3): |x⁻| = 2 ≥ s ✓ included. x = (−s/2, −s/2): |x⁻| = s/√2 < s: excluded ✓ (too close to corner). x = (−2,−2): √8 ≥ s ✓. So the erosion REMOVES a quarter-disk around the origin from K: K⊖s = K ∖ (quarter-disk of radius s at origin) = ({x₁ ≤ 0} ∪ {x₂ ≤ 0}) ∖ {x₁ < 0, x₂ < 0, |x| < s}. The new boundary near origin: the arc {x₁²+x₂² = s², x₁ ≤ 0, x₂ ≤ 0} — and the eroded set is on the OUTSIDE of this arc (away from origin). The arc's normal pointing INTO the removed disk (toward origin) = −x/s: is that outward or inward for K⊖s? K⊖s excludes the disk: so the direction toward origin is OUTWARD (leaving the set): outward unit normal at arc point y (|y| = s): ν = −y/s. So the erosion of the reentrant corner creates a CONVEX (from the set's perspective, outward) circular boundary with normals pointing toward the origin — normals ν = −y/s which, at the arc midpoint (−s/√2, −s/√2): ν = (1/√2, 1/√2) — exactly the "corner direction" that had NO proximal normal representation in K (N^P_K(0) = {0} but N^C_K(0) = ℝ₊²; the erosion's new boundary normals are the Clarke normals of K at the corner!). Beautiful — this is the quantitative incarnation of the D1/normal-blow-up phenomenon: **metric erosion converts Clarke (limiting) normals into genuine proximal normals of the eroded set.** The inward margin hypothesis on ∂K (w.r.t. proximal normals of K) says NOTHING about directions in N^C ∖ N^P — so the margin is vacuous exactly in the directions that become the eroded set's boundary normals. Counterexample then: make the field point outward along (1,1)/√2 near the corner with speed > 0 while satisfying margin on both faces.

Construct: dynamics (no disturbance, no observation error needed to kill the claim? The claim says: error ≤ ε ⟹ erosion invariant. To refute "for arbitrary closed K", one counterexample with SOME ε > 0 failing suffices; even with ε = 0? If ε = 0 then c·ε = 0: erosion = K: invariant by Part A. So need ε > 0 and show NO c > 0 works — i.e., for every c > 0, K⊖cε is not invariant. Cleanest: show even with ZERO observation error but the margin being only on ∂K... wait but with zero error the claim would give invariance of K⊖cε for c > 0 — and the hypothesis "strict inward margin on ∂K" holds; the conclusion fails for all c: even better refutation (shows the erosion claim fails regardless of error, for arbitrary closed K). Hmm, but is that fair? The source presumably intends: margin strict ⟹ some buffer ⟹ small errors tolerated. If the theorem is false even with ε = 0 (i.e., the strict margin on ∂K does not make any erosion invariant, for arbitrary closed K), that's the strongest refutation. But careful: with ε = 0, maybe they'd say c ε = 0... The claim: "observation/implementation error at most ε ⟹ K⊖cε invariant for some c > 0". Refutation: exhibit K, f, k with (i) K robustly invariant (full state, exact implementation), (ii) strict inward margin on ∂K, (iii) for arbitrarily small observation error ε (any ε > 0), NO erosion K⊖s with s > 0 is invariant (not just cε — no positive erosion at all). Then the claim fails for arbitrary closed K.

Design: K = {x₁ ≤ 0} ∪ {x₂ ≤ 0} ⊂ ℝ². Feedback/dynamics: want on face F₁ = {x₁ = 0, x₂ ≤ 0}: velocity strictly inward: ẋ₁ ≤ −m. On face F₂ = {x₂ = 0, x₁ ≤ 0}: ẋ₂ ≤ −m. But near the corner inside, velocity points along +(1,1) strongly, so that from points near the corner (which are in every erosion K⊖s? no wait: points near the corner on the arc... hmm: I need trajectories starting INSIDE K⊖s to exit K⊖s. Take the starting point x₀ = (−s, −s)·(1+δ)/... a point on the arc boundary? Invariance = trajectories starting IN the set stay: so pick x₀ ∈ K⊖s whose trajectory exits K⊖s. Points in K⊖s near the corner: outside the removed quarter disk, e.g., x₀ = (−2s, −2s)·? |x₀⁻| = 2√2 s ≥ s ✓ ∈ K⊖s. Trajectory: ẋ = (1,1)·v₀ (pointing toward origin): x(t) = x₀ + v₀(1,1)t: moves toward origin along the diagonal: |x(t)⁻| decreases: at x = (−a, −a): |x⁻| = a√2: when a < s/√2: |x⁻| < s: exited K⊖s (entered removed disk — but still in K! (−a,−a) ∈ K). So the trajectory leaves the EROSION while staying in K: the erosion is not invariant. And with observation error: even easier. But wait — the trajectory continues to the origin and beyond: (t, t) for t > 0: EXITS K! So K itself would NOT be invariant under ẋ = v(1,1)! But hypothesis (i) requires K robustly invariant. Contradiction? The margin on ∂K w.r.t. proximal normals: at corner N^P = {0}: vacuous; on faces: satisfied. But Part A's theorem would then say... Part A requires the proximal inequality at ALL x ∈ K: at interior points N^P = {0}: vacuous. So the proximal criterion holds?! But then CLSW strong invariance would imply K invariant — but our trajectory exits K! Contradiction with CLSW?? Resolution: CLSW requires F LOCALLY LIPSCHITZ. Our k: on faces push inward, near corner interior push along (1,1): such a k is necessarily discontinuous near the corner? Let's see: can a Lipschitz (or continuous) vector field satisfy: f₁ ≤ −m on {x₁ = 0, x₂ ≤ 0}, f₂ ≤ −m on {x₂ = 0, x₁ ≤ 0}, and f = (1,1)v₀ > 0 near the corner interior (points (−a,−a), a small)? At the corner (0,0) ∈ ∂K: by continuity, f(0,0) = lim f(−a,−a) = (v₀, v₀) with v₀ > 0. But also f(0,0) = lim f(0, −a) [face points → corner]: f₁(0,−a) ≤ −m ⟹ f₁(0,0) ≤ −m < 0. Contradiction with f₁(0,0) = v₀ > 0. So a CONTINUOUS field can't do this. The obstruction: continuity forces f(0,0) to satisfy both face margins. Indeed for continuous f, margins on faces force f₁(corner) ≤ −m and f₂(corner) ≤ −m — corner velocity points into the third quadrant: strongly inward. Hmm! So for continuous feedback, does the erosion claim survive at reentrant corners? Let's check: f Lipschitz with f₁ ≤ −m on F₁, f₂ ≤ −m on F₂. Erosion boundary arc: at arc point y = s(−cosθ... let me param: y = s(−c, −d), c² + d² = 1, c,d ≥ 0. Outward normal ν = (c, d) (pointing toward origin). Margin on arc: need ⟨ν, f(y)⟩ ≤ −η: ⟨ν, f(y)⟩ = c f₁(y) + d f₂(y). f₁(y): y is at distance from face F₁: y₁ = −sc ≤ 0: distance to F₁... f₁ ≤ −m holds only ON x₁ = 0. y₁ = −sc: |y₁| = sc ≤ s. By Lipschitz: f₁(y) ≤ f₁(y + (−y₁, 0)·... project y to F₁: ȳ = (0, y₂) = (0, −sd): f₁(y) ≤ f₁(ȳ) + L|y − ȳ| = f₁(ȳ) + L·sc ≤ −m + Ls. Similarly f₂(y) ≤ −m + Lsd. So ⟨ν, f(y)⟩ ≤ c(−m + Lsc) + d(−m + Lsd) = −m(c+d) + Ls(c² + d²) = −m(c+d) + Ls. Now (c+d) ≥ 1 (since c²+d²=1, c,d≥0: min of c+d on the quarter circle = 1 at endpoints). So ⟨ν, f⟩ ≤ −m + Ls. Need ≤ −η: s ≤ (m−η)/L. So c ≤ (m−η)/(Lε): the erosion IS invariant for small enough s, even at the reentrant corner, PROVIDED f is Lipschitz and the margin holds on the two faces with the SAME m. Wait — but then where's the counterexample for arbitrary closed K?! Hmm. But wait: I need to double check the proximal normal structure of the eroded set at the arc: K⊖s near origin = {x₁ ≤ 0, x₂ ≤ 0, |x| ≥ s} ∪ arms {x₁ ≤ 0, x₂ > 0? ...}. Let me recompute K⊖s fully: {|x⁻| ≥ s} where x⁻ = (min(x₁,0), min(x₂,0)). Cases: both coords ≥ 0: |x⁻| = 0 < s: excluded. x₁ ≥ 0 > x₂... x₁ ≥ 0, x₂ < 0: x⁻ = (0, x₂): |x⁻| = |x₂| ≥ s ⟺ x₂ ≤ −s: included iff x₂ ≤ −s. Similarly x₂ ≥ 0: included iff x₁ ≤ −s. Both negative: included iff x₁² + x₂² ≥ s². So K⊖s = {x₁ ≤ −s, x₂ ≤ −s} ∪ {x₂ ≤ −s, x₁ ≥ 0} ∪ {x₁ ≤ −s, x₂ ≥ 0} ∪ {x₁ < 0, x₂ < 0, x₁²+x₂² ≥ s²}. The "arms" {x₂ ≤ −s, x₁ ≥ 0} etc. connect to the quarter-annulus region. The eroded set = complement of the s-neighborhood... yes: K⊖s = ℝ² ∖ N_s(K^c) where N_s = open s-neighborhood of the open quadrant = {√(max(−x₁,0)² + max(−x₂,0)²) < s} = {(x₁,x₂): (x₁ < 0 or x₂ < 0) and dist < s} ∪ quadrant... anyway as computed. Boundary of K⊖s: (a) the quarter arc {|x| = s, x₁,x₂ ≤ 0}; (b) segments {x₁ ∈ (−s, 0), x₂ = −s} — hmm: point (−s/2, −s): x₂ = −s ≤ −s ✓ included (x₂ ≤ −s arm: "x₂ ≤ −s, x₁ ≥ 0"? no x₁ = −s/2 < 0: falls in "both negative, x₁²+x₂² = s²/4 + s² > s² ✓ included". Point (−s/2, −s + δ), δ > 0 small: x₂ = −s + δ: check: x₂ ≤ −s? No. x₁ ≤ −s? No (x₁ = −s/2 > −s). Both negative: x₁² + x₂² = s²/4 + (s−δ)² = s²(1.25) − 2sδ + δ² > s² for δ small ✓ INCLUDED?! Hmm: (−s/2, −s+δ): both negative, norm² ≈ 1.25 s² ≥ s² ✓: so it's in K⊖s. And (−s/2, −s−δ): x₂ ≤ −s: in. So around (−s/2, −s) everything is in: interior point. Let me recheck the boundary: the removed region is {|x⁻| < s} = {x₁ < 0, x₂ < 0, x₁²+x₂² < s²} ∪ {x₁ ≥ 0, x₂ < 0... wait: x₁ ≥ 0, x₂ ∈ (−s, 0): x⁻ = (0, x₂): |x⁻| = |x₂| < s ✓ removed. x₁ ∈ (−s, 0), x₂ ≥ 0: similarly removed. Quadrant x₁,x₂ ≥ 0: removed (dist 0). So removed set = (open quadrant) ∪ {x₁ ≥ 0, −s < x₂ < 0} ∪ {x₂ ≥ 0, −s < x₁ < 0} ∪ {x₁ < 0, x₂ < 0, |x| < s}: i.e., the s-neighborhood of the closed quadrant intersected with... = {√(max(−x₁,0)² + max(−x₂,0)²) < s}. ✓. So ∂(K⊖s) = {√(max(−x₁,0)² + max(−x₂,0)²) = s} =: arc + two segments: {x₁ = −s, x₂ ≥ 0} (since max(−x₁,0) = s, x₂ ≥ 0 ⟹ max(−x₂,0) = 0: √(s²) = s ✓) and {x₂ = −s, x₁ ≥ 0}. Right: the arms' inner edges at x₁ = −s, x₂ ≥ 0. Normals there: at (−s, y), y > 0: this is a boundary point of K⊖s: the set locally: {x₁ ≤ −s} ∪ {stuff}: locally {x₁ ≤ −s} (for y > 0, nearby points with x₂ > 0 need x₁ ≤ −s; points with x₂ < 0 nearby: in iff norm condition: (x₁, x₂) with x₂ slightly < 0, x₁ > −s: x⁻ = (x₁, x₂) if x₁ < 0: |x⁻|² = x₁² + x₂² > s²? x₁ ∈ (−s, 0), x₂ = −δ: x₁² + δ² vs s²: if x₁² > s² − δ²: in. So near (−s, y₀), y₀ > 0: the boundary is just the line x₁ = −s (with the quarter-disk cut only near the corner). OK.

So the eroded set is: {x₁ ≤ −s} ∪ {x₂ ≤ −s} — WAIT: is that it?! {x₁ ≤ −s} ∪ {x₂ ≤ −s}: check (−2s, 5): x₁ ≤ −s ✓ in. (−s/2, −s/2): neither coord ≤ −s: but earlier I computed it's excluded ✓ consistent? (−s/2,−s/2): both neg, norm² = s²/2 < s²: excluded ✓, and not in {x₁ ≤ −s} ∪ {x₂ ≤ −s} ✓. (−2s, −s/2): x₁ ≤ −s ✓ in: check directly: x⁻ = (−2s, −s/2): norm² = 4s² + s²/4 > s² ✓ in. ✓. (0, −2s): x₂ ≤ −s ✓ in; direct: x⁻ = (0, −2s): norm = 2s ≥ s ✓. So indeed K⊖s = {x₁ ≤ −s} ∪ {x₂ ≤ −s} EXACTLY (the erosion of a union of half-spaces = union of eroded half-spaces — of course! dist(x, A ∪ B) = min(dist(x,A), dist(x,B)) so {min(d_A, d_B) ≥ s} = {d_A ≥ s} ∪ {d_B ≥ s} ✓✓). I overcomplicated: K = H₁ ∪ H₂ (half-spaces): K⊖s = (H₁⊖s) ∪ (H₂⊖s) = {x₁ ≤ −s} ∪ {x₂ ≤ −s}. The "rounded corner" was wrong — good thing I checked. The reentrant corner persists under erosion (just translated). The eroded set = L-shape translated by (−s,−s)-ish (exactly: {x₁ ≤ −s} ∪ {x₂ ≤ −s} = (K + (−s,−s))? K + (−s,−s) = {x₁ ≤ −s} ∪ {x₂ ≤ −s} ✓ yes exactly the translate). Erosion of a union of convex sets is the union of erosions (for half-spaces/convex: dist to complement of union... anyway verified). So the reentrant corner at (−s,−s) of the eroded set has N^P = {0} again, and faces {x₁ = −s} (normal (1,0)) and {x₂ = −s} (normal (0,1)).

NOW the counterexample works: margins on ∂K (faces x₁ = 0: f₁ ≤ −m; x₂ = 0: f₂ ≤ −m) with f Lipschitz do NOT constrain f near the eroded faces x₁ = −s except through Lipschitz: f₁(x) ≤ −m + L·(distance to face) ≤ −m + Ls... as I computed: on eroded face {x₁ = −s, x₂ ≤ −s}: f₁ ≤ f₁(0, x₂) + Ls ≤ −m + Ls: fine for s < m/L: still inward. Hmm — so with Lipschitz f and uniform margin m on BOTH faces, the L-shape erosion IS invariant for s < m/(2L) say. So still no counterexample with the L-shape?!

The failure must come from a different mechanism. Options:

(A) Margin defined via PROXIMAL normals only: at points of ∂K where N^P is small (reentrant corners: N^P = {0}), the margin is vacuous — but as shown, Lipschitz + margins on nearby smooth faces control the corner by continuity... BUT: what if the reentrant structure is such that the faces with margins don't accumulate nicely — e.g., K with a SPIKE/cusp: K = {x₂ ≤ φ(x₁)} with φ Lipschitz but φ(0) = 0, φ < 0 for x₁ ≠ 0 — a downward cusp (reentrant in the horizontal direction): K = epigraph-ish region BELOW a cusp: near origin, K is the region under the curve x₂ = φ(x₁) which touches 0 only at 0. The boundary near 0: two "walls" φ(x₁) < 0 for x₁ ≠ 0, meeting at the cusp tip (0,0) where the boundary has a reentrant corner with interior angle → 2π? The cusp: interior of K near origin: {x₂ < φ(x₁)}: at the tip, K occupies almost the full neighborhood minus a thin sliver {x₂ ≥ φ(x₁), x₂ < 0}?? Hmm: if φ(x₁) < 0 for x₁ ≠ 0 and φ(0) = 0: the region ABOVE φ and BELOW 0 near origin: {φ(x₁) ≤ x₂ < 0} is a cusp-shaped NOT-K region (a needle pointing up at the origin... no wait: {x₂ ≥ φ(x₁)} ∩ {x₂ < 0} =: the sliver. Points (x₁, x₂) with x₂ ∈ [φ(x₁), 0), x₁ small ≠ 0: these are in the sliver = outside K? K = {x₂ ≤ φ(x₁)}: point (δ, −|δ|^{3/2})... let φ(x₁) = −|x₁|: K = {x₂ ≤ −|x₁|}: downward cone (45°): reentrant corner at origin with interior angle 270°? {x₂ ≤ −|x₁|}: at origin, K contains: directions with x₂ < 0 (all of them) — the missing wedge is {x₂ > −|x₁|, x₂ > 0-ish}: the upward wedge |x₂| > |x₁|, x₂ > 0: angle 90°. So it's the L-shape rotated (cone version). Same as before, not helpful. TRUE CUSP: φ(x₁) = −|x₁|^α with α > 1: K = {x₂ ≤ −|x₁|^α}: the complement near origin: {x₂ > −|x₁|^α}: includes the strip {(x₁, x₂): x₂ ∈ (−|x₁|^α, 0)}: a cusp needle. Erosion K⊖s = {dist(x, K^c) ≥ s}: the cusp needle is THIN (width |x₁|^α → 0): the s-neighborhood of the needle extends distance ~s perpendicular... The eroded set near the cusp: K⊖s ⊇ {x₂ ≤ −|x₁|^α − s}? No: erosion of {x₂ ≤ φ(x₁)}: for non-monotone... dist((x₁,x₂), K^c): K^c = {x₂ > φ(x₁)}: dist = max(0, ... for x ∈ K: x₂ ≤ φ(x₁): distance to the open set above the graph: roughly x₂... the vertical distance is φ(x₁) − x₂ ≥ 0 but true distance ≤ vertical distance, and ≥ vertical distance if φ Lipschitz with constant ≤ 1... For the cusp φ = −|x₁|^α, α > 1: φ is Lipschitz near 0? |φ(x₁) − φ(x₁')| ≤ |x₁|^α... derivative α|x₁|^{α−1} → 0: Lipschitz with tiny constant near 0 ✓ (C¹ actually). So dist(x, K^c) = φ(x₁) − x₂ (vertical distance) approximately = exactly if the nearest point is vertical. For a Lipschitz graph with slope < 1... slope → 0 near cusp: dist((x₁,x₂), {x₂ > φ}) = φ(x₁) − x₂ for x₂ ≤ φ(x₁)? The nearest point of the closed complement {x₂ ≥ φ}: minimize over (a, b): b ≥ φ(a): distance² = (a−x₁)² + (b−x₂)²: choose b = φ(a), minimize (a−x₁)² + (φ(a) − x₂)²: for x₂ well below φ: a = x₁ if φ'(x₁) small: not exactly but close. Roughly dist ≈ φ(x₁) − x₂. Then K⊖s ≈ {x₂ ≤ φ(x₁) − s}: the eroded set is ANOTHER cusp: {x₂ ≤ −|x₁|^α − s}: cusp tip at (0, −s). Now: the boundary of the eroded set near the tip: walls x₂ = −|x₁|^α − s: at wall points (x₁, −|x₁|^α − s), x₁ ≠ 0: the outward normal of the eroded set: the set is below the graph: outward normal ≈ (−φ'(x₁), 1)/√(1+φ'²) ≈ (0, 1) (nearly vertical up) with tilt (−φ'(x₁), 1). Fine. Invariance of eroded cusp: need at wall points: ⟨ν, f⟩ ≤ −η. Margin hypothesis on ∂K: at K's wall points (x₁, φ(x₁)): normals ν_K ≈ (−φ'(x₁), 1): margin: f₂ − φ'(x₁) f₁ ≤ −m·√(1+φ'²)... Now the cusp phenomenon: the cusp TIP of the eroded set, (0, −s): is a reentrant corner point; N^P at the tip: {0} (reentrant, like the L-corner: points outside the eroded set near the tip project to the walls, not the tip? For the cusp with α > 1: the tip (0, −s): exterior points near it: (0, −s + δ) (just above the tip, inside the needle's s-neighborhood... is (0,−s+δ) outside K⊖s? dist((0,−s+δ), K^c): K^c = {x₂ > φ(x₁)}: point (0, −s+δ): is it in K? x₂ = −s+δ ≤ φ(0) = 0 ✓ in K. dist to K^c: nearest point with x₂ ≥ φ: vertical: φ(0) − (−s+δ) = s − δ. So dist = s − δ < s: NOT in K⊖s ✓ outside the erosion. Projection of (0, −s+δ) onto K: (0, φ(0)) = (0,0)?? distance s−δ ✓ (vertical). So (0,0) is the projection: so is (0,0) related... The point (0,−s+δ) is exterior to K⊖s and its projection ONTO K⊖s: nearest point of {x₂ ≤ φ(x₁) − s}... ≈ (0, −s): distance δ. So the tip (0,−s) IS the projection of (0, −s+δ): hence (0,1)·... ζ = (0, δ)-direction: (0, 1) ∈ N^P_{K⊖s}((0,−s))!! Because (0,−s) is the nearest point of K⊖s to (0,−s+δ) ∉ K⊖s. Wait — N^P_{S}(x) := {ζ : x = proj_S(x + τζ)}: x = (0,−s), x + τζ = (0, −s+τ): for small τ: is (0,−s) the nearest point of K⊖s to (0,−s+τ)? K⊖s ⊇ {x₂ ≤ φ(x₁) − s}: nearest point of that closed set to (0,−s+τ): minimize: a: (a, φ(a) − s): distance² = a² + (φ(a) − s + s − τ)² = a² + (φ(a) − τ)²: with φ(a) = −|a|^α: minimize a² + (−|a|^α − τ)² = a² + (|a|^α + τ)²: at a = 0: τ². Any a ≠ 0: a² + (|a|^α + τ)² > τ². So minimum at a = 0: unique projection (0, −s) ✓✓. So (0,1) ∈ N^P_{K⊖s}(tip) — a NONTRIVIAL proximal normal at the cusp tip of the eroded set! Whereas at the ORIGINAL cusp tip (0,0) of K: (0,1) ∈ N^P_K((0,0))? Nearest point of K = {x₂ ≤ −|x₁|^α} to (0, τ), τ > 0: minimize a² + (|a|^α + τ)²: min at a = 0: value τ²: projection (0, 0) = the tip: so YES (0,1) ∈ N^P_K((0,0)) too! Hmm: so the original cusp tip DOES have a proximal normal (0,1). So margin must hold for ζ = (0,1) at the tip: ⟨(0,1), f(0,0)⟩ = f₂(0,0) ≤ −m. Then by Lipschitz/continuity f₂(0,−s) ≤ −m + Ls: at the eroded tip, proximal normal (0,1): ⟨(0,1), f(0,−s)⟩ ≤ −m + Ls < 0 for s < m/L ✓ inward. And eroded wall normals vs K's wall normals: eroded wall at (a, φ(a) − s) has normal ≈ (−φ'(a), 1); K's wall at (a, φ(a)) has the SAME normal (translate): margin at K's wall transfers via Lipschitz: |f(a, φ(a)−s) − f(a, φ(a))| ≤ Ls: ⟨ν, f(a, φ(a)−s)⟩ ≤ −m + Ls. So the cusp ALSO survives?! Hmm!!

Wait — but there's something I'm missing: the proximal normals of the ERODED set at wall points: the eroded set's boundary is x₂ = φ(a) − s only if the erosion is exactly the translate — but the true erosion K⊖s = {dist(x, K^c) ≥ s} is NOT exactly {x₂ ≤ φ − s}: for the cusp, the s-erosion near the tip: dist((x₁,x₂), K^c): if x₂ ≤ φ(x₁) − something... For points BELOW the graph: the nearest complement point might be vertical: dist = φ(x₁) − x₂ exactly when the vertical segment doesn't exit... Let me just accept approximate. The deeper issue: is the cusp a counterexample machine at all? Let me reconsider. Perhaps the right counterexample mechanism for arbitrary closed K is:

(B) Margin direction vs erosion normals mismatch via Clarke normals / non-proximal-regularity in a worse way: sets where ∂K has points with proximal normals in only SOME directions, and the erosion creates boundary with proximal normals in directions that are only LIMITING normals of K at nearby points — the L-corner did exactly this?? Let me recheck the L-corner: K = {x₁ ≤ 0} ∪ {x₂ ≤ 0}. Eroded: {x₁ ≤ −s} ∪ {x₂ ≤ −s}: corner at (−s,−s): N^P at eroded corner: nearest point of eroded set to exterior points: exterior of eroded set near corner: the removed region {x₁ > −s and x₂ > −s} ∩ (near corner): point (−s + δ, −s + δ): nearest eroded point: (−s, −s)? distance √2 δ; vs (−s, −s + δ)... not in set (x₂ > −s). vs (−s + δ... not in set. Points of {x₁ ≤ −s}: (−s, y) with y ≤ −s: distance from (−s+δ, −s+δ): √(δ² + (y + s − δ)²): minimized y = −s: √(δ² + δ²) = √2δ. Similarly for the other arm. So (−s,−s) is the projection: unique? Both arms give the same point. So (1,1)/√2·τ... ζ = ((δ,δ)/τ) direction: (1,1) ∈ N^P_{eroded}((−s,−s))? x + τζ = (−s + τ/√2, −s + τ/√2)... wait ζ = (1,1)/√2, τζ = (τ/√2, τ/√2): point (−s + τ/√2, −s + τ/√2): exterior ✓; its projection onto eroded set: (−s, −s): distance τ; any other candidate: (−s, y), y < −s: distance² = (τ/√2)² + (τ/√2 − (y+s))² > τ². ✓ unique. So (1,1)/√2 ∈ N^P_{K⊖s}((−s,−s)): NONTRIVIAL proximal normal at the eroded corner in the diagonal direction! And at the ORIGINAL corner (0,0) of K: N^P_K(0,0) = {0} (computed earlier: exterior points near origin project onto the faces/axes, not the corner — wait, do they? Exterior of K near origin = open quadrant {x₁ > 0, x₂ > 0}: point (δ, δ): projection onto K = {x₁ ≤ 0} ∪ {x₂ ≤ 0}: nearest points: (0, δ) (on face x₁ = 0... is (0, δ) ∈ K? x₁ = 0 ≤ 0 ✓): distance δ; (δ, 0): distance δ; (0,0): distance √2 δ. So nearest are (0,δ), (δ, 0): NOT the corner. So indeed N^P_K(0,0) = {0} ✓ (as I found). But at the ERODED corner, the diagonal IS proximal!! Why the asymmetry? Because for K, the exterior region near the corner is the QUADRANT {(0,∞)²} — points along the diagonal (δ,δ) have closer points on the faces (0,δ). For the eroded set, the exterior near the eroded corner is {x₁ > −s AND x₂ > −s} — a full quadrant — and its diagonal points (−s+τ/√2, −s+τ/√2) have nearest eroded point (−s,−s) — because the eroded faces are at x₁ = −s (x₂ ≤ −s only!) — the faces got TRUNCATED: (−s, y) ∈ eroded only for y ≤ −s. For K, the faces extend: (0, y) ∈ K for ALL y ≤ 0 — including y = δ' > 0? (0, δ) with δ > 0: x₁ = 0 ≤ 0 ✓ ∈ K! THAT's the difference: K's faces extend THROUGH the corner (the L-shape's arms are full half-planes), so exterior diagonal points find closer points on the arms beyond the corner. The eroded arms are truncated at the corner. So the erosion turned a reentrant corner with NO proximal normals into a corner WITH proximal normals in the Clarke directions. EXACTLY the normal blow-up phenomenon, and now it bites:

Margin hypothesis (source): strict inward margin on ∂K — presumably w.r.t. proximal normals (the natural strong-invariance condition): at the L-corner of K: N^P = {0}: vacuous: NO constraint on f(0,0) in the diagonal direction from the corner itself. Constraints only from faces: f₁ ≤ −m on {x₁ = 0} (all x₂ ≤ 0 — note: the face includes points (0, y), y ≤ 0; also (0, y) for y > 0 are interior-ish boundary? (0, y), y > 0: x₁ = 0: boundary of K ✓ with N^P = ℝ₊(1,0) locally (K locally = half-space x₁ ≤ 0 near (0,y) for y > 0 ✓). So margin: f₁(0, y) ≤ −m for ALL y (all boundary points with normal (1,0)): including y → 0⁺ and y → 0⁻. By continuity: f₁(0,0) ≤ −m and f₂(0,0) ≤ −m. Hmm — so continuity + margin on the FULL faces (which extend past the corner) forces the corner velocity deep inward (both components ≤ −m). Then the eroded corner (−s,−s): proximal normal (1,1)/√2: ⟨(1,1)/√2, f(−s,−s)⟩ = (f₁ + f₂)/√2 ≤ (−m + Ls + −m + Ls)/√2 < 0 for s < m/(2L)·... wait f₁(−s,−s) ≤ f₁(0,−s) + Ls ≤ −m + Ls ✓. So AGAIN the Lipschitz margin on faces rescues the eroded corner!! ARGH.

So for the L-shape with Lipschitz fields and margin on all boundary proximal normals, erosion invariance holds for s ≤ (m−η)/L-ish. Because the L-shape, though nonconvex, is a finite union of half-spaces with UNIFORM margin that propagates. The real counterexample needs the margin to DEGENERATE: margin m(x) → 0 along the boundary approaching a bad point, while "strict margin on ∂K" is interpreted pointwise-strict (m(x) > 0 for each x but inf = 0)? The source says "strict inward margin on boundary K" — if that means a UNIFORM m > 0, then... for which closed K does uniform proximal-margin + Lipschitz field still fail erosion? Consider: K with boundary where proximal normals exist in a set of directions that doesn't capture limiting directions, AND the faces carrying margins are SHORT (don't extend past the corner): e.g., K = closed square MINUS an open notch... any set where the boundary has a reentrant corner whose adjacent faces are bounded. Simplest: K = [−1,1]² ∖ ((0,1] × (0,1])?? That's the square with the open... let me take K = ([−1,0] × [−1,1]) ∪ ([−1,1] × [−1,0]): an L-shaped closed set (square minus open upper-right quadrant (0,1]×(0,1]... need closed: K = [−1,1]² ∩ ({x₁ ≤ 0} ∪ {x₂ ≤ 0})): L-shaped polygon. Reentrant corner at (0,0) with faces: {x₁ = 0, x₂ ∈ [−1, 0]} (short face, length 1) and {x₂ = 0, x₁ ∈ [−1, 0]}. Exterior near corner: the open quadrant (0,1)²: diagonal point (δ,δ): nearest K point: (0, δ)? δ ∈ (0,1): (0, δ): x₁ = 0 ≤ 0 ✓ but is (0,δ) ∈ K? K = square ∩ (x₁ ≤ 0 or x₂ ≤ 0): (0, δ): x₁ = 0 ≤ 0 ✓ ∈ K ✓ (as long as δ ≤ 1). distance from (δ,δ) to (0,δ) = δ. So still faces extend: N^P at corner still {0}... because the arms still pass through x₁ = 0 column for x₂ up to 1. To truncate the arms AT the corner: K = ({x₁ ≤ 0} ∩ {x₂ ≥ ... hmm. I want the two faces meeting at the reentrant corner to NOT extend beyond: faces {x₁ = 0, x₂ ≤ 0} and {x₂ = 0, x₁ ≤ 0} with K locally = {x₁ ≤ 0, x₂ ≤ 0} ∪ {x₁ ≤ 0, x₂ > 0}?? that's contradictory. Let me think: reentrant corner with truncated arms: K = {(x₁,x₂): x₁ ≤ 0, x₂ ≤ 1} ∪ {x₂ ≤ 0, x₁ ≤ 1}: hmm the corner at (0,0): exterior = {x₁ > 0, x₂ > 0} minus... no: complement of K = {x₁ > 0 and x₂ > 1} ∪ {x₂ > 0 and x₁ > 1} — wait: K = ({x₁ ≤ 0} ∪ {x₂ ≤ 0}) ∩ {x₁ ≤ 1... I'm confusing myself. Let me directly define the set: I want near origin: K = closed third quadrant ∪ positive extensions... The L-shape {x₁ ≤ 0} ∪ {x₂ ≤ 0} has arms = half-planes (infinite). Truncated version: K = ({x₁ ≤ 0} ∩ {x₂ ≤ 1}) ∪ ({x₂ ≤ 0} ∩ {x₁ ≤ 1}): near the origin (within distance < 1): K = ({x₁ ≤ 0, x₂ ≤ 1}) ∪ ({x₂ ≤ 0, x₁ ≤ 1}): locally near origin this is just {x₁ ≤ 0} ∪ {x₂ ≤ 0} again (the truncations bind far away: at (0, 2): x₁ ≤ 0 but x₂ = 2 > 1: not in first arm; x₂ ≤ 0? no: (0,2) ∉ K!). So near origin, same as before; the exterior point (0, 1.5) is outside K and its projection: nearest K points: (0, 1)? (0,1): x₁ = 0 ≤ 0, x₂ = 1 ≤ 1 ✓ ∈ K: distance 0.5. So the face {x₁ = 0, x₂ ≤ 1} ends at (0,1), a CONVEX corner of K at (0,1) with normals... The point: to get N^P ≠ {0} at the reentrant corner (0,0), I need exterior points near (0,0) whose nearest K-point is (0,0) itself: i.e., the s-neighborhood geometry: point (δ, δ) has candidates (0, δ) at distance δ — to beat that, (0,δ) must NOT be in K: need the arm {x₁ = 0} to be truncated BELOW δ for arbitrarily small δ: i.e., the face doesn't extend above 0: K's arm = {x₁ ≤ 0, x₂ ≤ 0}-ish only: then near origin K = just the third quadrant {x₁ ≤ 0, x₂ ≤ 0} (CONVEX corner — not reentrant!). Fundamental obstruction: a reentrant corner of angle > π has arms extending on both sides; exterior points in the notch project onto the arms unless the arms are truncated within the notch-distance... The notch (exterior wedge) has angle < π (for the L: notch = 90° quadrant); exterior points at distance δ from the corner along the notch bisector: distance to each arm ≈ δ sin(half-notch-angle) < δ: so arms always win UNLESS arms are truncated at distance < δ·something from the corner — truncating arms at distance r₀ from the corner: then exterior points at distance < r₀·sin(θ/2)... hmm: if arms are truncated AT the corner (arms have zero length): then there's no corner at all. Conclusion: for POLYHEDRAL reentrant corners, N^P at the corner is always {0} when notch angle < π... wait no: N^P at reentrant corner: I computed for L-shape = {0}. For erosion: eroded corner had N^P ∋ diagonal BECAUSE erosion truncated the arms (erosion of union = union of erosions: arms erode to {x₁ ≤ −s} (x₂ ≤ −s... the eroded arm {x₁ ≤ −s} retains x₂ ∈ (−∞, ...]: eroded set = {x₁ ≤ −s} ∪ {x₂ ≤ −s}: the arm {x₁ ≤ −s} includes x₂ > −s! e.g., (−s, 5) ∈ eroded ✓. So the eroded arms DO extend past the eroded corner (−s,−s) (analogous to original). Then why did the diagonal point (−s + τ/√2, −s + τ/√2) project to the corner and not to the arm point (−s, −s + τ/√2)? Check: is (−s, −s + τ/√2) ∈ eroded set? x₁ = −s ≤ −s ✓ YES it's in the arm {x₁ ≤ −s}!! Distance from (−s + τ/√2, −s + τ/√2) to (−s, −s + τ/√2) = τ/√2 < τ. So the projection is NOT the corner — I made an ERROR above! Let me redo: exterior point y = (−s + a, −s + a), a > 0: candidates on arm A = {x₁ = −s}: (−s, −s + t) for t ∈ [−s+... any (−s, v): distance² = a² + (v − (−s+a))²: minimized v = −s + a: distance a — WAIT (−s, −s+a): x₁ = −s ✓ ∈ arm A (arm A = {x₁ ≤ −s}, no constraint on x₂) ✓. Distance from y = (−s+a, −s+a) to (−s, −s+a) = a. Distance to corner (−s,−s) = a√2. So the arm point is CLOSER: projection is (−s, −s+a), NOT the corner. So N^P_{eroded}((−s,−s)) = {0} after all — consistent with erosion = translate of the L-shape (the eroded set {x₁ ≤ −s} ∪ {x₂ ≤ −s} is exactly the L-shape translated by (−s,−s), which has the same geometry: N^P = {0} at its corner). I made an arithmetic slip before ("(−s, y), y < −s" — I wrongly restricted the arm to y ≤ −s). OK so erosion of the L-shape is its translate: same normal structure: the erosion claim for the L-shape reduces to the translate having margins: Lipschitz transfer: ✓ works. So polyhedral reentrant corners are FINE under uniform margin + Lipschitz field?! Hmm, wait — but is that actually right? Erosion = translate holds for UNION of half-spaces. What about intersection-like reentrance or curved reentrant corners? General fact: for ANY closed K and s > 0: is it true that proximal normals of K⊖s at x relate to proximal normals of K? The general relation: x ∈ K⊖s ⟹ dist(x, K^c) ≥ s ⟹ B(x, s) ⊆ K... hmm K⊖s = {x : B̄(x,s) ⊆ K}? dist(x, K^c) ≥ s ⟺ cl B(x,s) ∩ K^c = ∅ ⟺ B̄(x,s) ⊆ K (K closed) ✓. So K⊖s = {x : x + sB̄ ⊆ K} = Minkowski erosion. And ζ ∈ N^P_{K⊖s}(x) ⟹ x is projection of x + τζ onto K⊖s ⟹ ... there's a classical relation: N^P_{K⊖sB}(x) ⊆ N^P_K(x)?? For CONVEX K: N_{K⊖sB}(x) = N_K(x + s·ζ-direction...)... For convex K: x ∈ ∂(K ⊖ sB): the outward normal ν of K⊖sB at x: x = p − sν for a boundary point p of K with normal ν (this is standard for convex: the erosion's support faces are translations of K's faces). And N^P_{K⊖s}(x) = N^P_K(p) (translated cone) — for the SQUARE example earlier this held. For general closed K: the relation can FAIL — the erosion can create new normals from limiting normals (normal blow-up). Where? At points where K is not prox-regular: e.g., K with a reentrant corner whose arms are NOT straight... but we just saw the L-shape (straight arms) is fine because erosion = translate. Hmm, when is erosion NOT a translate? For the L-shape it happened to be. Consider K = {x₂ ≤ −|x₁|} (the 90° downward cone — "vee"): K⊖s: {dist(x, K^c) ≥ s}: K^c = {x₂ > −|x₁|}: the region above the vee: erosion: K⊖s = {x₂ ≤ −|x₁| − s/√2·...}: the s-erosion of the vee: points at distance ≥ s above... the vee's erosion is the vee shifted down with the apex rounded? Dist from (0, y), y < 0 to K^c: nearest complement point: the complement is above the vee: from (0, −a) (a > 0): distance to the boundary lines x₂ = ±x₁... the apex angle 90°: distance from apex-interior point to complement: the inball: dist((0,−a), K^c) = distance to the two lines: a/√2·? The lines: x₂ = −|x₁| i.e. x₂ = x₁ for x₁ ≤ 0... wait −|x₁|: for x₁ ≥ 0: x₂ = −x₁; x₁ ≤ 0: x₂ = x₁. Lines through origin with slopes ±1 (only half-lines). Point (0, −a): distance to line x₂ = −x₁ (x₁ ≥ 0): the full line has distance a/√2; to the half-line: if the foot has x₁ ≥ 0 ✓: foot: (a/2·... foot of perpendicular from (0,−a) to line x₂ = −x₁ (direction (1,−1)/√2): parametrize (t, −t): distance² = t² + (t − a)²... minimize: 2t² − 2at + a²: t = a/2: foot (a/2, −a/2) with x₁ = a/2 ≥ 0 ✓ valid: distance = a/√2. So dist((0,−a), K^c) = a/√2 (if a small enough that feet are on the half-lines ✓). So K⊖s ⊇ {(0, −a): a ≥ s√2}: apex of the eroded vee at (0, −s√2): the eroded set = vee translated down by s√2? By symmetry the erosion of a convex cone (the vee K = {x₂ ≤ −|x₁|} is a CONVEX cone (intersection of two half-spaces: x₂ ≤ x₁ and x₂ ≤ −x₁ ✓ convex!). Convex: erosion = Minkowski erosion: K ⊖ sB: for a cone with apex angle: K⊖sB = {x : x + sB ⊆ K}: apex region removed: the erosion of a convex cone is the cone translated along its axis by s/sin(θ/2)-ish: yes standard. Eroded set = same cone translated: normals at eroded boundary = same as K's normals at corresponding translated points ✓ fine.

So convex sets: fine. Polyhedral nonconvex with reentrant corners (L-shape): erosion = union of erosions of the pieces... but that's special to half-space unions. General nonconvex: K = A ∪ B with A, B closed: K⊖s = (A⊖s) ∪ (B⊖s) ✓ (shown above: dist(x, A∪B) = min(dist(x,A), dist(x,B)) — wait is that right? dist(x, A ∪ B) = min(dist(x,A), dist(x,B)) ✓ trivially). So erosion distributes over unions: ANY closed K = union of pieces; the L-shape = union of two half-spaces. But a general reentrant corner with CURVED arms: K = {x₂ ≤ φ(x₁)} with φ Lipschitz: K⊖s ≈ {dist ≥ s}: for a Lipschitz epigraph, erosion is NOT a translate: at a reentrant corner of φ (φ convex kink upward? For hypograph {x₂ ≤ φ}: reentrant (from K's view) corner = φ has a local MAX kink? Hmm: K below the graph: at a kink where φ has a concave corner (peak), K has a reentrant corner... e.g., φ(x₁) = −|x₁| is a peak (concave kink) and K = hypograph = CONVEX vee. Wait: {x₂ ≤ −|x₁|}: is this convex? Check (1,−1) and (−1,−1) ∈ K; midpoint (0,−1): x₂ = −1 ≤ −0 = 0 ✓ ∈ K. (1,−1): −1 ≤ −1 ✓. General: hypograph of a CONCAVE function is convex ✓ (−|x₁| is concave ✓). So peak kinks (concave φ) give convex K. Reentrant corners of K arise from CONVEX kinks of φ (valleys): φ(x₁) = +|x₁|: K = {x₂ ≤ |x₁|}: the region below the vee: at origin: K contains everything below both lines: the notch (exterior) = {x₂ > |x₁|}: upward wedge 90°: K has a reentrant corner at origin (interior angle 270°) ✓. Erosion: K⊖s: {dist(x, {x₂ > |x₁|}) ≥ s}: the s-erosion of the region-below-vee: near the apex: the eroded set's boundary: parallel curves to the two half-lines at distance s INSIDE K, joined near the apex by... the erosion of a wedge-complement: {x₂ ≤ |x₁|} ⊖ sB: the apex region: points within the wedge K near origin: the inball of K at "height": the eroded boundary near apex = the two lines shifted down-inward by s (distance s from each), i.e., lines x₂ = |x₁| − s√2, meeting at... shifted lines: line x₂ = x₁ − c and x₂ = −x₁ − c (c = s√2): they meet at (0, −c): the eroded set contains {x₂ ≤ |x₁| − c}? Check (0, −c): dist to K^c = {x₂ > |x₁|}: distance from (0,−c) to the wedge boundary lines = c/√2 = s ✓ and to the apex... dist((0,−c), {x₂ > |x₁|}): nearest exterior point: on the lines: (c/2·?...): computed like before: c/√2 = s: so dist = s ✓ boundary point of erosion ✓. So eroded set near apex = vee {x₂ ≤ |x₁| − s√2}: SAME shape (translated down). AGAIN a translate! Because the vee is a cone: erosion of a cone = translate of the cone (cones are convex-or-not... {x₂ ≤ |x₁|} is NONconvex (hypograph of convex φ): but it's a CONE (closed under positive scaling): and for any cone C: C ⊖ sB = C ∩ (C⊖sB): scaling: x ∈ C⊖sB ⟺ x + sB ⊆ C ⟺ (x/s... let x = s·y: s y + sB = s(y + B) ⊆ C ⟺ y + B ⊆ C (cone) ⟺ y ∈ C⊖B: so C⊖sB = s·(C⊖B): the erosion of a cone is a SCALED cone (and translated? no — scaled, apex stays at origin!). Hmm: for the vee cone {x₂ ≤ |x₁|} (apex at origin): C⊖sB = s(C⊖B): C⊖B = {x₂ ≤ |x₁| − √2}? Check by the above: (0,−√2) ∈ C⊖B ✓. Is C⊖B = the translated vee {x₂ ≤ |x₁| − √2}? A translate of a cone is not a cone unless apex moves: {x₂ ≤ |x₁| − √2} has apex at (0, −√2): it IS a cone with apex (0,−√2) (not based at origin) — and s·(C⊖B) would scale toward ORIGIN: s·{x₂ ≤ |x₁| − √2} = {x₂/s ≤ |x₁|/s − √2} = {x₂ ≤ |x₁| − s√2} ✓ same thing! Nice: consistent: the eroded vee = vee with apex pushed down by s√2 = scaled version ✓. So erosion of cones = scaled cones: reentrant corner PRESERVED with same angles, apex moved. Normals at eroded boundary = same directions as normals of original at corresponding rays ✓. So again Lipschitz + margin transfers...

Hmm!! So maybe the erosion claim is more robust than the "rejection criteria" suggest?? But the rejection criteria explicitly say: "claims metric erosion preserves smooth normal margins for arbitrary closed sets" is a rejection trigger — implying the correct answer is that it does NOT for arbitrary closed sets. So there must be a genuine counterexample. Let me think harder about the failure mechanism for arbitrary closed K:

Mechanism candidates:

Infinitely crinkly / non-Lipschitz boundary: K whose boundary has a cusp where the margin direction degenerates. The margin is w.r.t. proximal normals; near a cusp tip, proximal normals at nearby boundary points rotate wildly (unbounded normal variation), so a UNIFORM margin m w.r.t. proximal normals at all boundary points... Consider K = {x₂ ≤ −|x₁|^α}, α ∈ (0,1) (sharp cusp, NOT Lipschitz at 0: φ' = α|x₁|^{α−1} → ∞). K is convex? φ = −|x₁|^α with α < 1: is it concave? −|x₁|^α: |x₁|^α is convex for α ≥ 1; for α < 1, |x₁|^α is CONCAVE on each side... |x₁|^α, α<1: concave on (0,∞) ✓ and on (−∞,0) ✓, but at 0 there's a kink pointing DOWN (value 0, but nearby values > 0? |x₁|^α > 0: at x₁ = ±ε: ε^α > 0: so φ(0) = 0 < φ(±ε)?? φ = −|x₁|^α: φ(0) = 0, φ(±ε) = −ε^α < 0: so 0 is a local MAX of φ: concave-ish kink ✓: K = hypograph of φ: at the cusp tip (0,0): K near tip = {x₂ ≤ −|x₁|^α}: the exterior = {x₂ > −|x₁|^α}: a cusp NEEDLE pointing DOWN?? Wait: exterior points just below the tip: (δ, −δ^α/2): x₂ = −δ^α/2 > −δ^α = φ(δ) ✓ exterior: so the exterior includes a thin needle going DOWN along the cusp on both sides?? No wait: at x₁ = δ > 0: exterior is x₂ > −δ^α: includes (δ, 0), (δ, −δ^α/2), etc. The exterior near origin = {x₂ > −|x₁|^α} — the region ABOVE the downward cusp: this is a needle pointing UP: thin near... no: at x₁ = 0: exterior = {x₂ > 0}; at x₁ = δ: exterior = {x₂ > −δ^α}: so the exterior's LOWER boundary dives down to −δ^α as |x₁| grows: the exterior near the origin is a cusp-shaped region opening UPWARD with walls diving down steeply: it's the COMPLEMENT region that is "sharp": the exterior has a sharp needle pointing DOWN into K?? The exterior {x₂ > −|x₁|^α} contains the entire upper half plane plus two thin wedges sneaking down along ±x₁. K = {x₂ ≤ −|x₁|^α} = the region below the cusp: K has a sharp NEEDLE pointing UP at the origin (the cusp tip): K's tip at (0,0), and K widens downward: at |x₁| = δ, K = {x₂ ≤ −δ^α} — wide. So K is a downward-opening region with a sharp upward needle tip: CONVEX? hypograph of φ = −|x₁|^α: φ concave? On x₁ > 0: −x₁^α concave ✓ (α<1); on x₁ < 0: −(−x₁)^α concave ✓; at 0: kink: φ(0) = 0 ≥ concave interpolation of φ(±ε) = −ε^α: 0 ≥ −ε^α ✓: so φ is concave on ℝ ✓✓: K = hypograph of concave function = CONVEX SET. Sharp needle tip pointing up. Now: N^P at the tip (0,0): exterior point (0, τ), τ > 0: projection onto K: minimize distance to {x₂ ≤ −|x₁|^α}: candidates (a, −|a|^α): distance² = a² + (|a|^α + τ)²: minimize over a: derivative: 2a + 2α sign(a)|a|^{α−1}(|a|^α + τ): for small τ: is a = 0 optimal? Consider a tiny: a² + (|a|^α + τ)² ≈ a² + τ² + 2τ|a|^α: the term 2τ|a|^α: for α < 1, |a|^α >> a² for small a: so increasing a INCREASES the distance?? d/da [a² + 2τ a^α] = 2a + 2ταa^{α−1} > 0 for a > 0 ✓ so minimum at a = 0: projection of (0,τ) is the TIP (0,0) ✓: so (0,1) ∈ N^P_K(tip) ✓. Also normals at the cusp WALLS: at (a, −a^α), a > 0: K is locally the hypograph: normal direction = (φ'(a), −1)/√(1+φ'²)·(−1)... outward normal of hypograph {x₂ ≤ φ} at graph point: (−φ'(a), 1)/√(1+φ'²): with φ'(a) = −αa^{α−1} → −∞ as a → 0⁺: normal → (αa^{α−1}, 1)/norm → (1, a^{1−α}/α·...) → (1, 0⁺): wall normals ROTATE from (1,0) (horizontal, at the needle walls near the tip) to... at the tip: N^P = ? proximal normals at the tip: which ζ? (0,1) ✓ (projection of (0,τ)); also ζ with horizontal component: exterior point (τ, y): y > −τ^α: projection: if y ≥ 0: projection ≈ (0,0)?: distance from (τ, y), y > 0: to K: candidates (a, −a^α): minimize (a−τ)² + (a^α + y)²: for y > 0 large-ish vs τ: a ≈ small: the minimizer: could be a = 0 (tip) if τ small: distance² = τ² + y²; a = τ: (0)² + (τ^α + y)² > y²... hmm τ² + y² vs (τ^α + y)² = y² + 2yτ^α + τ^{2α}: compare τ² vs 2yτ^α + τ^{2α}: for small τ, τ^{2α} >> τ² (α<1): so tip wins: projection = tip: so (τ, y) − (0,0) = (τ, y): ζ-direction (τ, y)/|·| — ANY direction with positive components?? For (τ, y) with τ, y > 0 small: is the tip the projection? Need τ² + y² ≤ min_a (a−τ)² + (a^α + y)²: at a = τ: τ^{2α} + 2yτ^α + y² ≥ y² + ...: is τ² ≤ τ^{2α} + 2yτ^α: YES for small τ (τ^{2α} >> τ²) ✓. At general a: (a−τ)² + (a^α+y)² ≥ ... plausibly tip wins for all a when τ, y small with τ not too small relative... roughly if τ^{2−2α} ≤ 1 + 2yτ^{−α}... fine: so for exterior points in a subquadrant region near the tip, the projection is the TIP: so N^P_K(tip) ⊇ {(ζ₁, ζ₂): ζ₁ ≥ 0, ζ₂ ≥ 0, ...} — actually a fat cone of proximal normals at the tip! (Like a convex corner.) Indeed K is convex with a "corner" at the tip (subdifferential of the convex function −φ... K convex: N_K(tip) = normal cone of convex set at the tip = the convex cone generated by the limiting wall normals = {(ζ₁,ζ₂): ζ₂ ≥ c|ζ₁|^{...}}: since wall normals → (1, 0) as a → 0 (from the right wall) and (−1, 0) (left wall) and (0,1): the convex cone generated: {(ζ₁, ζ₂): ζ₂ ≥ 0} — the ENTIRE upper half plane?! Wall normals: right wall at a: (αa^{α−1}, 1)/√(1 + α²a^{2α−2}) → (1, 0) as a→0 ✓; left wall → (−1, 0); tip normal (0,1): convex cone generated by {(1,0), (0,1), (−1,0)} = {ζ₂ ≥ 0} ✓. So N^P(tip) = {ζ₂ ≥ 0} (closed upper half-plane of normals) — plausible for the convex needle. NOW the margin: uniform margin m: ⟨ζ, f(x,k(x),d)⟩ ≤ −m for all unit ζ ∈ N^P(x), all x ∈ ∂K: at the tip: for ALL ζ with ζ₂ ≥ 0, |ζ| = 1: including ζ = (±1, 0): so f₁(tip) ≤ −m AND −f₁(tip) ≤ −m ⟹ f₁(tip) ≥ m: CONTRADICTION unless m ≤ 0!! So a UNIFORM positive margin at a convex needle tip is IMPOSSIBLE — the margin hypothesis itself fails. Interesting: so sharp convex corners forbid uniform strict margins (the normal cone is too fat). That's consistent: uniform strict margin requires |N^P| directions to lie in a half-space-ish compatible with a single velocity: need ⟨ζ, f⟩ ≤ −m ∀ζ ∈ N^P ∩ S: possible iff N^P lies in a strict half-space {⟨ζ, v₀⟩ ≤ −m}: i.e., the normal cone must be contained in a half-space whose boundary is at distance m from origin... For fat cones (angle ≥ π) impossible. So sets admitting uniform margins have "thin" normal cones everywhere: angle < π at all boundary points ⟺ ... prox-regular-ish! Indeed: a closed set K is prox-regular (positive reach) iff normal cones are... hmm, prox-regular ⟺ ∃r: N^P cones satisfy ⟨ζ, x − y⟩ ≤ |ζ−...|... There's a classical equivalence: K prox-regular with radius r ⟺ for all x ∈ ∂K, the normal cone N^P_K(x) is "r-strong": ⟨ζ, y − x⟩ ≤ (1/2r)|ζ||y−x|². Convex sets: r = ∞ but can have FAT normal cones (the needle: angle π). Hmm so prox-regular doesn't preclude fat cones (convex ⊂ prox-regular). But fat cones preclude uniform margins. So the margin hypothesis itself imposes "angle of N^P < π with slack" — which is precisely a kind of uniform prox-regularity / positive reach condition!! Wait: angle(N^P) < π ⟺ the boundary is locally "convex-ish with bounded curvature"? The normal cone at a boundary point has angle < π iff the TANGENT cone's... hmm, for a smooth boundary point N^P is a ray (angle 0). Fat cones = corners. Angle exactly π = needle/cusp. So uniform margin m ⟹ (given f bounded by F) the normal cone at each boundary point is contained in a half-space at distance m/F... wait ⟨ζ, f⟩ ≤ −m with |f| ≤ F: |⟨ζ, f⟩| ≤ F: need −m ≥ ... the normal cone must avoid the cap {⟨ζ, f/|f|⟩ > −m/F}: so all unit normals within angle < arccos(−m/F) < π... the cone angle ≤ 2·(π/2 − ... whatever: the normal cone must be contained in a closed half-space not containing 0 in its interior... a cone contained in a half-space {⟨ζ, w⟩ ≤ 0} has angle ≤ π; strict margin gives angle < π strictly with quantitative slack δ = arcsin(m/F)-ish. So margin ⟹ normal cones uniformly "pointed" (angle ≤ π − 2δ). Does pointedness + closedness give positive reach? NOT in general: consider a reentrant SPIRAL or a set whose boundary is smooth everywhere (N^P = rays, angle 0!) but with curvature → −∞ (concave boundary with unbounded negative curvature = "sharp concave notches" — boundary smooth, normals thin, but reach = 0!). Example: K = hypograph of a C¹ function with a "shrinking oscillation": φ smooth, φ(0) = 0, with increasingly sharp VALLEYS (concave-down spikes?) approaching 0... but wait valleys of φ (convex kinks/regions) create reentrant corners of K... smooth but high curvature: reach of a C² curve with curvature κ: reach ≈ 1/max|κ| (for the concave side). If curvature is unbounded (C¹ but not C², curvature → ∞), reach → 0. K = {x₂ ≤ φ(x₁)} with φ ∈ C¹, φ(0) = φ'(0) = 0, and φ has concave-down "bumps" with curvature → ∞ near 0: at those bumps, K's boundary is smooth (normals thin ✓ margins fine at each point) — BUT the EROSION K⊖s: for s > 0, near the high-curvature concave bumps: the eroded set loses... the erosion near a concave boundary arc with curvature κ > 1/s: the s-erosion SWALLOWS the arc: the eroded boundary near the arc: points at distance s from the concave arc: the parallel curve at distance s inside: exists only if s < 1/κ; for s > 1/κ, the erosion boundary near the arc degenerates — the eroded set's boundary develops from the FOCAL points (centers of curvature) — evolute/caustic: the eroded set near a high-curvature concave arc has boundary = the arc's evolute offset: and CRUCIALLY the eroded set can become... hmm, but actually here's the cleanest known counterexample mechanism: K with zero reach: for every s > 0, K⊖s can be EMPTY or fail to inherit margins, because erosion past the reach creates sets whose boundary normals are NOT related to K's boundary normals (they come from the cut locus/evolute: new boundary pieces (the "medial axis" sheets) with normals in ARBITRARY directions — exactly where margins are absent.**
Concretely: K = closed unit disk minus a thin notch... or simplest ANALYTIC counterexample: K = {x₂ ≤ φ(x₁)} where φ(x₁) = −|x₁|^α, α ∈ (0,1)... we showed the needle tip has fat normals: margin impossible there: so this K violates the margin hypothesis — can't use it directly (the counterexample must SATISFY the hypotheses: robust invariance + strict margin, and FAIL the erosion conclusion). Need: margin holds on ∂K (all proximal normals, uniform m > 0 — or maybe the source's "strict inward margin" is pointwise? If pointwise-strict (m(x) > 0 each x, inf = 0), then the counterexample is easy: margin degenerating along the boundary: any smooth K with margin m(x) → 0: then NO erosion is invariant... but trivial: even without error. Hmm, but the source presumably means uniform. Let me aim for: uniform margin + robust invariance + Lipschitz feedback (all "good" hypotheses), yet NO positive erosion invariant — via zero reach.

Design: K with a CONCAVE (reentrant) cusp — inward needle notch: K = {x₂ ≤ −|x₁|^α} flipped: K = complement of a needle: K = ℝ² ∖ {x₂ > |x₁|^α·(-...)}. Let me define K = {x₂ ≤ ψ(x₁)} with ψ(x₁) = |x₁|^α, α > 1: ψ convex with a FLAT-bottomed valley? ψ(0) = 0, ψ ≥ 0: K = below the valley: at origin: K's boundary has a VALLEY (ψ min at 0): K near origin = {x₂ ≤ |x₁|^α}: reentrant corner/cusp at origin: the exterior = {x₂ > |x₁|^α}: a needle pointing UP?? exterior at x₁ = 0: {x₂ > 0}; at |x₁| = δ: {x₂ > δ^α}: exterior = upward needle with widening... the exterior is FAT near origin (contains {x₂ > δ^α} which for small δ is almost the half-plane) — no: exterior = {x₂ > |x₁|^α}: at height x₂ = h > 0: exterior includes all x₁ with |x₁|^α < h: |x₁| < h^{1/α}: WIDE. The NOTCH into K: K = {x₂ ≤ |x₁|^α}: K near origin: below the valley curve: at x₂ = −h: all x₁; the notch = the region {0 < ... the valley: K's boundary dips to 0 AT x₁ = 0 and rises to δ^α at |x₁| = δ: so K's boundary has a local MINIMUM at origin: K is missing the region above the valley: the missing region near origin = {x₂ > |x₁|^α} ∩ {x₂ < small} — a lens/needle shape that is THIN: width at height h: 2h^{1/α}: with α > 1: h^{1/α} >> h: WIDE?? at height h: the missing region spans |x₁| < h^{1/α}: for α = 2: h^{1/2} >> h: the missing region is a wide flat needle (like a parabola opening upward: {x₂ > x₁²} — the region above a parabola: at height h: width 2√h: yes wide-ish, it's just the exterior of a parabola — the parabola region K = {x₂ ≤ x₁²} = BELOW the parabola: this is the classic NON-prox-regular set? Reach of the region below a parabola: the parabola x₂ = x₁² has curvature 2 at the vertex: reach of the CLOSED set K = {x₂ ≤ x₁²}: is it prox-regular? K is nonconvex (hypograph of convex function). The boundary curve (parabola) has reach = 1/max curvature = 1/2 (for the curve as a set); for the REGION below: prox-regularity fails at... hmm, actually {x₂ ≤ x₁²} as a set: points just above the vertex inside the "cup": (0, ε): distance to K: nearest K point: (±√ε·?...): minimize (a)² + (a² − ε)²: a² vs: at a = √(ε/2)-ish: distance ≈ ... the vertex (0,0) is at distance ε; points (a, a²) with a² < ε: distance² = a² + (ε − a²)²: minimize over a: let b = a²: b + (ε−b)²: b = ε − 1/2 + ... for ε < 1/2: derivative 1 − 2(ε−b) = 0: b = ε − 1/2 < 0: so min at b = 0: distance = ε: projection of (0, ε) = vertex (0,0): UNIQUE? distance ε at b=0; any other: b + (ε−b)² > ε² iff b(1 + b − 2ε) > 0: for b ∈ (0, 2ε−... for small ε: b + (ε−b)² vs ε²: at b = ε: ε + 0 = ε < ε²?? no ε > ε² for ε < 1: WAIT: at b = ε: distance² = ε + 0 = ε > ε². Hmm b = a² is the x₁-offset squared... I conflated. Redo: distance² from (0, ε) to (a, a²) = a² + (a² − ε)². Let g(a) = a² + (a²−ε)². g(0) = ε². g(√ε) = ε + 0 = ε > ε². g'(a) = 2a + 4a(a²−ε) = 2a(1 + 2(a² − ε)): zeros: a = 0 and a² = ε − 1/2. For ε < 1/2: only a = 0: g increasing in |a|: UNIQUE projection = vertex ✓. So (0, ε) projects uniquely to (0,0): and ζ = (0,1) ∈ N^P_K((0,0)) ✓ — the vertex of the below-parabola region has an upward proximal normal ✓ (makes sense: it's a reentrant... wait is the vertex reentrant for K = below-parabola? K contains points below; the missing region is above the parabola: at the vertex, the missing region is the thin parabola interior: K's interior angle at the vertex: the two branches leave the vertex with slopes 0 (tangent horizontal): the missing region between the branches above the vertex: angle 0° — a NEEDLE-notch: K's interior angle at vertex = 360° — infinitely reentrant (cusp). N^P at the vertex: computed (0,1) ✓ — for the cusp notch, the "corner" normal points INTO the notch ✓ (this is a reentrant cusp: proximal normal EXISTS (unlike the L-corner) because the notch is thin: exterior points above the vertex project to the vertex). What is the FULL proximal normal cone at the vertex? Exterior point (τ, y): projects to vertex iff τ² + y² ≤ a² + (a² − y)² ∀a ⟺ τ² ≤ a² + (a²−y)² − y² = a² + a⁴ − 2a²y = a²(1 + a² − 2y) ∀a: take a → 0: RHS → 0: need τ² ≤ 0: τ = 0!! So only exterior points with τ = 0 (directly above) project to the vertex: N^P_K(vertex) = ℝ₊·(0,1) — a thin ray ✓✓. GOOD: so the below-parabola region has thin proximal normals everywhere (smooth boundary + cusp with ray normal): **uniform strict margin is POSSIBLE** (no fat-cone obstruction)! Now the EROSION: K⊖s = {dist(x, K^c) ≥ s}: near the vertex: the notch {x₂ > x₁²} is a needle: its s-neighborhood: points at distance ≥ s from the notch: near the vertex, the notch is thin: dist((0, −h), K^c): (0,−h) ∈ K: distance to {x₂ > x₁²}: nearest notch point: from below the vertex: (0, −h): distance to the parabola region: minimize a² + (a² + h)²: g(a) = a² + (a²+h)²: g(0) = h²: g'(a) = 2a(1 + 2(a² + h)) > 0 for a > 0: min at a = 0: distance = h: so (0,−h) is at distance h from the notch (the vertex is the nearest notch... the notch is OPEN {x₂ > x₁²}: dist((0,−h), notch) = h (infimum at the vertex (0,0) ∈ ∂notch ✓). So the eroded set near the axis: {(0, −h): h ≥ s} ⊆ K⊖s ✓: the erosion pushes the cusp tip down to (0, −s)?? But ALSO laterally: point (x₁, x₂) with x₂ ≤ x₁²: distance to the notch: the notch = {x₂ > x₁²}: distance from (x₁, x₂) ∈ K to notch ≈ (vertical distance) x₁² − x₂ if the vertical foot is closest: vertical foot (x₁, x₁²): distance x₁² − x₂ ✓ (if that's the min — plausible near the boundary). So K⊖s ≈ {x₂ ≤ x₁² − s} near the vertex: eroded set = below-parabola shifted down by s: eroded cusp tip at (0, −s): eroded boundary: parabola x₂ = x₁² − s: at the eroded tip (0,−s): normals of eroded set: ray (0,1) ✓ (same structure, by the shift). Margin transfer: ⟨(0,1), f(0,−s)⟩ = f₂(0,−s) ≤ f₂(0,0) + Ls ≤ −m + Ls < 0 for s < m/L ✓. Walls: parabola shifted: normals at (a, a² − s) = normals at (a, a²) of K ✓ same directions: margin transfers by Lipschitz: −m + Ls. SO THE BELOW-PARABOLA CUSP ALSO SURVIVES?!?! Hmm!!! Wait — is the erosion of the below-parabola region really the shifted parabola region? dist to the notch: for points BELOW, the vertical foot isn't always nearest: for the region below a CONVEX curve: the distance to the complement... K = {x₂ ≤ x₁²} = hypograph of CONVEX ψ = x₁²: the complement/hnotch = epigraph {x₂ > x₁²}: for a point (x₁, x₂) with x₂ < x₁²: the nearest point of the closed epigraph {x₂ ≥ x₁²}: project onto the convex... the epigraph is CONVEX ✓: distance from a point below to the convex epigraph: the projection: normal cone of epigraph at the foot: foot = (a, a²) with normal (−ψ'(a), 1)/√: the projection condition: (x₁,x₂) − (a, a²) ∥ (−2a, 1): so points below the parabola project onto the parabola (not beyond the vertex) — the projection is onto the curve where the normal condition holds; near the vertex: points almost directly below project to the vertex region... For x = (0, −h): projection: (0,0)?? but (0,0) is on the boundary of the epigraph (vertex): normal cone of EPIGRAPH at vertex (0,0): the epigraph {x₂ ≥ x₁²} at its vertex: normal cone = ℝ₊·(0,−1)?? The epigraph is convex; at the vertex (0,0) (its lowest point), the normal cone = {(0, −t): t ≥ 0} (downward ray) ✓: projection of (0,−h) onto epigraph: (0,0) ✓ (since (0,−h) − (0,0) = (0,−h) ∈ normal cone ✓) ✓. So dist((0,−h), notch) = h ✓. Erosion: {x : dist(x, epi-closure) ≥ s}: the set of points at distance ≥ s from the convex epigraph = the epigraph "shrunk" from below = {x₂ ≤ x₁² − s}?? NO: distance ≥ s from the epigraph: the epigraph is ABOVE: points far BELOW are at distance ≥ s: the boundary of the erosion = the curve at distance s below the parabola = the parallel curve: NOT the shifted parabola! The parallel curve at distance s below a parabola: exists while s < 1/κ_max... parabola curvature at vertex = 2: 1/2: for s < 1/2: the downward parallel curve is smooth ✓: it's NOT the shifted parabola (it's the offset curve), but it's close near the vertex: offset curve at vertex: (0, −s) ✓ same tip. Normals of the erosion at the offset-boundary: SAME normal directions as the corresponding foot points on the parabola (offset curves share normals) ✓✓. So STILL the margin transfers (normals of eroded boundary = normals of original boundary at the foot, and |foot − x| = s: Lipschitz: ⟨ν_foot, f(x)⟩ ≤ ⟨ν_foot, f(foot)⟩ + Ls ≤ −m + Ls ✓). STILL SURVIVES. DAMN.

So when does it FAIL?? The offset curve at distance s exists only for s < reach = 1/κ_max. For s ≥ 1/2 (parabola): the erosion boundary develops singularities from the EVOLUTE: the eroded set's boundary near the evolute: new corners whose normals are NOT normals of the original boundary at any single point — they're limits/normals at distance... at s > reach: the erosion K⊖s: for the below-parabola: s > 1/2: the erosion boundary = offset curve develops cusps (on the concave side): the eroded set near the vertex: points at distance ≥ s from the epigraph: the evolute of the parabola (the semi-cubical parabola x₂ = ... at (0, 1/2)?? the evolute of x₂ = x₁²: center of curvature at vertex = (0, 1/2): the evolute lives ABOVE the parabola?? For the offset BELOW: the relevant focal point: the center of curvature at the vertex is (0, 0 + 1/2·(0,1)) = (0, 1/2)?? curvature center is on the concave side: parabola opens upward: concave side = above: center at (0, 1/2): that's INSIDE THE NOTCH. The erosion (offset below, distance s): the offset curve = locus (a, a²) − s·ν(a) where ν = upward unit normal (−2a, 1)/√(1+4a²): (a, a²) − s(−2a, 1)/√(1+4a²): at a = 0: (0, −s) ✓; as |a| grows: the offset curve: is it smooth for all a when s < 1/2? The curvature κ(a) = 2/(1+4a²)^{3/2}: max at a = 0: κ = 2: reach = 1/2: for s < 1/2: smooth ✓. For s > 1/2: near the vertex the offset curve self-intersects/cusps: the TRUE erosion boundary (points at distance exactly s with unique... ) the eroded set = {dist ≥ s}: its boundary near the axis: by symmetry the tip is at (0, −s)?? dist((0,−s), epigraph) = s ✓ boundary ✓. Points (x₁, x₂) near: the eroded set's boundary: for s > 1/2: consider the medial-axis: the erosion boundary = the s-level set of the distance-to-epigraph function: the distance function is semiconcave; its s-level: for s > 1/2 the level set near the vertex: hmm, the distance function to the epigraph near the vertex: dist((x₁,x₂), epi): for points below: ≈ min over a of distance to (a, a²): the s-level set: {(x₁, x₂): min_a |(x₁,x₂) − (a,a²)| = s}: this is the boundary of the union of balls B((a,a²), s) over a: = boundary of the s-neighborhood of the parabola (from below): the s-neighborhood of the parabola curve from below: for s > 1/2: the neighborhood's lower boundary: balls of radius s centered on the parabola: near the vertex: the ball B((0,0), s) dips to (0,−s): balls at (a, a²) dip to (a, a²) − s·ν(a): for s > 1/2: does the ball at a ≠ 0 dip below the ball at 0 near the axis? The envelope: the lower boundary of ⋃_a B((a,a²), s): the union's boundary = offset curve where it exists + possibly arcs of individual balls: for s > 1/2: the offset curve (envelope of the family) has a cusp at the evolute: the LOWER envelope near the vertex: min over a of [(x₁ − a)² + (x₂ − a²)²]: the s-level of this min: For x₁ = 0: min_a [a² + (x₂ − a²)²]: as before g(a) = a² + (x₂ − a²)²: for x₂ = −s + something... The key question: the ERODED SET'S boundary points and their proximal normals: at the tip (0, −s): exterior point (0, −s + τ): projection onto eroded set: is (0,−s) the unique nearest eroded point? The eroded set = complement of the s-neighborhood: near the axis below: eroded set = {(x₁,x₂): dist ≥ s}: (0,−s+τ) has dist = s − τ < s: exterior ✓: its nearest eroded point: points at distance exactly... hmm, the projection of (0, −s + τ) onto the eroded set: the eroded set's boundary near the axis: if the boundary near (0,−s) is just the two offset branches meeting at a CUSP (for s > 1/2 the offset curve has a cusp at the axis? The evolute cusp: the offset at s = reach has a cusp; for s > reach: the offset curve is not the boundary; the boundary = envelope of balls: near the vertex for s > 1/2: I claim the boundary contains an ARC OF THE BALL B((0,0), s) around (0,−s)!! Because for s > 1/2, near the vertex the parabola curves away faster than the ball: the union of balls near (0,−s): the ball at a=0 covers the disk of radius s around origin; balls at small a: centers (a, a²) ≈ (a, 0): they cover disks radius s around (a, ~0): the union near (0,−s): the lower envelope: min over a of the lower semicircles: lower semicircle of ball at a: x₂ = a² − √(s² − (x₁−a)²): minimize over a: d/da: 2a + (x₁−a)/√(s²−(x₁−a)²) = 0: at x₁ = 0: 2a − a/√(s² − a²)·... let me just: for s > 1/2: at x₁ = 0: minimize h(a) = a² − √(s² − a²): h(0) = −s: h'(a) = 2a + a/√(s²−a²) = a(2 + 1/√(s²−a²)) > 0 for a > 0: so min at a = 0 ✓: the lowest point at x₁ = 0 is from ball a=0: (0,−s) ✓. For x₁ ≠ 0 small: min over a of a² − √(s² − (x₁−a)²): try a between 0 and x₁: h(a) = a² − √(s² − (x₁−a)²): h'(a) = 2a + (x₁−a)/√(s²−(x₁−a)²): setting 0: 2a√(s²−(x₁−a)²) = −(x₁−a) = a − x₁: need a > x₁... a−x₁ > 0 ⟹ a > x₁: but then LHS > 0 and RHS > 0: possible: a√(s²−(x₁−a)²) = (a−x₁)/2: for x₁ > 0 small: solution a* ≈ x₁ + something: hmm at a = x₁: LHS 0 < RHS 0 — equal: a = x₁ gives h' = 2x₁ > 0. Hmm so for a slightly less than x₁: h'(a) = 2a + (x₁−a)/√(...): positive if x₁ > a... at a < x₁: both terms positive: h' > 0: h increasing on [0, x₁]: min at a = 0: h(0) = −√(s² − x₁²) ≈ −s + x₁²/2s. So near the axis, the lower boundary ≈ the ball a=0's lower arc: x₂ ≈ −√(s² − x₁²): THE ERODED SET'S BOUNDARY NEAR THE TIP IS A CIRCULAR ARC (of radius s around the ORIGIN-vertex) — for s > 1/2 = reach!! (For s < 1/2, the envelope would be the offset curve — let me double check with s < 1/2: h'(a) = 2a + (x₁−a)/√(s²−(x₁−a)²): the second term can be very negative when (x₁−a) → ±s: but near the bottom of balls: for s < 1/2: the curvature of the parabola (2) exceeds 1/s: balls overlap heavily: the envelope = offset curve: classic. For s > 1/2: individual ball arcs are exposed ✓.) So for s > reach: eroded boundary near the cusp tip = circular arc centered at the VERTEX (0,0) — radius s: and the eroded set is OUTSIDE this ball (below): so the eroded set near the tip = complement of B((0,0), s) ∩ K-ish: the boundary arc's normals: pointing TOWARD the origin (center): outward normal of eroded set at arc point y (|y| = s, below): eroded set is outside the ball: outward (leaving eroded set) = INTO the ball = toward origin: ν = −y/s. These normal directions: along the arc from (0,−s): ν = (0,1) at the tip... at y = (s sinθ·, −s cosθ): ν = (−sinθ, cosθ): rotating through directions between (0,1) and (±1, 0)-ish as we move along the arc: the arc extends until it meets the offset curve branches (at the tangency points). So the eroded boundary near the tip has normals sweeping a RANGE of directions — all of which are... the vertex's CLARKE normal cone? K's normals at the vertex: N^P_K(vertex) = ray (0,1); but the arc normals (−sinθ, cosθ) with θ ∈ (−θ₀, θ₀): these include directions with nonzero horizontal components — which are NOT proximal normals of K at the vertex, nor normals at any single boundary point of K (they're "diagonal" — limits of normals at points approaching the vertex along the parabola? Normals of K at parabola point (a, a²): (−2a, 1)/√(1+4a²) → (0,1) as a → 0: so limiting normals at the vertex: only (0,1). The arc normals (−sinθ, cosθ), θ ≠ 0: NOT limiting normals of K at the vertex — they're normals at distance s "mediated" by the ball geometry: the evolute/medial axis phenomenon: the erosion's new boundary normals are the normals of the s-ball at the vertex, spanning the angular range where the ball is exposed: range determined by where the parabola escapes the ball: the ball B(0, s) contains parabola points (a, a²) with a² + a⁴ < s²: |a| < a₀ ≈ √(s − ...): the arc is exposed for |x₁| up to where the offset/other balls take over: roughly |sinθ| ≤ 2a₀-ish... The exact range doesn't matter: KEY POINT: for s > reach(K) = 1/2, the eroded set's boundary near the cusp has proximal normals in directions that are absent from K's proximal (and even limiting) normal structure — the margin hypothesis on ∂K says NOTHING about these directions: put the velocity pointing along such a direction and trajectories cross the eroded boundary while never violating the margin on ∂K.

Wait — but I must double-check the margin is consistent: K = {x₂ ≤ x₁²}: margins on ∂K: at vertex: N^P = ray (0,1): margin: f₂(0,0) ≤ −m. On the parabola (a, a²), a ≠ 0: normals ±(−2a,1)/√(1+4a²) — wait which sign is outward for K = below-parabola? Outward = into the notch = upward-ish: (−2a, 1)/√(1+4a²) has positive x₂-component ✓ outward ✓. Margin: ⟨(−2a,1)/√, f(a, a²)⟩ ≤ −m: i.e., (f₂ − 2a f₁)/√(1+4a²) ≤ −m: as a → 0: f₂(0,0) ≤ −m ✓ consistent. For LARGE a: the normal → (∓1, 0): margin ⟹ ∓f₁ ≤ −m: |f₁| ≥ m with correct sign: f₁(a, a²) ≈ 2a·m·? — fine, designable. Now design f (with feedback built in, no disturbance): let f(x) = (0, −m) + something: constant field f ≡ (0, −m): margins: at vertex: ⟨(0,1), (0,−m)⟩ = −m ✓; at parabola points: (f₂ − 2a f₁)/√ = −m/√(1+4a²) ≥ −m... we need ≤ −m: −m/√(1+4a²) > −m (since √ > 1): FAILS for a ≠ 0: margin at (a, a²) is −m/√(1+4a²) ∈ (−m, 0): not uniformly −m: it's still NEGATIVE (strictly inward) but not uniformly bounded by −m... The margin constant: inf over boundary = inf over a of m/√(1+4a²) = 0 as |a| → ∞!! Because the normal becomes horizontal and the vertical field is asymptotically TANGENT to the steep parabola: uniform margin fails for constant vertical field on an unbounded parabola (the parabola gets steeper; a vertical field eventually points along it). Fix: truncate K: make the notch FINITE: K = {x₂ ≤ x₁²} ∩ B̄(0, R)-ish, or modify ψ to flatten: ψ(x₁) = x₁² for |x₁| ≤ 1, then smoothly flatten to slope bounded: e.g., ψ = x₁²/(1+x₁²)·? or just take K = {x₂ ≤ ψ(x₁)} with ψ(x₁) = x₁² for |x₁| ≤ 1/4 and ψ ≡ linear with slope ±1/2 beyond: ψ ∈ C¹ (match slopes: at x₁ = 1/4: slope 1/2 ✓), curvature = 2 on |x₁| ≤ 1/4, 0 beyond. Then with the constant field f ≡ (0, −m): margin at (a, ψ(a)): ⟨ν, f⟩ = −m·ν₂: ν₂ = 1/√(1+ψ'²) ≥ 1/√(1+1/4) = 2/√5: margin ≥ 2m/√5 > 0 UNIFORM ✓✓. Also need K closed and the "rest" of the boundary (wherever we close it) handled: close the notch-region K downward: K = {x₂ ≤ ψ(x₁)} with ψ ∈ C¹, ψ' bounded (|ψ'| ≤ 1/2), ψ(x₁) = x₁² near 0, ψ bounded below? For K to be closed and bounded-ish... K = hypograph of ψ: if ψ: ℝ → ℝ bounded above... hypograph is closed iff ψ usc ✓ C¹ fine. Unbounded below: fine for invariance (field points down, trajectories go down forever: forward complete? f = (0,−m): x₂ → −∞ linearly: complete ✓ no escape). But hmm: is K robustly invariant under f ≡ (0,−m)? Trajectories: straight down: from any x ∈ K: x(t) = (x₁⁰, x₂⁰ − mt): x₂ decreasing: stays ≤ ψ(x₁⁰) ✓ IN K ✓✓. Margin on ∂K: shown ≥ 2m/√5 ✓ uniform. Feedback: k trivial (no control needed — take U a singleton or let f be the closed loop). Disturbance: none (D singleton) or add robustness: f_d = (0, −m) + small bounded: still fine. NOW THE EROSION: K⊖s for s > reach(K): reach of K: K's boundary = graph of ψ: curvature κ = ψ''/(1+ψ'²)^{3/2} = 2 near 0: the REENTRANT side... wait which side has the reach issue: reach of the SET K = hypograph: the relevant failure: points in the NOTCH near the cusp having non-unique projections: computed: (0, ε) projects uniquely to vertex ✓; two symmetric points... the medial axis of the notch: points equidistant to two parabola branches: on the axis x₁ = 0: distance to branch at a and −a: equal: but is the projection unique: (0, y), y > 0: g(a) = a² + (y − a²)²: minima at a = 0 (vertex, distance y) and possibly at a = ±√(y − 1/2) for y > 1/2 (from g'(a) = 2a(1 + 2(a² − y)) = 0: a² = y − 1/2: for y > 1/2: distance² = (y − 1/2) + (1/2)² = y − 1/4 < y²?? y − 1/4 < y² ⟺ y² − y + 1/4 > 0 ⟺ (y − 1/2)² > 0 ✓ always (y ≠ 1/2): so for y > 1/2: the projections onto the BRANCHES (a = ±√(y−1/2)) are CLOSER than the vertex: dist = √(y − 1/4) < y: and there are TWO of them (±): NON-UNIQUE PROJECTION: (0, y) for y > 1/2 is on the medial axis (equidistant to both branches): so reach(K) = 1/2 ✓ (points at distance > 1/2 from K along the axis have 2 projections — actually the definition of reach: sup r such that EVERY point within distance r of K projects uniquely: at (0, y), y ∈ (0, 1/2]: unique (vertex) ✓; y slightly > 1/2: two projections: reach = 1/2 ✓✓.)

So: for s > 1/2: erosion K⊖s: boundary near the cusp = arcs of balls centered at boundary points?? Wait — the erosion = {dist(x, K^c) ≥ s}: K^c = the notch {x₂ > ψ(x₁)}: the erosion = points at distance ≥ s from the notch: near the cusp: the notch is thin: dist((x₁,x₂), notch) for x below the cusp: = dist to the closed notch = dist to the graph region: as computed: for s > 1/2: the erosion boundary near the axis = arc of the ball B((0,0), s)?? Hmm wait: the erosion is {dist(·, notch) ≥ s} = complement of the s-neighborhood of the notch. The s-neighborhood of the notch near the cusp = ⋃_{y ∈ notch} B(y, s): the notch includes points just above the vertex AND the whole upper region: the s-neighborhood from below: the LOWER boundary of the s-neighborhood of the notch = the upper... hmm I need the boundary of the erosion = {dist = s}: near the axis BELOW the vertex: dist((0, x₂), notch): for x₂ < 0: = −x₂ (distance up to the vertex (0,0) ∈ cl notch... wait (0,0): x₂ = 0 = ψ(0): (0,0) ∈ ∂notch ⊂ cl(notch): dist((0,x₂), notch) = |x₂| for x₂ ≤ 0?? Only if the nearest cl-notch point to (0, x₂) is the vertex: from the computation: g(a) = a² + (x₂ − a²)² minimized at a = 0 for x₂ < 1/2: for x₂ < 0: g(a) = a² + (x₂ − a²)²: g'(a) = 2a(1 + 2(a² − x₂)): for x₂ < 0: a² − x₂ > 0: g' > 0 for a > 0: min at 0 ✓: dist = |x₂| ✓. So dist((0, x₂), notch) = −x₂ for x₂ ∈ (−∞, 1/2): so the erosion on the axis = {x₂ ≤ −s} ✓ tip at (0, −s) ✓ (for ANY s — even s > 1/2: dist((0,x₂), notch) = −x₂ as long as −x₂... wait for x₂ very negative the nearest notch point might be the branches at |x₁| large?: g(a) = a² + (x₂ − a²)²: for x₂ = −10: minimize: d/da: 2a + 4a(a² − x₂)·... = 2a(1 + 2(a² − x₂)) = 2a(1 + 2(a² + 10)) > 0: min at a = 0 ✓ dist = 10 ✓ (vertex nearest: makes sense: the notch is a needle above, the vertex is the lowest notch point). ✓. Now off-axis: (x₁, x₂), x₁ ≠ 0: dist to notch = min_a [(x₁ − a)² + (x₂ − ψ(a))²] (projection onto the graph, for points below-ish): the erosion boundary {dist = s}: near the tip for s > 1/2: is it the ball-arc around the vertex? The s-level set: min_a [...] = s: at the tip (0,−s): the minimizing a = 0 (vertex). Nearby (x₁, −s + δ...): hmm: the level set near (0,−s): the distance function D(x₁, x₂) = min_a |x − (a, ψ(a))|: near (0, −s): D ≈ ? The distance to the CURVE (graph) — but the notch is the region ABOVE the graph: for points below the graph, dist to the region = dist to the graph (boundary) ✓: so D(x) = dist(x, graph(ψ)) for x below: the s-level set of the distance-to-curve function: for s < reach of the curve... the curve (graph of ψ) as a SET has reach 1/2 (from the medial axis computation? the medial axis of the curve: points equidistant to two curve points: (0, y), y > 1/2 ✓): so for s < 1/2: the s-level set below the curve = smooth offset curve ✓; for s > 1/2: the s-level set: near the axis: D(0, x₂) = |x₂| for x₂ < 1/2 (vertex nearest, unique for x₂ < 1/2... wait the non-uniqueness was for x₂ > 1/2: for x₂ ∈ (−∞, 1/2): unique projection to vertex ✓): D(0, −s) = s ✓; for x₁ ≠ 0 small: D(x₁, x₂): the nearest curve point: could be vertex or branch points: for s > 1/2: consider (x₁, −s): nearest curve point: min_a (x₁−a)² + (−s − a²)² (using ψ = a² near 0): g(a) = (x₁ − a)² + (s + a²)²: g(0) = x₁² + s²; g'(a) = −2(x₁−a) + 4a(s + a²): at a = 0: g'(0) = −2x₁: for x₁ > 0: g decreasing at 0: min at a* > 0: g(a*) < s² + x₁²: so the nearest point is NOT the vertex: the level set near (0, −s) is determined by the envelope: the s-level set of D: {(x₁,x₂): min_a g = s}: for s > 1/2: hmm: is the level set near the tip the circular arc {x₁² + (x₂ + ... } — Let me directly test: is the point (x₁, x₂) = (s sinθ, −s cosθ) (on the circle of radius s around ORIGIN) at distance s from the curve? D((s sinθ, −s cosθ)) = min_a [(s sinθ − a)² + (a² − s cosθ)²]... wait x₂ = −s cosθ: distance to (a, a²): (s sinθ − a)² + (−s cosθ − a²)²: at a = 0: s² sin²θ + s² cos²θ = s² ✓: so D ≤ s: and if the vertex is the UNIQUE minimizer, D = s: but we need to check other a: g(a) − s² = (a − s sinθ)² + (a² + s cosθ)² − s² = a² − 2as sinθ + s²sin²θ + a⁴ + 2a² s cosθ + s²cos²θ − s² = a² − 2a s sinθ + a⁴ + 2a² s cosθ = a[a − 2s sinθ + a³ + 2 a s cosθ]: for small θ, a > 0: the bracket at small a: ≈ −2s sinθ + a(1 + 2s cosθ) + a³: NEGATIVE for a < a* ≈ 2s sinθ/(1 + 2s cosθ): so g(a) < s² for a ∈ (0, a*): the nearest curve distance is LESS than s: so points on the circle around the origin (θ ≠ 0) are at distance < s from the CURVE — because the curve RISES (a² > 0) toward them?? Hmm wait: the curve point (a, a²) with a > 0 is ABOVE the vertex: the circle point (s sinθ, −s cosθ) is below-right: distance² = (s sinθ − a)² + (s cosθ + a²)²: with the a² term INCREASING the vertical gap... but the horizontal term decreases: for a ≈ s sinθ: horizontal ≈ 0: vertical = s cosθ + a²: distance² ≈ (s cosθ + s² sin²θ)² vs s²: (s cosθ + s² sin²θ)² < s² ⟺ s cosθ + s² sin²θ < s ⟺ s² sin²θ < s(1 − cosθ) ⟺ s sin²θ < 1 − cosθ ⟺ s(1 + cosθ) < 1 (using sin²θ = (1−cosθ)(1+cosθ)): so IF s(1 + cosθ) < 1: i.e., θ LARGE (cosθ small): the branch point beats the vertex... wait I think I have the inequality direction confused. Let me recompute cleanly: circle point y = (s sinθ, −s cosθ), θ > 0 small (just right of the bottom). Vertex (0,0): |y − 0|² = s². Branch point (a, a²), a > 0: |y − (a, a²)|² = (s sinθ − a)² + (s cosθ + a²)². We want to know whether this is < s². Expand: s² sin²θ − 2as sinθ + a² + s² cos²θ + 2s cosθ a² + a⁴ − s² = −2as sinθ + a²(1 + 2s cosθ) + a⁴. For small a > 0: ≈ a(−2s sinθ + a(1 + 2s cosθ)): negative for a < 2s sinθ/(1+2s cosθ) ✓ positive a range exists: SO YES: g(a) < s²: the branch point (a, a²) is CLOSER to y than the vertex is: D(y) < s: so y is NOT on the erosion boundary (it's inside the s-neighborhood: D < s: NOT in the erosion). Hmm!! So near the tip, the s-level set is NOT the circle around the origin — the branches "cut in": the erosion boundary = the offset curve which for s > 1/2... but the offset curve doesn't exist (self-intersects at the evolute)... Let me reconsider: the s-LEVEL SET of D near the axis: points with D = s: by symmetry consider x₁ ≥ 0: the boundary point at "height" x₂: find (x₁, x₂) with min_a g = s: the minimizer a*(x) satisfies the normal condition: (x₁,x₂) − (a, a²) ∥ (−2a, 1) (normal to the parabola at (a, a²) pointing down-ish: normal directions ±(−2a, 1)/√: the one pointing AWAY from the notch (downward): (2a, −1)/√(1+4a²)): so boundary points at distance s from the curve lie at (a, a²) + s·(2a, −1)/√(1+4a²): the OFFSET CURVE (below): x₁(a) = a + 2sa/√(1+4a²), x₂(a) = a² − s/√(1+4a²). The offset curve parametrized by a: at a = 0: (0, −s) ✓ the tip. For a > 0: x₁ > 0 ✓: does the offset curve have a cusp/turnover for s > 1/2? dx₁/da = 1 + 2s/(1+4a²)^{3/2} > 0 ✓ always increasing: so the offset curve is a GRAPH over x₁ (no self-intersection from below?): hmm: dx₂/da = 2a + 4sa/(1+4a²)^{3/2}·? d/da[−s(1+4a²)^{−1/2}] = −s·(−1/2)(1+4a²)^{−3/2}·8a = 4sa(1+4a²)^{−3/2}: so x₂'(a) = 2a + 4sa/(1+4a²)^{3/2} = 2a[1 + 2s/(1+4a²)^{3/2}] > 0 for a > 0: x₂ increasing in a: so the offset curve rises from (0,−s) monotonically: NO cusp?! But the classical result: offsets of a parabola at distance > 1/κ have cusps — that's for offsets toward the CONCAVE side (above): our offset is toward the CONVEX side (below): convex-side offsets are always smooth (they're the "outward" parallels: for the region below the parabola, the erosion offset is on the convex side of the curve = always fine, reach ∞ side)! Hmm!! Wait — I need to recheck the reach/medial axis: the non-unique projection point (0, y), y > 1/2: which side: ABOVE the vertex: INSIDE THE NOTCH: the medial axis of the CURVE is inside the notch: for the SET K = below-parabola: points above the vertex: are they within distance... the reach of K as a SET: points x ∉ K near K: x in the notch: (0, y), 0 < y < 1/2: unique projection (vertex) ✓; y > 1/2: two projections (branches) — but y > 1/2 means dist to K = √(y − 1/4)?? wait dist((0,y), K): K = {x₂ ≤ a²-curve}: nearest K points: the branches at a = ±√(y − 1/2): distance² = (y−1/2) + (y − (y−1/2))² = y − 1/2 + 1/4 = y − 1/4: dist = √(y − 1/4): and at y = 1/2: dist = 1/2·... √(1/4) = 1/2: hmm wait dist((0, 1/2), K) = √(1/2 − 1/4) = 1/2: but the vertex is at distance 1/2 too ✓ (the branch projections: at y = 1/2: a² = 0: same point: degenerate ✓). So: points in the notch at distance < 1/2 from K project uniquely ✓; the medial axis of K (as a set) = the segment {(0, y): y > 1/2}: distance from K: √(y − 1/4) > 1/2 ✓: so reach(K) = 1/2 ✓✓: the reach is limited by the notch-side medial axis: BUT the EROSION (from inside K, offsetting the boundary downward = the convex side!) only needs offsets on the INNER side: the inner parallel set K⊖s: its boundary = the downward offset curve: smooth for ALL s?? Then the erosion never encounters the medial axis?! Hmm — wait: that can't be right: the erosion K⊖s = {x : x + sB ⊆ K}: near the cusp: is (0, −s) really in K⊖s? B((0,−s), s) ⊆ K = {x₂ ≤ ψ(x₁)}? The ball around (0,−s) radius s: its top point is (0,0): the ball near (0,0): does the ball stay below the curve ψ(x₁) = x₁²? Ball: x₁² + (x₂ + s)² ≤ s² ⟺ x₂ ≤ −s + √(s² − x₁²) ≈ −x₁²/2s: need ball ≤ ψ = x₁²: −x₁²/(2s) ≤ x₁² ✓ ALWAYS (LHS negative!) ✓✓: so B((0,−s), s) ⊆ {x₂ ≤ x₁²} ✓: (0,−s) ∈ K⊖s ✓: and in fact points (0, −s + δ)?: ball top at (0, δ): need B((0,−s+δ), s) ⊆ K: near the top (0, δ): the ball boundary: x₂ ≈ δ − x₁²/(2(s−δ)): need ≤ x₁²: δ − x₁²/(2(s−δ)) ≤ x₁² ⟺ δ ≤ x₁²(1 + 1/(2(s−δ))): for x₁ → 0: RHS → 0 < δ: FAILS: so (0, −s+δ) ∉ K⊖s ✓ consistent: tip at (0,−s) ✓. So the eroded set's tip = (0, −s) for ALL s > 0 and the eroded boundary near the tip = the smooth downward offset curve ✓✓. So the below-parabola cusp erosion is FINE?!?! Hmm wait, but hold on: K⊖s for LARGE s: the ball B(x, s) ⊆ K: near the cusp, K is thin?? K = below-parabola is NOT thin — it's everything below a graph: hugely fat downward. The notch is a needle ABOVE. The erosion only shrinks the notch-side boundary inward (downward): the needle notch gets... the eroded set near the notch: the boundary = offset of the graph: the offset of a needle-shaped valley: the valley x₂ = x₁² (width at height h: 2√h): offset downward by s: x₂ = x₁² − s: the eroded "valley" is the same shape shifted: fine: smooth. So NO failure: hypographs of C¹ graphs with bounded slope: erosion = nice offset: margins transfer via the shared-normal property of offsets: dist(foot, x) = s: Lipschitz: margin − m + Ls: INVARIANT for s < m/(2L)·... ALWAYS FINE. Hmm!!! So smooth-graph hypographs are fine. WHAT SETS FAIL?? Let me reconsider. The failure of "erosion inherits margins" requires the eroded boundary to have normals NOT matching (nearby) original normals: this happens exactly when the offset crosses the MEDIAL AXIS (cut locus) of K — i.e., s > reach: then the eroded set's boundary contains points whose projection structure changes: new boundary = "shocks": the eroded set near a medial-axis crossing: boundary points equidistant from TWO boundary pieces with DIFFERENT normals: the eroded boundary develops CORNERS whose normals are combinations... but wait: in the hypograph case, the medial axis was on the OUTSIDE (notch side): the inner offset never crosses it. When is the medial axis INSIDE K? When K has "thick" features: e.g., K = a SLAB or a set with internal bottlenecks: K = two blobs connected by a thin neck: the medial axis inside the neck: eroding by s > neck-half-width: the neck PINCHES OFF: the eroded set = two disconnected blobs: the pinch point: the eroded boundary near the pinch: new corner with normals from BOTH sides of the neck: THE PINCH-OFF IS THE FAILURE MODE! At the pinch, the eroded set's boundary has a corner whose proximal normals include directions from both neck walls — but wait: if K is say the region between two graphs (a neck: K = {φ₁(x₁) ≤ x₂ ≤ φ₂(x₁)} with the graphs converging: neck width w(x₁) → 0 at x₁ = 0): K = hourglass: erosion by s > w(0)/2 pinches: eroded set = {x : dist(x, K^c) ≥ s}: at the pinch: the eroded set's boundary near the pinch-point: the two offset curves (from upper and lower walls) meet: corner: normals at the corner: the proximal normals of the eroded set at the pinch corner: exterior point at the pinch: projects to... the pinch corner: normals = cone generated by the two wall normals (which are roughly OPPOSITE: upper wall normal points down-ish (into K), lower wall normal points up-ish (into K)): at the pinch, the eroded set's outward normals: hmm: the eroded set near the pinch = two blobs; the pinch point y* (on the eroded boundary of, say, the left blob): its proximal normals: exterior points just right of y* (in the pinched gap): project to y*?: the gap is the s-neighborhood of the complement... the nearest eroded-set point to a gap point: could be the left blob's pinch corner or the right blob's pinch corner: equidistant: the medial axis of the ERODED set passes through the gap: so exterior gap-points on the axis have TWO projections: but off-axis gap points project uniquely to one corner: N^P at the pinch corner: directions pointing into the gap from that corner: a FAT cone?? or thin? Consider the simplest hourglass: K = {(x₁, x₂): |x₂| ≤ w(x₁)} with w(x₁) = 1 + |x₁|·? no wait we want a NECK: w small at x₁ = 0: w(x₁) = 1 − e^{−...}: let w(x₁) = |x₁|^β + w₀ with w₀ > 0 small: neck half-width w₀ at x₁ = 0, widening: K = {|x₂| ≤ |x₁|² + w₀} say (so walls: x₂ = ±(|x₁|² + w₀)): K is a closed "bowtie-less" strip with a smooth neck of half-width w₀ at x₁ = 0. Hmm wait: walls converge to ±w₀ at 0: neck width 2w₀. Erosion by s: K⊖s = {dist(x, K^c) ≥ s}: K^c = {|x₂| > |x₁|² + w₀}: near the neck: dist((x₁, 0), K^c) = |x₁|² + w₀ (vertical to the walls — if vertical is the nearest: walls have slope 2|x₁|: near 0: shallow ✓ vertical nearest ✓): so K⊖s ∩ axis = {|x₁|² + w₀ ≥ s} = {|x₁| ≥ √(s − w₀)} for s > w₀: PINCHED: the eroded set near the neck = two components meeting... at s = w₀: the eroded set's neck = single point (0,0)?? dist((0,0), K^c) = w₀: at s = w₀: (0,0) ∈ ∂(K⊖s): and for s > w₀: (0,0) ∉ K⊖s: the eroded set = {|x₂| ≤ |x₁|² + w₀ − s-ish}: the eroded walls: offset curves: x₂ = ±(|x₁|² + w₀) ∓ s·(normal): near the pinch: the eroded set for s slightly > w₀: two blobs joined... no: {|x₂| ≤ |x₁|² + w₀ − s}: at |x₁| < √(s − w₀): empty: so TWO components (left and right) — wait no: {|x₂| ≤ |x₁|² + w₀ − s}: for |x₁| large: fine: it's a strip that's empty near x₁ = 0: TWO components ✓ pinched off. The eroded boundary near the pinch: corners at (±√(s−w₀), 0)-ish where the two offset walls (upper and lower) MEET: at the meeting point: the eroded set's boundary has a corner: normals: the upper wall offset's normal (pointing up, out of the eroded set) and the lower's (pointing down): the corner is a REENTRANT... the eroded set near the meeting point: {|x₂| ≤ |x₁|² + w₀ − s}: locally like a cusp pointing RIGHT (for the left component: the component extends leftward, the boundary pinches at x₁ = −√(s−w₀)): the pinch corner of the LEFT component: locally the set = {x₂² ≤ (x₁ + √(s−w₀))²·...}: a needle tip pointing right: CONVEX tip? The left component near its rightmost tip: {(x₁, x₂): x₁ ≤ −√(s−w₀) + tiny, |x₂| ≤ f(x₁)}: the tip is like the below/above-parabola cusp: a NEEDLE TIP: whose proximal normals: as computed for needle tips: FAT cone (the needle tip of the below-parabola had N^P(tip) = ray only because the needle was infinitely sharp (C¹ with horizontal tangent): for a needle tip with a well-defined opening angle (two walls meeting at angle 2γ < π: convex corner: N^P = the full normal CONE = angle π − 2γ: fat if γ small). In our case: the eroded walls near the pinch: x₂ = ±(|x₁|² + w₀ − s): at the pinch x₁* = −√(s − w₀): the walls meet with slope ±2|x₁*| = ±2√(s−w₀): opening angle 2·arctan(2√(s−w₀)): for s slightly > w₀: nearly FLAT walls (slope → 0): the needle tip is nearly a flat half-plane: normal cone nearly a ray. For s >> w₀: slopes large: needle sharp: normal cone fat: angle up to... anyway: the normals at the eroded pinch corner: the fat cone spanned by the two wall normals: (±2|x₁*|, 1)-ish upward for the upper wall and (±, −1) for the lower: the cone generated by (2√(s−w₀), 1) and (2√(s−w₀), −1): this cone contains (1, 0) (the horizontal direction INTO the gap): and margins in direction (1,0) at the pinch point: what does the ORIGINAL margin say? The original boundary near the neck: walls x₂ = ±(|x₁|² + w₀): normals at wall points: upper wall (|x₁|² + w₀)' = 2x₁: normal = (−2x₁, 1)/√: at x₁ = 0: (0, 1) and lower (0,−1): the margin at the neck walls: for the field to keep trajectories in K, at the upper wall it must push DOWN-ish, at the lower wall push UP-ish: near the neck, the field must be nearly horizontal (squeeze through the neck): e.g., field = (v, 0) constant horizontal: margins: at upper wall (a, a² + w₀): ⟨(−2a, 1)/√(1+4a²), (v, 0)⟩ = −2av/√(1+4a²): for a > 0: sign = −sign(v): margin ≤ −m requires... at a > 0: need −2av/√ ≤ −m: v > 0 with 2av/√(1+4a²) ≥ m: for LARGE a: 2a/√(1+4a²) → 1: need v ≥ m: at a < 0: need −2av/√ ≤ −m ⟺ 2|a|v/√(1+4a²) ≥ m with −2a = 2|a| > 0: need v·(2|a|/√) ≥ m and −2av = +2|a|v: wait: a < 0: −2av = −2a·v = 2|a|v > 0 if v > 0: then ⟨ν, f⟩ = +2|a|v/√ > 0: VIOLATES margin (points outward through the upper-left wall!). So a constant horizontal field violates margins on one side. The neck requires the field to CHANGE direction across the neck (or the set must be traversed another way). Since the neck walls at x₁ < 0 have normals (−2x₁, 1)/√ = (2|x₁|, 1)/√ (upper-left wall: normal points up-RIGHT) and at x₁ > 0: (−2x₁, 1)/√ = (−2x₁... x₁ > 0: (−2x₁, 1)/√ (up-LEFT)): margins on the four wall-quadrants force the field to point toward the neck axis region... e.g., f(x) = (−c·sign?...) Let me instead use the SIMPLEST failing configuration. Honestly, maybe I'm overengineering. Let me step back and think about what the CLEANEST counterexample to "erosion invariance for arbitrary closed K under uniform margin" is. Known literature: "input-to-state safety" (Kolathaya, Singer, Ames; and "isoft" constraints), robust CBFs under disturbances: the standard result: if ḣ ≤ −α(h) + robustness terms, then a smaller level set is invariant: that's the BARRIER-function version: h-superlevel erosion: CLEAN and TRUE (no geometry needed: it's a 1-D comparison lemma!). The SET-erosion version (dist(x, K^c) ≥ cε): requires geometry: dist-function is only Lipschitz, and D⁺dist along the flow needs the boundary margin + curvature: the failure for arbitrary closed K: at medial-axis/medial-region points the distance function is nonsmooth with Dini derivatives in "wrong" directions. The neck/pinch example is the canonical failure: let me make it CONCRETE and MINIMAL:

K = closed rectangle-ish neck: K = ([−1, 0] × [−1, 1]) ∪ ([0, 1] × [−1, 1])... that's just a rectangle (no neck). Neck: K = ([−2, −1] × [−2, 2]) ∪ (neck) ∪ ([1, 2] × [−2, 2]) with neck = [−1, 1] × [−w, w], w < 1: K = union of three rectangles: a dumbbell with a RECTANGULAR neck of half-width w. Boundary: reentrant corners where the neck meets the chambers (four reentrant corners at (±1, ±w)): at those corners N^P = {0}-ish (reentrant, arms extend: like the L-corner: N^P = {0})... margins on the boundary faces: neck walls {x₁ ∈ (−1,1), x₂ = ±w}: normals (0, ±1): margin: at upper neck wall: f₂ ≤ −m; at lower: f₂ ≥ m. INSIDE the neck: by continuity (if f continuous... but f is the CLOSED LOOP: k measurable allowed? The source's setting: feedback k(x): if k can be MEASURABLE/discontinuous: f₂ ≤ −m on the upper wall and f₂ ≥ +m on the lower wall with the walls only 2w apart: a discontinuous k can switch at mid-neck: f₂ = −m above the axis, +m below: fine, no contradiction. With LIPSCHITZ k: |f₂(upper wall) − f₂(lower wall)| ≤ 2m ≤ L·2w: need Lw ≥ m: Lipschitz constant must exceed m/w: for w small: L huge: possible but strained; for the counterexample, ALLOW discontinuous/measurable feedback (the source never assumed Lipschitz feedback! Part A showed measurable feedback is the generic case!). Then: dynamics in the neck: ẋ₂ = −m·sign(x₂) (push toward the axis: keeps the neck invariant ✓), and ẋ₁ = v(x) toward... margins on the vertical faces of the chambers: {x₁ = ±2}: normals (±1, 0): margins: at x₁ = 2: −f₁ ≤ −m: f₁ ≥ m? wait outward normal at right face = (1, 0): margin: f₁ ≤ −m (inward = leftward) on the right face; at left face: −f₁ ≤ −m: f₁ ≥ m on the left face: fine: chambers push inward. Neck walls handled ✓. ALSO the reentrant corners and the chamber/neck junction faces: the faces {x₁ = ±1, |x₂| ∈ (w, 2)}: these are chamber boundary facing... the chamber [−2,−1]×[−2,2]: its right face {x₁ = −1, |x₂| ≤ 2}: for |x₂| > w: this face is on ∂K (exterior to the right: outside the neck region... the exterior near (−1, 0.5): points (−1+δ, 0.5), δ > 0: x₁ ∈ (−1, 1)? then in K iff |x₂| ≤ w: 0.5 > w: NOT in K: exterior ✓: so the face {x₁ = −1, w < |x₂| ≤ 2} ⊂ ∂K with outward normal (1, 0): margin: f₁ ≤ −m there: pushing LEFT (into the chamber) ✓. So near the neck entrance, the field on the chamber side pushes left, and we need trajectories to... whatever: design f: ẋ₁ = −m·sign(x₁ + 1)·? Let me simply design the CLOSED-LOOP field (no control constraints; k ≡ the field; no disturbance) as: f(x) = (−m·s(x₁, x₂)?, −m·sign(x₂)): want: (i) margins on all of ∂K (uniform m); (ii) K invariant ✓ (implied by margins + Part A if the field is nice — but measurable field: Part A route (b): need G Lipschitz: not available: but invariance of K can be verified DIRECTLY by construction); (iii) some erosion K⊖s NOT invariant for all s ∈ (0, s₀) — indeed for ALL s > 0. Design: in the neck: ẋ = (v, −m sign(x₂)) with v > 0 (drift RIGHT through the neck): margins on the neck walls ✓ (vertical component pushes toward axis ✓): at the neck's right end, the trajectory enters the right chamber ✓ stays in K ✓ (chamber walls push inward: in the chamber: ẋ₁ = −m sign(x₁ − 1)·? chamber [1, 2]: push left: ẋ₁ ≤ ... but we also want... hmm, wait: what does the EROSION fail on? The erosion of the dumbbell: K⊖s: the neck (half-width w) erodes to half-width w − s: the eroded neck walls: {x₁ ∈ (−1, 1), x₂ = ±(w − s)}: margin needed at eroded walls: the field's vertical component at x₂ = ±(w−s): ẋ₂ = −m sign(x₂): at x₂ = +(w−s) > 0: ẋ₂ = −m < 0: inward for the eroded neck ✓✓ STILL FINE!! Because the vertical pushing is uniform in x₂: erosion of the neck is fine. The failure must be HORIZONTAL: the eroded set's vertical faces: {x₁ = ±2 ∓ s...}: erosion of the chambers: chamber [−2,−1]×[−2,2] erodes to [−2+s, −1−s]×[−2+s, 2−s]: the eroded chamber's RIGHT face: {x₁ = −1 − s, |x₂| ≤ 2 − s}: outward normal (1, 0): margin at the eroded face: need f₁(−1−s, ·) ≤ −η: but the original margin on the chamber face was only at x₁ = −1: f₁(−1, ·) ≤ −m (pushing left, INTO the chamber ✓): at x₁ = −1 − s (INSIDE the chamber, s deep): if the field there pushes RIGHT (toward the neck: e.g., the drift v > 0 carrying flow toward the neck): f₁ = +v > 0: then trajectories at the eroded face {x₁ = −1−s} move RIGHT: they CROSS it: do they exit the eroded set? The eroded set near there = eroded chamber: crossing the face x₁ = −1−s rightward EXITS the eroded chamber... but wait: the eroded set is the union: eroded chamber ∪ eroded neck ∪ ...: the eroded neck = [−1+s, 1−s] × [−(w−s), w−s]-ish: is the eroded face {x₁ = −1−s, |x₂| ≤ 2−s} actually on the boundary of the ERODED union? The eroded set = K⊖s = (chamber1 ⊖ s) ∪ (neck ⊖ s) ∪ (chamber2 ⊖ s) ✓ (erosion distributes over union ✓): chamber1 ⊖ s = [−2+s, −1−s] × [−2+s, 2−s] ✓ (rectangle erosion ✓); neck ⊖ s = [−1+s, 1−s] × [−w+s, w−s]: for s < w ✓. The union: chamber1⊖s spans |x₂| ≤ 2−s; the neck⊖s spans |x₂| ≤ w−s < 2−s: the eroded set near x₁ = −1−s: points (−1−s, x₂) with w−s < |x₂| ≤ 2−s: on ∂(chamber1⊖s) ✓ boundary of the eroded set ✓ (nothing else there). So: trajectory in chamber1 drifting right at height |x₂| > w (ABOVE the neck: in the chamber but not aligned with the neck): it hits the eroded face x₁ = −1−s moving right: EXITS the eroded chamber1: enters the region x₁ ∈ (−1−s, −1+s): which is NOT in the eroded set at that height (the eroded neck only covers |x₂| ≤ w−s; the region between the eroded chamber and eroded neck at height w−s < |x₂| ≤ ... is excluded): trajectory has LEFT K⊖s while staying in K ✓✓✓ COUNTEREXAMPLE COMPLETE — provided the field satisfies the uniform margin on ∂K and keeps K invariant. Let me now design f explicitly and verify everything:

Let m = 1 (margin), w = 1/4 (neck half-width), chambers [−2,−1] × [−1, 1] and [1, 2] × [−1, 1], neck [−1, 1] × [−w, w] = [−1,1] × [−1/4, 1/4]. K = C_L ∪ N ∪ C_R (closed rectangles). Define the closed-loop field (state feedback, possibly discontinuous; no disturbance):

f₁(x) =

in the neck region and its approach: +2 (rightward drift) — need to check margins at x₁ = 2 face;
in chambers away from neck: push inward.
Hmm, but the margin on the RIGHT chamber's right face {x₁ = 2}: need f₁ ≤ −1 (leftward). And we want flow to go RIGHT through the neck into the right chamber and then... the flow in the right chamber must respect margins on its boundary (left face {x₁ = 1, |x₂| > w}: outward normal (−1, 0): margin: −f₁ ≤ −1 ⟹ f₁ ≥ 1 (rightward!) — so at the right chamber's LEFT face (above/below the neck), the field must point RIGHT (into the chamber) ✓ that's fine and consistent with the rightward drift!). And the rightward drift in the right chamber hits the right face {x₁ = 2} where margin forces f₁ ≤ −1 (leftward): CONTRADICTION with continuity? Not with a discontinuous field: switch f₁ from +2 to −1 at x₁ = 2−? hmm: but then trajectories pile at the switching surface: fine (they stay in K: sliding along x₁ = 2−δ... let me design f₁ in the right chamber: f₁ = +2 for x₁ < 3/2, f₁ = −1 for x₁ ≥ 3/2: discontinuous at x₁ = 3/2 (interior switching: Filippov sliding: fine: trajectories in the right chamber move right until x₁ = 3/2 then... Filippov at the discontinuity: velocity ∈ co{+2, −1}·(1,0) = [−1, 2]×{0}: trajectories can pass or slide: STAY IN K either way ✓). Margins: right face x₁ = 2: f₁ = −1 ✓ ≤ −1 ✓ margin exactly 1 ✓. Similarly left chamber: f₁ = −2 for x₁ > −3/2 (pushing LEFT: toward the left face?? wait: the flow should go RIGHT through the neck: in the LEFT chamber, flow should move RIGHT toward the neck: f₁ = +2: but the left chamber's LEFT face {x₁ = −2}: outward normal (−1,0): margin: −f₁ ≤ −1 ⟹ f₁ ≥ 1: rightward ✓ CONSISTENT (+2 ≥ 1 ✓): and the left chamber's RIGHT face {x₁ = −1, |x₂| > w}: outward normal (1, 0): margin f₁ ≤ −1: LEFTWARD: but we want rightward flow there to enter the neck! CONFLICT: at the face {x₁ = −1, w < |x₂| ≤ 1}: the margin demands f₁ ≤ −1 (push back into the chamber), but the drift wants f₁ = +2. Resolve: the rightward drift only at heights |x₂| ≤ w (the neck corridor): design f₁ = +2 for |x₂| < w (everywhere: in both chambers and the neck: a horizontal "jet" along the neck corridor), and f₁ = −1·(push inward) for |x₂| > w in the chambers... but wait: in the LEFT chamber at heights |x₂| > w: margins: left face (−2): f₁ ≥ 1; right face (−1): f₁ ≤ −1: so f₁ must be ≥ 1 near x₁ = −2 and ≤ −1 near x₁ = −1: fine with f₁ = −sign-ish: f₁(x) = −2·sign(x₁ + 3/2) for |x₂| ∈ (w, 1] in the left chamber: pushing away from the mid-line x₁ = −3/2: toward both faces: hmm: that pushes flow INTO the faces: margins hold ✓ (velocity INTO the face?? margin: at left face x₁ = −2: need f₁ ≥ +1: pushing RIGHT = INTO the chamber = away from the face ✓ "inward" ✓): trajectories in the left chamber outside the jet: move toward x₁ = −3/2 line: stay ✓. And the vertical component: f₂ = −2·sign(x₂)·? margins on horizontal faces: chamber top faces {x₂ = 1, x₁ ∈ [−2,−1]}: outward normal (0,1): margin: f₂ ≤ −1: ✓ with f₂ = −2 sign(x₂) (for x₂ > 0: f₂ = −2 ✓; bottom faces x₂ = −1: normal (0,−1): margin: −f₂ ≤ −1 ⟹ f₂ ≥ 1 ✓ f₂ = −2 sign(x₂) = +2 for x₂ < 0 ✓). Neck walls {x₂ = ±w, x₁ ∈ (−1,1)}: normals (0, ±1): f₂ = ∓2 ✓ margins 2 ≥ 1 ✓. In the jet {|x₂| < w}: f₂ = −2sign(x₂) ✓ keeps jet flow centered. Also the reentrant corners (±1, ±w) and convex corners (±2, ±1) etc.: convex corners (chamber corners): N^P = quadrant: margin needs ⟨ζ, f⟩ ≤ −1 for ALL ζ in the quadrant: e.g., corner (−2, 1): N^P = cone{(−1,0), (0,1)}: f near there: f₁ ≥ 1 (from left face margin... at the corner: need ⟨(−1,0), f⟩ ≤ −1 ⟹ f₁ ≥ 1 ✓ and ⟨(0,1), f⟩ ≤ −1 ⟹ f₂ ≤ −1 ✓ and everything between: ⟨(−a, b)/|·|, f⟩ = (−af₁ + bf₂)/|·| ≤ (−a·1 + b·(−1))/... = −(a + b)/|·| ≤ −1·(a+b)/√(a²+b²) ≥ ... (a+b)/√(a²+b²) ≥ 1 ✓ (≥ √2·? min over the quadrant of (a+b)/√(a²+b²) = 1 at the axes ✓): so margin ≥ 1 ✓ at convex corners automatically from face margins + cone structure ✓ (with the DISCONTINUOUS field: at the corner the field value: define f at corners consistently: f(−2, 1) := (1, −2)?? need ⟨ζ, f⟩ ≤ −1 ∀ζ ∈ quadrant cone: f = (2, −2): ⟨(−a, b), (2,−2)⟩ = −2a − 2b ≤ −2(a+b) ≤ −2·? with |ζ| = 1: ≤ −2·(a+b)/√(a²+b²) ≤ −2 ✓.) Reentrant corners (±1, ±w): N^P = {0} (arms extend: the neck wall and the chamber face extend past the corner? At corner (−1, w): arms: neck wall {x₂ = w, x₁ ∈ (−1, 1)} extends RIGHT past the corner ✓ and chamber face {x₁ = −1, x₂ ∈ (w, 1]} extends UP past the corner ✓: so like the L-corner: exterior points project onto the arms: N^P = {0} ✓: margin vacuous there ✓ no constraint ✓.) GOOD. Now: (i) uniform margin 1 on ∂K w.r.t. proximal normals ✓ (verified on all faces; vacuous at reentrant corners; automatic at convex corners); (ii) K invariant under the Filippov flow? The field is discontinuous: use FILIPPOV solutions: F_Fil(x): at discontinuity surfaces (x₂ = 0? no: f₂ = −2sign(x₂): discontinuous at x₂ = 0: Filippov: f₂ ∈ [−2, 2]·? co{−2, +2} = [−2,2] at x₂ = 0: vertical velocity anywhere in [−2,2]: hmm: that ALLOWS upward drift at x₂ = 0: at x₂ = 0, ẋ₂ ∈ [−2, 2]: could exit through the neck wall? From x₂ = 0 with ẋ₂ = +2: rises to x₂ = w: at x₂ > 0: ẋ₂ = −2: comes back: Filippov solutions: can they escape the neck? At x₂ = w (neck wall, x₁ ∈ (−1,1)): Filippov set at x₂ = w: f₂ = −2 (continuous there: sign(x₂) = +1 on both sides? f₂ = −2sign(x₂): for x₂ > 0: −2; x₂ < 0: +2: discontinuous ONLY at x₂ = 0 ✓: at x₂ = w > 0: f₂ = −2 continuous ✓: velocity (f₁, −2) with f₁ = +2 (jet): points down-right: INTO the neck ✓ no exit ✓. Chamber top faces x₂ = 1: f₂ = −2 ✓ inward ✓. Right face x₁ = 2: f₁: discontinuous at x₁ = 3/2 not at 2: at x₁ = 2: f₁ = −1 ✓ inward ✓. Left chamber: faces: x₁ = −2: f₁ = +2? my design: in left chamber for |x₂| > w: f₁ = −2sign(x₁ + 3/2): at x₁ = −2 < −3/2: f₁ = +2 ✓ inward (rightward) ✓; at x₁ = −1 (face): −1 > −3/2: f₁ = −2 ✓ inward (leftward) ✓. In the jet (|x₂| < w): f₁ = +2 everywhere (both chambers + neck): at x₁ = −2, |x₂| < w: face: f₁ = +2: inward ✓; at x₁ = 2: f₁ = +2?? VIOLATES the right-face margin (need ≤ −1)!! Fix: in the jet, f₁ = +2 for x₁ < 3/2 and −1 for x₁ ≥ 3/2 (the same switch): then at x₁ = 2 (jet heights): f₁ = −1 ✓ margin ✓. But at x₁ = 3/2 (inside the right chamber, |x₂| < w): Filippov: f₁ ∈ [−1, 2]: trajectories decelerate... they might oscillate/slide at x₁ = 3/2: STAY in K ✓ (sliding surface is interior ✓). (iii) Now the EROSION failure: consider the trajectory starting at x⁰ = (−3/2, w/2) = (−1.5, 0.125): in the jet (|x₂| = 0.125 < w = 0.25 ✓), x₁ = −3/2 < 3/2: velocity (f₁, f₂) = (+2, −2·sign(0.125) = −2): moves right and down: x₂ → 0 quickly then stays near 0 (Filippov at x₂ = 0: ẋ₂ ∈ [−2,2]: fine, drift right at ẋ₁ = 2): the trajectory travels right along the jet: passes through the neck ✓ exits into the right chamber ✓ reaches x₁ = 3/2, slides/decelerates, ends near x₁ = 3/2, x₂ ≈ 0: STAYS IN K ✓✓. Now the erosion: K⊖s for any s ∈ (0, w) = (0, 1/4): the trajectory starts at (−1.5, 0.125): is it in K⊖s? dist((−1.5, 0.125), K^c): nearest exterior: vertical to the neck wall? x = (−1.5, 0.125): x₁ = −1.5 ∈ chamber left (−2,−1): the point is INSIDE the left chamber at height 0.125 < w: distance to the chamber's boundary: the chamber extends |x₂| ≤ 1: the nearest chamber wall: x₂ = ±1: distance 0.875; x₁ = −2: 0.5; x₁ = −1: 0.5: BUT ALSO the exterior notch regions adjacent to the neck: the exterior near (−1, 0.5): the point (−1.5, 0.125)'s distance to the exterior: the exterior regions: {|x₂| > 1}, {x₁ < −2}, {x₁ > 2}, and the notches {(x₁ ∈ (−1,1)-ish, |x₂| > w)}: the nearest notch: points (−1+δ, 0.25+ε): distance from (−1.5, 0.125): ≥ 0.5 horizontally: so dist(x⁰, K^c) = 0.5 (to the faces x₁ = −2 or x₁ = −1 — both at 0.5: TIE: (−1.5, 0.125) is equidistant 0.5 from the two vertical faces: medial axis point! dist = 0.5 ✓): so x⁰ ∈ K⊖s iff s ≤ 0.5 ✓ for s < 1/4 ✓✓ x⁰ ∈ K⊖s ✓. Trajectory: moves right along (x₂ ≈ 0, x₁ from −1.5 → 1.5): at x₁ = −1 − s (crossing the eroded left chamber's right face... wait: is the trajectory EXTING the eroded set? The eroded set along the jet corridor: for x₁ ∈ (−1+s, 1−s), |x₂| ≤ w − s: the trajectory at (x₁, 0): x₁ ∈ (−1, 1): |x₂| = 0 ≤ w − s ✓: IN the eroded neck ✓; for x₁ ∈ (−2+s, −1−s): the eroded left chamber: |x₂| = 0 ≤ 1 − s ✓ IN ✓; the GAP: x₁ ∈ (−1−s, −1+s): at these x₁ (near −1), |x₂| = 0: covered by the eroded neck? eroded neck: x₁ ∈ [−1+s, 1−s]: x₁ = −1 + s/2 ∉: covered by eroded left chamber? x₁ ≥ −1 − s: NO: x₁ = −1 + s/2 > −1 − s: hmm: eroded left chamber = [−2+s, −1−s] × [−1+s, 1−s]: x₁ = −1+s/2: is −1+s/2 ≤ −1−s? ⟺ s/2 ≤ −s: NO: so x₁ ∈ (−1−s, −1+s) with |x₂| small: NOT in the eroded left chamber; eroded neck: x₁ ∈ [−1+s, 1−s]: x₁ = −1+s/2 < −1+s: NOT in the eroded neck; eroded right chamber: far. SO: the points (x₁, 0) with x₁ ∈ (−1−s, −1+s) are NOT in K⊖s!!! Wait, really?? dist((−1+s/2, 0), K^c): the point is in the neck at x₁ = −1+s/2, x₂ = 0: the nearest exterior: the notch above the neck: (−1+s/2, w+ε): distance w = 0.25... so dist = w = 0.25?? Then (−1+s/2, 0) ∈ K⊖s iff 0.25 ≥ s ✓ s < 1/4 ✓ IT IS IN THE EROSION. CONTRADICTION with my component computation?! Recheck: K⊖s = (C_L ⊖ s) ∪ (N ⊖ s) ∪ (C_R ⊖ s): is this right? K = C_L ∪ N ∪ C_R: dist(x, K^c): K^c = (C_L ∪ N ∪ C_R)^c: dist(x, K^c) = min over the pieces?? NO!! dist(x, A ∪ B) = min(dist(x,A), dist(x,B)): that's for x OUTSIDE measuring distance TO the union. Here: dist(x, K^c) where x ∈ K: K^c = C_L^c ∩ N^c ∩ C_R^c: dist(x, K^c) = dist(x, C_L^c ∩ N^c ∩ C_R^c) ≠ min of anything simple! I conflated. dist(x, K^c) for x ∈ K = the distance to the boundary of the union = the inradius ball: B(x, s) ⊆ K ⟺ dist(x, K^c) ≥ s: B(x,s) ⊆ C_L ∪ N ∪ C_R: NOT equivalent to B ⊆ one piece! So erosion does NOT distribute over unions in the "inner distance" sense: K⊖s ⊋ (C_L⊖s) ∪ (N⊖s) ∪ (C_R⊖s)?? Hmm: actually wait: {x : B(x,s) ⊆ A ∪ B} ⊇ {x: B(x,s) ⊆ A} ∪ {x: B(x,s) ⊆ B} ✓ ⊇: and generally STRICTLY bigger (balls can straddle pieces). Right: earlier I "verified" erosion distributes over unions via dist(x, A∪B) = min(...): that computes dist TO the union from OUTSIDE — irrelevant. The correct identity: (A ∪ B) ⊖ s = {x: x + sB̄ ⊆ A ∪ B} ⊇ (A⊖s) ∪ (B⊖s), with strict inclusion possible. Hmm!! So my L-shape "erosion = translate" computation: K = {x₁ ≤ 0} ∪ {x₂ ≤ 0}: K⊖s = {B̄(x,s) ⊆ K}: point (−s, −s): ball of radius s around it: does it fit in the L-shape? The ball contains (−s + s/√2, −s + s/√2) = (s(1/√2 − 1), s(1/√2 − 1)): NEGATIVE coordinates ✓ ∈ K; the ball's top-right point: (−s + s/√2, −s + s/√2) ✓ negative ✓; actually the farthest point in the (1,1) direction: (−s,s)·... ball around (−s,−s) radius s: the point in direction (1,1)/√2: (−s + s/√2, −s + s/√2): both coords = s(1/√2 − 1) < 0 ✓; every point of the ball has x₁ ≤ −s + s = 0 or...: the ball: x₁ ∈ (−2s, 0), x₂ ∈ (−2s, 0): any ball point with x₁ > 0? x₁ ≤ −s + s = 0 ✓ (max x₁ = 0, achieved at (0, −s) ∈ K ✓): so all ball points have x₁ ≤ 0 OR x₂ ≤ 0? Ball points: x₁ ≤ 0 always?? x₁ ∈ [−2s, 0] ✓ ALWAYS ≤ 0 ✓✓: so B((−s,−s), s) ⊆ {x₁ ≤ 0} ⊆ K ✓: so (−s,−s) ∈ K⊖s ✓: and points (−s + δ, −s + δ), δ > 0: ball max x₁ = δ > 0, max x₂ = δ > 0: the point (δ', δ') with δ' = ... the ball around (−s+δ, −s+δ) radius s contains (−s+δ+s/√2, −s+δ+s/√2): coords = δ + s(1/√2 − 1) = δ − 0.29s: POSITIVE if δ > 0.29s: so for δ > 0.29s: the ball pokes into the open quadrant: ∉ K: so (−s+δ, −s+δ) ∉ K⊖s for δ > s(1 − 1/√2): so the eroded L-shape near the corner: tip at... the eroded set = {B(x,s) ⊆ {x₁≤0} ∪ {x₂≤0}}: this is NOT {x₁ ≤ −s} ∪ {x₂ ≤ −s}: e.g., (0, −s): ball around it: x₁ ∈ (−s, s), x₂ ∈ (−2s, 0): ball points with x₁ > 0 need x₂ ≤ 0 ✓ ALL ball points have x₂ ≤ 0?? x₂ ∈ (−2s, 0]: max x₂ = 0 ✓ ≤ 0 ✓✓: so B((0,−s), s) ⊆ {x₂ ≤ 0} ⊆ K ✓: (0, −s) ∈ K⊖s: but (0, −s) ∉ {x₁ ≤ −s} ∪ {x₂ ≤ −s} (x₂ = −s: on the boundary of {x₂ ≤ −s}: ∈ ✓ actually (0,−s) ∈ {x₂ ≤ −s} ✓ hmm fine that one's in). Point (s/2, −s/2): ball: x₂ ∈ (−3s/2, s/2): contains points with x₂ > 0 AND x₁ > 0 (e.g., (s/2, s/4)): ∉ K: so (s/2, −s/2) ∉ K⊖s. And {x₂ ≤ −s} ∋ (s/2, −s/2)?? x₂ = −s ≤ −s ✓ ∈: but we just showed the ball doesn't fit: CONTRADICTION?! B((s/2, −s/2), s): does it fit in K = {x₁ ≤ 0} ∪ {x₂ ≤ 0}? Ball point (s/2, s/4): x₁ = s/2 > 0, x₂ = s/4 > 0: ∉ K ✓ ball NOT ⊆ K ✓ so (s/2, −s/2) ∉ K⊖s ✓: but is (s/2, −s/2) ∈ {x₂ ≤ −s}?? x₂ = −s/2: is −s/2 ≤ −s? NO! −s/2 > −s: NOT in {x₂ ≤ −s} ✓ phew no contradiction: I misread. OK so what IS the erosion of the L-shape? K⊖s = {x : B̄(x,s) ⊆ {x₁ ≤ 0} ∪ {x₂ ≤ 0}}: a ball fails iff it contains a point with x₁ > 0 AND x₂ > 0: the ball pokes into the open quadrant. The ball around x pokes into the quadrant iff dist(x, (0,∞)²) < s. So K⊖s = {x ∈ K: dist(x, (0,∞)²) ≥ s} = {x: |x⁻| ≥ s} (computed before! ✓ consistent: the ORIGINAL computation dist(x, K^c) = |x⁻| was RIGHT: K^c = (0,∞)² ✓ dist(x, K^c) = dist(x, (0,∞)²) = |x⁻| ✓✓: so K⊖s = {|x⁻| ≥ s} = the ROUNDED-corner set I first computed (quarter-circle near the corner + arms {x₁ ≤ −s, x₂ ≥ 0}? let me recompute: |x⁻| ≥ s: x = (x₁, x₂): x⁻ = (min(x₁,0), min(x₂,0)): |x⁻|² = min(x₁,0)² + min(x₂,0)² ≥ s²: cases: x₁ ≥ 0, x₂ ≥ 0: 0 ≥ s²: no (s>0): excluded. x₁ ≥ 0 > x₂: |x⁻| = |x₂| ≥ s: x₂ ≤ −s ✓ included. x₂ ≥ 0 > x₁: x₁ ≤ −s ✓. both < 0: x₁² + x₂² ≥ s² ✓. So K⊖s = {x₁ ≥ 0, x₂ ≤ −s} ∪ {x₁ ≤ −s, x₂ ≥ 0} ∪ {x₁ < 0, x₂ < 0, x₁²+x₂² ≥ s²}: near the origin: the QUARTER DISK of radius s is REMOVED from the third quadrant, and the arms are cut at (0, −s) and (−s, 0), with the boundary near the origin = quarter circle {x₁² + x₂² = s², x₁, x₂ ≤ 0} — the ROUNDED version (my FIRST computation was right; the "translate" claim was the error — good, and now the corner normal analysis: at the arc: the eroded set is OUTSIDE the quarter-disk: outward normal at arc point y (|y| = s, y ≤ 0): pointing INTO the disk = toward origin: ν = −y/s: e.g., at y = (−s/√2, −s/√2): ν = (1/√2, 1/√2): DIAGONAL: and N^P_{K⊖s} at arc points = ray ℝ₊ν ✓ (smooth arc ✓). Is the diagonal ν a proximal normal of K anywhere near? K's proximal normals near the corner: on the arms: (1, 0) (arm {x₁ = 0, x₂ ≤ 0}... wait the arm boundary {x₁ = 0}: normal (1, 0) for points (0, x₂), x₂ < 0; and (0,1) on {x₂ = 0}; at the corner (0,0): N^P = {0}. The eroded arc's normals: diagonals (cosθ, sinθ)-ish with θ ∈ (0°, 90°): e.g., (1/√2, 1/√2): NOT a proximal normal of K at ANY point (K's boundary normals near the corner are only (1,0)-ray, (0,1)-ray, and {0}): the diagonal is a CLARKE normal at the corner (N^C_K(0,0) = ℝ₊² ✓ contains it). So the L-shape erosion DOES have the normal blow-up: the eroded boundary (arc) has proximal normals in Clarke-but-not-proximal directions of K. NOW: does the margin + Lipschitz transfer save it? The margin on K's arms: f₁ ≤ −m on {x₁ = 0, x₂ ≤ 0} and f₂ ≤ −m on {x₂ = 0, x₁ ≤ 0}. At the eroded arc point y = −s·ν̂ (ν̂ = (1/√2, 1/√2), y = (−s/√2, −s/√2)): need ⟨ν̂, f(y)⟩ ≤ −η: write f(y) = (f₁, f₂): ⟨ν̂, f⟩ = (f₁ + f₂)/√2: from arm margins: f₁(y) ≤ f₁(0, y₂) + L|y₁| ≤ −m + Ls/√2 (projecting y = (−s/√2, −s/√2) to the arm {x₁ = 0}: the arm point (0, −s/√2) ∈ ∂K ✓ margin applies ✓ |y − (0,−s/√2)| = s/√2 ✓): similarly f₂(y) ≤ −m + Ls/√2: so ⟨ν̂, f(y)⟩ ≤ −√2(m − Ls/√2) = −√2 m + Ls: need < 0: s < √2 m/L ✓ FINE for small s. ARGH: the Lipschitz transfer STILL saves the L-shape erosion!! Because the arm margins f₁ ≤ −m, f₂ ≤ −m BOTH hold near the corner (the arms pass through the corner region), and the diagonal margin follows from the two coordinate margins. Hmm!!! Is there any way the erosion fails with Lipschitz fields + uniform proximal margins?? Maybe... the failure needs the margin on the two arms to be incompatible with the diagonal: the margin on arm 1 constrains f₁ only; arm 2 constrains f₂ only; the diagonal needs f₁ + f₂ ≤ −η√2: if BOTH f₁ ≤ −m and f₂ ≤ −m hold near the corner ✓ then fine. But WAIT: do the arm margins hold at points (0, x₂) for x₂ ∈ (−s, 0) (the part of the arm INSIDE the removed quarter-disk region... the arm {x₁ = 0, x₂ < 0}: ALL of it is in ∂K ✓ margins hold on ALL of it ✓ including (0, −s/2) ✓: the projection point (0, −s/√2) used above: ✓ margin there ✓.) So Lipschitz + uniform margin on ∂K (proximal) DOES imply erosion invariance for the L-shape, with c ≈ m/(√2 L)... Hmm, wait — but this contradicts my expectation. Let me look for the actual failure more carefully. Perhaps the failure is when the margin is NOT uniform... or when K is not "epi-Lipschitz"... or maybe the claim is actually TRUE under (uniform margin + Lipschitz field)?? But the rejection criteria say "claims metric erosion preserves smooth normal margins for arbitrary closed sets" is a rejection trigger — implying the claim is false for arbitrary closed K. The subtlety: for arbitrary closed K, the margin hypothesis itself can be satisfied while the boundary is so crinkly that the Lipschitz transfer FAILS: the transfer argument needs: at each eroded-boundary point x with outward normal ν, find a NEARBY boundary point p of K with a proximal normal ζ ≈ ν and |p − x| ≤ ~s: then ⟨ν, f(x)⟩ ≤ ⟨ζ, f(p)⟩ + [Lipschitz terms] ≤ −m + L' s. For the L-shape: the arc's diagonal normal found nearby arm points with EXACTLY matching coordinate constraints ✓. When does this FAIL? When the eroded boundary point's normal has NO nearby proximal-normal counterpart: e.g., the cusp/needle where nearby boundary normals converge to something else... Consider K = {x₂ ≤ −|x₁|^α} α ∈ (0,1) — the convex needle (computed: N^P at tip = {ζ₂ ≥ 0}: FAT: margin impossible). Not usable. Consider K with a REENTRANT cusp where the erosion boundary normal is diagonal but nearby K-boundary normals are only axis-ish with margins that DON'T combine: the L-shape arms had margins on BOTH arms near the corner; what if only ONE arm carries a margin near the corner and the other arm is... margins must hold on ALL of ∂K: both arms always have margins. The diagonal combines both: f₁ ≤ −m and f₂ ≤ −m ⟹ diagonal ≤ −√2m + Lipschitz-correction: always fine. Hmm: what if the eroded normal is (1, −1)/√2 (mixing a margin-constrained component with an UNCONSTRAINED one)? Eroded boundary normals of the "wrong sign": the arc normals of the L-erosion: ν = −y/s with y in the third quadrant: ν ∈ first quadrant ✓ both components positive ✓ both constrained ✓. Could an erosion create normals with a component in a direction where NO margin exists nearby? The eroded set's boundary normals are "inward-pointing" limits... Let me think about the DUMBBELL again with the CORRECT erosion (balls ⊆ union): the eroded neck region: points (x₁, x₂) with x₁ ∈ (−1, 1), |x₂| ≤ w: ball ⊆ K: the ball around (x₁, x₂) radius s: pokes out through the neck walls if |x₂| + s > w: so the eroded neck = {|x₂| ≤ w − s} horizontally extended... but ALSO the balls can poke into the NOTCHES (exterior above the neck next to the chambers): for x₁ near ±1, |x₂| ≤ w − s: the ball around (−1 + δ, 0), δ small: radius s: does it fit in K? The ball pokes into the region {x₁ > −1, |x₂| > w} (the notch): ball point (−1 + δ + a, b) with a² + b² < s², b > w: possible iff δ + a... need some ball point with x₁ > −1 AND |x₂| > w: b > w = 1/4: a² < s² − w²: if s > w: possible: so for s > w: balls near the neck-entrance poke into the notches: the eroded set PINCHES at the neck for s ≥ w?? dist((x₁, 0), K^c) for x₁ ∈ (−1, 1): the nearest exterior: the neck walls at |x₂| = w: distance w: ALSO the notches: for x₁ = 0: nearest notch point: (±1, w + ε): distance √(1 + w²) ≈ 1: so dist = w = 1/4: so the neck interior points are at distance exactly w from K^c: K⊖s ∩ neck-axis = ∅ for s > w, = the whole axis for s < w ✓. For s < w: the eroded neck = {x₁ ∈ (−1+? , 1−?), |x₂| ≤ w − s}: the horizontal extent: points near x₁ = ±1: dist((1 − δ, 0), K^c): nearest exterior: neck wall (1−δ, ±w): distance w; notch: (1 + ε, w): distance √(δ² + w²) ≥ w: so dist = w for δ ≤ ...: hmm: actually dist((1−δ, 0), K^c) = min(w, √(δ² + w²), distances to other exterior) = w: so (1−δ, 0) ∈ K⊖s for s < w ✓ ANY δ: even δ < 0 (into the chamber)? (1 + δ', 0), δ' > 0 (right chamber): dist to exterior: chamber walls at |x₂| = 1: distance 1; the notch (1+δ', w+ε)... the notch adjacent to the right chamber: exterior points (1 + something, |x₂| > w): from (1 + δ', 0): nearest: (1 + δ', w): but (1+δ', w): x₁ = 1 + δ' > 1: is it exterior? In the right chamber region x₁ ∈ (1, 2): K iff |x₂| ≤ 1: (1+δ', w): w < 1: INTERIOR of K ✓ not exterior: the exterior near there: {|x₂| > 1} ∪ {x₁ > 2} ∪ notches {x₁ ∈ (−1,1), |x₂| > w}: from (1 + δ', 0): nearest notch point: x₁ ≤ 1: (1, w + ε): distance √(δ'² + w²) ≥ w ✓: so dist((1+δ', 0), K^c) = min(w, 1 − 0, ...) hmm: the neck wall at x₁ = 1 + δ' > 1: NOT a boundary there (the neck ends at x₁ = 1: for x₁ > 1: the boundary is the chamber at |x₂| = 1): so from (1+δ', 0): the exterior: |x₂| > 1 at distance 1; x₁ > 2 at distance 1 − δ'; the notches at distance ≥ √(δ'² + w²): so dist = min(1, 1 − δ', √(δ'² + w²)): for δ' small: √(δ'² + w²) ≈ w < 1: dist ≈ w ✓: so points just right of the neck entrance on the axis are ALSO at distance ≈ w: fine: they're in K⊖s for s < w ✓. So the eroded set (s < w) contains the full corridor along the axis INCLUDING through the neck ✓ and the eroded neck walls at |x₂| = w − s ✓. NOW the trajectory: my jet trajectory runs along x₂ ≈ 0 from x₁ = −1.5 to 1.5: it's INSIDE the eroded corridor ✓ stays inside ✓... the erosion is invariant along the corridor ✓✓. Hmm!!! So the dumbbell with a through-jet ALSO survives?! Wait — but I wanted the trajectory to exit the erosion: the failure I hypothesized was for trajectories at heights w < |x₂| (above the neck) crossing the eroded chamber faces: trajectory at height x₂ = 0.5 (in the left chamber, above the neck): the left-chamber flow at heights |x₂| > w: my design: f₁ = −2sign(x₁ + 3/2) (push toward x₁ = −3/2): trajectories at height 0.5 move toward x₁ = −1.5 and STAY: never approach the eroded faces x₁ = −1 − s: NO EXIT. To make the counterexample, I need a trajectory that crosses x₁ = −1 − s at height |x₂| > w − s: e.g., a RIGHTWARD drift at height 0.5 in the left chamber: but the margin at the left chamber's right face {x₁ = −1, w < |x₂| ≤ 1} FORCES f₁ ≤ −m there (leftward): a rightward drift at height 0.5 would VIOLATE the margin at that face... unless the drift is confined to |x₂| < w... but then it's in the corridor and the corridor erosion is invariant. Hmm!!! Wait: can the trajectory cross x₁ = −1−s at height |x₂| ∈ (w−s, w)? At those heights: the trajectory in the left chamber moving right: at x₁ = −1−s, x₂ = 0.2 (with w = 0.25, s = 0.1: w − s = 0.15: x₂ = 0.2 ∈ (0.15, 0.25)): the eroded set at (−1−s, 0.2) = (−1.1, 0.2): is it in K⊖s? dist((−1.1, 0.2), K^c): nearest exterior: the notch: points (x₁ ≥ −1?, |x₂| > 0.25)... the notch adjacent: {(−1,1)×(0.25, ∞)}-ish exterior: from (−1.1, 0.2): nearest notch point: (−1, 0.25): distance √(0.01 + 0.0025) = √0.0125 ≈ 0.112; the neck wall: (−1.1, 0.25): x₁ ∈ (−1,1) ✓ |x₂| = 0.25 = w: boundary ✓: distance 0.05: so dist ≈ 0.05 < s = 0.1: NOT in the erosion. So the point (−1.1, 0.2) is outside K⊖s: a trajectory crossing from the eroded left chamber into the eroded neck must pass through the "waist": the eroded passage near x₁ = −1: which heights are in K⊖s near x₁ = −1? (−1 + δ, x₂): dist to exterior: neck wall: w − |x₂| (if x₂ > 0); notch: (−1, w): distance √(δ² + (w − x₂)²) ≥ w − x₂: so dist = w − |x₂| for δ ≥ 0-ish... for δ ∈ (0, 1): dist = w − |x₂| ✓ (neck wall nearest): so near the entrance (δ > 0): in K⊖s iff |x₂| ≤ w − s ✓ ONLY the corridor heights. For δ < 0 (left of −1): (−1 − |δ|, x₂): dist: neck wall doesn't extend (x₁ < −1): chamber: exterior above: |x₂| > 1: distance 1 − x₂; notch: (−1, w): distance √(δ² + (w − x₂)²): for |x₂| < 1: dist = min(1 − |x₂|, √(δ² + (w−x₂)²)): at x₂ = 0.2, δ = 0.1: √(0.01 + 0.0025) = 0.112 ≥ s = 0.1 ✓ and 1 − 0.2 = 0.8 ✓: dist = 0.112 ≥ s ✓ IN the erosion ✓. So: at height x₂ = 0.2: the point (−1.1, 0.2) ∈ K⊖s (dist 0.112) — wait I computed dist((−1.1, 0.2)) = 0.05 via the neck wall: WRONG: the neck wall {x₂ = w, x₁ ∈ (−1, 1)}: the point (−1.1, 0.2): its nearest point ON the neck wall: the wall exists only for x₁ ∈ (−1, 1): nearest wall point: (−1, 0.25): distance √(0.01 + 0.0025) = 0.112 ✓ (NOT (−1.1, 0.25): that's not on the wall since x₁ = −1.1 < −1: correct: the wall's edge is at x₁ = −1). And the chamber's own boundary: the left chamber = [−2,−1] × [−1,1]: the point (−1.1, 0.2) is INSIDE the chamber (x₁ = −1.1 ∈ (−2,−1) ✓): the chamber's boundary: x₂ = ±1 (distance 0.8), x₁ = −2 (0.9), x₁ = −1 (0.1: the face at x₁ = −1: but wait: the face {x₁ = −1, |x₂| ≤ 1}: is it ALL boundary? The part {x₁ = −1, |x₂| ≤ w}: adjacent to the neck: NOT boundary (interior of K: the neck continues) ✓; the part {x₁ = −1, w < |x₂| ≤ 1}: boundary ✓: from (−1.1, 0.2): the nearest point of THAT part: (−1, 0.25): distance 0.112 ✓. So dist((−1.1, 0.2), K^c) = 0.112 ≥ 0.1 ✓ IN K⊖s ✓. And (−1.05, 0.2): nearest boundary point: (−1, 0.25): √(0.0025 + 0.0025) = 0.0707 < 0.1: NOT in K⊖s. And (−1.05, 0.1): nearest: (−1, 0.25): √(0.0025 + 0.0225) = 0.158 ≥ 0.1 ✓ IN. So the eroded set near the neck entrance: a waist that pinches: at x₁ = −1.05: heights |x₂| ≤ ~0.09-ish... the eroded passage through the neck: heights |x₂| ≤ w − s = 0.15 for x₁ ∈ (−1, 1)... wait recompute: (x₁, x₂) with x₁ ∈ (−1, 1): dist = w − |x₂| (neck wall): ∈ K⊖s iff |x₂| ≤ w − s = 0.15 ✓. So the eroded set = eroded chambers + eroded neck + the eroded "junction" regions (like (−1.1, 0.2) ✓). NOW: a trajectory at height 0.2 moving right: passes (−1.1, 0.2) ∈ K⊖s, then (−1.05, 0.2) ∉ K⊖s: EXITS the erosion ✓✓ — while staying in K (the left chamber/neck: (−1.05, 0.2): x₁ = −1.05 < −1: in the left chamber ✓ ∈ K ✓✓). So IF the field at height 0.2 near x₁ ∈ (−1.2, −1) points RIGHTWARD (f₁ > 0), trajectories exit the erosion. But the MARGIN at the face {x₁ = −1, w < |x₂| ≤ 1}: forces f₁(−1, 0.2) ≤ −m (LEFTWARD at the face itself). Can f₁ be rightward at x₁ = −1.1, height 0.2, while leftward (≤ −m) at x₁ = −1, height 0.2? With a LIPSCHITZ field: |f₁(−1.1, 0.2) − f₁(−1, 0.2)| ≤ L·0.1: f₁(−1.1) ≤ −m + 0.1L: rightward (positive) requires 0.1L > m: L > 10m: POSSIBLE if the Lipschitz constant is large!! The margin hypothesis doesn't bound L! So: take L huge: the field swings from +V (rightward, fast) at x₁ ≤ −1.1 to −m at x₁ = −1: over 0.1: L = (V + m)/0.1: fine: NO CONTRADICTION. But WAIT: does the rightward-fast field at (−1.1, 0.2) violate any margin? The margins at height 0.2: the chamber faces: left face x₁ = −2: f₁ ≥ m ✓ (set f₁ = +V ≥ m there ✓ consistent!); right face x₁ = −1: f₁ ≤ −m ✓ (set f₁ = −m at x₁ = −1 ✓). So along the line x₂ = 0.2, f₁ goes from +V at x₁ = −2... wait we need f₁ ≥ m at x₁ = −2 (margin: outward normal (−1,0): ⟨(−1,0), f⟩ ≤ −m ⟹ f₁ ≥ m ✓ rightward): so f₁(x₁, 0.2) ≥ m at x₁ = −2 and ≤ −m at x₁ = −1: it must cross zero somewhere in between: at the crossing, trajectories... fine: trajectories at height 0.2: move right where f₁ > 0, left where f₁ < 0: they accumulate at the zero of f₁: STUCK at the equilibrium-ish line: they DON'T reach x₁ = −1: hmm: so to have a trajectory EXIT the erosion, we need the trajectory to actually cross x₁ = −1.1 → −1.05 at height 0.2: i.e., f₁ > 0 on that whole segment: f₁(−1.05, 0.2) > 0: but then by Lipschitz-in-x₁... no constraint violated: f₁(−1.05) > 0, f₁(−1) ≤ −m: Lipschitz: m ≤ |f₁(−1) − f₁(−1.05)| ≤ 0.05L: L ≥ 20m ✓ allowed. But WAIT: then the trajectory crosses x₁ = −1 at height 0.2?? NO: the trajectory EXITS the erosion at x₁ ≈ −1.07 (dist < s) — before reaching the face x₁ = −1: at x₁ = −1.07, height 0.2: f₁ > 0: continues right: at x₁ = −1: the field = −m (leftward): the trajectory DECELERATES... with a Lipschitz field, f₁ transitions from +V to −m over x₁ ∈ (−1.1, −1): the trajectory oscillates/settles near where f₁ = 0: at x₁* ∈ (−1.1, −1) with f₁(x₁*) = 0: the trajectory settles at x₁ = x₁*: WHICH IS OUTSIDE THE EROSION (x₁* ≈ −1.05: dist ≈ 0.07 < 0.1 ✓ outside!) but INSIDE K ✓. So: trajectory starts INSIDE K⊖s (e.g., at (−1.5, 0.2): dist((−1.5, 0.2), K^c): nearest boundary: the face x₁ = −1 at (−1, 0.25): √(0.25 + 0.0025) = 0.502 ≥ 0.1 ✓ also the top face x₂ = 1: 0.8: so dist = 0.502 ✓ ∈ K⊖s ✓) and converges to (x₁, 0.2) with dist((x₁, 0.2), K^c) = √((x₁*+1)² + (w − 0.2)²): x₁* = −1.05: √(0.0025 + 0.0025) = 0.07 < 0.1: OUTSIDE K⊖s ✓✓✓: THE TRAJECTORY EXITS THE EROSION while all hypotheses hold: uniform margin on ∂K (need to double check EVERY boundary point), K invariant, field Lipschitz (with large constant), NO disturbance, NO observation error needed (ε can be 0... well the claim needs ε > 0: take any ε > 0 and any c > 0: s = cε: choose parameters so that the exit happens at distance-scale s: i.e., for EVERY c > 0 the erosion K⊖cε fails: need the failure at arbitrarily small s: the junction geometry: dist((x₁, x₂), K^c) near the reentrant corner (−1, w): the eroded set near the reentrant corner (−1, 0.25): the erosion rounds the reentrant corner: the eroded boundary near the corner = arc of radius s around (−1, w)

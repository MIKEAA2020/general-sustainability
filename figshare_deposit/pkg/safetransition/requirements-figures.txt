# Figure regeneration environment (only needed for regenerating figures;
# the library itself has no dependencies). Versions pinned to those used
# for the committed figures, mirroring the master deposit's practice:
matplotlib==3.10.9
Pillow==12.3.0

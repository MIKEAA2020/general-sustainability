# ER009 Normalized Capture — A021 Parallel Proof Response 4

## Structure of the response

ER009 supplies:

1. a five-part diagnosis of the original claim;
2. a strong compact-NHIM theorem with an explicitly finite-dimensional compact binding manifold, inverse tangent estimates, full binding/slack normal contraction, and `beta > r alpha`;
3. a finite-time tracking theorem;
4. a conjecture for fractal/nonmanifold attractors;
5. a time-T-map proof using RFDE smoothing, C1 perturbation bounds, NHIM persistence, map-to-semiflow invariance, vertical projection, and continuous-time attraction;
6. a detailed hypothesis table;
7. a direct full-RFDE Hopf persistence theorem with delay as the bifurcation parameter;
8. a yield-parity limitation;
9. publication-ready LaTeX and a nine-item concrete checklist.

## Original-claim diagnosis

ER009 identifies:

- noncompactness of unrestricted history families without equicontinuity;
- absence of manifold structure;
- omission of transverse binding-history directions from the normal bundle;
- one-sided/noninvertible RFDE semiflow;
- displacement of both coordinates and need for a graph over the perturbed projected base.

## Conditional NHIM theorem

ER009 assumes:

- a compact connected finite-dimensional `Ck` invariant manifold `A_x` with a two-sided group on the base;
- a binding splitting `T X = T A_x ⊕ E_x^s`;
- both forward tangent and inverse tangent bounds;
- exponential binding-normal and slack-normal contraction;
- `beta=min(beta_x,beta_y)>r alpha`;
- smooth coupling and small yield-gap parameter.

It concludes a nearby `C^r` normally attracting invariant manifold, vertical graph over the perturbed base, and exponential attraction.

## Time-T proof route

ER009 takes `T>tau`, claims the RFDE time-T map is compact and `Ck`, derives `C1 O(epsilon)` closeness by Gronwall/variational equations, checks the product splitting, applies a Bates–Lu–Zeng theorem, then attempts to recover full semiflow invariance by commutation and uniqueness. It derives vertical graph bounds by projecting the persistent embedding and uses discrete attraction plus a bounded time-s remainder to obtain continuous-time attraction.

## Direct Hopf theorem

ER009 assumes a binding simple imaginary pair at delay `tau_*`, slack spectral stability, no other imaginary spectrum, and transverse crossing in delay. It derives an equilibrium curve, a coupled characteristic determinant, a perturbed root curve, and a critical-delay curve `tau_*(epsilon)=tau_*+O(epsilon)`, then invokes an RFDE Hopf theorem.

## Parity

ER009 correctly notes that `Delta_y→0` eliminates exponential smallness. It says the yield-gap reduction then fails, while other mechanisms are not ruled out.

## Final proposed publication status

ER009 proposes a full conditional invariant-graph theorem in publication text, a finite-time theorem, a direct Hopf theorem, and a parity limitation, while requiring concrete manifold/spectral/yield-gap verification.
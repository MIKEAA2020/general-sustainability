# ER019 Normalized Capture — Proposed Explicit Delay Oscillator and BLZ Promotion

## Receipt status

Complete substantive capture of the second of two post-implementation parallel audits. ER019 introduces a new two-dimensional radial oscillator, sets the delayed coefficient to zero, declares its periodic orbit a concrete A021 binding NAIM, assigns tangent/normal rates, assumes a stable slack block, claims a line-by-line BLZ 1999 match, and promotes the conditional graph to a persistent `Ck` attracting invariant circle with vertical representation.

## Claimed construction

1. New binding equations with parameters `d,omega_0,r_0,a,tau`.
2. Baseline `a=0` and claimed orbit `r_0(cos omega_0 t,sin omega_0 t)`.
3. History-circle embedding and rigid restricted flow.
4. Tangent rate `alpha=0`, radial rate `beta_x=2r_0^2`, assumed slack rate `beta_y`, and domination for every regularity order.
5. Claimed BLZ 1999 TAMS Theorems 2.1/3.1 match.
6. Claimed `Ck O(epsilon)` persistence, asymptotic phase, and vertical graph.

The proposed equations and parameters do not come from the supplied A021 record.
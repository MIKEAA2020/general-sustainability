# ER011 Normalized Capture — A021 Parallel Proof Response 6

## Headline result

ER011 concludes that the original invariant-graph claim is not proved or well posed. It recommends implementing only:

1. a finite-time `O(epsilon)` perturbation theorem;
2. a conditional direct characteristic/Hopf theorem;
3. a conjectural NHIM/vertical-graph programme;
4. a narrow parity limitation.

## Diagnosis

ER011 supplies examples/mechanisms for:

- noncompact unrestricted history families;
- compact invariant sets without manifold structure;
- omitted binding-transverse normal directions;
- RFDE history collapse and backward nonuniqueness;
- displacement of both coordinates and graph only over a perturbed base.

## Finite-time theorem

ER011 proves, for same initial histories and common Lipschitz/bounded region,

\[
\sup_{0\le t\le T}
\|z_t^\varepsilon-z_t^0\|
\le
\frac{|\varepsilon|M}{L}(e^{LT}-1)
\]

with the `L=0` limit. It also records the initial-mismatch extension

\[
e^{LT}\|z_0^\varepsilon-z_0^0\|
+
\frac{|\varepsilon|M}{L}(e^{LT}-1).
\]

It explicitly refuses invariant-graph, attraction, and all-time conclusions.

## NHIM status

ER011 does not invoke a persistence theorem because A021 supplies no compact manifold, splitting, domination, projection, or exact time-map hypotheses. It gives only a conjectural programme and a checklist.

## Direct Hopf

ER011 states a conditional theorem with separate bifurcation parameter `mu` and coupling `epsilon`, a smoothly varying equilibrium, analytic/C1 characteristic matrix, a simple isolated imaginary pair, spectral separation, and transverse crossing in `mu`. It yields `mu_*(epsilon)=mu_*+O(epsilon)`. A nonlinear branch remains conditional on the exact RFDE Hopf smoothness and nondegeneracy assumptions.

## Parity

ER011 correctly concludes only that the yield-gap exponential smallness certificate disappears at parity; other reductions are not excluded.

## Publication text

The proposed replacement makes the graph a conjecture, proves the finite-time theorem, gives a conditional Hopf statement, and includes a detailed verification checklist.
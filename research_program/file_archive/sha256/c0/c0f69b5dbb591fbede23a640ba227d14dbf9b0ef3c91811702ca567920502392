# ER018 Normalized Capture — Missing-Model NAIM, Domination, and BLZ Audit

## Receipt status

Complete substantive capture of the user-supplied first of two parallel audits following final A021 implementation. ER018 argues that the actual binding equations, parameters, delay structure, equilibria/orbits, and characteristic/Floquet data have not been supplied, so the three remaining concrete verification tasks cannot be completed without fabrication.

## Main content

1. Lists the data required to construct a concrete compact binding NAIM.
2. Supplies two hypothetical templates only: a hyperbolic attracting equilibrium (dimension zero) and a hyperbolic attracting periodic orbit (dimension one).
3. Refuses to invent a dimension-two-or-higher manifold.
4. Gives explicit time-map inverse-tangent bunching inequalities with prefactors:
   `M_s M_c exp(-(beta-alpha)T)<1` for `C1`, and
   `M_s M_c^r exp(-(beta-r alpha)T)<1` for `Cr`.
5. Separates tangent growth, binding-normal decay, slack decay, missing modes, and transient prefactors.
6. Gives a detailed—but explicitly not source-opened—BLZ hypothesis table.
7. Lists a 17-step data package that would close the binding-manifold, domination, and theorem-matching obligations.
8. Reaffirms that finite-time tracking, the confined slack tube, equilibrium continuation, and characteristic crossing remain the proved hierarchy; graph and nonlinear Hopf promotion remain conditional.

No publication block is proposed. No A021 manuscript change is requested from ER018 alone.
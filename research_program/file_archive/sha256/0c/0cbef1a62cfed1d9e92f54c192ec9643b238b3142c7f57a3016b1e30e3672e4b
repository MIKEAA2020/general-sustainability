# ER014 Normalized Capture — Time-T Graph Transform and Matched-Smoothness Hopf

## Receipt status

Complete substantive capture of the user-supplied response beginning “The conditional graph is now a theorem.” ER014 replaces ER013’s Lyapunov–Perron construction with a time-`T` graph transform, distinguishes the abstract conditional theorem from concrete A021, and separates `C1` spectral crossing from nonlinear Hopf. No text is implemented before joint adjudication.

## Claimed graph result

ER014 assumes a compact `C2` binding NAIM with a two-sided restricted flow, a `C1` invariant tangent/normal splitting, binding-normal and slack contraction, tangent norm and inverse-tangent bounds, and `beta>alpha`. It:

1. constructs normal-bundle tubular coordinates;
2. proves `C1 O(epsilon)` closeness of RFDE time-`T` maps for `T>tau`;
3. chooses `T` so stable contraction dominates inverse tangent growth;
4. defines a graph transform without inverting the ambient RFDE time map;
5. claims a unique Lipschitz fixed graph for the time-`T` map;
6. attempts to promote sampled invariance to full-semiflow invariance by commutation and uniqueness;
7. applies fiber contraction to obtain `C1 O(epsilon)` regularity;
8. claims local exponential attraction and a vertical graph over the perturbed projected base;
9. sketches higher-jet regularity under `beta>r alpha`.

It explicitly excludes the original history cube and leaves construction/rate verification of a concrete A021 binding NAIM open.

## Claimed nonlinear Hopf result

ER014 correctly preserves the `C1` characteristic-crossing theorem as non-Hopf. It invokes a classical RFDE Hopf theorem under `C3` smoothness, a simple transverse pair and nonresonance, and invokes continuity of the first Lyapunov coefficient under `C4` perturbations for nondegenerate type and stability. It excludes global folds and leaves concrete spectra and coefficients unverified.
# Specialist Prompt — Formal A018-to-A021 Binding/Slack and NAIM Crosswalk

## Role

Act as a specialist in retarded functional differential equations (RFDEs), Banach-space semiflows, normally attracting invariant manifolds, Floquet theory for delay equations, characteristic-root perturbation, and model-to-theorem traceability.

Your task is to determine whether one of the **existing A018 companion cores** can be instantiated as A021's binding block and, if so, to construct the complete formal crosswalk needed for A021's conditional compact-NAIM theorem. You must use the supplied sources. Do not invent a new oscillator, kinetic law, parameter point, equilibrium, periodic orbit, spectrum, or coupling.

The correct outcome may be:

1. a complete concrete crosswalk and verified theorem match;
2. a partial crosswalk with precisely enumerated missing data;
3. rejection of every candidate as unsuitable for the A021 NAIM role.

The weakest correct outcome is preferred to an unsupported promotion.

---

# 1. Source packet and authority

Read all supplied files completely, including appendices and tables:

1. `revised_articles/A018_capital_liquidation_corrected.tex` — canonical corrected companion source containing the candidate three-, four-, and five-state cores, parameters, characteristic data, Hopf results, periodic branches, and selected Floquet evidence.
2. `uploads/paper_IV_liebig_cm.txt` — immutable original A021 source defining the abstract vector-Liebig binding/slack split.
3. `revised_articles/A021_liebig_graph_corrected.tex` — implemented A021 source and current theorem hierarchy.
4. `research_program/article_A021_liebig_graph/data_availability_crosswalk.md` — current data-availability assessment.
5. `research_program/external_reviews/joint_audit_A021_internal_ER006_ER010.md` — controlling joint mathematical audit.
6. `research_program/external_reviews/A021_joint_decision_docket.csv` — proposition-level decisions.
7. If supplied, A018 verification appendices, parameter tables, characteristic derivations, and computational records referenced by the canonical source.

Treat A018's computational claims as verified at their exact source-stated model/operator status. Missing local archives are publication obligations, not grounds for relabeling verified source claims as unverified. However, do not infer an unreported full spectrum, invariant projection, uniform bundle estimate, or theorem hypothesis from a selected multiplier or branch diagnostic.

The corrected sources are authoritative over older prose where they conflict. Preserve exact distinctions among:

- the gated three-state core `(N,Z,E)`;
- the ungated comparison core;
- the turnover-corrected gated four-state working core `(N,A,Z,E)`;
- the five-state extension including detritus `U`;
- Candidate A and Candidate B;
- equilibria, Hopf small branches, large periodic families, and fold/turning-region events;
- characteristic-pinned claims, continuation-supported claims, and Floquet evidence.

---

# 2. Existing A021 abstraction

A021 writes each companion block abstractly as

\[
X^i=(N_i,A_i,Z_i,E_i),
\]

and, after selecting a binding block `k`, uses

\[
\dot x(t)=F^k(x_t)+\varepsilon f(x_t,y_t),
\qquad
\dot y(t)=G(y_t)+\varepsilon g(x_t,y_t),
\tag{A021}
\]

on

\[
X=C([ -\tau,0],\mathbb R^m),
\quad
Y=C([ -\tau,0],\mathbb R^n),
\quad
B=X\times Y.
\]

The perturbation scale is

\[
\varepsilon=C_{\mathrm{gap}}e^{-\rho\Delta_y}+\varepsilon_{\mathrm{phys}}.
\]

The soft-minimum yield-gap estimate may be accepted as proved. It supplies a possible small-coupling certificate; it does not supply compactness, a manifold, a splitting, or domination.

The current A021 source proves finite-time tracking, a confined slack tube, equilibrium continuation, and simple characteristic-crossing persistence. It states compact-NAIM persistence and nonlinear Hopf only conditionally. Do not weaken those proved results or promote conditional claims unless every required row below is closed.

---

# 3. Candidate A018 cores that must be compared

At minimum compare the following candidates.

## Candidate C3 — gated three-state core

The corrected A018 source gives

\[
\dot N=S(N)-qEN,
\]

\[
\dot Z=\tau_m^{-1}\left[
\max\left(0,
\operatorname{softplus}_k(qEN-S(N))-\frac{\ln2}{k}+\delta
\right)-Z
\right],
\]

\[
\dot E=\left(1-\frac{E}{E_{\max}}\right)
\left[
\eta E\left(\frac{Z(t-\tau)}{\Delta_{\mathrm{ref}}}-\frac{E}{E_{\max}}\right)
+\delta_0\frac{Z(t-\tau)}{Z_{\mathrm{ref}}+Z(t-\tau)}
\right],
\]

with `S(N)=rN(1-N/K)` in the frozen-`A` specialization. Candidate A baseline values include

\[
r=0.02,
\ K=100,
\ q=0.001,
\ \eta=0.914,
\ E_{\max}=30,
\ \delta_0=0.01,
\ \Delta_{\mathrm{ref}}=1,
\ \tau_m=5,
\ k=10,
\ \delta=\ln2/10,
\]

subject to confirmation against the canonical table.

## Candidate C4 — turnover-corrected gated four-state working core

The corrected A018 source gives

\[
\dot N=R(N,A)-qEN,
\]

\[
\dot A=-B(N,A)+\omega_A(A^{\mathrm{eq}}-A),
\]

with the same `Z` and delayed `E` equations after replacing `S(N)` by `R(N,A)`. The exact definitions of `R`, `B`, the dynamic target, and all parameter values must be copied from A018 rather than reconstructed from memory. The Candidate A frozen-donor equilibrium and the characteristic-pinned four-state Hopf data must be reported at their exact source-stated status.

## Candidate C5 — five-state extension

The corrected A018 source includes `(N,A,U,Z,E)`, with `U` driven or feeding back depending on the selected closure. Determine whether this is an actual autonomous binding block suitable for A021 or only an extension/reduction scaffold. Do not silently drop `U` if the selected closure feeds it back.

## Other candidates

You may evaluate Candidate B or a named ungated core only if it serves a clearly stated A021 purpose. Do not choose it merely because its bifurcation is easier. Explain why the selected candidate is scientifically and architecturally consistent with A021's vector-Liebig block.

---

# 4. Non-negotiable model-identity safeguards

1. Do not introduce equations absent from A018.
2. Do not relabel an illustrative normal form as A021.
3. Do not mix parameters from Candidate A and Candidate B.
4. Do not mix gated and ungated spectra or periodic branches.
5. Do not use three-state Floquet data as four-state Floquet data.
6. Do not identify a fold event, Hopf small branch, and large attracting cycle as the same invariant object.
7. Do not claim a compact NAIM from the history cube `C([−tau,0],K_x)`.
8. Do not infer the full normal spectrum from one dominant Floquet multiplier.
9. Do not infer a stable slack block without writing its equations, equilibrium, and complete characteristic-root bound.
10. Do not claim a theorem match from a bibliographic title alone.

If the intended A021 vector system requires multiple copies of an A018 core, write the product system and the block-index map explicitly.

---

# 5. Task A — Source-identity and equation crosswalk

Construct a source-traceable table:

| A021 symbol/object | Selected A018 equation/object | Exact source label/line | Transformation or specialization | Status |
|---|---|---|---|---|

The table must include:

- `x`, `y`, `F^k`, `G`, `f`, `g`;
- every state coordinate and its units;
- every delayed argument;
- every algebraic closure used;
- the physical/yield-gap coupling and any other cross-block coupling;
- the selected parameter point;
- the working phase space and norm.

For every A021 term, assign one of:

- `EXACT_CROSSWALK`;
- `DERIVED_WITH_PROOF`;
- `ADDITIONAL_ASSUMPTION`;
- `MISSING_FROM_SOURCE`;
- `INCOMPATIBLE`.

If `f` and `g` cannot be derived explicitly from the vector soft-minimum and companion equations, stop any concrete persistence claim and state exactly what formula is missing.

---

# 6. Task B — Select the binding invariant object

Evaluate, separately, the following possible bases for the selected candidate core.

## B1. Equilibrium base

Let

\[
\mathcal A_x=\{\widehat x_*\}.
\]

Determine:

- whether the equilibrium is explicitly defined at the selected parameter point;
- whether zero is excluded from the characteristic spectrum;
- whether every binding root is stable if a normally attracting zero-manifold is claimed;
- whether the result adds anything beyond the equilibrium implicit-function lemma.

A point does not produce a graph over a positive-dimensional history region.

## B2. Periodic-orbit base

Select one and only one named A018 periodic orbit, specifying:

- core and gate status;
- Candidate A/B;
- delay value;
- period and amplitude/status;
- branch identity (small Hopf branch, large family, etc.);
- whether it is attracting, unstable, or only continuation-supported.

Then establish or mark missing:

\[
\mathcal A_x=\Gamma_x=\{\gamma_t:t\in\mathbb R\}\subset X.
\]

Provide:

- dimension, compactness, boundary status, and two-chart atlas;
- regularity of the history embedding;
- restricted flow;
- simple phase multiplier `1`;
- every nontrivial Floquet multiplier/exponent;
- a complete invariant history-space complement and projections.

Selected dominant multipliers are insufficient if other multipliers are unreported.

## B3. Higher-dimensional base

Do not propose a torus, inertial manifold, or higher-dimensional NAIM unless A018 explicitly constructs it and supplies the required theorem and spectrum.

End Task B by selecting the strongest supportable object or concluding `NO_CONCRETE_NAIM_SELECTED`.

---

# 7. Task C — Binding/slack decomposition

If A021 contains one binding resource block and several slack copies, write the full uncoupled product and coupling explicitly:

\[
\dot x=F^k(x_t),
\qquad
\dot y=(F^1(y_t^1),\dots,F^{k-1}(y_t^{k-1}),F^{k+1}(y_t^{k+1}),\dots).
\]

Then derive the perturbed terms from the soft-minimum service and physical couplings. Specify:

- which state equations receive off-limiting derivatives;
- whether the perturbation is genuinely `O(e^{-rho Delta_y})` in `C^1`;
- the domain on which the yield gap is uniform;
- whether `epsilon_phys` couples other coordinates;
- whether the slack equilibrium is a product of named equilibria;
- whether every slack factor is exponentially stable at the chosen parameters.

Do not use a single scalar `beta_y` until every slack block has been checked and the minimum taken.

---

# 8. Task D — Complete linear and Floquet data

For the selected object, produce a table:

| Quantity | Definition | Existing A018 evidence | Value/bound | Missing work |
|---|---|---|---|---|
| `alpha` | tangent forward growth | | | |
| `alpha_inv` | inverse-tangent growth | | | |
| `M_c` | tangent transient prefactor | | | |
| `beta_x` | worst binding-normal decay | | | |
| `M_x` | binding-normal prefactor | | | |
| `beta_y,j` | slack decay for block `j` | | | |
| `M_y,j` | slack prefactor | | | |
| full complement | all modes assigned | | | |

For an equilibrium, derive the characteristic matrix from the exact equations and distinguish:

- invertibility at zero;
- absence of imaginary roots;
- strict stability of all roots.

For a periodic orbit, report the complete Floquet spectrum at the selected orbit. If only selected multipliers are in A018, state `INCOMPLETE_FLOQUET_SPECTRUM`; do not substitute a dominant multiplier for the full set.

All computational values already reported in A018 may be trusted at their exact stated status. New calculations must be reproducible and separately identified.

---

# 9. Task E — Domination with prefactors

Set

\[
\beta=\min(\beta_x,\beta_{y,1},\ldots),
\qquad
M_s=\max(M_x,M_{y,1},\ldots).
\]

For a time-`T` map, evaluate—not merely state—

\[
q_1(T)=M_sM_c e^{-(\beta-\alpha_{\mathrm{inv}})T}
\]

for `C1` persistence. If higher regularity is claimed, evaluate

\[
q_r(T)=M_sM_c^r e^{-(\beta-r\alpha_{\mathrm{inv}})T}.
\]

State the selected theorem's exact conorm/bunching expression and reconcile it with these exponential bounds. Include all prefactors and specify `T>tau`.

Return a numerical table if data permit. Otherwise identify each missing number and conclude `DOMINATION_NOT_VERIFIED`.

---

# 10. Task F — Exact persistence-theorem match

Candidate references are:

- P. W. Bates, K. Lu, and C. Zeng, *Existence and Persistence of Invariant Manifolds for Semiflows in Banach Space*, Memoirs AMS **135** (1998), no. 645, DOI `10.1090/memo/0645`;
- Bates, Lu, and Zeng, *Persistence of Overflowing Manifolds for Semiflow*, CPAM **52** (1999), 983–1046;
- Bates, Lu, and Zeng, *Invariant Foliations Near Normally Hyperbolic Invariant Manifolds for Semiflows*, TAMS **352** (2000), 4641–4676.

Open the actual source used. Do not cite the repeatedly proposed but unverified “1999 TAMS 351, 4341–4368” entry unless the document is produced and bibliographically verified.

Provide:

- exact citation;
- theorem number and verbatim mathematical hypothesis summary;
- flow/semiflow/map status;
- manifold regularity and boundary assumptions;
- complete splitting and bunching;
- positive-time and `t=0` differentiability requirements;
- perturbation topology;
- localization and tubular assumptions;
- exact persistence, uniqueness, attraction, foliation/asymptotic-phase, and regularity conclusions.

Then complete:

| External theorem hypothesis | Selected A018/A021 object | Evidence | Status |
|---|---|---|---|

Use only `VERIFIED`, `ASSUMED`, `MISSING`, or `INCOMPATIBLE`.

If the source theorem cannot be opened, return `THEOREM_NOT_SOURCE_MATCHED` and do not promote A021.

---

# 11. Task G — Vertical projection and graph meaning

If persistence yields an embedding

\[
\iota_\varepsilon:\mathcal A_x\to B,
\]

separately establish whether

\[
q_\varepsilon=\pi_x\circ\iota_\varepsilon
\]

is a global embedding. Use compact-embedding stability, not derivative injectivity alone. Define

\[
\mathcal A_{x,\varepsilon}=q_\varepsilon(\mathcal A_x),
\qquad
h_\varepsilon=\pi_y\circ\iota_\varepsilon\circ q_\varepsilon^{-1}.
\]

State clearly:

- the graph is over the perturbed projected base;
- its dimension;
- whether it is only a point, a closed curve, or a higher-dimensional manifold;
- whether attraction is in-tube, local all-time, or global;
- whether an asymptotic-phase foliation is actually supplied by the cited theorem.

Never describe a point or periodic orbit as a graph over a region of binding histories.

---

# 12. Task H — Hopf and periodic-orbit consistency

Cross-check the selected invariant object against A018's local and global bifurcation data.

- A Hopf equilibrium is not a periodic NAIM at the critical parameter.
- A small unstable Hopf branch is not a normally attracting base.
- A large attracting periodic orbit may be a candidate only at a parameter where its complete Floquet complement is strictly stable.
- A fold/turning event with a multiplier approaching `+1` is not normally hyperbolic at the fold.
- Full orbital stability requires the full complementary spectrum, not only the first Lyapunov coefficient.

If using delay as a parameter, keep the phase space fixed through a model-valid normalization or use an exact varying-delay theorem. Do not assume generic dilation is differentiable on `C0`.

---

# 13. Required outputs

Return all outputs in this exact order.

## Output A — Executive decision

Choose one:

- `COMPLETE_CROSSWALK_AND_PROMOTION_SUPPORTED`;
- `PARTIAL_CROSSWALK_ONLY`;
- `NO_A018_CANDIDATE_SUPPORTS_A021_NAIM`.

Give a concise reason and name the selected candidate, if any.

## Output B — Source-identity crosswalk

Provide the full table from Task A with exact source labels.

## Output C — Candidate comparison

Compare C3, C4, C5, Candidate A/B, gated/ungated, equilibrium/periodic bases. Explain the selection or rejection of each.

## Output D — Selected invariant object

Give its equations, parameters, phase space, dimension, atlas, boundary status, restricted flow, and source provenance. If none is selected, state the precise missing items.

## Output E — Binding/slack/coupling decomposition

Write `F^k`, `G`, `f`, and `g` explicitly or mark each missing term.

## Output F — Complete spectral/Floquet table

Provide every tangent and normal rate, prefactor, projection, and missing datum.

## Output G — Domination calculation

Evaluate the exact theorem inequality, including prefactors. No sign-only conclusion.

## Output H — External-theorem matching table

Use a source-opened theorem and the four status labels required above.

## Output I — Vertical graph and attraction status

State exactly what geometric object would persist and what attraction/foliation conclusion is justified.

## Output J — Hopf consistency report

Separate characteristic crossing, periodic-branch existence, criticality, complete Floquet stability, and global folds.

## Output K — Minimal missing-data request

List the smallest additional files or numbers needed to close every unresolved row. Do not ask for data already present in A018.

## Output L — Publication consequence

State one of:

- no A021 source change;
- add a source crosswalk but retain conditional status;
- promote a specific conditional result;
- promote a concrete theorem.

Provide publication-ready LaTeX only for changes supported by the completed tables. Do not rewrite unrelated sections.

## Output M — Machine-readable decision table

End with CSV-formatted rows having columns:

`item_id,claim,selected_source,status,evidence,missing_dependency,publication_action`

This table must include every theorem hypothesis and every proposed manuscript change.

---

# 14. Acceptance criteria

A response is acceptable only if it:

1. reads the complete A018 and A021 sources;
2. preserves model/operator distinctions;
3. gives exact equation and parameter provenance;
4. does not fabricate missing couplings or spectra;
5. reports complete rather than selected normal data, or explicitly marks incompleteness;
6. evaluates prefactor-aware bunching;
7. opens and matches the actual external theorem;
8. distinguishes embedding persistence from vertical projection;
9. distinguishes equilibrium, Hopf, periodic orbit, and fold claims;
10. yields an auditable source-change decision.

The purpose is not to force A021 promotion. The purpose is to determine, without fabrication, whether the existing A018 record can support one.
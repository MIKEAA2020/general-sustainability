# Joint Audit — ER034 and ER035 Validated-Numerics Specification

## Decision

Approve the five-module CAP specification for execution. Implement only a concise numerical seed/specification status paragraph. Do not promote the compact-NAIM, graph, attraction, foliation, or nonlinear Hopf claims.

## Accepted facts

- phase-corrected period seed `370.9311778463889 yr`;
- endpoint mismatch `4.90e-7`;
- RFDE spectral residual at most `6.80e-6`;
- statewise residual vector;
- numerical full floor-argument margin `1.4755423e-3`;
- 512 positive/negative Fourier modes per state archived;
- radii-polynomial/Floquet/slack/bunching/promotion modules fully specified;
- interval execution pending.

## Required wording

- seed values are numerical, not interval enclosures;
- floor margin applies to the approximate seed; the validated ball must preserve it using the full composed floor functional;
- no `nu,Y,Z0,Z1,Z2,r` values are claimed;
- no continuum multiplier, projection, or bunching result is claimed;
- the internal slack scaffold is named, but actual A021 `G,f,g` remain source-unspecified;
- no theorem number or theorem promotion.

## Implementation

Add one paragraph to the existing numerical crosswalk subsection. The paragraph may say the CAP specification is ready and list its five modules. It must state execution is pending and conditional status is unchanged.
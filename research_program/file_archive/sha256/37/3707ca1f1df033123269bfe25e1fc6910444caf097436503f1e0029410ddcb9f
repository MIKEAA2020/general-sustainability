# ER013 Normalized Capture — Claimed Full Invariant-Graph and Nonlinear-Hopf Proofs

## Receipt status

Complete substantive capture of the user-supplied response titled “Rigorous Proofs of the Invariant Graph and Nonlinear Hopf Theorems.” The response claims to close both remaining abstract problems, supplies detailed proofs, a publication-ready LaTeX block, and concrete-model obligations. No claim is implemented pending joint mathematical adjudication.

## Claimed invariant-graph package

The response assumes a compact finite-dimensional `C^k` binding manifold in the RFDE history space, a two-sided flow on that manifold, an invariant tangent/normal splitting, forward normal contraction, a stable slack equilibrium, and `beta=min(beta_x,beta_y)>alpha`. It then:

1. asserts Banach tubular coordinates `(theta,v,eta)` near the product manifold;
2. rewrites the RFDE as tangent and stable-fiber differential equations;
3. cuts off nonlinearities with a purported smooth radial bump;
4. defines a Lyapunov–Perron operator on continuous/Lipschitz bundle sections using complete perturbed base trajectories;
5. claims contraction, `C^1` regularity by fiber contraction, `C^1 O(epsilon)` size, positive invariance, projection diffeomorphism, vertical graph representation, and exponential attraction.

The publication theorem states these conclusions from three primary hypotheses: compact binding NAIM, slack stability, and `beta>alpha`.

## Claimed nonlinear-Hopf package

The response assumes a `C^k`, `k>=3`, parameter-dependent coupled RFDE with a smooth equilibrium branch, a simple transverse binding pair, no other binding imaginary spectrum, no slack imaginary spectrum, and nonzero uncoupled first Lyapunov coefficient. It then:

1. introduces Hale’s bilinear form and primal/dual eigenvectors;
2. claims `O(epsilon)` perturbation of eigenvectors;
3. constructs a center manifold and quadratic coefficients `w20,w11`;
4. displays a cubic coefficient formula and claims `c1(mu_*(epsilon),epsilon)=c1_x(mu_*)+O(epsilon)`;
5. claims a unique periodic family with quadratic parameter and period expansions;
6. classifies criticality and claims full-space orbital asymptotic stability;
7. claims the slack profile is `O(a^2+epsilon a)`.

## Concrete obligations acknowledged by ER013

ER013 leaves the concrete A021 application conditional on exhibiting a compact invariant binding manifold, computing tangent/normal exponents, checking domination, computing a nonzero first Lyapunov coefficient, and maintaining a uniform yield gap.
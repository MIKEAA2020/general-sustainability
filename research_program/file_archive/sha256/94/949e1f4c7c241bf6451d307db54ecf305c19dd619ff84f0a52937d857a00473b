# ER012 Normalized Capture — A021 Parallel Proof Response 7 (Truncated)

## Receipt status

The supplied response ends mid-proof at the equilibrium Jacobian matrix in Lemma C7. Outputs E–H and the remainder of Output C are not available in the received message. This capture records the available substantive content only.

## Available content

ER012 adopts Route F and states that only the following are proved:

1. local RFDE well-posedness;
2. finite-time `C0` perturbation, plus a claimed `C1` time-map perturbation;
3. a slack-neighborhood theorem based on exponential stability and a modulus of continuity.

It keeps the invariant graph as a conjecture and begins an equilibrium lemma intended for a later direct Hopf result.

## Diagnosis

ER012 repeats the five failures:

- noncompact unrestricted history family;
- no manifold structure;
- omitted binding-transverse normal directions;
- noninvertible/collapsing RFDE semiflow;
- embedding rather than automatic vertical graph over the old base.

## Finite-time theorem

ER012 proves the same-history and initial-mismatch estimates:

\[
\sup_{0\le t\le T}\|z_t^\varepsilon-z_t^0\|
\le
\frac{|\varepsilon|M}{L}(e^{LT}-1)
\]

and

\[
e^{LT}\|z_0^\varepsilon-z_0^0\|
+
\frac{|\varepsilon|M}{L}(e^{LT}-1).
\]

## Slack-neighborhood theorem

Assuming an exponentially stable slack semigroup and using the `C1` remainder through a modulus

\[
|N(\psi)|
\le
\kappa(\delta)
\|\psi-\widehat y_*\|,
\]

ER012 derives exponential decay of initial slack error plus an `O(epsilon)` forcing floor, conditional on the binding history remaining in a bounded set. It correctly distinguishes order-one and order-epsilon initial errors.

## Graph conjecture

ER012 proposes a conditional compact-manifold conjecture with complete splitting, stable/unstable/tangent estimates, domination, uniform tubular regularity, and projection diffeomorphism. It explicitly does not define a graph-transform operator.

## Hypothesis review

ER012 explains why Routes A–E are unavailable from the original assumptions and treats time-map closeness as the only established part of Route B.

## Truncation point

The response begins Lemma C7 on equilibrium persistence and ends after writing the block-diagonal Jacobian. The rest of the lemma, direct Hopf theorem, parity output, publication text, and final checklist were not received.
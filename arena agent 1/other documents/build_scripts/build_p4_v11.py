#!/usr/bin/env python3
"""Build paper4_delay_dynamics_v11.md from v10: the certified second fold at tau_f2 = 64.4023272.

v10 is left untouched. Every replacement is exact-matched and assert-counted.
"""
import re

src = open("/home/user/arena agen1/paper4_delay_dynamics_v10.md").read()
edits = []

def rep(old, new, count=1):
    global src
    assert src.count(old) == count, f"pattern count mismatch ({src.count(old)} != {count}): {old[:70]!r}"
    src = src.replace(old, new, 1)
    edits.append(old[:55])

# ---- R1: abstract (second fold + abstract trims to hold the word cap) ----
rep("Global numerics give a five-regime topology; the committed continuation record resolves the lower boundary into one certified fold of an S-shaped branch (saddle-node classification provisional).",
    "Global numerics give a five-regime topology; the committed continuation records certify both global folds (lower S-branch 5.5872362; second fold 64.4023272 yr), the 148.6–149.5 capture onset a basin boundary, not a fold (saddle-node classification provisional).")
rep("per the exact held-measurement update (stable at every tested interval, maximum spectral radius 0.9967)",
    "per the exact held-measurement update (max spectral radius 0.9967)")
rep("at 6.50 yr under the exact update (the Euler-reported 47.536 yr crossing a command-step artefact)",
    "at 6.50 yr exactly (the Euler-reported 47.536 yr crossing a command-step artefact)")

# ---- R2: contributions item 6 ----
rep("resolves the lower boundary into a single fold at τ = 5.5872362 yr of one S-shaped branch — the subcritical-Hopf small arm and the stable large arm meet at one Krawczyk-certified fold — superseding the inherited two-fold reading (basin collapse in τ ∈ [5.574, 5.576] yr versus a small-branch fold near 5.587 yr, whose generating records are not in the repository); the saddle-node-of-periodic-orbits classification remains provisional against a crisis alternative.",
    "resolves the lower boundary into a single fold at τ = 5.5872362 yr of one S-shaped branch — the subcritical-Hopf small arm and the stable large arm meet at one Krawczyk-certified fold — superseding the inherited two-fold reading (basin collapse in τ ∈ [5.574, 5.576] yr versus a small-branch fold near 5.587 yr, whose generating records are not in the repository); the upper boundary is likewise resolved by the second-fold campaign: a second certified fold at τ_f2 = 64.4023272 yr of a second S-branch, whose stable returning arm is not generically reachable near the fold — the 148.6–149.5 capture onset is a basin boundary, not a fold; the saddle-node-of-periodic-orbits classification remains provisional against a crisis alternative.")

# ---- R3: five-regime regimes (iii)/(iv) ----
rep("(iii) $\\tau_f < \\tau < \\tau_{\\mathrm{cap}}$, settling to the equilibrium from the tested histories (finite searches support, but cannot prove, interior monostability); (iv) $\\tau_{\\mathrm{cap}} < \\tau < \\tau_+$, the captured family reappears beside the still-stable equilibrium (upper window, basin-grid bracket $[148.6, 149.5]$ yr);",
    "(iii) $\\tau_f < \\tau < \\tau_{f2}$, settling to the equilibrium from the tested histories (finite searches support, but cannot prove, basin monostability; the second-fold branch records show an unstable upper arm existing mathematically through this window, without generic basin); (iv) $\\tau_{f2} < \\tau < \\tau_+$, mathematical bistability: the second S-branch's stable returning arm coexists with the still-stable equilibrium from the certified second fold $\\tau_{f2} = 64.4023272$ yr, but the committed basin records find the stable arm generically unreachable near the fold — the capture onset in $[148.6, 149.5]$ yr is a basin boundary inside this window, not a fold;")

# ---- R4: figure reference + caption ----
rep("![Figure 1](figs_p4/fig2_five_regime_topology.png)",
    "![Figure 1](figs_p4/fig2_five_regime_topology_v2.png)")
rep("**Figure 1.** The five-regime attractor topology from the committed continuation record (pre-registered campaign, 2026-09-02, first run, no independent rerun yet; every panel drawn from the deposited campaign CSVs, see Data availability; no inherited number is drawn). (a) The lower S-branch: the subcritical-Hopf small arm (red, $\\mu_1 > 1$) and the stable large arm (blue, $\\mu_1 < 1$) meet at the single Krawczyk-certified fold $\\tau_f = 5.5872362$ yr. (b) The upper region: the collocated Hopf small branch, the basin-grid capture-onset bracket $[148.6, 149.5]$ yr, and the certified upper Hopf $\\tau_+ = 150.35848$ yr; the captured $E \\approx E_{\\max}$ face-cycle family exists in the basin archive only (not Fourier-collocatable at $m = 64$ or $128$). (c) The dominant real multipliers of both arms crossing $+1$ at the same fold. (d) Basin classifications (captured / settles / intermediate) on the $27 \\times 3$ grid (dt = 0.02): the depleted history is captured at $\\tau = 5.575$ (the reverse of the inherited asymmetry); the interior grid settles; capture returns only inside $[148.6, 149.5]$ yr.",
    "**Figure 1.** The five-regime attractor topology from the two committed continuation records (five-regime campaign and second-fold campaign, 2026-09-02, both first runs, no independent reruns yet; every panel drawn from the deposited campaign CSVs, see Data availability; no inherited number is drawn). (a) The lower S-branch: the subcritical-Hopf small arm (red, $\\mu_1 > 1$) and the stable large arm (blue, $\\mu_1 < 1$) meet at the single Krawczyk-certified fold $\\tau_f = 5.5872362$ yr. (b) The upper S-branch: the unstable arm from the Hopf at $\\tau_+$ folds at the certified $\\tau_{f2} = 64.4023272$ yr and returns as a stable arm (inset: its real multiplier through the fold), with the basin capture onset $[148.6, 149.5]$ yr — a basin boundary, not a fold — and the certified $\\tau_+ = 150.35848$ yr. (c) The lower branch's dominant real multipliers crossing $+1$ at the same fold. (d) Basin classifications (captured / settles / intermediate) on both committed grids ($27 \\times 3$ plus the second-fold grids A/B, dt = 0.02): the depleted history is captured at $\\tau = 5.575$ (the reverse of the inherited asymmetry); every tested history settles from $5.6$ to $146$ (H1 intermediate at $145$–$146$); capture returns only inside $[148.6, 149.5]$ yr.")

# ---- R5: upper-boundary paragraph -> second-fold record ----
old_p3 = "At the upper boundary the committed record's basin grid places the capture onset in $[148.6, 149.5]$ yr (the large-stock history H1 first captured at $149.5$ and not at $148.6$), while the inherited persistence-bisection bracket $[148.125, 148.438]$ yr (summarised 148.3 yr) is not reproduced; the exact fold type and branch connection remain undetermined. The captured object is an $E \\approx E_{\\max}$ face cycle (peak-to-peak $N \\approx 44$; $E \\in [0.35, 29.91]$ on the basin seeds), which the campaign could not Fourier-collocate at $m = 64$ or $m = 128$ (two recorded honest failures: Newton divergence and stall at residuals $4.5\\times10^2$ and $9.0\\times10^2$) — its existence and location are carried by the basin archive only, with no collocation branch record, no Moore–Spence fold location, and no Floquet record. The inherited \"interior large family\" (residual $\\sim10^{-13}$ on $\\tau \\in [147.5, 160]$, amplitude $15.9$–$19.5$) is NOT-TESTED by the campaign: the pre-registered independence discipline forbids continuation seeding from inherited numbers, and the campaign's basin histories find the face cycle instead. The Hopf small branch is reproduced at both ends of the inherited amplitude window ($0.100$ at $\\tau = 150.31$ versus $0.11$; $1.874$ at $130$ versus $1.87$ — both within $15\\%$). The inherited third family \"at $E \\ge E_{\\max}$ collocating down to $\\tau \\approx 144.5$\", reported as an inadmissible continuation branch excluded from the topology, is superseded by the committed record's finding that the face-riding cycle is reached from admissible histories in the upper window: the admissible topology of regimes (iv)–(v) is carried by the face cycle."
new_p3 = "At the upper boundary the committed second-fold record (the pre-registered second-fold campaign of 2026-09-02; same deposit and status discipline as the five-regime campaign) resolves the boundary into a **certified second fold at $\\tau_{f2} \\in [64.402327203368, 64.402327203372]$ yr**: the upper branch, carried downward from the converged cycle at $\\tau_+ - 0.05$, neither terminates at $\\tau = 130$ nor fails collocation anywhere — $183$ accepted natural steps at residuals $\\le 3.7\\times10^{-12}$ from $\\tau = 150.308$ to the turning-point stall at $64.402327895$ — and turns at the interval-Krawczyk-certified Moore–Spence zero (unique in the box, $G'$ nonsingular throughout, both nondegeneracy constants excluding zero; three-order agreement $5.53\\times10^{-7}$ at $m = 64/96/128$). The branch is a second S: the unstable subcritical arm from the Hopf at $\\tau_+$ (real multiplier $> 1$ at every record, peaking at $1.27745$ at $\\tau = 74.902$) meets the fold at amplitude $\\approx10.5$, and sixteen pseudo-arclength points past the turn record a **stable** returning arm ($\\mu_1 = 0.9993$ falling monotonically to $0.9435$ at $\\tau = 64.438$). **The stable arm is not generically reachable near the fold**: the campaign's basin grids find every tested history settling around $\\tau_{f2}$ ($21$ runs at $\\pm0.5/\\pm1/\\pm2$; dt-halving at $64.4$ unchanged) and throughout the $133$–$146$ gap ($21$ runs; H1 intermediate at $145$–$146$, critical slowing toward the capture window). The capture onset $[148.6, 149.5]$ yr is therefore a **basin boundary** inside the mathematically bistable window $(\\tau_{f2}, \\tau_+)$ — not the stable family's birth (that is $\\tau_{f2}$) and not the equilibrium's stability boundary (that is the certified $\\tau_+$) — and the five-regime campaign's provisional description of that boundary as a possible fold location is superseded. The captured object there remains the $E \\approx E_{\\max}$ face cycle (peak-to-peak $N \\approx 44$; $E \\in [0.35, 29.91]$ on the basin seeds), which no campaign could Fourier-collocate at $m = 64$ or $m = 128$ (two recorded honest failures): the connection from the stable arm's last record ($\\tau = 64.438$) to the face-cycle family is the natural conjecture, unverified over the $64.438$–$148.6$ gap (owner-gated follow-up campaign). The inherited \"interior large family\" (residual $\\sim10^{-13}$ on $\\tau \\in [147.5, 160]$, amplitude $15.9$–$19.5$) remains NOT-TESTED (the independence discipline forbids seeding from inherited numbers). The inherited upper bracket $[148.125, 148.438]$ yr is not reproduced and is superseded as a fold location: it was a basin-bisection estimate of the capture onset, which the committed grids place at $[148.6, 149.5]$ yr. The Hopf small branch is reproduced at both ends of the inherited amplitude window ($0.100$ at $\\tau = 150.31$ versus $0.11$; $1.874$ at $130$ versus $1.87$ — both within $15\\%$). The inherited third family \"at $E \\ge E_{\\max}$ collocating down to $\\tau \\approx 144.5$\", reported as an inadmissible continuation branch excluded from the topology, is superseded by the committed record's finding that the face-riding cycle is reached from admissible histories in the upper window: the admissible topology of regimes (iv)–(v) is carried by the face cycle."
rep(old_p3, new_p3)

# ---- R6: §9.3 M3-B register ----
rep("the upper capture onset is $[148.6, 149.5]$ yr (basin grid), the inherited bracket $[148.125, 148.438]$ yr is not reproduced, and the fold type remains undetermined.",
    "the upper boundary is the certified second fold at $\\tau_{f2} \\in [64.402327203368, 64.402327203372]$ yr (interval Krawczyk on the $m = 64$ system; three-order agreement $5.53\\times10^{-7}$), the upper branch an S whose stable returning arm is not generically reachable near the fold — the capture onset $[148.6, 149.5]$ yr (basin grid) is a basin boundary inside the bistable window $(\\tau_{f2}, \\tau_+)$, the inherited bracket $[148.125, 148.438]$ yr is not reproduced, and the fold type (saddle-node of periodic orbits) remains undetermined.")
rep("Multiple far-from-equilibrium seeds failed to reach a large cycle throughout the tested interior range $5.57 < \\tau < 148.3$ yr — finite searches that support, but cannot prove, interior monostability.",
    "Multiple far-from-equilibrium seeds failed to reach a large cycle throughout the tested interior range $5.57 < \\tau < 148.3$ yr — finite searches that support, but cannot prove, interior monostability (basin-scoped: the second-fold branch records show an unstable upper arm existing mathematically on $64.4 < \\tau < 150.4$ yr without generic basin, so the statement concerns basins, not branch existence).")

# ---- R7: certification-hierarchy paragraph ----
rep("and the fold certification now committed for the lower fold at the discrete collocation level (rebuilt Moore–Spence/Krawczyk stage; the continuum off-grid residual stage remains lost and the continuous-delay lift open), documented component by component in the supplementary material.",
    "and the fold certification now committed for both folds at the discrete collocation level (the lower fold's rebuilt Moore–Spence/Krawczyk stage, and the second fold's interval-Krawczyk certificate on the $m = 64$ system with three-order agreement $5.53\\times10^{-7}$; the continuum off-grid residual stage and the continuous-delay lift remain open for both), documented component by component in the supplementary material.")

# ---- R8: §9.4 "upper fold moves from 148 to 64" (first occurrence) ----
rep("The upper global fold moves from $\\approx148$ yr (three-state) to $\\approx64$ yr (four-state) while the Hopf pair stays within a few percent — the global objects are model-version-specific in a way the local ones are not;",
    "The certified three-state second fold at $64.4023$ yr matches the four-state's upper fold bracket ($\\approx64.4$ yr, persistent orbit of amplitude $\\approx11$ at $64.5$, continuation failure by $64.25$) within $0.05\\%$ — the upper fold location is version-robust, the three-state's earlier $\\approx148$ yr reading having been a basin-boundary estimate — while the Hopf pair stays within a few percent and the basin reachability of the stable arm is version-specific;")

# ---- R9: §10.x second occurrence ----
rep("the upper global fold moves from $\\approx148$ yr (three-state) to $\\approx64$ yr (four-state) while the Hopf pair stays within a few percent, the equilibrium memory differs",
    "the upper fold is version-robust at $\\approx64.4$ yr (certified three-state second fold $64.4023$; four-state bracket $[64.25, 64.5]$) while the Hopf pair stays within a few percent, the equilibrium memory differs")

# ---- R10: early-warning paragraph ----
rep("while the unresolved events of Section 9 may be basin-boundary reorganisations or numerical capture transitions rather than folds, so each indicator must be tied to a hypothesised mechanism before it is used;",
    "while the capture onset of Section 9.2 is now known to be a basin boundary — the certified second fold sits $84$ yr lower in $\\tau$ — and the lower fold's crisis alternative remains live, so an indicator tuned to a fold threshold would be watching the wrong event, and each indicator must be tied to a hypothesised mechanism before it is used;")

# ---- R11: conclusion ----
rep("a five-regime attractor topology on the explored fundamental-delay range whose lower boundary is a single fold of one S-shaped branch (Krawczyk-certified at the discrete collocation level; saddle-node classification provisional)",
    "a five-regime attractor topology on the explored fundamental-delay range whose global boundaries are both certified folds — the lower S-branch fold at $5.5872362$ and the second fold at $64.4023272$ yr, the $148.6$–$149.5$ capture onset being a basin boundary, not a fold (both certificates at the discrete collocation level; saddle-node classification provisional)")

# ---- R12: data availability ----
rep("the rebuilt fold-certification stage sits beside the A025 pipeline at `research_program/validated_computations/a025_fold/`.",
    "the rebuilt fold-certification stage sits beside the A025 pipeline at `research_program/validated_computations/a025_fold/`; the second-fold records of Section 9.2 (200 branch records, 45 basin runs, the interval-Krawczyk certificate and its preserved frozen-box failure record) are deposited at `research_program/validated_computations/a025_second_fold/` with their pre-registered plan and execution report.")

open("/home/user/arena agen1/paper4_delay_dynamics_v11.md", "w").write(src)

# ---- abstract word count gate ----
m = re.search(r"Classical delay studies.*?Keywords:", src, re.S)
body = m.group(0)[:-len("Keywords:")].strip()
body = re.sub(r"!\[.*?\]\(.*?\)", "", body, flags=re.S)
body = re.sub(r"\$\$.*?\$\$", " EQ ", body, flags=re.S)
body = re.sub(r"\$[^$]*?\$", " M ", body)
body = re.sub(r"\*\*|\*", "", body)
words = re.findall(r"[A-Za-z0-9][A-Za-z0-9./\-–']*", body)
print("edits applied:", len(edits))
print("abstract words:", len(words), "(cap 250)")

# Addendum — *From Obstruction to Design: Fibre Conditions for Monitoring and Indicator Choice*, Edition 3

**Lineage.** `monitoring_design_v1` (D8) → `v2` (round 4) → `v3` (this edition; earlier editions retained unmodified).
**Audit.** Sixth joint-audit round: a single grok piece proposing the scoped primal–dual selector principle. Two of its findings were verified against this lineage and ship here.
**Repair 1 (the delay identity — round-4 repair completed).** Edition 2 restated the delay rule as "the identity z0 ≥ 1 + T_obs, equivalently T_obs < σ*(z0)" — the *equivalently* was false: on the integer grid the boundary cell z0 = 1 + T_obs is viable (the review lands exactly at exit), so the strict form fails exactly there (e.g. (z0, T_obs) = (2, 1); check E9 pins both the zero-mismatch non-strict identity T_obs ≤ σ*(z0) = z0 − 1 and the single strict-form failure). The reviewer's round-4 phrase "the delay-rule off-by-one" is acknowledged as having been under-read in round 4; this edition completes the repair.
**Repair 2 (the coarsest-partition scoping).** The maximal common-action sets of the audited target, {(1,2),(2,2)} and {(2,1),(2,2),(3,1)}, overlap in (2,2); a coarsest adequate monitoring therefore exists (upper-set property) and is non-unique (two 2-cell minimizers), and is *not* "the" partition into maximal common-action sets — no such partition exists on this target (check E9). This corrects the reviewer's Corollary C wording, which asserted existence of that partition.
**Verification.** `monitoring_design_v3_verify.py`: nine check families (E1–E8 retained from Edition 2, with the E4 message corrected to the non-strict identity; E9 the scoping corrections), exact arithmetic, 9/9 passing.

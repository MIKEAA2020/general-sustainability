# Residual gpt-audit adjudication — worked systems v9, computational certification v6

**Input.** The two unread audit files: `worked gpt audit.txt` (1,566 lines; targets the worked-systems paper) and `computational gpt audit.txt` (1,764 lines; targets the computational paper), read in full this round. The third named file, `computational elevation audit.txt` (981 lines), was read and adjudicated point-by-point in the previous round (comp v5 + S1 v5; addendum on record). Directive: find remaining points worth implementing — as is or after correction — and **adjudicate contradictory points before implementing anything**.

**Method.** Both gpt audits were written against earlier editions; every candidate point was therefore checked against the *current* sources (`paper2_worked_systems_v8.tex`, `paper2_computational_certification_v5.tex`, S1 v5, both verification harnesses) before any verdict. Verdicts: **shipped** (already in the current edition — no action), **implement** (this round), **corrected** (implemented with modification), **refuted**, **moot/edition-drift**, **ledger** (owner-side, standing). Contradictions were adjudicated first; no contradicting edit was made before its adjudication.

---

## 1. Contradiction adjudications (decided prior to implementation)

| # | Audit A | Audit B | Adjudication |
|---|---|---|---|
| X1 | comp gpt: "reference library" in the title is a material mismatch — drop it or implement `viacert.continuous` | elevation-audit grok upgrades §X: "Do not remove 'reference library' from the title; specify `viacert.continuous` so the title is accurate" | **Resolved in the shipped v5/v6 wording.** Title reads "a Finite-Side Reference Library", the abstract states the bridge's programs live in the campaign scripts *not* the library, and §10 names consolidation as the recorded next step. This satisfies A's honesty demand without B's demotion of the claim; `viacert.continuous` remains the owner-side ledger path that makes B's version literally true. No further edit. |
| X2 | comp gpt §7: restructure the paper (exact duality before the finite theorem; split Theorem 4) — "essential before publication" item 1 adjacent | grok on gpt: "Mandatory split of Theorem 4 … strongly recommended, not logically required. One theorem with a real proof of (iv) can stand" | **Against restructure.** Theorem 4(iv) has been proved in full since round 3 (Sion + atomic attainment + weak-* density, verified in S1); restructuring a machine-verified, DOI-staged edition to reorder sections trades correctness risk for presentation. The audit's own priority list demotes the split to "strongly recommended". Standing. |
| X3 | grok review: "Do **not** do formal computability theory for semidecidability" | grok upgrades §VII ships Theorem 7 (rational Turing/Tarski model); comp gpt 1.9: "either specify the algorithm or drop the word" | **Middle path, already taken.** The shipped item (iii) specifies the algorithm (refinement enumeration, halt when a positive exact certificate is found) and — since the last round — the dyadic halting bound ρ_k ≥ γ − C·T/2^k; no recursion-theoretic machinery is imported. This satisfies the gpt demand ("specify the algorithm") without the grok-review prohibition. No further edit. |
| X4 | comp gpt #18: add hardware/OS/version detail for timings | grok: "Wall-clock hardware: good practice; not a correctness issue" | **Not in conflict** — implement (both endorse). Done: environment line added to the verification-methods section with the real machine facts. |
| X5 | worked gpt §11: extensive demands on the *entropic* certainty-equivalent section (sign convention, CE expansion, transcendental exactness, dynamic risk recursion) | current ws edition | **Moot by edition drift.** The audited entropic-CE section no longer exists: the shipped §11 is the exact rational drift audit (g(s) = s², law s ↦ s + 1/10, drift s/5 + 1/100), all values rational, no transcendental computation anywhere (grep: no `log`/`exp` in ws v9 or its scripts). The audit's only still-applicable demand — scope the exactness claim — is implemented. |
| X6 | worked gpt Table 1 counts "17, 20, 23, 28" | shipped kernel counts 24, 26, 25, 28 | **Edition drift, no action.** The audit reviewed a pre-v7 edition; the shipped counts are re-derived by the harness (P-blocks + master table) on every run. |
| X7 | worked gpt: "add pairwise-intersection insufficiency" (§1.7) | shipped adequacy text already states the opposite *and* the fix: pairwise is sufficient in the two-instrument universe (Helly number two) and the three-set counterexample {1,2},{2,3},{1,3} is exhibited for larger action sets | **Already shipped correctly** — the audit's demanded correction and the current text agree; no edit. |

## 2. Worked systems — remaining points → **ws v9** (22 edits + harness 38/38, chained 57/57)

| Audit point | Verdict | Disposition in ws v9 |
|---|---|---|
| 1.1 σ* strictness inconsistency (abstract/conventions/§6/Table 3/Fig 2) | **shipped + residual** | Conventions ¶ and §6 already carried the full τ* vs ⌊τ*⌋ distinction (shipped v8). Residual ambiguity closed: abstract now says "the strict form **against the real exit bound** fails at exactly one cell (against the floored count σ*, on all six boundary-integer cells)"; fig:timing caption carries the same σ* clause. |
| 1.2 "kernel" not justified at H = 12 | **implement** | Two-horizon verdict upgraded to a **proved setwise stabilization**: harness now asserts W₁₂ = W₁₀ = audited kernel for all four classes and for the mode-blind kernel (new P7 checks); text states "W₁₂ = W₁₀ for each policy class, verified … a stabilized recursion on the audited pair population". |
| 1.3 no-repeat register formalization (values? init? u = 0? decentralization?) | **shipped + residual** | Register semantics were formalized in v8 (values = the two instruments; initial register unconstrained; pessimistic threads; setwise coincidence with the single-thread implementation — M2b/M2c). Residual closed: the recursion's silence on u = 0 is now justified in text ("instrument 0 is never uniquely viable") **with the premises newly machine-verified** (P8: F₀ ≤ F₁, F₀ ≤ F₂ pointwise; safe set an up-set). |
| 1.4 step-one witness too weak | **implement** | Witness sentence rewritten: the certificate is the recursion invariant (winning-set membership at every horizon level, universal over adversarial successors and register threads); the archived step-one vector is its first level. |
| 1.5 transition map incomplete (clipping? safety before/after?) | **implement** | Explicit transitions printed: F_j(z₁,z₂) = (min{3, z_j+1}, max{0, z_{3−j}−1}), F₀ both-lose-one with clipping; "coordinates clip at zero, the cap is absorbing; safety is evaluated after each transition" — transcribed exactly from the shipped scripts. |
| 1.6 "neither protocol dominance holds" incorrect | **shipped** | v8 text already states the correct form (within-column inclusions strict; across columns neither middle kernel contains the other). |
| 1.7 pairwise-intersection insufficiency misattributed to the census | **shipped** (see X7). |
| 1.8 refinement order undefined | **shipped** | v8 conventions define the order ("P coarser than P′ when every fibre of P′ lies in a fibre of P"; "coarsest adequate" relative to it). Residual terminology harmonized: abstract now says "the two coarsest adequate two-cell partitions (minimal in the refinement order)". |
| 1.9 static-duality sign at value zero | **shipped** | v8 already states the convention ("a certificate exists exactly where the value is negative, and at a zero value the instance is boundary-critical"). |
| 1.10 strong duality without hypotheses; "m+1-point" | **implement** | Duality sentence rewritten with hypotheses (compactness, affinity/sufficient continuity, finite label family; nonconvex counterexample retained); abstract "m+1-point witness" → "m+1-**constraint** witness". |
| 2.1 "every system in the family is finite" | **implement** | Now "every discrete census in the family is finite — the continuous objects reduce to exact rational calculations on rationally specified domains". |
| 2.2 exactness vs figure rendering; decimals | **implement** | Exactness ¶ extended: floating point confined to graphics rendering; displayed decimals abbreviate exact rationals. |
| 2.3/§14 reproducibility (DOI, checksums, second implementation, witness files, horizon hashes) | **ledger** | Owner-side archival ledger (DOI/tagged release/lockfile are standing owner decisions; arena paths are not an archive — recorded, unchanged). |
| §9 fibre width "Δx₁(Y) = Y − 4" without derivation | **implement (deleted)** | The audit offers "add the parametrization or delete"; the sentence is dangling (nothing else uses it) — deleted; stale harness needle removed with it. |
| §12 multiple floors underdefined | **implement** | New "Quantifier and dual form" block: ∃u ∀j∈I(x) vs ∀j ∃u_j; the dimension-one counterexample (g₁ = u − 1/2, g₂ = −u − 1/2 on [−1,1]: separately maintainable, jointly impossible) with the exact ℓ₁ dual certificate sup_u (λ₁g₁+λ₂g₂) = −1/2 < 0; aggregation caveat (positive weighted average can hide a violated component); m+1 support scoped to the convex static case, tied to prop:helly (m+1 = 2 necessary at m = 1). |
| §13 design rules: evidence levels mixed | **implement** | Rules now tagged: Timing (proved general law, Prop), Coarseness (exact census), Aggregation (exact census), Bias (exact census), Authority (proved zero law, Prop); "transfer as default guidance only at that evidence level". |
| §6/§13 "deadline exactly at worst-case exit is admissible" | **implement** | Qualified: "under the audited endpoint convention (closed safe set; safety evaluated at the deadline review); no open-set or alternative-exit-convention claim is made". |
| §7 conclusion too broad | **implement** | Closing sentence added: the examples "do not constitute a general taxonomy of viability failures; they supply exact counterexamples, finite censuses, and reproducible benchmarks…". |
| Abstract grammar (fragment "the classes a two-by-two product"; opening sentence; "audit frame") | **implement** | All three corrected. |

## 3. Computational paper — remaining points → **comp v6** (6 edits + harness 26/26)

| Audit point | Verdict | Disposition in comp v6 |
|---|---|---|
| Abstract quantifiers, refinement-sequence language, r+1 existence, sandwich wording, scoped exactness, pairwise carried exactly, title/library (§1.1–1.2, #3–#6, #9) | **shipped** | All already conforming in v5 (checked against the audit's recommended replacements nearly verbatim). |
| §1.3 convexity at compactness; §1.4–1.5 sandwich proof + center cancellation; §1.6 β sign; §1.7 grid/label product/covering radius; §1.8 Lipschitz connect; Thm 4(iv) full proof | **shipped** | v3–v5 rounds; round 4 added the instance moduli (R_U = 1, V_U = 6/5, δ_β = 0, L = 11/5). |
| §3.1 instance arithmetic "displayed in a compact proposition" | **implement (corrected)** | Shipped as an *unnumbered* collected-arithmetic paragraph ("Instance arithmetic, collected.") — the numbered-proposition form was rejected because S1's proof headers are number-aligned to the main text (Proposition 1/2/3, Theorem 4) and inserting an environment would silently break that alignment. All identities in it are machine-verified. |
| ρ unrestricted in program (5) | **implement** | "The level ρ is an unrestricted real variable; negative values record strict safety slack." |
| u^π_θ defined (audit "Policies" ¶) | **implement** | "…the realized control in mode θ being u^π_θ(t) = u_{r,C_r(θ)}(t) on I_r." |
| Full-info viability: verify velocity/noncritical facets/−n_j ∈ U (§6 bullets) | **implement** | Parenthetical added at the 93/50 sentence (‖v(t)‖_∞ ≤ 1 ≤ 6/5; n_iᵀp < 0 on the n_j-ray; −n_j ∈ U). |
| #18 timing environment | **implement** (X4) | Methods line: single runs; Linux x86_64, Intel Xeon @ 2.60 GHz, Python 3.13, SciPy 1.17 HiGHS dual-simplex interface, solver defaults. |
| Script-count/structure (§13 vs code availability) | **implement (residual)** | Code availability named the stale v2 harness; now names the edition harness (`…_v6_verification.py`) first, alongside the nonstandard verifier, campaign, and package. Methods' "three scripts and one package, one master entry point" was already accurate. |
| Prop 5 polish (scalar states, pathwise sufficiency, necessity, rank q); eq (11) both inequalities; sparsification precision; "resolution bound not certificate-size"; complexity qualification; (12) fixed-witness caveat; (13) oracle definitions + soundness; 3,240 demoted; s = 7, p_U = 6 in caption; three-label support instance-specific; Abaee 2026b bibliography; belief convention; Lift ambient; partition order; δ_β decomposition; "necessary condition" wording; independence semantics | **shipped** | Each verified present in v5 by direct inspection this round (loci cited in the round record). |
| #10 archive/DOI/certificate-file verifier; §10 legacy counts to appendix | **ledger** | Owner-side (standing): tagged release + DOI flow + single-DOI consolidation; the certificate-file parser is already named in-text as the recorded next step. |
| Broken `efmethods`/`exttt`, smashed fractions, "surrogate.Data availability" | **refuted (artifact)** | Extraction artifacts of the pasted text; PDF probes clean (no '??', no broken commands, headings render standalone). The one real cross-reference defect (trailing period in every prose `\ref`) was root-caused and macro-fixed in v5. |
| "Every number is regenerated" too broad; future date; wall-clock reproducibility phrasing | **shipped** | v5 methods already scope to "every reported mathematical benchmark value and every table entry"; timing caption already flags single runs and solver-version variance; the 2026 date is intentional. |

## 4. Edition state and evidence

- `paper2_worked_systems_v9.tex` / `.pdf` — 8 pp., zero overfull, no undefined references; 22 edits above + code-availability pointer.
- `paper2_worked_systems_v9_verification.py` — **38/38 checks pass** (v8's 32 + P7 stabilization ×5 + P8 WLOG premises), chained lineage **57/57**; needles extended to 102 (+15 new, −1 removed with the deleted fibre-width sentence).
- `paper2_computational_certification_v6.tex` / `.pdf` — 8 pp., zero overfull, no undefined references; 6 edits above.
- `paper2_computational_certification_v6_verification.py` — **26/26 checks pass** (Layer 2 dual identities, moduli, h = 1/15 witness, refutation pins unchanged; needles +6).
- Prior editions byte-untouched; no claim softened; contradictions adjudicated before any edit; both audits' remaining valid findings implemented or explicitly dispatched (shipped / refuted / moot / ledger).

# Addendum — Probabilistic Sufficiency for the Obstruction Calculus (edition 1)

**Subject.** `paper2_probabilistic_sufficiency_v1.tex/.pdf/_v1_verification.py` — the P3 flagship of the consolidation decision (roadmap v17: N = 4 flagships + 1 pending applied paper), executing the v17 fold map's P3 row: "Stochastic / belief-state safety values (probabilistic sufficiency); seeds: v49 §10, `paper2_stochastic_selector_v2`, `hidden_parameter_learning_v1` (D10)".

## What was consolidated (fold map executed; originals retained, byte-untouched)

| Seed | Contribution to the flagship |
|---|---|
| `paper2_stochastic_selector_v2.tex` | The augmented model (total transition rows, absorbing ⊥ with its own observation, masked witnesses), the selector split (attaining set vs value-one level), the class-restricted rational-alpha-vector proposition with the masked backup and the segment form, the two-operators remark, the survived-mass degeneration, the deficit-bounds proposition, the class-scoped timing remark, the S1–S15 verified record, the Smallwood–Sondik/Bertsekas–Shreve framing |
| `paper2_belief_state_v2.tex` | The support identity with the deficit bound and the closed form on the delayed class (+ derivation), the exactness campaign (384 exact vectors; three types 72/156/156; the complete 16 × 12 value table), the deterministic-limit audits (two-floor δ = 1/2; asymmetric prior deficit 3/10 attained), the agreement theorem and jump-loci derivation, the class-declaration theorem (36 timing combinations, enumerated), the adaptive-observation section, and the five-figure set `figs_bs2` (regenerated by `paper2_belief_state_figures.py`) |
| `hidden_parameter_learning_v1.tex` | The D10 framing (structural uncertainty as a timing obstruction; augmented beliefs; joint floor-servable emptiness), the hold-then-probe-then-act schedule family, the learning-deadline proof (probe threshold 21/10; deadline identity 21/10 + T_learn/10; kernels |K| = 5, 4, 3, 2, 1), the two-patch unknown-regeneration instance, the "what is not claimed" delimitations |
| P1 v49 §10 | The theory pointer: the belief-state lift as the calculus's probabilistic development, with the 48-cell worked slice and the load-bearing class declaration |

No new mathematical claims are made: every theorem, number, table, and figure is carried from the machine-verified seeds. New text is consolidation framing only (intro, interfaces, scope, related-flagship paragraph), plus the code-availability and verification-methods sections describing the new chain.

## Verification chain

`paper2_probabilistic_sufficiency_v1_verification.py` — **15/15, exit 0**:
- **Layer 1 (chain):** runs the three per-system scripts, asserting exit 0 — belief-state campaign **16/16** (itself chaining 21 per-instance checks), stochastic selector **15/15**, hidden-parameter **6/6**. Total carried: 58 checks + 21 nested.
- **Layer 2 (own exact re-derivations, all in `fractions.Fraction`):** closed-form declared-class values on the whole 192-combination grid against direct branch-mass enumeration (census: **36 unit values, 156 half values**); the class-difference set **equals** the timing-cell combinations at k ≥ 2 (36), by brute force over all open-loop blind sequences with pathwise survival; the alpha-vector type census (1,1)×72, (1,0)/(0,1)×156 each = 384; the asymmetric audit at (3/2, 2) with prior (3/10, 7/10) sequence by sequence — masses {3/10, 3/10, 7/10, 7/10}, value 7/10, deficit 3/10 = minimal mass, attained (pathwise survival; the first draft's endpoint-only check produced spurious masses {3/10, 7/10, 1, 1} and was rejected before acceptance); the learning-deadline chain with exact kernel sets down to {2.5}.
- **Layer 3 (text/structure):** 36 needles; FORBIDDEN (seed-edition audit histories and superseded framings: "Edition 2, revised after a joint audit", "The first edition conflated", …); verbatim AI-declaration wording; label/ref integrity; source hygiene across the flagship and the three seed tex files; the five-figure set on disk; code-availability pointers (each chained script named exactly once).

## Build

Tectonic 0.15.0, `--keep-logs`: exit 0, **6 pp, overfull 0**; pymupdf probes all green (ligature-aware: the ﬀ ligature in "Sufficiency" defeated the first title probe — probe artifact, not a build defect; fractions extract stacked, per the standing normalization rule).

## Status

P3: **built, verified, pushed** (commit recorded in the push log; fresh-clone byte-comparison in the round evidence). P1 frozen (v49), P2 at v9 + S1 v6, P4 at v10, P5 pending owner data (R12). Seed lineages (`paper2_belief_state_v1/v2`, `paper2_stochastic_selector_v1/v2`, `hidden_parameter_learning_v1`) remain in place; supersession banners belong to build-order step 5 (programme-wide, not per-paper).

# Computational certification — edition 3 addendum

**Edition:** `paper2_computational_certification_v3` + `paper2_computational_certification_v3_supplementary` (tex/pdf/verify), replacing v2 per the new-editions-only protocol. **Trigger:** round-3 external audit (`computational gpt audit.txt`). **Verification:** `paper2_computational_certification_v3_verification.py` — 20/20 checks pass (chained: 51/51 exact record, 9/9 campaign, 12/12 viacert selftest); builds clean (0 overfull; `main.pdf` asserted).

## Content classes (v2 → v3), full table in the round-3 record

1. **Source repair.** The v2 tex shipped with literal TAB/CR control bytes eating `\t`/`\r` in one sentence (renders "Section efmethods.; as a extttviac-ert.continuous module"); repaired; verify now regresses both tex files for control bytes and bare `ef{`/`exttt{` remnants.
2. **Claim discipline.** Abstract rewritten on all five audited points (dual-feasible vs obstruction; existence wording for r+1 with Prop-5 saturation; admissible refinement sequences; pairwise policies explicit; exactness scoped); §12 "exactly when" → "rests on three requirements"; "solver-certified" → "solver-assisted, exactly certified" (title included); "Every number" → "Every reported mathematical benchmark value and every table entry"; certificate-size → margin-dependent resolution bound; §10 "retirement" → archive-preservation.
3. **Theorem hygiene.** Prop 2 compactness with convexity explicit; program (5) over 𝒮_G with grid/covering-radius spec; (iii) semidecidability input model + procedure; (v) rank definition (coefficient matrix; LP basic-solution theory); proof-outline error terms named correctly ("block variables"); equation (3) written with summed indices; δ_β decomposition; Remark 1 tightness (min over policies = Γ).
4. **Pairwise policies in the main text.** {1,2}/{1,3}: blind hold u = 0, brake −(2/5)n_j on [τ, τ+1/5], then −n_j to rest; peak ≤ 12345/6250 = 1.9752. {2,3}: blind hold u = n₁, brake −(3/5)n_j, then −n_j; peak ≤ 2419/1250 = 1.9352. {1,3} by reflection. These are the exact values the campaign script has verified all along; the script's check label "12193/6250" was a display typo (assertion always used 19352/10000) — documented, archived script untouched, S1 v3 states the fractions.
5. **§6–§9 precision.** s = 7 stated; opaque parenthetical removed (Σλ = 1, Σλn = 0); τ_max with equality-viability and the upper-bound policy; mesh law scoped to the five aligned meshes; 465-row archive pointer; residual test with both displayed inequalities + multiplier-redundancy note; sparsification algorithm; Prop 5 scalar-state phrasing, pathwise sufficiency, subbelief-certificate necessity; erosion as same-witness statement; oracle-recourse operational definitions + soundness chain.
6. **§10–§11 scoping.** Title now "…and a Finite-Side Reference Library"; library framed finite-side with archive preservation; "Exactness" principle scoped; legacy counts framed as self-tests; convexity structural-why; full-information necessary-not-sufficient; tangency rephrased; discrete-games phrasing.
7. **Methods/repo.** Artifact structure (three scripts + package + master entry point); independence defined with the certificate-file parser as recorded next step; commit-pinned repo phrasing; Abaee 2026b bibliography entry completed; "programme" → "program".

**Fidelity:** every audited number bit-identical to v2; the only numeric-notation change is the 12193/6250 → 2419/1250 label correction above (no assertion touched).

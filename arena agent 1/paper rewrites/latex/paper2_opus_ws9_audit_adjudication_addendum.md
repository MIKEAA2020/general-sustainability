# Round-6 Adjudication — Opus Audit of Worked Systems v9

**Scope.** `uploads/opus audit of ws v9.txt` (30 lines), evaluated line-by-line against `paper2_worked_systems_v9.tex`, its verification script (`paper2_worked_systems_v9_verification.py`, 38/38 + chained 57/57), and the shared audit primitives — every mathematical claim re-derived here in exact rational/integer arithmetic (Fraction-based batteries over the full belief universe, the 256-law space, the timing grid, and the regime model). **Strengthen + complete:** the confirmed findings are shipped as a new edition, `paper2_worked_systems_v10.tex` (v9 byte-untouched), with a derived verification script (`paper2_worked_systems_v10_verification.py`, **46/46** checks pass, chained lineage **57/57**; 139 text needles; PDF 9 pp, overfull 0). The audit's own errors are adjudicated below by computation.

**Method note.** Adjudication batteries were run as standalone exact-arithmetic scripts before any edit (verify-before-fix); the pessimistic shared-register semantics, the fixed-thread and per-branch register variants, the authority restrictions, the law quotient, the extended timing grid, and the explicit hidden-regime model were each recomputed from the shared primitives, and the v10 harness re-derives every one of them (checks M2b, M6, M6b, P4b, P5, P9–P15).

---

## A. Verdict table — audit findings

| # | Audit finding | Verdict | Computed evidence (exact) | v10 disposition |
|---|---|---|---|---|
| 1 | Incomparable middle cells depend on the shared register; per-branch registers collapse the protocol cost | **CONFIRMED** (sizes corrected, see §B) | Pessimistic shared-register kernels **24/26/25/28** (reproduced); per-branch (own-record) semantics: inst **26 = agg setwise**, cod **27**, full 28; agg ⊊ cod with cod−agg = {12\|21}; full−cod = **{(1,3),(3,1)}** exactly | Prop 3 rewritten (E5); per-branch proposition added; **check P9** |
| 2 | Prop 3 "independent of the admissible thread" contradicts check M2c; "the scripts' single-thread implementation" undefined | **CONFIRMED** | The pessimistic survival rule quantifies universally over threads → thread-independent **by construction**. Fixed-thread rules (cells sorted by min state; first **or** last fibre's action): **25/26/26/28** — neither rule yields the audited inst/cod kernels, and the two rules disagree **setwise**. Optimistic codex = 27, strictly containing the audited one | Prop 3 restated (thread-independence by quantifier structure; single-thread divergence computed); M2b reworded (sizes + setwise equality, no thread-order claim); **checks P10**, M2c kept |
| 3 | §5 agency asymmetry refuted by Prop 4 itself; local fix gives 4 (list exact); mirror kills the target; swap symmetry privileged no agency | **CONFIRMED** | Local fixings: (a1→1) = **12 = K_dec**; (a2→2) = **12**; (a2→1) = **4** = {13\|22, 13\|33, 22\|33, 23\|32} (audit's list exact); (a1→2) = **4** = the **swap mirror** of that set (not equal setwise); whole-law: **0 in all four cases**; swap symmetry maps {12\|21} to itself | Prop 4 + prose corrected (mirror sets, symmetry argument); **check P12** |
| 4 | Printed transition formula breaks reproduction (literal reading → inst 8, cod 8; agg/full unchanged) | **CONFIRMED EXACTLY** | Literal regenerated-coordinate-first formula: **8/8/8/8** (audit exact); index-wise definition reproduces 24/26/25/28 | Transition map displayed index-wise (F_j(z)_j = min{3, z_j+1}; F_j(z)_{3−j} = max{0, z_{3−j}−1}) + misreading excluded by script (E2b) |
| 5 | §8 never defines the model; corner-viability unverifiable ("as far as I can reconstruct it, the corner exits immediately"); "36 law pairs" should be belief pairs | **CONFIRMED** (definition gap; corner claim refuted, see §B) | Under the explicit audited model (instrument u regenerates z_u +1 cap 3; acting regime r damages z_r −1 floor 0): frozen kernels = **all 36 belief pairs** for r = 1 and r = 2; corner **viable** (held below the cap; freed in one step from capped coordinates); mode-blind kernel = **28 = full-information kernel setwise** | §8 model made explicit; "belief pairs"; counts machine-verified; **check P13** |
| 6 | Two design rules tagged "exact census" with no census; §11 empties-claim underived; "tube failure"/"fibre crossing" never audited | **PARTIALLY CONFIRMED** | Aggregation rule: the census exists (15 partitions; aggregate kernel 26 vs full 28 = the certainly-safe content) — tags now cite the sections. Bias rule: the empties-sentence was asserted without a model — v10 states the audited mechanism (fixed lead misreads the state; drift audit bounds it; corrected drift identically zero **by construction**). Intro mechanism list now enumerates only audited sections (tube/fibre-crossing phrasing removed) | Rules reworded + section cross-references; §11 reworded (E14); intro list anchored (E2) |
| 7 | Prop 5 identity "{T ≤ z₀−1} = {T < σ*} off-integer" false (z₀ = 5/2 → {0,1} vs {0}) | **CONFIRMED** | Correct identity: {T ≤ z₀−1} = {T ≤ σ*} (integer T, all z₀); off-integer form {T < z₀−1} = {T ≤ σ*} | Prop 5 proof fixed (E8d); **check M6** identity on all 93 cells |
| 8 | Prop 5 "exits at or before z₀−1" contradicts "boundary included"; τ* is the last safe time; "level sets" → superlevel | **CONFIRMED** | The stock is safe **through** z₀−1 (floor safe; closed set); first violation at z₀. Superlevel sets {z₀ ≥ 1+k} | Proof reworded ("last safe review"); superlevel sets (E8c, E8b) |
| 9 | Prop 6 "Y ≥ 27/5" needs strict; abstract ties 3/50 to Y* but it belongs to Y = 6 | **CONFIRMED** | At Y* = 27/5 the caps (29/25, 21/25) sum exactly 2; witness feasible; margin **0 at Y***; 3/50 belongs to Y = 6 | Strict inequality; abstract re-anchored (E9, E11) |
| 10 | Prop 7 slope is 1−2λ₁ not 1−2λ₂; symmetry alone doesn't locate the minimum | **CONFIRMED** | A(λ₁) = (2/5)λ₁ − (3/5)(1−λ₁); slope **1−2λ₁**; averaging bound (A(λ)+A(1−λ))/2 = −1/10 ∀λ, equality only at λ = 1/2 — **convexity of the max** locates it | Proof corrected + averaging bound added; **check P4b** |
| 11 | §10: the paper's own U = {0,1} example meets the listed hypotheses yet gaps → convexity of U missing; "or sufficient continuity" false (ψ₁ = max(1−4u,−1), ψ₂ = max(4u−3,−1): −1 vs 0) | **CONFIRMED** | Counterexample re-verified: max−min **−1**, min−max **0** on [0,1] — continuity does not repair convexity; **concavity in u** is the load-bearing hypothesis | Hypotheses: compact **and convex**; rows affine (or concave); both failure modes stated (E12c) |
| 12 | Prop 8: simplex facets meet in the simplex → wrong tightness witness; need reversed halfspaces; rational grid not exhaustive | **CONFIRMED** | Reversed halfspaces {uᵢ ≥ 1} + Σuᵢ ≤ 1 in R^m: meet m at a time, never m+1; triple infeasibility certified by the exact contradiction 2 > 1 (grid search corroborative only) | Tightness witness corrected; exact certification wording (E13); **check P5** |
| 13 | Prop 2 "antitone" wrong — monotone | **CONFIRMED** | Class inclusion ⇒ kernel inclusion (order-reversal is between the two orders, not within kernels) | Gloss added, direction fixed (E4) |
| 14 | Instrument-0 elimination argued only for full/free; conclusion holds (auditor checked all four with u = 0) | **CONFIRMED** | Pessimistic recursion over actions (0,1,2): kernels **unchanged setwise** (24/26/25/28); WLOG premises (F₀ ≤ F₁, F₀ ≤ F₂ pointwise; safe set an up-set) verified | §7 scoped (one-step adequacy) + u = 0 admitted and counted; **check P11** |
| 15 | W₁₂ = W₁₀ only checked on pairs; holds on the whole universe, stable by horizon 2 | **CONFIRMED** | Whole 2⁹-belief universe (511 nonempty subsets of the safe states): W₁₂ = W₁₀ = W₁₁ setwise for all four classes; sizes **122/143/178/255**; stable from horizon **2** (horizon 1 for the two non-alternation classes) | Abstract + frame claims extended; **check P15** |
| 16 | Hidden-regime model never defined; grid barely tests (42 trivially non-viable, T = 2,3 never viable, one boundary cell); extend z₀ to 4.0 | **CONFIRMED** | v9 grid: 48 cells, 6 viable, 42 trivially non-viable, T = 2, 3 never viable ✓. Extended grid (z₀ → 4.0): **93 cells, 33 viable**; T = 2 viable from z₀ ≥ 3, T = 3 at z₀ = 4; boundary cells **(2,1), (3,2), (4,3)** — three, not one; floored-strict fails on one cell per row z₀ ≥ 2 (**21 cells**), boundary included | Model defined explicitly; grid extended, table printed in full (38 rows); **checks M6, M6b** |
| 17 | 256-law space inflated 4× (z = 0 votes never consulted); 16 sustaining pairs are 4 distinct laws out of 64 | **CONFIRMED EXACTLY** | z = 0 votes unread on safe trajectories (kernel invariant under any z = 0 relabeling): 256 raw → **64 behavioral** (votes at z ∈ {1,2,3}); 16 sustaining pairs → **4 distinct behavioral law pairs**, each extending by exactly the 4 never-consulted z = 0 vote pairs | §6 quotient added; abstract "(64 behaviorally distinct)"; **check P14** |
| 18 | §7 census only checks one-step safety; u = 0 without justification | **CONFIRMED** | See #14 | §7 scoping sentence + justification (E10) |
| 19 | §9 never defines stocks/Y/drift; "best feasible allocation" at Y = 6 where nothing is feasible; §§9–10 reuse "two-floor instance" for different objects | **CONFIRMED** | Feasibility at Y = 6: caps sum 47/25 < 2 — **nothing is feasible** ✓; caption clause removed. State/aggregate/fibre defined; §10 opening renamed (drift-row abstraction); §12 referent tied to §9/§10 | §9 definitions (E11); caption fixed; §10/§12 naming unified (E12, G-fix) |
| 20 | §11 zero corrected drift true by definition; §12 "two-floor extension" never appears | **CONFIRMED** | Corrected law = the lead removed → drift ≡ 0 by construction (stated); §12 referent now tied to the benchmark's two floors + the Helly-tight triple | §11/§12 reworded (E14, G-fix) |
| 21 | Minor: six boundary-integer cells; "two coarsest" vs "no coarsest"; "17.-8."; Table ??; "check M-new" placeholder; undefined codex/institutional/zero-law; "absorbing" cap; literal "max--min" in Figure 5 | **CONFIRMED (all eight)** | — | All fixed: boundary-cell phrasing; abstract "two incomparable coarsest adequate … (hence no single coarsest)"; `\thesubsection` de-perioded + `\@seccntformat` (renders "1. Introduction", verified in-PDF); both dangling refs → `tab:laws` (renders Table 2); M-new → P12 (whole-law 0 ×4); class glosses at the master table + rules tag reworded; "saturating"; figures annotation `max$-$min` + all five figures regenerated |
| 22 | Confirmed correct: Table 1's 180 marks, census, timing grid, Tables 5–6, Y* = 27/5, both saddle values, Helly triple | **CONFIRMED** | Master-table projection recomputed row-by-row (36 × 5 marks + column sums 24/26/25/28/12) | Unchanged; **check M5** et al. |

## B. The audit's own errors (adjudicated by computation)

| # | Audit claim | Verdict | Computed refutation |
|---|---|---|---|
| B1 | "If each branch keeps its own register, codex = 28 = full and institutional = 26 = aggregate" | **REFUTED in part** | Per-branch (own-record) semantics, exact: inst **26** = agg ✓ (that half is right), but cod **27 ≠ 28**. The codex kernel loses exactly **{(1,3),(3,1)}** relative to full information (28 − 1 = 27); the pair dies because its branches can merge onto a shared state carrying conflicting records, and no action choice avoids it. Three independent per-branch semantics (token-level, fibre-record, merge-consistency) agree on (26, 26, 27); token-level and fibre-record give full = 28. v10 ships the audited values (26, 26, 27, 28) with the loss set stated — **check P9** |
| B2 | §8: "as far as I can reconstruct it, the corner exits immediately" | **REFUTED** under the explicit audited model | From (1,1) with the regime frozen to r: acting u = r holds the state (regeneration +1, damage −1, both on z_r < cap) — the corner is **viable under each frozen regime**; every safe state reaches a fixed point (capped coordinates freed in one step by the other instrument). Frozen kernels = **all 36 belief pairs**, r = 1 and r = 2 — **check P13**. The audit's underlying point (the model was never defined) stands: v10 defines it |
| B3 | Timing: "there's only one boundary cell" (extending to z₀ = 4.0 implied) | **REFUTED** | On the extended 93-cell grid the boundary cells (z₀ = 1 + T_obs) are **(2,1), (3,2), (4,3)** — three, not one — and the strict form fails on exactly those three against the real exit bound — **checks M6, M6b** |
| B4 | Fixed-thread: "first fibre's action gives codex 26, the last fibre's gives 26 (a different extra pair)" | **CONFIRMED** (refined) | Sizes (25, 26, 26, 28) for both fixed rules — cod 26 each, with different extra pairs ✓; the two rules' kernels moreover differ **setwise** — **check P10** |

## C. Edition shipped

- `paper2_worked_systems_v10.tex` — new edition (~40 edits over v9; v9 byte-untouched). Headline changes: register-semantics proposition rewritten and strengthened (thread-independence by construction, fixed-thread divergence, per-branch collapse); authority census corrected (mirror four-belief kernels, whole-law zeros); transition map index-wise; timing model defined + grid extended to 93 cells with the boundary-triple identity; benchmark strict inequality + margin re-anchoring; duality slope and convexity/averaging proof; §10 hypotheses (convex control set, concavity — both failure modes exhibited); Prop 8 reversed-halfspace tightness with exact certification; law-space quotient (64 behavioral; 16 = 4 × 4); §8 explicit regime model; abstract reconciled (coarsest partitions, margin anchor, frozen-regime clause); all eight minor defects (numbering, ??, M-new, glosses, saturating, figure annotation).
- `paper2_worked_systems_v10_verification.py` — derived harness: **46/46** checks pass + chained lineage 57/57. New checks: **P9** per-branch kernels (26/26/27/28, loss set), **P10** fixed-thread divergence, **P11** instrument-0 admission, **P12** authority census (12/12/4/4 + mirror + 0×4), **P13** explicit regime model (frozen 36 ×2, mode-blind 28 = full setwise), **P14** law quotient (256→64; 16 = 4 × 4), **P15** whole-universe stabilization (511 subsets; W₁₀ = W₁₁ = W₁₂; sizes 122/143/178/255); M2b reworded (pessimistic sizes + setwise equality); M6/M6b extended (93 cells, 33 viable, boundary triple, 21-cell floored-strict set); P4b averaging bound; P5 exact contradiction; 139 text needles against the tex.
- `paper2_worked_systems_v10.pdf` — 9 pp, **overfull 0**; render probes: headings "1. Introduction"-style (de-perioded numbering), no "Table ??", no "M-new", boundary triple and all new counts render.
- `paper2_worked_systems_figures.py` + `figs_ws4/*` — duality-figure annotation corrected (literal `max--min` → `max$-$min`); all five figures regenerated (6/6 figure checks).

## D. Chain of custody

Audit read end-to-end before any edit; every finding adjudicated by exact computation before the corresponding edit; each edit applied with a uniqueness assertion; the harness re-derives every new claim independently of the prose; the PDF build is byte-present with zero overfull boxes and pass-render probes. v9 and its artifacts are untouched.

# Paper 2 — Worked-Systems Flagship v1 Addendum (the P4 fold, executed)

**Edition:** `paper2_worked_systems_v1` (tex + pdf + master verification + this record).
**Decision executed:** the v17 fold map's P4 row — the catalogue facts consolidated into one worked-systems flagship under one audit frame. **No new theorem; no source edition modified; every absorbed edition retained in the repository (supersession recorded, never deletion).**

## 1. What was consolidated (and from where)

| Section | Content | Source edition (retained, script green) |
|---|---|---|
| §3 | C1–C8: delay identity + boundary (2,1); adequacy + coarsest-partition refutation (15/7 partitions; maximal sets overlap in (2,2)); four-cell concordance 24/26/25/28; decentralized 256 laws / kernel 12 / 16 lost / 16 sustain / 12→0; mode-blind 28 setwise; CE 21/100 vs 0; σ* = ⌊z0−1⌋; benchmark Y* = 27/5, margin 3/50 | `paper2_selector_concordance_v1` (8/8) |
| §4 | two-floor duality −1/10; λ = (1/2,1/2); convexity gap; Helly-tight triple; caps 3/2−(Y−2)/10 and 59/50−(Y−2)/10; fibre width Y−4 | `minimax_dual_certificates_v1` (8/8) |
| §5 | monitoring rules E1–E9, non-strict timing identity with E9 boundary pin | `monitoring_design_v3` (9/9) |
| §6.1–6.4 | vector floors (multi-floor Farkas; minimal infeasible subsystem), policy-class lattice (missing cell 25/36; Boolean product; meet/join), hybrid modes (singleton-generated loss; corner mode-blind-dead), decentralized observation (law space; deadlock; delayed own reading) | `vector_floor_certificates_v2`, `policy_class_lattice_v2`, `hybrid_mode_viability_v2`, `institutional_observation_v2` (8/8 each) |
| §7 | absorbed-lines registry + practitioner reading (the four design rules as operational guidance) | finite-horizon completeness, certificate duality, comparison drift–exit, buffered viability, hidden-parameter learning; applications note + utility record |
| Registry table | the fold map as shipped | roadmap v17 |

## 2. Verification

`paper2_worked_systems_v1_verification.py` — three layers, **16/16**:
1. **Chain (7 checks + total):** subprocess-runs all seven lineage scripts and asserts green; chained total **57/57**.
2. **Own recomputation (6 checks):** the crown jewels recomputed from the shared system's primitives, independent of the lineage scripts — pair family 36 + corner nonviability + top-28; the four-cell counts with the meet identity; the partition census 15/7 with the (2,2) overlap; the benchmark Y* = 27/5 with cap-sum and dual margin; the CE drift bounds; the 48-cell delay identity with the strict-form failure at (2,1).
3. **Text needles:** every headline number asserted present in the tex; declarations carry the AI line and the master script name.

## 3. Build record

3 pages (two-column), zero errors, **zero overfull** (the two reference/declaration overfulls repaired by making the long edition names breakable), all internal references resolve.

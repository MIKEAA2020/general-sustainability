# Belief-State Safety Values, Edition 1 — Addendum (build record)

**Edition:** `paper2_belief_state_v1` (tex + pdf + verification). **Content:** the probabilistic flagship — the belief-state safety value with the support identity (posterior support = set-valued post-state; value-one level = the viable-set recursion; deficit ≥ minimal mass), exact rational piecewise-linear values (384 campaign alpha-vectors), the deterministic-limit survived-mass operator, certificates as deficit lower bounds, the 48-cell agreement with the closed form (branch-mass enumeration at k ≤ T_obs reproduces it definitionally), the sharpened class declaration (classes coincide at k = 1; differ exactly on the timing cells 2 ≤ z0 < 1 + T_obs for k ≥ 2, where an alternating hold saves both branches), and the adaptive-observation audit (probe threshold 21/10; learning-deadline identity z0 ≥ 21/10 + T_learn/10; kernel |K| = 5, 4, 3, 2 antitone).

**Verification:** 10/10 — chain of the two per-system scripts (15 + 6 = 21, both green), plus independent recomputation: branch-mass enumeration for the declared class on all 96 cell-horizon pairs with k ≤ T_obs, brute-force unrestricted class on all 48 cells (V = 1 exactly on z0 ≥ 2), the class-difference locator, the attained minimal-mass bound on the two-floor instance, and the jump loci (+1/2 at z0 = 1 + min(k, T_obs)). **Register:** formal from birth — no project narration, no phantom references (two references: the submitted calculus paper and the published Zenodo source); separate declaration headings; mandated AI declaration.

**Build:** 3 pages two-column, zero errors, zero overfull (two displays split). One process note of record: the independent-verification layer caught a bug in its own enumeration harness (constant-hold sequences built with length 1 regardless of horizon) before shipping — the layer's purpose demonstrated; no claim was affected.

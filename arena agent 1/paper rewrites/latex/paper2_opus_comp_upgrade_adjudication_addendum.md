# Round-8 Adjudication — Opus Computational Upgrade Menu (plus S1 v6 / comp v8 consistency round)

**Scope.** `uploads/opus computational upgrade.txt` (16 lines): five "strongest bet" upgrades and four longer-horizon directions proposed for the computational paper. Adjudicated against the artifacts (comp v7 + its record/campaign/library chain, S1 v5) by direct computation and inspection; the implementable-now items are shipped as **comp v8** (`paper2_computational_certification_v8.tex/.pdf/_v8_verification.py`, **36/36** checks + chained record 51 + campaign 9 + library 12; PDF 9 pp, overfull 0) and **S1 v6** (`paper2_computational_certification_supplementary_v6.tex/.pdf`, 4 pp, overfull 0), which also closes the two cross-edition consistency items left open by round 7. The rest is adjudicated to the roadmap with reasons.

---

## A. Verdict table — the upgrade menu

| # | Proposal | Verdict | Evidence / computation | Disposition |
|---|---|---|---|---|
| 1 | Certificate → control redesign guidance ("prescriptive certificates"; decode (λ, a); use (12)'s conflict intervals; allocate braking authority per mode) | **IMPLEMENTED NOW (exact, on the instance)** | New **shared-authority ladder** (Proposition, §ladder): (i) the pooling identity ∑λⱼnⱼ = 0 with λ > 0 ⇒ every common control leaves some branch unbraked — the blind window is unbrakeable for the triple, which is exactly why the certificate prices it at zero; (ii) optimal shared braking authorities, exact two-row LPs: **t\*({1,2}) = t\*({1,3}) = 2/5** (attained at (−2/5,−4/5) and its v₂-mirror) and **t\*({2,3}) = 3/5** (attained at n₁) — **the printed §6 blind-window controls are exactly these optima**; (iii) peak identity 93/50 + (1−t\*)τ + ((t\*)²−t\*)τ²/2 (increasing on [0, 1/t\*]) gives the exact **delay hierarchy**: triple obstructed past τ = 7/50, {1,2}/{1,3} past **(15−√183)/6 ≈ 0.2454**, {2,3} past **(10−√58)/6 ≈ 0.3974**, singletons never (93/50 delay-independent); seven-facet scans exact at rational surrogates (τ = 6/25, 39/100) with velocity rows strict | New §ladder + abstract clause; harness checks (pooling identity, LP optima by vertex + equal-projection-segment enumeration, field-arithmetic root verification in Q(√183) and Q(√58), monotonicity, surrogate scans) |
| 2 | Incremental mesh refinement + warm-starting; benchmark the sparsification | **PARTIALLY ADJUDICATED; tooling half → roadmap** | Exact part shipped: for affine kernels the moment error is **exactly Σ_J\|J\|²/4 on every mesh** (uniform or not), equal blocks minimize it at fixed count — so on this instance *time*-refinement has no conflict structure to target; (12) prices *information* splits, a different axis (the proposal's "refine the blocks driving conflict per (12)" conflates the two; the exception is non-affine kernels). Warm-starting: the audited toolchain (scipy HiGHS interface) does not expose warm starts; wall-clock benchmarking is machine-dependent and outside the exact-certificate discipline | Identity + axis-distinction sentences added to §instance; "warm-started incremental refinement … benchmarked against cold re-solve" and "conflict-targeted block refinement where kernels are non-affine" recorded as viacert.continuous tooling directions in §library |
| 3 | Partial state observation + measurement delays | **ROADMAP (section-scale)** | The audit itself prices it ("six weeks of formalization"). The model's history partitions P_r anticipate it, but labels, kernels, and β would need cell-conditioned lifts — new machinery, not an incremental edit | Recorded as the leading extension direction in the addendum record; consistent with the paper's scoping (§model: observations exogenous; §scope) |
| 4 | Stochastic disturbances (second-moment bounds; SOCP/PSD) | **ROADMAP** | Changes the certificate class (support-function rows become second-moment rows; LP → conic program) and the comparison study — a separate construction | Roadmap |
| 5 | Floating-point certification for nonrational data (interval arithmetic + arbitrary-precision dual re-solve) | **MOSTLY ALREADY SCOPED; fallback → roadmap** | The paper already: never uses solver output as proof objects (exact witnesses close every gap); defines δ_num in the budget; scopes interval-enclosed inputs and the residual test (11) with enclosure errors; and requires uncertain row data relaxed toward the controller. The genuinely new tool is the mpath-style dual re-solve fallback | Recorded in §library as a tooling direction ("arbitrary-precision dual re-solve fallback for interval-coefficient programs") |
| 6–9 | Decentralized/networked; infinite-horizon/periodic; nonconvex/piecewise-smooth; CORA/SMT/barrier comparison | **ROADMAP** | Each is a new construction or a benchmarking study; §scope already delimitates several (nonconvex actuators, rate constraints, infinite-horizon completeness) | Recorded; no text change needed beyond §scope's existing delimitations |

## B. Consistency items closed (from the round-7 review)

| Item | Disposition |
|---|---|
| S1 v5 Step 5b(b) certified the label-continuity hypothesis in the **sup-norm form the round-7 audit refuted** | **S1 v6**: Step (b) restated in L¹ with the estimate sup_π\|F_a − F_a′\| ≤ **M_U**‖k_a − k_a′‖_{L¹} + \|β_a − β_a′\| (M_U := sup_{v∈U}‖v‖; Hölder against M_U-bounded controls) and the sup-norm falsity stated; the moduli section gains the **L¹ Gronwall modulus** ‖k_t − k_t′‖₁ ≤ β̄c̄(1 + M_X ĀT)\|t − t′\| (+ \|β_t − β_t′\| ≤ L^tr\|t − t′\|). All hard-coded proof headings (Step 1 … Step 6, S1/S2) preserved; Sion/Arzelà–Ascoli structure untouched |
| S1 Step 3 concluded with the uniform R_U V_U h_u form of (8) | **S1 v6** Step 3 concludes with **max_{â∈S_G} ē_â** + δ_β + L h_t (the corrected (8)), the uniform choice noted as the special case |
| S1 Step 5's completeness mesh choice omitted δ_init/δ_num | **S1 v6**: "meshes and stored scenarios … including the stored-scenario widths δ_init and δ_num" |
| comp v7's §computation detection bound silently used the pre-fix sandwich (R_U V_U h_u + R_D V_D h_d + L h_t < γ) | **comp v8**: detection bound now charges max_a ē_a + R_D V_D h_d + δ_init + δ_num + L h_t < γ, uniform special case noted |
| comp v7 caption "HiGHS-attained values" (the other half of round-7 item 14) | **comp v8**: "returned values" |
| comp v7's (iv)-proof pairing estimate used R_U where the norm bound is M_U | **comp v8**: M_U defined and used |

## C. Editions shipped

- `paper2_computational_certification_v8.tex/.pdf` — new edition (v7 byte-untouched): §ladder (Proposition + proof + prescriptive reading), affine-mesh refinement identity with the time/information axis distinction, detection-bound fix, caption and pairing-word fixes, library roadmap sentence, abstract ladder clause. **9 pp, overfull 0**; render probes green.
- `paper2_computational_certification_v8_verification.py` — **36/36** (+ chained 51 + 9 + 12): new ladder battery (pooling identity; LP optima; peak identity incl. field-arithmetic root checks and monotonicity; facet/velocity scans at surrogates; affine-refinement identity), needles (25 new), superseded-string absences (old detection bound, "HiGHS-attained", the R_U pairing form).
- `paper2_computational_certification_supplementary_v6.tex/.pdf` — new S1 edition (v5 file untouched): L¹ Step (b) with M_U, L¹ Gronwall modulus in the moduli section, max-ē Step 3, δ-clause Step 5. **4 pp, overfull 0**; headings preserved; probes green.

## D. Chain of custody

Menu read end-to-end before any edit; each implementable claim computed exactly first (ladder battery, 8 checks, all green before the tex was touched); the roadmap adjudications cite the artifact inspection that motivated them (scipy interface, existing interval scoping); edits applied with uniqueness assertions; harness re-derives every new claim; both builds byte-present with zero overfull. v7 and the S1 v5 file are untouched.

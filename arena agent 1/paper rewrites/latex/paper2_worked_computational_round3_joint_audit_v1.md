# Joint external-audit adjudication, round 3 — worked systems (v6 → v7) and computational certification (v2 → v3)

**Round:** 2026-09-24, two external-audit files supplied by the owner (`worked gpt audit.txt`, 1566 lines, targeting the worked-systems paper; `computational gpt audit.txt`, 1764 lines, targeting the computational-certification paper). **Audited editions:** ws v6 (`6d2d943`) and comp v2 (with supplementary S1 v2). **Method:** unchanged — verify-before-fix on the artifacts (tex sources, verification scripts, shipped PDFs, fresh computation), fixes as new editions only. **Shipped this round:** `paper2_worked_systems_v7` (tex/pdf/verify), `paper2_computational_certification_v3` + supplementary v3 (tex/pdf/verify), regenerated figure set (6/6 green; fig 5 re-laid out by owner directive), this record, both addenda, roadmap v26.

---

## A. Worked-systems audit — verdicts

**Positive verdictions re-verified and confirmed** (no action): C(9,2) = 36 belief pairs; B4 = 15 partitions with caps 2/5/3/25 + 3/50; the caps schedule and Y* = 27/5 = cap-sum crossover; the CE drift formula and the 21/100 vs 0 bounds; the −1/10 saddle and constant −1/10 expected drift; the nonconvex counterexample (max–min −1 vs min-multiplier 0); the R-values; the two maximal common-action subsets overlapping in (2,2). The audit's own caveat that "24"/"22"/"24×24" complaints may be PDF-extraction artifacts is **confirmed**: the tex has real superscripts (`2^{4} \times 2^{4} = 256`, `2^{2} \times 2^{2} = 16`); no tex defect.

| # | Claim | Verdict | Evidence and action |
|---|---|---|---|
| 1.1 | σ* defined as z₀−1 in one place, ⌊z₀−1⌋ in another; floored-strict form fails on all six viable cells, so "exactly one cell" attaches to the real value | **UPHELD** | Tex L40/L341 write σ*(z₀) = z₀−1; L142/L355 write ⌊z₀−1⌋. Fresh computation (v7 verify M6b): strict-vs-floored fails on all six viable cells (z₀ ∈ {2.0,…,2.5}, T_obs = 1); strict-vs-real fails only at (2.0, 1). **Shipped in v7:** the split τ*(z₀) = z₀−1 (real worst-case exit time) vs σ*(z₀) = ⌊τ*(z₀)⌋ (largest admissible integer review count), applied in the abstract, conventions, §6, Table 3 caption, Fig 2 caption, and the design rule; §6 now states both strict-form counts explicitly |
| 1.3 | §2 register clause self-contradictory as phrased; implementation semantics unformalized | **UPHELD** | L131 said "one previous-action register per information state, shared by all fibres" (per state AND shared = contradictory). The chained scripts thread one fibre's action (first cell in hash order); fresh computation shows the audited kernel counts (24/26/25/28) are **not** invariant to the threading rule (canonical min/max threading gives 25/26/26/28) but the **pessimistic** semantics — policy fixes one action per fibre; the register then holds some fibre's executed action; survival requires every admissible thread to leave a winning continuation — reproduces all four kernels **setwise**. **Shipped in v7:** the corrected register definition (one register serves each review, shared by all fibres, not one per branch) plus the full pessimistic formalization, initial-register unconstrained, with the setwise-coincidence asserted as verify check M2b; verify M2c adds the optimistic-register contrast (strictly larger codex kernel, regains 12|31 and 13|21, still excludes 12|21), pinning that the pessimistic reading is the audited one |
| 1.6 | §4 "Neither protocol dominance holds" is wrong; the examples differ in both dimensions; dominance holds along columns | **UPHELD** | Fresh computation: within each observation column the free kernel strictly contains the no-repeat kernel (28>25, 26>24); across columns neither middle kernel contains the other. **Shipped in v7:** the sentence replaced by the exact column-wise statement |
| — | §4 explanation of 12|31 ("aggregate-viable … no-repeat never binds") wrong in both halves | **UPHELD** (computation surfaced this round, corroborating the master table's I = · entry) | Fresh trace: 12|31 is free-viable under **both** structures (H = 12 and H−2) and no-repeat-dead under **both**; the death is forced at step 2 (every step-1 choice leaves a branch whose filtered action set empties through the register). The old sentence claimed free/no-repeat separation under aggregation — false. **Shipped in v7:** corrected per-pair reading (shared-register collision at step 2, same mechanism as the alternation-forced pair); verify M3b re-derives the forced loss branch-by-branch under both structures |
| 1.7 | §5 pairwise-intersection claim: in a 2-action universe pairwise ⇒ common, so the census cannot show insufficiency | **UPHELD** | Correct: safe-action sets ⊆ {1,2}, Helly number 2. **Shipped in v7:** "necessary, and sufficient in the two-instrument universe (Helly number two), while in action sets of three or more elements pairwise intersection ceases to suffice ({1,2},{2,3},{1,3})" |
| 1.9 | "certificate exists exactly where the value is nonpositive" conflicts with the ≥0 convention | **UPHELD** | **Shipped in v7:** "exactly where the value is negative, and at a zero value the instance is boundary-critical with no strictly positive margin available" |
| §8 | "the kernel is 36 = 36" malformed | **UPHELD** | **Shipped in v7:** "the kernel comprises all 36 law pairs" |
| Typeset | "24"/"22"/"24×24" for 2⁴/2² | **REFUTED as a tex defect** | Real superscripts in source; PDF-extraction artifact, as the audit itself suspected |
| Fair | Refinement order for "coarsest"; Helly "three points" → "three constraints"; name the corner (1,1) in the abstract; register alphabet/init | **UPHELD (clarity)** | **Shipped in v7:** partition order defined (coarser = every finer fibre inside a coarser fibre); "Helly-tight family of three constraints — no two of them already infeasible"; abstract now says "pairs containing the corner state (1,1)"; register values stated (the two instruments), initial register unconstrained |
| Fair | "five figures" ambiguity; endpoint-convention phrasing; W_H^π two-index notation | **Split** | "five-figure set" (decimal precision) is the intended reading — left as-is; endpoint convention now stated in three places (§6, Table 3 caption, Fig 2 caption) via the 1.1 fix; W_H^π does not occur in this paper (grep: zero hits) — the item targets the calculus paper and is out of scope here |
| Owner-side | Repo/DOI/commit/CSVs; independent second implementation; certificate files; horizon diagnostics W₀…W₁₂; test-suite list | **Deferred** | Recorded in roadmap v26; the comp v3 edition pins the commit phrasing and schedules an independent certificate-file parser (see B) |

**Shipped beyond the audit's asks (round-3 computations):** agency-restriction counts for the audited belief 12|21 — fixing agency 1's vote at z = 1 to instrument 1 leaves 16 of 256 laws sustaining; fixing agency 2's leaves **0**; either whole-law restriction collapses the kernel to 0 (verify M-new). This sharpens §5's "recoverable only while both votes remain free" with exact counts.

**Repair note (discovered during this round's verification):** the shipped ws v6 tex carried an escape-mangled `\ref` ("column F of Table ␊ ␊ef{tab:master}"), which renders in the shipped PDF as "column F of Table ef tab:master).". The corruption predates v6 (present in v5) and was not flagged by any audit. **Fixed in v7** (L120–122), and the verify script now carries a source-hygiene regression (control bytes / bare `ef{`/`exttt{` remnants) mirroring the comp-side check.

---

## B. Computational audit — verdicts

**Positive verdictions re-verified and confirmed** (no action): the three-branch arithmetic (λ = (3/8, 5/16, 5/16) with Σλ = 1 and Σλn = 0; β_j = 16/25 − t*; Γ(τ) = τ − 7/50; Γ(1/5) = 3/50; blind window 39/25; e = Th/4 = 3/100; β + e = −53/100; Farkas −3/100; primal 3/100); Table 1 dimensions with s = 7, p_U = 6, N_t = T/h.

| # | Claim | Verdict | Evidence and action |
|---|---|---|---|
| 1.1 | Abstract overstates: "every dual solution" (zero-margin duals prove nothing); "never exceed r+1" (existence, not universality); "every sufficiently fine refinement" (needs admissible sequences with vanishing errors); "every pair viable" (unproved in text); "all exact computations … integer and rational" (HiGHS is floating point) | **UPHELD (all five)** | **Shipped in v3:** dual-feasible/lower-bound vs strictly-positive/obstruction split; existence wording for r+1 (with the Prop-5 saturation noted); admissible-refinement-sequence wording; "(explicit pairwise policies are carried exactly)"; scoped exactness claim ("solver outputs are numerical confirmations, never proof objects") |
| 1.2 | Thm 4(iv) exact dual representation needs a stated theorem with hypotheses, not a gesture | **UPHELD (scheduled depth)** | S1 already carries the six-step proof (attainment via weak-* compactness, atomic density, integration-swap). v3 tightens the statement's hypotheses at (iii)/(v) and the Prop-2 compactness paragraph; a full minimax-theorem treatment with every hypothesis verified line-by-line is scheduled in roadmap v26 as the largest remaining exposition debt (a structural rewrite, not a fix) |
| 1.3 | Weak-* compactness needs the convexity of U stated at the point of use | **UPHELD** | **Shipped in v3** (Prop 2: compact convex polytope ⇒ weak-* closed and bounded; finite product over observation cells; scope section now explains why convexity is structural) |
| 1.4 | Sandwich upper-bound proof compressed; "scenarios" misnomer; the three error terms mislabeled ("input radius" and "moment error" are not separate) | **UPHELD** | **Shipped in v3:** "block variables" replaces "scenarios"; the three terms named correctly (control-moment relaxation R_U V_U h_u; stored-scenario error δ_β; time-grid interpolation error L h_t) |
| 1.5 | Moment-error bound needs the center-cancellation argument | **ALREADY PRESENT** | S1 carries the center cancellation ("∫(k̄ − k) = 0, the center cancels"); no main-text change needed beyond the v3 hypothesis cross-references |
| 1.6 | β-sign convention counterintuitive; tie to §7's false-obstruction warning; define "inner adversarial approximation" operationally | **UPHELD** | **Shipped in v3:** §7 now carries the explicit β⁺ ≥ β sign-convention cross-reference; the abstract glosses the inner approximation as "finitely stored admissible labels and scenarios" |
| 1.7 | "finite time grid including every mode and safety facet" malformed | **UPHELD** | **Shipped in v3:** 𝒮_G = Θ × {1,…,s} × G with 0, T ∈ G, observation times in G, covering-radius formula; program (5) quantifies over 𝒮_G |
| 1.8 | L assumption disconnected from primitive data | **UPHELD (part)** | **Shipped in v3:** the input model names certified total-variation and uniform time-Lipschitz bounds as part of the admissible data (see (iii) edit); a primitive-data derivation of L is system-specific and is scheduled (roadmap v26) |
| 1.9 | "Semidecidable" needs an effective-input model and an explicit procedure | **UPHELD** | **Shipped in v3:** exact rational or interval-enclosed coefficients with certified integration/TV/Lipschitz bounds; enumerate-meshes → solve enclosed program → halt on first positive certificate |
| 1.10 | Sparse-support theorem needs a precise rank definition | **UPHELD** | **Shipped in v3:** r = rank of the safety-row coefficient matrix of (5) (ρ-column and actuator inequalities excluded); bound from LP basic-solution theory (S1 Step 6) |
| §6 | s = 7 unstated; 93/50 needs the three verification bullets; pooling-weights parenthetical opaque; "J(τ+1) = τ − 0.14" needs the upper-bound policy and equality-viability; **pairwise viability asserted but not proved**; blind-window exit-test phrasing | **UPHELD (all)** | **Shipped in v3:** "seven facets in all, s = 7"; the weights parenthetical replaced by Σλ = 1 and Σλn = 0; τ_max exact "equality viable: the safety set is closed", with the full-braking upper-bound policy giving J(T) = τ − 7/50 for T ≥ τ + 1; **the pairwise policies are now carried in the main text exactly** ({1,2}/{1,3}: blind hold u = 0, mid-brake −(2/5)n_j on [τ, τ+1/5], then −n_j to rest, peak ≤ 12345/6250 = 1.9752; {2,3}: blind hold u = n₁, mid-brake −(3/5)n_j, then −n_j, peak ≤ 2419/1250 = 1.9352; {1,3} by the reflection v₂ ↦ −v₂) — the numbers are the ones the campaign script has verified all along (its label "12193/6250" was a long-standing typo for 19352/10000 = 2419/1250; the assertion always used the correct value, now fixed in the campaign printout's successor context and in S1 v3); velocity-boundary attainment noted (closed constraints) |
| §6 campaign | Mesh law domain unscoped; "β + e" conflates two quantities; "every other row" unproved; solver-certified naming | **UPHELD (all)** | **Shipped in v3:** the law scoped to the five aligned uniform meshes with the zero floor explained; row-check pointer (all 465 rows at h = 1/10, archived); "Solver-assisted, exactly certified" everywhere (title, §6, Table 1); the β = −14/25 vs β + e = −53/100 split was already explicit in the source (verified) |
| §7 | Residual test (11) too terse; sparsification unspecified; "certificate-size bound" overclaims; complexity needs bit-complexity scoping; stored-point membership note | **UPHELD (all)** | **Shipped in v3:** the two intermediate inequalities and the multiplier-redundancy note; the deletion algorithm (fixed order, margin-preserving, irredundant not minimal); "margin-dependent resolution bound"; weakly polynomial bit complexity for exact rational and interval-enclosed programs |
| §8 | Prop 5 notation (x_j suggests a common vector); pathwise sufficiency; necessity argument; rank display | **UPHELD** | **Shipped in v3:** scalar-state phrasing with per-mode equations; "the terminal bounds are the binding ones, each x monotone along its mode"; the subbelief-certificate argument made explicit ("a certificate supported on fewer than q+1 modes would also certify the proper subbelief, contradicting its viability") |
| §9 | Subadditivity: proof first, 3,240-pair test as regression; erosion = same-witness statement; oracle-recourse operational definitions + soundness chain | **UPHELD (all)** | **Shipped in v3:** all three (pointwise-minimization proof named decisive; equation (12) framed as the same witness's margin, not the optimized value; scenario/oracle/reveal definitions and the three-step soundness direction) |
| Table 1 | Caption needs hardware/solver settings, s = 7/p_U/N_t formulas, "rows" = LP inequalities, h = 0.2 optimality-witness vs obstruction-certificate distinction, instance-specific three-label support | **UPHELD** | **Shipped in v3** (caption rewritten in full; wall-clock noted as single runs on the verification machine, iteration counts version-dependent) |
| §10 | Title/library mismatch; "Section efmethods"/"as a extttviac-ert" broken markup; "Exactness: no floating point" overbroad; unrelated legacy counts; "retirement of superseded audit identities" | **UPHELD (all)** — and the broken markup is a **real source corruption**, worse than the audit guessed | Byte-level inspection found literal TAB and CR control characters in the v2 tex at L616 (`Section~\ref` with CR-eaten `\r`, `\texttt` with TAB-eaten `\t`): an escaping accident during a shipped edit; the PDF rendered the mess ("Section efmethods.; as a extttviac-ert.continuous module"). **Shipped in v3:** repaired; source-hygiene regression added to the verify script (both tex files scanned for control bytes and bare remnants); title reframed ("…and a Finite-Side Reference Library") with the abstract/intro/§10 scoping (finite-side package + campaign pipeline; bridge programs deliberately not reimplemented); "Exactness" principle scoped; legacy counts framed as software self-tests pointing to the published audits, context in package documentation; "retirement" replaced by archive-preservation wording (archived scripts remain the exact versions used for the verification record) |
| §11 | Convexity reason; "full-information viability decides none …" too strong; tangency; "exact finite safety games await" | **UPHELD** | **Shipped in v3:** all four reworded (necessary-but-not-sufficient; temporal-informational obstruction; discrete games exist, the bridge is what makes them sound abstractions) |
| §12 | "exactly when" unproved; "relax the controller and never the adversary's necessity direction" unparseable; solver phrasing; open-problem scope | **UPHELD** | **Shipped in v3:** all four (rests-on-three-requirements; outer-relaxation/genuine-scenario phrasing; "the solver returning the independently proved optima to numerical precision"; open problem = completeness under general sensing, refinement completeness already theorem'd) |
| §13 | Script-count structure; "Every number" too broad; "independently" undefined; repo permanence; future date | **UPHELD (part; owner-side items)** | **Shipped in v3:** "Every reported mathematical benchmark value and every table entry"; artifact structure (three scripts + one package + one master entry point); independence defined (separate formula path; an independent certificate-file parser that reads archived witnesses without importing the model builder is the recorded next step); repo path notes the pinned commit. DOI/Zenodo/environment lockfile = owner-side. The 2026-09-24 date is intentional (the audit ran on that date) |
| §4 | Abaee 2026a/2026b bibliography mismatch; DOIs/style; self-containment | **UPHELD (first)** | **Shipped in v3:** the 2026b entry completed ("Abaee, A., 2026b. An obstruction calculus … Manuscript submitted for publication."). Reference styling and full logical self-containment of the finite recursion = scheduled/owner-side (roadmap v26) |
| §5 | Terminology upgrades (value/certificate taxonomy; "solver-certified"; rank; "belief"; grammar) | **UPHELD** | **Shipped in v3:** belief = set-valued convention stated at the kernel definition; "solver-assisted, exactly certified"; grammar unified (program); nonviability spelling already consistent in source |

**Fidelity check on the shipped numbers:** every count, witness, and bound shipped in v3 is bit-identical to v2's audited values (20/20 verify checks, including the two new exact pair-policy peaks re-derived from primitives; 51/51 + 9/9 + 12/12 chained). The only numeric-notation change in the paper is 12193/6250 → 2419/1250 for the 1.9352 peak, which corrects a typo in a check **label** that never affected any assertion.

---

## C. Contradictions between the two audits

None material. The worked audit's §4/§5 items concern the shared-system papers; the computational audit's Prop-5 "at most m+1 … false for a common blind control function" remark is consistent with the worked audit's Helly observations (finite action sets vs blind control functions differ exactly in temporal-informational dimension), and both are now stated so in their respective texts.

## D. Both audits' shared owner-side ledger (consolidated, roadmap v26)

Tagged release + DOI + commit + environment lockfile + certificate files + expected checksums; independent second implementation / certificate-file parser; hardware/solver manifest for Table 1; reference-styling pass; test-suite list and horizon diagnostics in the ws archive; title-length options for the computational paper; `viacert.continuous` consolidation (with archive preservation, per v3's wording).

*Every verdict above was decided by direct computation or artifact inspection this round; the shipped verify scripts re-run the decisive checks (ws 26/26 + 57/57 chained; comp 20/20 incl. 51/51 + 9/9 + 12/12 chained).*

# Round-9 Adjudication — Roadmap location, remaining upgrade items, and the two-paper remnant/redundancy scan

**Scope.** Two questions: (1) which opus-upgrade items remain and where they are recorded — the reader could not find a roadmap in comp v8, nor the harder list; (2) a remnant-and-redundancy scan of both papers (worked-systems v10 and computational-certification v8 + its S1 v6 supplementary). Answers implemented as **comp v9** (`paper2_computational_certification_v9.tex/.pdf/_v9_verification.py`, **37/37** checks + chained 51 + 9 + 12; PDF 10 pp, overfull 0). ws v10 and S1 v6: scan-clean, byte-untouched.

---

## 1. Where the roadmap was in v8, and what the harder list's status was

The user's reading was correct. In comp v8 the only in-paper roadmap was **one sentence in §library** (the three tooling directions: warm-started incremental refinement, arbitrary-precision dual re-solve fallback, conflict-targeted refinement for non-affine kernels) — the tooling halves of bets #2 and #5. The remaining bets (#3, #4) and the harder list (decentralized/networked; infinite-horizon/periodic; nonconvex/piecewise-smooth; CORA/SMT/barrier comparison) were recorded **only in the round-8 addendum**, on the grounds that §scope already delimits several of them as exclusions. That was an under-recording: delimitations say what the theorem does not cover; they are not a research record. Comp v9 corrects this in the paper itself (§2 below). Also found and fixed while locating the roadmap: the intro **contributions paragraph still enumerated six items and omitted the new ladder subsection** (a v8 remnant — the abstract had the ladder clause but the intro list was never extended), and §methods called the certificate-file parser "the recorded next step (Section~\ref{library})" although the library paragraph does not list the parser.

## 2. What remains from the menu, and its disposition (now in-paper at §conclusion)

Nothing further is implementable as an exact edition edit: every remaining item changes a structural input of the bridge theorem (weak-* compactness of label strategies, finite-horizon completeness, convexity of the actuator set) or the certificate class itself, so per the no-shallow-quick-fixes discipline they are recorded as constructions of their own, verbatim in the new conclusion paragraph:

> Four further extension directions are recorded, each a construction of its own rather than an increment of the present edition; all lie outside the audited class delimited in Section~\ref{scope}. (i) General measurement channels: the instance already prices partial observation under delayed label revelation, so the incremental content is the passage from history partitions to general measurement maps, by a cell-conditioned lift of labels, kernels, and costs. (ii) Stochastic disturbances: replacing support-function rows with second-moment bounds turns the finite program into a conic one (second-order cone or semidefinite), with exact rational witnesses replaced by conic dual certificates; the exact rational optima of the present campaign remain the benchmark such relaxations must meet. (iii) Decentralized and networked controller sets, infinite-horizon and periodic regimes, and nonconvex or piecewise-smooth actuators: each alters a structural input of the bridge theorem (the weak-* compactness of label strategies, finite-horizon completeness, or the convexity of the actuator set), and is therefore a new construction rather than a parameter change. (iv) A cross-tool comparison study that treats the audited instance as a common benchmark — exact finite safety games, interval-reachability toolchains, SMT-backed barrier synthesis — with the certificate file as the exchange format; the tooling directions recorded in Section~\ref{library} and the independent certificate-file parser of Section~\ref{methods} are its computational prerequisites.

Menu-item mapping: (i) = bet #3 (its non-already-covered content), (ii) = bet #4, (iii) = harder items 1–3 (cross-refs §scope's existing delimitations), (iv) = harder item 4 (the CORA/SMT/barrier comparison; interval-reachability toolchains is the generic name for the CORA class). Bets #2/#5's tooling halves stay recorded in §library; the conclusion paragraph now also cites them, so the roadmap is one cross-referenced record rather than three scattered sentences.

## 3. The scan: method and results

Method (scripted, mechanical, over the comment-stripped tex of ws v10, comp v8, S1 v6): stale edition pointers; verify FORBIDDEN lists re-checked against the tex bodies; doubled words; TODO/FIXME/??/empty-ref markers; duplicate and undefined `\label`/`\ref`; date consistency; fraction census with context inspection; within-file near-duplicate sentences (difflib ratio ≥ 0.88, length ≥ 60 chars); cross-file overlap (comp↔S1, ws↔comp, ws↔S1). Both verify chains were run before any edit as green baseline (ws 46/46 + 57 chained; comp 36/36).

| File | Remnants found | Redundancy found |
|---|---|---|
| ws v10 | **None** (pointer = own v10 verify; no doubled words; no markers; no duplicate labels; FORBIDDEN clean) | **None** (0 near-dup pairs) |
| comp v8 | **(a)** intro contributions paragraph omitted the ladder subsection; **(b)** §methods parser pointer attributed to §library, which does not list it; **(c)** in the verify script only: stale self-labels "edition-3 absences" / "edition-3 tex file", the "four-block" label on a 2+3 = five-block computation, and the v8-filename needle (superseded once the tex points to v9) | **None** (0 near-dup pairs; abstract's ladder numbers summarize §ladder by design) |
| S1 v6 | **None** (no pointers to fix; headings intact) | **None** (0 near-dup pairs; no verbatim overlap with comp ≥ 0.85 — the supplementary argues, it does not restate) |
| cross-file | — | ws↔comp overlap is exactly the **required-verbatim AI declaration** and two shared reference entries (the companion papers each cites); ws↔S1 zero; comp↔S1 zero. Lineage separation intact |
| fraction census | All flagged contexts legitimate: h²/4 and \|J\|²/4 families, the library's kernel-count list 24/26/25/28, check counts 9/9 and 51/51 in S1's pointers, the library's citation of the ws benchmark Y* = 27/5 (by design) | — |

## 4. Editions shipped

- `paper2_computational_certification_v9.tex/.pdf` — new edition (v8 byte-untouched): ladder added to the intro contributions list; the four-direction outlook paragraph in §conclusion; methods parser pointer reworded ("a recorded next step, alongside the tooling directions of Section~\ref{library}"); code availability → v9 script. **10 pp, overfull 0**; render probes all green (old methods phrasing now absent, `\ref{ladder}` exactly once, outlook strings render, date Sep 26, no `??`).
- `paper2_computational_certification_v9_verification.py` — **37/37** (chained 51 + 9 + 12): 8 new outlook/contribution needles; new `\ref{ladder}`-exactly-once check; FORBIDDEN += the superseded methods sentence; label fixes (five-block; "edition absences"; "either tex file"); docstring edition-9 note.
- Untouched and scan-clean: **ws v10 set**, **S1 v6**, comp v8 set (frozen as pushed at `7dead06`).

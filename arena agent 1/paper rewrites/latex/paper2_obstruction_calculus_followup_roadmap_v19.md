# Paper 2 — Obstruction Calculus: Follow-up Roadmap (v19)

**Subject:** *An Obstruction Calculus for Viability under Incomplete Observation*
(current: `paper2_obstruction_calculus_v49_Automatica_routes.tex` + supplementary v46).

**v19 changes:** The EIGHTH ROUND executes the owner's implementation
directive ("proceed with implementations using recommended judgements and
root cause analysis"). Two of the three remaining builds are DONE; the third
is scoped. (1) **P4 SHIPPED — the worked-systems flagship**
(`paper2_worked_systems_v1`, 3 pp two-column, zero overfull): the fold map's
consolidation row executed — the concordance backbone (C1–C8), the static
duality edition, the monitoring design rules (E1–E9), and the four audited
instance lines (vector floors v2, policy-class lattice v2, hybrid modes v2,
institutional observation v2) stated once under one audit frame, with the
absorbed-lines registry and the practitioner reading. Master verification
16/16: seven lineage scripts chained green (57/57) + independent
recomputation of the crown jewels + text needles. Source editions retained
(supersession recorded, never deletion); addendum
`paper2_worked_systems_v1_addendum.md`. (2) **P2's LIBRARY ARTIFACT SHIPPED
— `viacert/`**: the library MVP as the audits specified it — the
finite-horizon selector recursion (Theorem 1; codex threaded as a
parameter) and the fibre/partition criteria (adequacy converse, maximal
common-action sets, partition census, review-timing identity, CE drift
audit), packaged with a pluggable `FiniteSystem`, the audited systems as
default instances, a 12-check CLI selftest, a unittest suite, and a
provenance README. Selftest 12/12; unittest suite green. (3) **P2's paper
and P3's consolidation remain**, both now seed-complete: P2's paper wraps
the companion campaign + the library; P3 wraps stochastic selector v2 +
calculus §10 + hidden-parameter learning. Build order unchanged (P2 paper,
then P3), each a new edition set with the same verification discipline.

## Standing owner-side items (unchanged)

Venue picks; figshare upload → DOI → master v5 → item v2; figshare 33764023
hygiene; `paper2_`-prefix rename decision; rotate the `.github_pat` found in
the `edwards-framework-e1` release asset; pin the 2026 SAR values when CSAS
posts them.

*Every claim above is a pointer into the cited sources; nothing here creates
theorem status.*

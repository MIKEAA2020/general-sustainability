# vector_floor_certificates_v1 — addendum

**Lineage:** `vector_floor_certificates_v1` (standalone name from birth; no paperN prefix — the
naming rule adopted after the paper-2 prefix lesson). **D-item:** Vector Floors (D4)
of Track D (source: `uploads/suggested generalizations.txt`).

**Content.** Farkas-certified obstruction under multiple floors; stacked-system equivalence both directions; dynamic vector-floor re-reading of the audited verdicts; floor monotonicity 28 vs 36; complementarity multiplier zero.

**Verification.** Exact rational/integer arithmetic; stdlib only;
deterministic. 6/6 check families:
E1 stacked-system equivalence (Farkas, both directions); E2 branchwise/joint belief gap; E3 dynamic vector floors (five-structure verdict table); E4 floor monotonicity 28 vs 36; E5 exact multiplier recheck; E6 three-floor complementarity.

**Build.** Tectonic 0.15.0, first build: 0 overfull boxes; 2 pages;
79763 bytes; post-build probes pass (title/author/ORCID/AI declaration/
no unresolved references/declarations block).

**Defects found during verification (before ship).** One non-Latin character slipped into a draft sentence (review catch, fixed before any build; no rendered artifact affected).

**Status.** Shipped to `latex/` (tex + pdf + verify + this addendum) via
cp + cmp; source zip `vector_floor_certificates_v1_source.zip` (4 entries); pushed to the
programme repository under the batch commit for this item.

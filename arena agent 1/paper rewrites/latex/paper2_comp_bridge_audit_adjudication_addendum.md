# Comp "Viability Certification Bridge" audit — adjudication addendum

**Date:** September 25, 2026. **Audit:** `uploads/Viability Certification Bridge upgrade.txt` (two reports: gemini, luna), addressed to the computational-certification line — routing confirmed (the bridge is comp's continuous-to-finite theorem). **Method:** every quoted defect located in the shipped texts and in the edition history before adjudication; generalizations implemented only where they were new constructions; ships `paper2_computational_certification_v13` (verifier 48/48, chained lineage 51 + 9 + 12; build gate green: exit 0, zero overfull, 10 pages, `SOURCE_DATE_EPOCH`-pinned rebuild byte-identical), superseding v12 per the map.

## A. The four core repairs: real defects of editions v3–v6, discharged in v7

All four quoted repairs match defects that existed in v3–v6 and were fixed in v7; none survives in v12. The reports correspondingly read a pre-v7 edition.

| Quoted repair | State in v3–v6 | Discharge (v7) | v12/v13 state |
|---|---|---|---|
| "correct the Section 6 pair policies" / "fix the pair-policy arithmetic" | v3's {2,3} policy carried a −(3/5)nⱼ leg after τ and attributed the peak 2419/1250 to it; that policy peaks at 2501/1250 = 2.0008 > 2 — the printed pair claim was false | v7 replaced the {2,3} blind-window policy (hold u = n₁, then u = −nⱼ: peak exactly 2419/1250, slack 81/1250) and moved the failing leg to the printed falsified-variants list (2501/1250 at t = 29/25, alongside 2719/1250 and 103/50) | script-certified since the correction: the verifier's pair-policy layer recomputes all five policies and variants exactly, plus the seven-facet scans (worst slacks 31/1250, 81/1250) |
| "replace sup-norm kernel continuity with the L¹ condition the proof actually needs" | pre-v7 hypothesis read sup-norm | v7 states the label-map continuity in L¹ and says so explicitly: "the L¹ form is the operative one — sup-norm continuity is neither needed nor true here, the step kernels below jumping at σ = t" | shipped hypothesis; the S1 v6 supplementary carries the L¹ pairing step and the L¹ Gronwall modulus |
| "include initial-state and numerical enclosure errors in (8)" | pre-v7 sandwich budget omitted them | v7: with (4), the budget δ_β = δ_init + δ_dist + δ_num and the display after (8) reads max ē_a + R_D V_D h_d + δ_init + δ_num + L h_t; verified row widths added on the right | shipped; dated by string presence v6 = absent, v7 = present |
| "retract monotone 'survival under refinement' claims" | pre-v7 wording | v7 states "positivity is not itself monotone under refinement" and quantifies the drop as the erosion identity (12) — "splitting it can dissolve the certificate while the system's verdict is unchanged" | shipped; the φ_U subadditivity direction carries a 3,240-pair exact regression test |

Verdict on part one of the audit: **upheld for the audited edition, already discharged for five editions, verified current** — the audit's own headline demand ("make the core airtight") describes work the line already completed and script-certified.

## B. The generalizations: one executed, one newly quantified, one remains recorded

1. **Belief cells over (θ, x) for partial, noisy, delayed observation — executed in v13.** This was comp's own recorded extension direction (conclusion, "a cell-conditioned lift of labels, kernels, and costs"); v13 carries it as Proposition `prop:beliefcells`: cell-supremum row labels β_a^C = sup over the certified cell plus enclosure radius δ_C; the program stays linear (conic where support functions enter); the sandwich (8) and soundness hold with the cell-filtration value; cell splitting erodes a witness by exactly (12), singleton cells recovering the stored-scenario labels (13); margins are radius-bounded both ways (witness slack s tolerates δ_C ≤ s; a dual certificate of margin Γ survives δ_C < Γ). Instance instantiation: the compatible-mode hierarchy {1,2,3} → {1,2}/{2,3} → singletons is the cell-refinement family (thresholds 7/50, (15−√183)/6, (10−√58)/6, unbounded), and noisy state cells are priced exactly — pair witnesses tolerate radius up to their worst row slacks 31/1250 and 81/1250; the triple's certificate survives δ_C < Γ(1/5) = 3/50, the margin moving to τ − 7/50 − δ_C.
2. **Actionable duals — largely present, now with exact rates.** The ladder subsection already decoded the witness prescriptively (shared braking authorities, the pair-delay hierarchy, "the binding resource is the shared window"). v13 adds Proposition `prop:redesign`, making the decode quantitative: observation timing buys margin at exactly rate one (Γ_a(τ) = τ − 7/50 − (a−1)/2); dilated post-revelation authority buys half a unit of delay per unit (τ_max(a) = 7/50 + (a−1)/2, equality viable); shared-window authority buys nothing at any scale (the pooled kernel Σλ_jk_j ≡ 0 as a vector identity and the witness charges no pre-observation input facet); the velocity budget buys nothing (the dual is velocity-row-free, so deleting the velocity rows preserves the value). New verifier layer: dilated dual witnesses (η_j/a on aF, af), the closed-form values at a ∈ {3/2, 2}, the thresholds {7/50, 39/100, 16/25}, the rate identity, the exact interval-integral sum Σa_k = 1/2 behind the positional Farkas value, and the radius laws — nine new exact checks, 39 → 48/48.
3. **Benchmarking against conservative reachability tools — remains recorded, now cited as the paper's own direction.** This is conclusion direction (iii) (formerly (iv)): exact finite safety games, interval-reachability toolchains, SMT-backed barrier synthesis on the audited instance as common benchmark, with the certificate file as exchange format; its computational prerequisites (the independent certificate-file parser, warm-started incremental refinement) are recorded in the methods and library. The comparison has no proof content — conservative tools yield conservative bounds — and the exact rational optima remain the benchmark such relaxations must meet; executing it awaits the parser, as recorded.

## C. Outcome

- Shipped: `paper2_computational_certification_v13.{tex,pdf}` (10 pp) + `paper2_computational_certification_v13_verification.py` (48/48; chained 51 + 9 + 12), supersession-map row, this addendum. comp v12 retired.
- The audit's four core repairs: adjudicated stale (fixed in v7, script-certified since, verified against v12 line-by-line).
- The audit's strongest generalization: executed as the paper's own recorded direction, now a proposition with exact instance content; the redesign ask executed as exact rates; the benchmark ask remains recorded with its prerequisites named.

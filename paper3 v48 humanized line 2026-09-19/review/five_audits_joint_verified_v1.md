# The five audits in `uploads/p3 profound upgrades.txt` — joint evaluation, independent verification, and dispositions

**Date:** 2026-09-15 · **Object:** `p3 profound upgrades.txt` (97,505 bytes · 13,247 words · 1,499 lines; each line pair duplicated in places, and the file opens mid-word at `astra:`). It bundles **five** audits, delimited by name labels:

| Label | Lines | Voice | Its load-bearing proposals |
|---|---|---|---|
| **astra** | 1–579 | five numbered upgrades + a revised contribution statement | observation map `z=H(x,η)+ε`; identifiability definition; **Witness A** (anomaly translation) and **Witness B** (aggregate dynamics); identification sets `T_±`; **critical-margin budget theorem**; **latest safe intervention time** benchmark; four claim-tightening notes (4A–4D); proof-carrying certificate fields |
| **fable** | 580–719 | "where novelty is thin" + T1–T11 + architecture + impact programme | aggregation triad (Liebig faithfulness, Gale–Hoffman substitution interpolation, closure cone); exergy fourth predicate; certified-horizon interval; frozen-rate sign law; anomaly `O_p(σ/β)`; positivity unification; liquidation share; barrier-compatibility identity; renewability relativity (SEEA) |
| **opus** | 720–969 | Upgrades 1–9 + a cut list + a restructured 12-section paper | uniqueness of min under (M)+(I)+(NC); predicate transport (refute-only); compensation premium arithmetic for Earth Overshoot Day; signed-bias calculus + **R/P crossover** `η ≷ ½gτ`; yield-inflation non-identifiability; closure kernel; control margin; Farkas/SOS with completeness + tractability boundary; metrology (overidentification test, **MDV**, five standards amendments); manipulation resistance |
| **sol** | 970–1383 | layered semantic architecture; certificate vector; theorem families A–E | four-layer report architecture; assumption ladder + **promotion-rule table**; closure hierarchy with primal/dual certificates; certificate schema |
| **kimi** | 1384–1499 | four theorem families + architecture hardening + artefacts, with honest risk notes | reclassification-invariance audit criterion; hidden-liquidation identifiability; time-expanded LP with min-cut dual; **last responsible moment** `t*(τ,ū)` + decision window; certificate calculus; sharpened noncompensation (§6, see V10 — false as stated); fourth predicate via Gibbs energies; circulation-time closure predicate; **SDE sandwich** |

**Judged against:** `work/paper3.txt` (source of record) and `revision/v4/paper3_v4.md` (the manuscript of record, 108,043 characters; 34-check verify run passes). Cross-referenced to the earlier four audits in `review/ledger_audits_verified_v1.md` (buckets T/P/R) and to `review/upgrade2_verified_v1.md`.

---

## 1. Independent verification — what I recomputed, and what came back

I did not take any of these on trust. Each row is a claim I re-derived or re-ran this turn.

| # | Claim | My check | Verdict |
|---|---|---|---|
| V1 | **astra Witness B** — identical aggregate trajectory, different component failure times: `ẋᵢ=−kxᵢ`, `ẇᵢ=kxᵢ`, `Z=x₁+x₂`, `(2,98)` vs `(50,50)`, barriers 1, `T₁=log2/k`, `T₂=log50/k` | `x₁=2e^{−kt}=1` at `log2/k` and `x₂=98e^{−kt}=1` at `log98/k` ⇒ first hit `log2/k` ✓; `(50,50)` ⇒ both at `log50/k` ✓; `Z=100e^{−kt}` identical ✓ | **CORRECT.** This is the dynamical companion of the article's static non-compensation, and the strongest single *cheap* addition in the whole five-audit set: aggregate observations plus *exact* aggregate dynamics still do not identify a component event time. "Dynamical closure and barrier observability are separate obligations" is a sentence the article's §10.2 non-reduction boundary generalises directly |
| V2 | **astra critical-margin budget theorem**: `m=Gx+a≥0`, `y=c^⊤v`, if `λ≥0` with `λ^⊤GS_T+c^⊤≤0` then `∫₀^T y ≤ V(x₀)+∫₀^T λ^⊤Gb`, and with `y≥y_req`, `λ^⊤Gb≤β<y_req` then `T≤V(x₀)/(y_req−β)` | `V̇=λ^⊤G(S_Tv+b) ≤ −c^⊤v+λ^⊤Gb = −y+λ^⊤Gb` ✓ (uses `v≥0`); integrate with `V(T)≥0` from `λ≥0, m≥0` ✓; the `T` bound is `y_req T ≤ V₀+βT` ✓ | **CORRECT as stated, five lines.** It contains the article's own Theorem 14 extraction-integrability as the one-margin instance, and it is the object the article's recharge-law table only gestures at. Its two self-declared qualifications (no witness ⇒ no safety; relaxation feasibility ⇒ not realizability) and "not a scalar safety certificate" are exactly the article's disciplines, so it can be adopted with no softening anywhere |
| V3 | **astra latest safe intervention time**: `m_needed = d₀τ + d₀²/(2ρ)`; feasible iff `m₀ ≥ m_needed`; `L_max = m₀/d₀ − τ − d₀/(2ρ)`; illustration 100, 10, 2, 1 ⇒ `H=10`, `L_max=3` | loss during delay `d₀τ` ✓; ramp area `½·d₀·(d₀/ρ)` ✓; `L ≤ m₀/d₀−τ−d₀/(2ρ)` ✓; `10−2−5=3` ✓ | **CORRECT arithmetic and construction.** Note it needs no reduction of the ledger to the companion delay system — `τ,ρ` enter as declared interface assumptions — so it does not touch §10.2. The article's `H_A^loc` appears as the comparison object, exactly as the article defines it |
| V4 | **fable T1(b)**: no continuous strictly-increasing aggregator is both faithful and complete (proof on the face `b₁=τ₁`) | completeness ⇒ `g≥0` on the face; faithfulness applied to `b−εe₁ ∉ τ+ℝⁿ₊` ⇒ `g<0`; continuity ⇒ `g≡0` on the face, contradicting strict increase in `b₂` ✓ | **CORRECT.** T1(a) ("faithful+complete ⟺ `{g≥0}=τ+ℝⁿ₊`, hence sign-equivalent to the min") is a restatement of the definitions, not a theorem — cite it as an observation |
| V5 | **fable T2**: adequacy-after-substitution ⟺ `Σ_{i∈U}b_i + Σ_{j∉U,i∈U}κ_{ji} ≥ 0` for all `U`, with `κ≡0 ⇒ b≥0` and `κ≡∞ ⇒ Σb_i≥0` | subset-condition is Hoffman's circulation feasibility test with demands on nodes and arc capacities ✓; both endpoint reductions check out ✓ | **CORRECT**, and it is the strongest framing gift to §1.3: weak and strong become the two endpoints of one feasibility predicate, with every finite `κ` an LP-decided intermediate. The flow theorem is standard; the *dichotomy as endpoints* is the novelty, as the audit itself says. Cite Gale, *Pacific J. Math.* **7** (1957) 1073–1082 — verified in §7; no Hoffman reference is needed, since Gale's feasibility theorem is the same statement (the bounds-with-lower-bounds form is in Ahuja–Magnanti–Orlin, *Network Flows*, 1993) — and see §5 on the compositionality attribution |
| V6 | **fable T7**: for `a_k=−βk+ε_k`, the index (fitted distance to record minimum over fitted slope) is `O_p(σ/β)` — bounded in probability as the record grows, independent of the stock | I simulated it: 400 replicates per record length, `β=σ=1`, `n=100, 10³, 10⁴, 10⁵` ⇒ index **0.2, 0.2, 0.2, 0.2** (mean), while the stock falls by five orders of magnitude. The reason: with a downward trend the running minimum sits at the record's end, so the numerator is a noise-scale gap, not `βn` | **CORRECT — and I can now state it as a computed witness, which the article does not have.** This is strictly better than the current "record-relative" prose: no lengthening or sharpening of an anomaly record can produce a horizon, because the statistic converges to a trend-detection quantity. (My earlier §8.1 sentence argued this by the constant-shift invariance; the bounded-in-probability result is the stronger version.) |
| V7 | **fable T10**: `Σ S̲_k ≤ M ≤ Σ S̄_k` feasibility, and the margin identity `(S₁−S̲₁)+(S̄₃−S₃) = M−S̲₁−S̄₃−S₂` | feasibility condition ✓ (with the caveat that sufficiency needs the readouts realisable by `x≥0`, i.e. it is exact for `C=I`). Identity: substituting `S₁=M−S₂−S₃` gives LHS `= M−S₂−S̲₁+S̄₃−2S₃` ✗. The invariant is the **difference**: `D−W = M−S₂−S̲₁−S̄₃`. Numeric check with `M=100, S₂=20, S̲₁=10, S̄₃=40`: `(S₁,S₃)=(50,30)` gives `D−W=30`; transferring 10 from sink to stock `(60,20)` gives `D−W=30` again, while `D+W` moves 50→70 | **SIGN SLIP in the audit; the corrected form is stronger.** With `D−W` constant, a moiety-consistent transfer from the sink to the stock raises the depletion margin *and* the sink headroom simultaneously — so "depletion and pollution are the same predicate at the two ends of one cycle" becomes an exact statement about joint improvement, not a trade-off slogan. This is an improvement I am supplying, not one the audit made |
| V8 | **fable T9 / kimi §2** liquidation share `(Y−Y_s)/Y = ℓ/g` for linear `Y`; `ℓ=(g−R(A))₊=(−Ȧ)₊` | `Y−Y_s = α(g−min(g,R)) = α(g−R)₊` and `Y=αg` ⇒ ratio `(g−R)₊/g = ℓ/g` ✓ | **CORRECT.** Adoption caveat: `σ` is already the donor fraction in this article's alias discipline — the object must be renamed (I use `ϱ`, liquidation share) or it re-creates the symbol collision the audits were asked about in the first place |
| V9 | **opus U1a**: (M) monotone + (I) `A(c1)=c` + (NC) `A(r)≥c ⇒ r≥c·1` ⟹ `A=min`; compensation premium `Π=A−min≥0` | `c=A(r)` in (NC) ⇒ `min_i r_i ≥ A(r)`; `r ≥ (min r)·1` ⇒ `A(r) ≥ A((min r)·1)= min r` by (M)+(I) ✓ | **CORRECT and better than fable T1**: it yields exact uniqueness rather than sign-equivalence, in three lines, and it converts §7 from prohibitive to constructive. Note (I) is doing the work — see V10 |
| V10 | **kimi §6**: "no additively separable continuous certificate exists; any continuous `s` with `s⁻¹(ℝ₊)=ℝⁿ₊` is locally dictatorial at the argmin" | Counterexample: `s(b)=Σ_i min(0,b_i)` is continuous and nondecreasing in each argument, and `s(b)≥0 ⟺ b≥0` exactly (every term is `≤0`, so the sum vanishes iff all terms do). It is faithful and complete, yet it is neither strictly increasing, nor idempotent, nor dictatorial at the argmin | **FALSE AS STATED**, and instructively so: the uniqueness results in this family all turn on the calibration axiom (I) or on strict increase. If the article adopts U1a it must keep (I) in the statement, or a referee will produce `Σ min(0,·)`. `min` and `Σmin(0,·)` are also genuinely different diagnostics — the second aggregates *how deep* the deficits are, which the article can report as the deficit sum while continuing to certify with the margin |
| V11 | **opus U3a/3b**: sign law for `T=∫dA/φ(A)` vs `H=(A₀−A_min)/φ(A₀)`; and `T=(1/g)ln(1+gτ/(1−η))`, `T≥τ ⟺ η ≳ ½gτ` | 3a: `φ` nondecreasing in `A` ⇒ `1/φ(A) ≤ 1/φ(A₀)` on the interval ⇒ `T ≥ H` ✓ (worked: `φ=qA`, `A₀=100, A_min=1` ⇒ `T=4.605/q > H=0.99/q` ✓ conservative). 3b: `R(t)=R₀−(1−η)P₀(e^{gt}−1)/g=0` ⇒ the stated `T` ✓; `T≥τ ⟺ ln(1+x)≥x(1−η)` with `x=gτ/(1−η)`, first-order expansion `x−x²/2 ≥ x−xη` ⇒ `η ≳ x/2 ≈ gτ/2` ✓ | **BOTH CORRECT.** 3a is a strict sharpening of the article's Proposition 17 (symmetric bracket) into a one-sided sign verdict with a data test (regress `Ȧ` on `A`). 3b is the *rehabilitation* of the criticized object under a declared condition — and with the article's own phosphate figures (`g=0.03`, `τ≈309`) the threshold is `½gτ≈4.6`, i.e. R/P is optimistic unless reclassification elasticity exceeds ~4.6, which is a sentence the paper can print next to its Tilton and Illakwahhi citations at no empirical cost |
| V12 | **opus U2** predicate transport: conservation preserved by moiety-respecting coarse-graining, reflected only if the partition is moiety-saturated; `Σ violation ⇒ fine violation`, aggregate satisfaction implies nothing; composition conservative iff interface fluxes match with sign reversal | `Σx_i ≥ Σx_i^min` is strictly weaker than componentwise ✓; pushforward of left-null vectors ✓; pairwise sign-reversed interface flux is exactly the article's "every unit of mass is routed once" ✓ | **CORRECT**, and it restates the article's §7.3 rule 1 and §3.2 as one calculus. Its EOD corollary is verified separately (V13) |
| V13 | **opus U2's** Earth-Overshoot-Day arithmetic: `τ_agg = 365Σb/Σd = 365Σw_i r_i` with `w_i=d_i/Σd`, `r_i=b_i/d_i`; `τ_agg ≥ τ_min = 365 min_i r_i`; premium `=365Σw_i(r_i−r_min)` | `Σ (d_i/Σd)(b_i/d_i) = Σb_i/Σd` ✓; a weighted mean dominates its minimum with equality iff the positive-weight components coincide ✓; the difference identity is immediate ✓ | **CORRECT, one-signed, and computable from published component tables.** Sign reading: the aggregate day is later than the binding-component day ⇒ EOD is weakly optimistic relative to its own non-compensatory counterpart. This is the cheapest citable empirical object any of the nine audits (four earlier + five here) has produced |
| V14 | **kimi §9** SDE sandwich: `dA=−b dt+σdW`, `b∈[b_min,b_max]` ⇒ hitting-time law bracketed by two inverse-Gaussian laws, deterministic bracket as `σ→0` | Comparison-theorem argument ✓; the `σ→0` limit matches the article's Corollary 19 zero-noise statement ✓ | **CORRECT modulo hypotheses stated on the class.** Adoptable only as a statement about the *declared surrogate class* — the article's §9.2/§9.7 non-completion discipline must not be traded for it, and it need not be: the bracket is about `T`'s law, not the ledger's mass |
| V15 | **opus U6** hitting times are `+∞` for `ε>0` and finite for `ε≤0`, hence discontinuous and locally unbounded at the frontier | ✓ correct | **CORRECT but not a disqualification of Definition 5**, which the article defines with `inf ∅=+∞` precisely so the frontier case is reported rather than mis-ranked; and §6.4 already forbids promoting a number across robustness classes. The margin is additive (staged); its use as a *replacement* is the one case in this set where an audit proposes displacing a registered object |
| V16 | **fable T4** exergy: `ω^min=RT ln(c_use/c)` per mole; return admissible iff `c ≥ c_use exp(−ηΩ/(φRT))` | `φη⁻¹RT ln(c_use/c) ≤ Ω` rearranged ✓; `c<c_use` makes the log positive ✓ | **CORRECT algebra.** It supplies the missing independence witness for the fourth predicate (a mass-closing, closure-admitting ledger that fails admissibility), which is precisely what the earlier four audits and my own review flagged as under-proved |

---

## 2. Where the five audits are wrong *about this manuscript*

Verified against `revision/v4/paper3_v4.md`, not against the compressed starting point these audits were evidently written from.

| Audit charge | Status against v4 |
|---|---|
| "The noncompensation theorem … proves only a negative; it does not say what is admissible" (fable 0.1; opus U1 "purely prohibitive"; astra 4B "narrow the claim") | **Already answered.** §7.2 ends with the non-compensatory min-margin with the binding component named, and §1.1/§11 carry the same precision (applied last turn from bucket T1). The *constructive* half (U1a's uniqueness theorem) is genuinely new and is staged |
| "The fourth predicate is named in §1.1 and never formalized" (fable 0.2; kimi §7) | **Partly outdated.** v4 names it 4× and Proposition 2 states both the implication and the converse failure with a proof clause (restored last turn). What remains true: no exergy/affinity model, so the independence witness of V16 is still missing |
| "Diagnoses without repairs … the minimal declaration that would certify each number is not given" (fable 0.4) | **Half true.** §8.1 already registers the requirement (absolute storage or saturated thickness plus storage parameters) and says it is registered, not discharged; what v4 lacks is the ladder+table form (sol §A) and the certified interval (fable T5) |
| "The paper uses the left null space of `S_T` but not the right null space" (fable 0.3) | **TRUE and important.** `right null|closure cone|ker S_T` → **0 hits** in v4. The single best structural gap the nine audits found |
| "Conservation consistency should bound the residual `d_x`; MFA's statistical discrepancy voids the barrier certificate" (fable 5) | **TRUE and important.** v4 has 6 mentions of `d_x` and **no** bound on `∫ℓ^⊤Cd_x dt`, and 0 hits for "statistical discrepancy". An unbounded distributed term makes the conservation predicate vacuous — this is a defect in the article's own statement of Prop 1's content, not a new scope request |
| "Replace the depletion date with a margin" (opus U6) | Displacement **declined** (it subtracts a registered object); the margin is staged alongside Definition 5 (V15) |
| "Cut the inverse-Gaussian/GBM derivations, the five closed finite-donor propositions, the EOD/R/P/G3P prose, the generic separation counterexamples" (opus, *What to cut*) | **Declined in full.** These are precisely the apparatus whose deletion I spent two turns repairing: the first-passage section carries the seven non-claims that bound the surrogate reading; the finite-donor propositions include Theorems 11–15 whose scope clauses are the article's novelty; the separation counterexamples are the *content* of §3.7 ("every exhaustion statement must name its referent") and of Prop 2 (V4). Their replacement arguments are already in the text as remarks |
| "Revise 'recoverability is a property of the rate'" (astra 4C) | **Adoptable as completion, not softening** — and the article already half-does it: §8.1 (restored last turn) says over-extraction can make the loss permanent "in which case it is not recoverable at all". Staged: add the cross-reference so the §1.2 slogan is read with its own exception attached. The external literature I checked earlier agrees (Neubauer et al. 2019, hysteresis; a 2026 PLOS ONE study of 77 stocks found life-history traits poor recovery predictors) |

## 3. Cross-audit agreement (five audits) and coverage against the earlier four

| Proposal | How many of the five | In the earlier four? | State in v4 |
|---|---|---|---|
| Identifiability/non-identifiability as the article's next layer (witnesses, promotion rules, identification sets) | astra, fable (T7), sol, kimi | named as a *term* by A1/A2/A3 (bucket T3) | word present once; substance in §8.1/§9.4; no `Ω(D)` sets, no `T_±` |
| Refute-only predicate transport / one-directional inference | opus, sol (E), kimi (§5 partially) | A2 T4 (M4) | the static version is proved; the transport calculus is absent |
| LP/Farkas dual as the audit mechanism, with the dual as endogenous weights | astra (engine-free certificate fields), fable (T3 LP), opus (U7), sol (B), kimi (§3) | A3 T5-style | absent: v4 has "the polytope what is realizable" but no dual |
| A margin/deadline object (latest safe intervention / control margin / last responsible moment) | astra, opus, kimi | A1 §3 | absent |
| Liebig-min as the unique admissible aggregator, with a premium metric | fable, opus | A4 item 40 (M6) | the exception is stated; uniqueness is not |
| Right-null-space closure cone / closure deficit | fable (T3), opus (U5), sol (B), kimi (§8) | — | **absent (0 hits)** |
| Bound the conservation residual / statistical discrepancy | fable only | — | **absent** — a defect, see §2 |
| Minimum detectable violation as a required field | opus only | — | absent |
| Empirical purchase: one worked ledger, public data | astra, fable, opus, sol, kimi (5/5) | my E-list (no code, zero figures) | §8 has four classification records, not a worked ledger |
| Formal artefacts (Lean/SOS/JSON schema/checker) | astra, opus, sol, kimi | A1 §7, A3 | declared supplementary only |

## 4. Dispositions

**Not applied to the manuscript.** Every item in this file that is additive is either new mathematics (needs the author's sign-off and, for some, a written proof) or project infrastructure. `revision/v4/paper3_v4.md` therefore remains the manuscript of record; the five audits change it through **`revision/v5/proposed_inserts_v1.md`**, where eight items are written in the article's register at their placement anchors, each with its status label and the verification I ran. That keeps the standing rule intact — nothing enters the article without a decision, nothing is subtracted, no claim is re-framed.

**Staged for decision** (see the v5 file): S1 right-null-space closure cone; S2 the conservation-residual bound + discrepancy corollary; S3 critical-margin budget theorem (V2) with Theorem 14 as its instance; S4 Witness B as a dynamical non-identifiability proposition; S5 the anomaly-index `O_p(σ/β)` witness (V6, with my simulation numbers); S6 the `Σmin(0,·)` remark + U1a uniqueness with the calibration axiom (V9/V10); S7 the signed-bias sign law + R/P crossover with `½gτ≈4.6` printed (V11); S8 the EOD compensation premium identity (V13).

**Declined**: opus's cut list (§2); replacing Definition 5 with a margin; fable T9's `σ` symbol; "certified" as a status label (unchanged from the upgrade2 evaluation); astra 4A's premise that the article claims universal logical independence (it does not — Prop 1 is an implication and §1.1 says the layers are separated *and related*); the "one full application instead of three classifications" restructuring, which would subtract §8.2–8.4.

## 5. Citation and prior-art checks I can and cannot close here

- Real and appropriate to cite: **Fong, decorated cospans**; and for stock-and-flow composition specifically, *Compositional Modeling with Stock and Flow Diagrams* (Applied Category Theory 2022; arXiv:2205.08373), which builds a symmetric monoidal category of open stock-flow diagrams via decorated cospans [3](https://arxiv.org/pdf/2205.08373) [2](https://msp.cis.strath.ac.uk/act2022/papers/ACT2022_paper_8631.pdf). Opus's loose "Baez–Pollard is directly applicable" should be replaced by these.
- **Superseded by §7 (below)**: the MFA-composition paper opus gestures at does not exist under the title given (I checked, and the user's inability to find it was correct); Gale, Prajna–Jadbabaie, Müller–Kamke and the companion Zenodo record were all subsequently verified, and §7 carries the corrected strings. What remains genuinely unverified is listed at the end of §7.
- My earlier lineage notes stand and are now corroborated as *needed*: SEEA Central Framework (adopted 2012; revision adoption slated 2028) and the 2025 SNA's treatment of depletion as a cost of production — fable T11's "renewability relativity" is the natural sentence to hang that on, and it is the item that closes the SEEA gap I recorded as open twice before.

---

## 6. Second pass — the points the first pass had not reached

Four further blocks of the five audits carry claims of their own. Verified here so the evaluation is complete.

| Item | Claim | My check | Verdict and disposition |
|---|---|---|---|
| **opus U5** | closure deficit `\(\delta_m=1-\kappa_m(\tau_{\text{use}})\)`, with `\(\kappa_m(\tau)\)` the fraction of mobilised flux returned within lag `\(\tau\)`; "barrier reachability **iff** `\(\delta_m>0\)` sustained", horizon `\(\approx m/(\delta_m\times\text{mobilisation rate})\)` | With mobilisation `\(g\)` and return `\(\kappa g\)`, net drawdown is `\(\delta_m g\)`, so a **sustained** deficit gives `\(\mathcal H\approx m/(\delta_m g)\)` — that is the article's own `\(H^{\mathrm{loc}}\)` applied to the liquidation flux ✓. The "iff" fails both ways: a deficit that decays with the stock need never reach the barrier, and a positive `\(\delta\)` is not necessary when the binding constraint is an upper barrier | **Definition correct, proposition needs demotion to a one-sided bound** under a declared sustained-deficit assumption — stated that way it is a clean addition (staged as S2′). Its best consequence is verified and sharp: recycled-input-rate metrics, including the EU circularity rate, are `\(\kappa_m(\infty)\)` reported as if it were `\(\kappa_m(\tau_{\text{use}})\)` ✓ — a timescale omission, dimensionally exact and citable |
| **sol §2** | certificate vector `\(\mathrm{Cert}\in\{\top,\bot,?\}^{8}\)` with the third value `\(?\)`: "not established" ≠ "false" | Matches the article's status apparatus exactly (`registered`, `declared, not computed`, `not established`, the quarantine convention) and adds nothing mathematical | **Adoptable as a table**, consolidation only: the article currently states the eight obligations in prose across §2.1, §3.2–3.3, §5, §6.4. Staged as S11 |
| **sol §3C** | `\(\dot A=r-c\)`, `\(c\ge\alpha Y\)` ⇒ `\(\widehat{A(0)-A(T)}\ge \alpha\int_0^TY-\int_0^Tr\)`; "cumulative output certifies a minimum of support liquidation" | One line: `\(\widehat{A(0)-A(T)}=\int_0^T(c-r)\ge\alpha\int Y-\int r\)` ✓ correct, no `\(\lambda\)` machinery | **Correct and cheaper than S3** for the same rhetorical work; also yields the falsifiability hook (disagreement with observed stock change exposes a wrong coefficient, a missing flux, or measurement error). Staged as S9 |
| **sol §3D** | the exact noncompensation claim `\(n\ge2,\ w\ge0,\ w\ne0\Rightarrow\exists b: w^{\top}b\ge0,\ \min_i b_i<0\)`, plus **worst concealed deficit** `\(\delta_j^{*}(z)=\max\{-b_j: w^{\top}b=z,\ \ell\le b\le u\}\)` | The first is verbatim what §7.1 proves ✓; the second is an LP over the article's declared bounds, always finite on a bounded box, and it is the audit question the theorem should be asked ("given this score, how bad can the unseen deficit be?") | **Correct.** `\(\delta_j^{*}\)` pairs with opus's compensation premium as its two-sided companion: one measures what the aggregate bought with trades, the other what it can hide. Staged as S10 |
| **sol §3E + §4, kimi §5, opus U2** | composition: conservation certificates compose across ports with equal-and-opposite moiety fluxes; positivity composes under donor-limited interface kinetics; barrier safety needs an interface contract | All three directions check out; the first is the article's §7.3 rule 4 in transport language, the second is Theorem 11's Nagumo condition applied port-wise, the third is a restatement of §10.1's "changes the incidence and thereby leaves the interface" | **Convergent (3 of 5 audits) and correct.** It is the strongest candidate for a genuine §10.1 strengthening — but it needs a definition of ledger morphism, so it stays staged (S12, note only) |
| **sol §3A / astra 4A** | promotion-rule table: `\(\widehat{R/P}\)` becomes an event time only if `\(R\)` is a fixed physical inventory, `\(\dot R=-P\)`, `\(P\)` constant, no discovery or reclassification; `\(\Delta z/[-\dot z]_+\)` only if the observation trend persists and the observation threshold maps to a physical event; `\(\log(B/B_{\min})/F\)` only if `\(\dot B=-FB\)` with constant `\(F\)` | All three condition-sets are exactly what the article says in prose in §8.1, §8.2 and §8.3 (with the fisheries case already given as an exact hitting time under `\(\dot B=-FB\)`) | **Verified as consolidation, and it is the direct answer to fable's "diagnoses without repairs" charge** — the repairs exist, they are just not in one place. Highest-risk-free addition in the set: a table, no new mathematics |
| **astra 4A / sol §2 last line** | "describe the predicates as separately certified and generally non-equivalent, not blanket pairwise independent; safety in a barrier set contained in `\(\mathbb R^n_+\)` implies positivity over that horizon" | Correct, and it is already half-implied by Propositions 1–2 — but the article's §2.1 sentence "are four different predicates" could be read as claiming pairwise independence | **Worth one clause** (staged in S11's caption): it *protects* §2.1 from a reviewer's easy counterexample rather than weakening it |

Net effect of the two passes: counting the items in §1 and §6 together, **twenty-three** substantive proposals — the sixteen rows of §1 and the seven of §6 — are verified as correct-as-stated or correct with the demotions noted above, found **3** with errors (fable T10's sign slip, kimi §6's false non-existence, opus U5's "iff"), confirmed **4** charges that v4 already answers, and identified **1** proposal that would displace a registered object (opus U6's replacement of Definition 5).

## 7. Citation ledger (this turn's checks) — and a correction of mine

I recommended one citation last turn that does not exist. Correcting it here rather than leaving it in the record:

- **"Pollard, Gampbell, Cunniff, Erhard & Baez, *Compositional Resource Flow Accounting*"** — **not found**, by title search or otherwise; opus's phrase "the decorated-cospan setting of Baez–Pollard is directly applicable" is a loose conflation, and I should not have carried it forward as a to-be-checked real title. What actually exists and is apt: **Baez & Pollard, *A Compositional Framework for Reaction Networks*** (arXiv:1704.02051) and **Baez, Fong & Pollard, *A compositional framework for Markov processes***; for stock-and-flow specifically, **Baez, Li, Libkind, Osgood & Patterson, *Compositional Modeling with Stock and Flow Diagrams*, ACT 2022, EPTCS 380 (2023) 77–96**, doi:10.4204/EPTCS.380.5 (decorated cospans, open stock-flow diagrams, the `StockFlow` package) [5](https://arxiv.org/abs/2205.08373); also **Anand, Pollard, Breiner, Nolan & Subrahmanian, *Compositional models for power systems*** (EPTCS, 2019) [3](https://www.nist.gov/publications/compositional-models-power-systems).
- **Gale** — **verified exactly as fable cited it**: D. Gale, *A theorem on flows in networks*, Pacific Journal of Mathematics **7** (1957) 1073–1082, whose feasibility theorem is "the demands are feasible iff `\(d(S)\le c(S,\bar S)\)` for every subset `\(S\)`" [1](https://msp.org/pjm/1957/7-2/pjm-v7-n2-p04-p.pdf) [2](https://projecteuclid.org/journals/pacific-journal-of-mathematics/volume-7/issue-2/A-theorem-on-flows-in-networks/pjm/1103043501.pdf) — i.e. it is precisely the cut condition T2 needs, so T2's attribution is sound and no Hoffman citation is required; where the lower/upper-bound circulation form is wanted, cite **Ahuja, Magnanti & Orlin, *Network Flows* (Prentice-Hall, 1993)** [4](https://link.springer.com/article/10.1007/BF01580084).
- **Prajna–Jadbabaie** — **verified**: *Safety Verification of Hybrid Systems Using Barrier Certificates*, HSCC 2004, 477–492; and with Pappas, *A framework for worst-case and stochastic safety verification using barrier certificates*, IEEE TAC **52**(8) (2007) 1415–1428 [6](https://dblp.org/pid/28/2145.html). The 2007 paper is the one to cite for opus U7b, since its stochastic case is what the article's §9 surrogates need, and the SOS/Positivstellensatz formulation the audit describes is in the HSCC paper [7](https://web.mit.edu/~jadbabai/www/papers/hscc04_2.pdf).
- **Müller–Kamke** — **verified as a named result**: the comparison theorem for quasimonotone (cooperative) systems, "the early works of Müller (1926) and Kamke (1932)"; cite **H. L. Smith, *Monotone Dynamical Systems*, AMS Surveys 41, 1995**, whose Kamke condition implies monotonicity of the forward flow [8](https://www.ams.org/books/surv/041/surv041-endmatter.pdf). fable T5's "attained under monotone dynamics" is properly supported.
- **The three companion Zenodo records in the manuscript's own reference list — verified live.** `doi:10.5281/zenodo.22554217` resolves to *Delay-Induced Regime Change in Harvested Stocks*, **Abaee, Amin** (ORCID 0000-0002-0019-1842), posted 2026-09-06, v1, with `paper4_delay_dynamics_v30.pdf` and a supplementary file [9](https://zenodo.org/records/22554217). Its abstract supplies two figures the article's §10 cites: Hopf crossings interval-certified "near **3.7 and 150 yr**" ✓ (the τ₊≈150 yr object) and, under annual mobilising review, restabilisation "only for review intervals above about **6.5 yr**". That last number also disposes of `qwen p3 upgrade2.txt`'s invented "review interval longer than 3 years": it is not merely unsourced, it is contradicted by the registered companion at the same interface.
  Consequence for §10.1's citation format: the "Author, D., et al., *in review*" placeholder can be replaced by this DOI'd record.
- **Still not verified (left open deliberately):** `10.5281/zenodo.22554297` and `10.5281/zenodo.22545740` (the other two companions, same numbering family, same author, so likely fine); the SEEA revision timetable; the Jasechko/Neubauer/PLOS figures carried from earlier turns. Each is a one-lookup task at submission.

# Deep mine of the two long memos — remaining points, ranked

Sources read beyond the parts already dispositioned: `uploads/p3 profound upgrades.txt` lines 585–1499
(novelty-thin list, aggregation triad T1–T11, Upgrades 1–9, layered architecture A–D, theorem families
A–E, Parts I–III) and `uploads/ledger upgrade.txt` lines 411–1342 (§6–§9, the *v2 memo* items A–N,
theorem set v2, pilots, priorities, anticipated attacks, and the final "Proof-Carrying Sustainability
Accounting" T1–T4). Status column is a substance grep of the phrase-family against
`revision/v7/paper3_material_ledgers_v34.md` and the clone's v32 md (`0/0` = in neither).

## Tier 1 — small, self-contained, and unlocked by the v34 kernel

| point | where | now | what it becomes |
|---|---|---|---|
| Compensation admissible **exactly along certified substitution morphisms**; weak/strong as a property of the substitution hypergraph | ledger 814 | `0/0` | A corollary of Def 34 + Def 21: an exchange is compensating iff its interface price π_φ ≤ 0 and the identification is admissible; a moiety with no admissible morphism is "critical" — the article's central distinction becomes a definition inside the calculus, not a stance |
| **Yield inflation as a latent-variable identification problem**; output records provably uninformative | profound 820 (Upgrade 4) | `0/0` (only the one-line mention at Def 21) | Proposition: with the deficit unobserved, the fibre of output records contains realizations with arbitrarily different liquidation shares, so no function of the record identifies it — Def 38/Prop 39 machinery applied to the §5.4 deficit |
| **Non-displacement / additionality gate** for improvement claims (declared counterfactual, matched boundaries, leakage and rebound) | ledger 448–500 | `0/0` | Proposition on the composition: an intervention at an interface is non-displacing iff the composition's closure deficit does not fall, Λ*(whole) ≥ Λ*(parts' min); an "avoided depletion" claim is then a certificate about a declared counterfactual ledger, and a larger recycling flux alone proves nothing |
| **Supportable-output envelope** replacing attribution percentages | ledger 448 | `0/0` | Definition: max Σy s.t. declared barriers, floors and the joint capacity boxes are all satisfied — the dual of the Def 21 programme, one sentence to state, auditable |
| **Corridor-emptiness witness** naming which floors conflict with which barriers | ledger 844 | partial (Prop 30's infeasibility reading) | The Farkas dual of the infeasible feasibility LP names the conflicting pair; pairs with the "infeasibility ≠ evidence of safety" rule already in the text |
| **Governability ratio** G_m = T_K,m/(τ + h), with G < 1 forcing automatic triggers | ledger 826–830 | `0/0` (my §9 sentence gives the admissible interval, not the ratio) | One definition + a one-line proposition, using the companion's verified 6.5 yr admissible review interval as h; it is the decision object the audits keep asking for and it costs nothing to state honestly (T_K,m declared, not computed) |
| **Baseline-covariance lemma** for record-relative indices | profound 1019–1027 | `1/1` only in passing | Proposition: shifting the reference period by δ moves the anomaly-persistence index by a computable amount and leaves the *sign* comparison untouched; strengthens Prop 32's "cannot be extended into a horizon" |
| **Typed noise**: process noise on a conserved moiety must be transfer noise (zero-sum across pools or an explicit boundary term); a diffusion that creates mass is a type error | ledger 834 | `0/0` | One proposition in §7.1 next to the surrogate declaration — and it is the stochastic counterpart of my Def 23 residual budget |

## Tier 2 — worth doing, each needs one real proof

| point | where | now | note |
|---|---|---|---|
| **Latest safe intervention time** as the central decision object; trajectory safety ≠ transition feasibility; the exact benchmark linking margin m₀, drawdown d₀, delay and deployment | profound 293–418 | `0/0` | The memos' most-cited idea. Implementable as Def + Prop in §6.4/§6.6: τ_last = sup{t : a declared response started at t reaches the barrier at m ≥ 0 given the deployment lag}, with the hitting-time discontinuity (profound 864) as its motivation. Do NOT attach the numerical illustration (line 387) unless re-derived — that is the family of staged numbers I have twice had to withdraw |
| **Control margin μ(x₀) = sup{ρ : x₀ ∈ K under ρ-perturbed controls/demand}** | profound 864–877 | `0/0` | The stated properties (dimensionless, min-type, monotone, Lipschitz) are 4 claims; only the first three are safe to assert without work, so take the first two and label the rest conditional |
| **Manipulation resistance as an invariance theorem** over the attack surface (unit substitution, boundary redrawing, reclassification, aggregate offsetting, timescale swapping) | profound 909–917 | `0/0` | Each reclassification/offsetting case already has a theorem in the article (Prop 28, Prop 29); the paper needs only the umbrella proposition "the report set is invariant to X at declared elasticity" — and the honest scope note that boundary redrawing is handled by typing, not by luck |
| **Make the fourth predicate real**: assign species their Gibbs energies, declare a flux admissible iff every conversion has non-positive affinity at declared conditions | profound 1452 | `exergy 3/3, Gibbs 0/0, affinity 0/0` | The article *names* it and never formalizes it — the audit that flagged this is right. A Definition + "positivity is not implied by admissibility" example is enough; the exergy *instance* is still the missing piece |
| **Representation form of the aggregation result**: f certifies box safety at level c iff {f ≥ c} ⊆ B + ℝ₊ᴹ; no half-space lies in a shifted orthant, so no weighted sum certifies for M ≥ 2 | ledger 810–816 | `superlevel 0/0, half-space 0/0` | Equivalent to my Prop 29 but geometrically cleaner and one line shorter; worth replacing the proof, not the claim, if the author agrees |
| **Circulation time τ_m^circ and use rate u_m as the units of the closure reading** | profound 1456 | `0/0` | Complements my Def 21/22 with the network-side quantity (mean recurrence under the restricted rate matrix); needs a hypothesis (irreducibility on the recurrent class) |
| **Extend the uniform-drift bracket into the stochastic section** via SDE comparison: hitting-time law sandwiched between the bracketed drifts | profound 1460 | `sandwich 1/1` (elsewhere) | Genuine relative of Prop 26; a short proposition with the comparison theorem I already cite (Smith 1995 for the ODE; the SDE version needs a stated condition) |
| **Identifiability status of every parameter behind a claim** (structurally / practically / jointly identified / unidentified) | ledger 830–834 | `0/0` | A column of the certification state, not new mathematics: extend my §3.1 vector with a per-parameter status field and the rule "non-identified rates cannot enter a registered predicate" |

## Tier 3 — deliberately not for the article (project, companion, or author's call)

Proof-producing compiler / certificate dependency management and expiry (ledger 505–589, 836–845:
engineering, and the article's Prop 30 + supp S8 already carry the *mathematical* part); semialgebraic
safe sets and the box-vs-coupled trade-off (one sentence of scope at most); barrier passport with
normative basis labelling (author's register design); MDV/falsifiability-metrology layer (profound 899)
as a *supplementary* conformance table; the three pilots, retro-audit corpus of 100–200 published
depletion numbers, adversarial conformance benchmark, decision-performance novelty claims (profound
548 "use one full application, not three shallow demonstrations"; ledger 628–697); the "human payoff"
sentence per section and the intro cuts (style, author); T11 renewability relativity as SEEA framing —
my Def 22's second reading already says it, so only a citation-forward remains; T10 barrier
compatibility for a conserved moiety (fable's corrected invariant) — `compatib 6/5` shows partial
presence; needs the author's version of the invariant before it is safe to state.

Also still open from the earlier passes, unchanged: A12's re-scope (the universal-failure instance does
not use the ledger), the stale 5× citation to "Theorem 15" with no label-15 head, and my own two
notation defects (Λ* vs λ, and the invented readout matrix C in Definition 23) — the v35 patch carrying
those fixes plus the Remark 33 premium figures computed from `Country_Trends.csv` remains prepared and
unshipped.

## If only three get done

1. The **substitution-morphism corollary** (Tier 1 row 1) — highest conceptual payoff per line, and it
   is the only item that makes the paper's title-pair (weak/strong) a theorem of the ledger rather than
   a reading of it.
2. **Latest safe intervention time**, statement only, no numerics — the decision object every memo asked for.
3. **Fourth predicate formalized** (affinity sign condition) — closes the one place where the article
   names a certification layer and never defines it.

## Status after v35 (same day) — what shipped, what is still open

Shipped into `revision/v7/paper3_material_ledgers_v35.{md,tex,pdf}` (22 logged edits, gate
`verify_v35_build.py` -> ALL CHECKS PASS):

- **Tier 1, corridor-emptiness Farkas witness naming the conflicting floors** -> Proposition 40 (+ its
  Gale/Auja proof line, and the "withdraw a bound, do not re-tune a controller" reading).
- **Tier 1, governability ratio G_m = T_{K,m}/(tau + h)** -> Definition 44, with G > 1 / G <= 1 read as a
  comparison of clocks, the automatic-trigger consequence stated, and the 3.7 yr / 6.5 yr figures attributed
  to the companion's certified thresholds rather than asserted for the class.
- **Tier 1, baseline-covariance lemma** -> Lemma 4.
- **Tier 1, typed-noise rule** -> §7.1: process noise on a conserved moiety must be zero-sum across pools or
  carry a boundary term, else it is a residual of Definition 23; plus the converse discipline that a drift
  bracket is not a support or exit-time bracket.
- **Tier 2, control margin mu(x0) = sup{rho : x0 in K_rho}** -> Definition 41, asserting only dimensionless-
  ness and the min-type reading.
- **Tier 2, fourth admissibility predicate (Gibbs energies, non-positive affinity)** -> Definition 42, with
  "energies not declared => predicate reported as not established".
- **Tier 2, circulation time tau^circ** -> Definition 43, including the per-cycle vector caveat and the
  yield-inflation remark.

Still open, in the order that avoids re-work: non-displacement/additionality gate (needs the Def 22 deficit
functional quoted); supportable-output envelope (needs a numeric anchor from the author); LSIT statement
(needs the acronym pinned from the memo, `uploads/p3 profound upgrades.txt` ~387); mechanism-design umbrella
proposition (do with the D4 wording item); per-parameter identifiability status field and MDV (one
supplementary-table batch, not two article edits); superlevel-set representation of Prop 29 (supplementary,
proof-only). Tier 3 project items (compiler, certificate expiry, semialgebraic safe sets, passport design,
pilots, retro-audit, dashboards) remain out of the article by the earlier triage.

## Status after v36 (same day) — the open.txt pass

`uploads/open.txt` evaluated item by item in **`review/open_items_v4_closed.md`**: its "no literal LSIT"
observation is true repo-wide, but its proposed fix (`LSIT` = `LS_T` = left null space of `S_T`) is refuted —
`LS_T=0` at `uploads/p3 profound upgrades.txt:430` is the equation `L S_T = 0` in a proof-obligations display,
while LSIT is the memo §3 heading at line 293 ("latest safe intervention time"), whose illustration is line 387.
Its quoted numerics are genuine and were re-derived by a grid LP before shipping. Its mechanism-design
"umbrella theorem" and its `established | not established | refuted` vocabulary were both declined, with
reasons recorded.

Now shipped in v36 (8 logged edits, gate ALL CHECKS PASS, 52-page PDF): **Definition 45** (latest safe
intervention time as an infimum-over-responses supremum), **Proposition 41** (closed form
`m_needed = d_0 τ + d_0²/(2ρ)`, exact feasibility equivalence,
`t_last = m_0/d_0 − τ − d_0/(2ρ) = H^loc − τ − d_0/(2ρ)`, the two instances, the negative-deadline case),
**Definition 46** (supportable-output envelope, three readings, frozen-rate corner = Definition 22's horizon),
**Remark 35** (non-displacement gate as a comparison of two declared deficits), the per-parameter
identifiability status field in §3.1 next to `Cert`, Remark 33's carbon convention now **cited** to the NFA
Guidebook §9.1.2 with the 209-day/213-day vintage reconciliation, and both companion pointers rewritten to
name eq. (1) and Section 2.4 of the deposited PDF (verified against that PDF).

Still open after this pass, unchanged in kind: D4 wording (author's), the MDV + status *table* in the
supplementary, and uploading `revision/v5/code/`. Closed as no-ops so they are not re-proposed: the
superlevel-set "alternate proof" of Proposition 29, and any new definition of the left-null space.

## v37 (same day): the last two "author's items" were not author's items

D4 was already-written text in §1.5 whose facts I had not pinned; MDV kept bouncing because the article used
"typing" as a load-bearing predicate in five places without ever defining it. Both closed by work, not by
drafting: `review/open_items_v5_d4_mdv.md` records the standards verification (2025 SNA depletion-as-cost-of-
production, NDP less depreciation *and* depletion, the 2008 other-changes-in-volume contrast, the SEEA CF
lineage, and depletion defined as extraction in excess of growth) and the new apparatus shipped in v37 —
Definition 47 (type structure), Proposition 42 (no conservation law crosses a type class), Remark 36
(boundaries / valuation / reclassification separated), plus four forward pointers from the passages that were
using the word, and the G3P exhibit's baseline period and June-2005 defect notice. Gate ALL CHECKS PASS,
10 reversible edits, 61 labels, PDF 54 pp. `revision/v7/supplementary_v9_candidate.md` does the deferred
supplementary merge and carries the per-parameter identifiability table (S11) and the MDV correspondence
table (S12); exactly one `[author]` cell remains in it (the archived RAM Legacy pull date), plus a yes/no on
keeping the §1.5 paragraph.

## v38 (next round): funding, the §1.5 causal link, a false proof step, and the review file closed

`uploads/p3 prompt response.txt` (16 markers, 8 issues) adjudicated in `review/points_v5_prompt_response_closed.md`:
most were already shipped in v35–v37 and were verified present rather than assumed. Four live gaps are fixed in
**v38** (9 logged edits, gate ALL CHECKS PASS, PDF 55 pp): the missing **Funding** declaration ("None declared.",
house form); §1.5 restored to support the back-reference Remark 36 makes (its asset-boundary clause was dropped
in v37) plus an explicit one-way, non-inferential direction statement; **Definition 34's rows-vs-quotient
ambiguity** resolved by giving both constructions with the class-wide type/unit check; **Definition 40's
three-way verdict** with its infeasibility witness, not-established branch and no-dynamic-safety limit; and
**Remark 34** now owns the certificate-relativity of the interface price and the multiplier-set reading of a
capacity price (verified: V(k)=min(2,k), one-sided slopes 1.000/0.000 at the kink, optimal multiplier set
[0,1]). A genuine mathematical error surfaced while checking item 5/6 and is corrected: the fibre proof claimed
extrema over a polytope sit at vertices, contradicted by its own instance (max 3.912023 at the interior point
y=50). Both author cells are closed without the author: the RAM Legacy extract is release v4.66 (Zenodo
14043031, 2024-11-06 — the supplementary's label was right; my earlier non-confirmation is withdrawn) with its
vintage verified from the extract's own published-F anchors, so "the pull date is archived in the analysis
repository" became a checkable citation in §6.5.2 and Data availability.

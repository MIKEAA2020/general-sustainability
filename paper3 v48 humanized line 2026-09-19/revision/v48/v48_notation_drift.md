# The maths in the 306 reused sentences, held against the deposit

Every inline `$...$` span of any length in a sentence marked for reuse (221 spans over 306
sentences) was normalised and looked for in the deposited article, at four levels of generosity. 126
occur as written, 20 once meaningless grouping braces and the end-of-proof glyph are identified,
19 once the font wrappers are taken off, 25 once sub- and superscript positions are
identified - the humanizer re-typeset the notation without changing the claim, and a document cannot carry two
spellings of one object. 31 occur in no form, and those are listed below one at a time for reading.


| span status | count |
|---|---|
| written as the deposited article writes it | 126 |
| differs only in grouping braces or the QED glyph | 20 |
| font wrapper differs (`\mathbf` to `\mathbb`, `\mathsf` added) | 19 |
| label moved between sub- and superscript | 25 |
| no matching span in the deposited article | 31 |

## Where the notation differs

### font wrapper - 19 spans over 13 sentences

| the draft writes | the deposit writes | in how many reused sentences |
|---|---|---|
| `\mathsfR_{\mathrm{ext}}` | `\mathcal{R}_{\mathrm{ext}}` | 2 |
| `\frac{d}{dt}\mathbb1^{\top}z=0` | `\frac{d}{dt}\mathbf{1}^\topz=0` | 1 |
| `\dot{\mathcalB}` | `\dotB` | 1 |
| `\gamma_UU=\mathcalT^*\approx4.47` | `\gamma_UU=T^*\approx4.47` | 1 |
| `\kappa_AK=\mathbf{5.000}` | `\kappa_AK=5.000` | 1 |
| `\mathsfR_K` | `\mathcal{R}_K` | 1 |
| `\mathsfR_{\mathrm{frozen}}` | `\mathcal{R}_{\mathrm{frozen}}` | 1 |
| `\mathsfR_0=\mathsfR_{\mathrm{ext}}\cup\mathsfR_K` | `\mathcal{R}_0=\mathcal{R}_{\mathrm{ext}}\cup\mathcal{R}_` | 1 |
| `\ell=\mathbb1` | `\ell=\mathbf{1}` | 1 |
| `\mathcalB^*\approx4.652` | `B^*\approx4.652` | 1 |
| `G_0/\mathcalB^*\approx8.6\times10^{4}` | `G_0/B^*\approx8.6\times10^4` | 1 |
| `m<\nu=\mathcalH^{\mathrm{win}}_{\mathrm{GW}}` | `m<\nu=H^{\mathrm{win}}_{\mathrm{GW}},\qquadF_T(\nu)=\fra` | 1 |
| `I_{\mathsfN}` | `I_N` | 1 |
| `Q_{\mathsfP}` | `\dotS=g(S,N)-H,\qquad\dotK=\theta_KH-\theta_\deltaK,\qqu` | 1 |
| `(\mathsf{S},\mathsf{K},\mathsf{N},\mathsf{P})\in\mathbb{` | `(S,K,N,P)\in\mathbb{R}^4_+` | 1 |
| `\mathbbE[\mathcalT_{\mathrm{fish}}]=\log(\mathcalB_0/\ma` | `\mathbb{E}[T_{\mathrm{fish}}]=\log(B_0/B_{\min})/(h+\var` | 1 |
| `\mathbbE[T^{\sharp}]=(A_0-\mathcalA^{\sharp})/|\mu|` | `\mathbb{E}[T^\sharp]=(A_0-A^\sharp)/|\mu|` | 1 |
| `\mathcalB^*-R^*=\mathcalT^*` | `B^*-R^*=T^*` | 1 |

### script position - 25 spans over 18 sentences

| the draft writes | the deposit writes | in how many reused sentences |
|---|---|---|
| `A_{\mathrm{act}}` | `A^{\mathrm{act}}/(A^{\mathrm{act}}+A_0)` | 3 |
| `A_{\mathrm{act},*}=397.87` | `A^{\mathrm{act,*}}=397.87` | 2 |
| `N^*>0` | `N_*>0` | 1 |
| `e_{GA}-e_{AG}=\omega_A(A_{\mathrm{eq,intrinsic}}-A_{\mat` | `e_{GA}-e_{AG}=\omega_A(A^{\mathrm{eq,intrinsic}}-A^{\mat` | 1 |
| `N+A_{\mathrm{act}}+A_{\mathrm{geo}}+U+P+W+I` | `N+A^{\mathrm{act}}+A^{\mathrm{geo}}+U+P+W+I` | 1 |
| `A_{\mathrm{eq,intrinsic}}` | `e_{GA}=\omega_AA^{\mathrm{eq,intrinsic}}\sigma,\qquade_{` | 1 |
| `A_{\mathrm{eq},W}=A_{\mathrm{eq,intrinsic}}+\kappa_AK/\o` | `A^{\mathrm{eq,W}}=A^{\mathrm{eq,intrinsic}}+\kappa_AK/\o` | 1 |
| `\dotM=-(1-\alpha)qEN-C_{A,\mathrm{lim}}` | `\dotM=-(1-\alpha)qEN-C^{A,\mathrm{lim}}` | 1 |
| `E\equivE^*>0` | `E\equivE_*>0` | 1 |
| `C_A=0` | `C^A=0` | 1 |
| `A_{\mathrm{eq,intrinsic}}=50` | `A^{\mathrm{eq,intrinsic}}=50` | 1 |
| `(N,A_{\mathrm{act}},U)` | `(N,A^{\mathrm{act}},U)` | 1 |
| `x_L=(N,A_{\mathrm{act}},A_{\mathrm{geo}},U)` | `x_L=(N,A^{\mathrm{act}},A^{\mathrm{geo}},U),\qquads=\fra` | 1 |
| `A_{\mathrm{geo}}` | `A^{\mathrm{geo}}/(A^{\mathrm{geo}}+A_{g0})` | 1 |
| `L^{\top}S=0` | `\mathsf{L}^\top\mathsf{S}=0` | 1 |
| `\omega_A(A_{\mathrm{eq},W}-A_{\mathrm{act},*})=4.652` | `\omega_A(A^{\mathrm{eq,W}}-A^{\mathrm{act,*}})=4.652` | 1 |
| `qEN-R(N,A_{\mathrm{act}})=O(N)` | `qEN-R(N,A^{\mathrm{act}})=O(N)` | 1 |
| `A_{\mathrm{act}}\toU` | `A^{\mathrm{act}}\toU` | 1 |

## Spans with no counterpart in the deposited article

### D0399 · ratio 0.944 · draft §4.5

```
(N^*,A_{\mathrm{act}}{}^*)=(89.526,\,397.87)
```

> In particular the working point $(N^*,A_{\mathrm{act}}{}^*)=(89.526,\,397.87)$ **is not** a rest point at $E=E^*\approx2.090$: $\dot N=0$ holds there by construction ($R^*=qE^*N^*\approx0.187>0$), and the rest condition of the proof fails on the abiotic pair.*

### D0572 · ratio 0.933 · draft §front

```
\mu=\hat\mu<0
```

> **Definition 6 (Observed-drift Brownian surrogate).** *Let $A_0$ (local to §7.2–7.4; **not** the half-saturation constant of §2.2) be the latest observed anomaly and $\mu=\hat\mu<0$ the fitted drawdown rate.

### D0265 · ratio 0.928 · draft §2.4

```
\{\mathsf S\ge \mathsf S_{\min},\,0\le K\le K_{\max}\}
```

> In both cases the viability kernel (Aubin, 1991) of the constraint set $\{\mathsf S\ge \mathsf S_{\min},\,0\le K\le K_{\max}\}$ — the states from which *some* admissible harvest keeps both constraints forever — **is empty**.

### D0331 · ratio 0.925 · draft §front

```
\ell^{\top}S^{\top}=0
```

> **Proposition 4 (Conservation-law reduction).** *If $\ell^{\top}S^{\top}=0$ for some vector $\ell\in\mathbb R^n$, then $\ell^{\top}x(t)=\ell^{\top}x(0)+\int_0^t\ell^{\top}b(\tau)d\tau$; in a closed system ($b=0$), $\ell^{\top}x$ is invariant.*

### D0611 · ratio 0.924 · draft §7.7

```
\mathcal H^{\mathrm{gross}}_A
```

> The gross active-pool horizon $\mathcal H^{\mathrm{gross}}_A$ of Definition 3 and its productivity-illusion interpretation — the misreading of a large gross-turnover horizon as evidence of slow net depletion, the false implication recorded in §6.1 — are not first-passage results treated here. 7.

### D0453 · ratio 0.8 · draft §front

```
\hat M
```

> with $C$ the operative extraction-law readout and $\hat M$ the declared demand-coverage matrix mapping the moiety readout $S=Cx$ — the composition matrix $C$ of Lemma 3, **a different object from the coverage vector $C(t)$ despite the shared letter** — into the units of that coverage vector: rows indexed by covered services, columns by moieties, entries the declared stoichiometric coefficients of 

### D0469 · ratio 0.79 · draft §6.1

```
J^{\mathrm{gross}}_A=g(X,A)/A
```

> **Definition 3 (Gross turnover intensity and support coverage).** *With assimilation $g(X,A)>0$, the gross turnover intensity is $J^{\mathrm{gross}}_A=g(X,A)/A$ and the gross support-coverage ratio is $H^{\mathrm{gross}}_A=(A-A_{\min})/g(X,A)$. **Neither is a time to depletion.***

### D0469 · ratio 0.79 · draft §6.1

```
H^{\mathrm{gross}}_A=(A-A_{\min})/g(X,A)
```

> **Definition 3 (Gross turnover intensity and support coverage).** *With assimilation $g(X,A)>0$, the gross turnover intensity is $J^{\mathrm{gross}}_A=g(X,A)/A$ and the gross support-coverage ratio is $H^{\mathrm{gross}}_A=(A-A_{\min})/g(X,A)$. **Neither is a time to depletion.***

### D0650 · ratio 0.76 · draft §9

```
A_{\mathrm{eq},W}=50+\kappa_AK/\omega_A=5{,}050
```

> The three registered numbers display the separation: $A_{\mathrm{eq,intrinsic}}=50$, the working active pool $A_{\mathrm{act},*}=397.87$, and $A_{\mathrm{eq},W}=50+\kappa_AK/\omega_A=5{,}050$ — the two equilibria differ by a factor of eight and the two targets by two orders of magnitude. 2. **Same state, different fields.** At the working equilibrium the two $A_{\mathrm{act}}$ vector fields, writt

### D0693 · ratio 0.724 · draft §10.2

```
S^{\top}
```

> Adding unlike units — biomass, money, biodiversity indices, exergy — into one conserved scalar is not authorized by any conservation theorem. 2. **Explicit stoichiometry.** Entries are added within an incidence row only when their types and units agree; every conversion is an explicit coefficient in $S^{\top}$, never an implicit sum (§2.1). 3. **Yield routing.** A transformation represented with y

### D0354 · ratio 0.719 · draft §3.6

```
S(t)>\underline B
```

> **Proposition 6 (Finite exhaustion under uniform negative drift).** *Assume (H1) $S$ is absolutely continuous with $\dot S(t)\le-\varepsilon<0$ whenever $S(t)>\underline B$, where $\underline B$ is a constant lower barrier and $\varepsilon>0$ a uniform drift bound; (H2) $S(0)>\underline B$.

### D0354 · ratio 0.719 · draft §3.6

```
S(0)>\underline B
```

> **Proposition 6 (Finite exhaustion under uniform negative drift).** *Assume (H1) $S$ is absolutely continuous with $\dot S(t)\le-\varepsilon<0$ whenever $S(t)>\underline B$, where $\underline B$ is a constant lower barrier and $\varepsilon>0$ a uniform drift bound; (H2) $S(0)>\underline B$.

### D0201 · ratio 0.715 · draft §front

```
S^{\top}
```

> A physical disturbance on represented material is a different object from a structural discrepancy term. 2. $S^{\top}$ may contain signed entries even though $v\ge 0$.

### D0310 · ratio 0.707 · draft §3.1

```
\ell^{\top}S^{\top}=0
```

> **Proposition 1 (Layer 2 ⇒ Layer 1, for the conserved quantities).** *If $\ell^{\top}S^{\top}=0$, then $\tfrac{d}{dt}(\ell^{\top}x)=\ell^{\top}b$, and in a closed system ($b=0$) $\ell^{\top}x$ is invariant.*

### D0355 · ratio 0.702 · draft §3.6

```
\tau_{\underline B}=\inf\{t\ge0:S(t)\le\underline B\}\le\dfrac{S(0)-\underline B}{\varepsilon}
```

> Then the first hitting time satisfies $\tau_{\underline B}=\inf\{t\ge0:S(t)\le\underline B\}\le\dfrac{S(0)-\underline B}{\varepsilon}$.*

### D0565 · ratio 0.69 · draft §6.5.4

```
\mathcal H^{\mathrm{loc}}_{\mathcal B}=(\mathcal B-\mathcal B_{\mathrm{lim}})/[-\dot{\mathcal B}]_+
```

> A genuinely local biomass-decline ratio $\mathcal H^{\mathrm{loc}}_{\mathcal B}=(\mathcal B-\mathcal B_{\mathrm{lim}})/[-\dot{\mathcal B}]_+$ would require a compatible net $\dot{\mathcal B}$ estimate, and a demographic hitting time would require a fully specified population model; RAM Legacy SSB and $F$ data (Ricard et al., 2012) do not by themselves supply these quantities or models.

### D0408 · ratio 0.685 · draft §front

```
\int_0^\infty(qE(s)N(s)+C_{A,\mathrm{lim}}(s))\,ds\le M(0)
```

> *in particular $qEN\in L^1(0,\infty)$, and **no trajectory maintains extraction at the working value $qE^*N^*\approx0.187$ for all time**; with mining restored, $\int_0^\infty(qE(s)N(s)+C_{A,\mathrm{lim}}(s))\,ds\le M(0)$.*

### D0561 · ratio 0.684 · draft §6.5.4

```
\mathcal B_{\mathrm{lim}}=0.2\max\mathrm{SSB}
```

> With $\mathcal B_{\mathrm{lim}}=0.2\max\mathrm{SSB}$ this is the construction tabled as ADH in §6.5.2; the two notations are kept because the boundary hypotheses stated here ($F_{\mathrm{now}}>0$, $\mathrm{SSB}_{\mathrm{now}}>\mathcal B_{\mathrm{lim}}$) are exactly the conditions of the positive sub-cohort of §6.5.2 (35 stocks, median 2.9 yr).

### D0561 · ratio 0.684 · draft §6.5.4

```
\mathrm{SSB}_{\mathrm{now}}>\mathcal B_{\mathrm{lim}}
```

> With $\mathcal B_{\mathrm{lim}}=0.2\max\mathrm{SSB}$ this is the construction tabled as ADH in §6.5.2; the two notations are kept because the boundary hypotheses stated here ($F_{\mathrm{now}}>0$, $\mathrm{SSB}_{\mathrm{now}}>\mathcal B_{\mathrm{lim}}$) are exactly the conditions of the positive sub-cohort of §6.5.2 (35 stocks, median 2.9 yr).

### D0682 · ratio 0.66 · draft §10.1

```
w\in\mathbb R^m_+
```

> For every weight vector $w\in\mathbb R^m_+$, $w\ne0$, there exist a typed ledger as in §2, a demand vector $d$, and an admissible state–operation pair $(x,u)$ with $u\in\mathcal U(x,t)$ whose attainable balance $b=O(x,u,\theta)-d$ satisfies $w^{\top}b\ge0$ while $b_m<0$ for some component $m$.

### D0682 · ratio 0.66 · draft §10.1

```
b=O(x,u,\theta)-d
```

> For every weight vector $w\in\mathbb R^m_+$, $w\ne0$, there exist a typed ledger as in §2, a demand vector $d$, and an admissible state–operation pair $(x,u)$ with $u\in\mathcal U(x,t)$ whose attainable balance $b=O(x,u,\theta)-d$ satisfies $w^{\top}b\ge0$ while $b_m<0$ for some component $m$.

### D0415 · ratio 0.652 · draft §front

```
\dot\chi=S\eta+b
```

> Assume (H1) $\chi$ is absolutely continuous between locally finite event times, with left and right limits at events; (H2) $\dot\chi=S\eta+b$ with $\eta\ge0$, separate reverse columns, and donor-limited negative boundary flows; (H3) $L^{\top}S=0$.

### D0486 · ratio 0.523 · draft §6.2

```
\mathcal H^{\mathrm{loc}}_A=(A(0)-A_{\min})/[-\dot A]_+\in[H_0/(1+\varepsilon),H_0/(1-\varepsilon)]
```

> **When the clocks coincide.** Under the rate bracket the frozen-rate ratio and the true hitting time agree within the bracket: $-\dot A\in[(1-\varepsilon)v_0,(1+\varepsilon)v_0]$ gives $\mathcal H^{\mathrm{loc}}_A=(A(0)-A_{\min})/[-\dot A]_+\in[H_0/(1+\varepsilon),H_0/(1-\varepsilon)]$, hence

### D0574 · ratio 0.502 · draft §front

```
\mathcal A^{\mathrm{win}}_{\min}
```

> *where $W$ is a standard Wiener process, $\varsigma>0$ a chosen noise scale, and the process is stopped at first reaching the record-relative barrier $\mathcal A^{\mathrm{win}}_{\min}$.

### D0494 · ratio 0.466 · draft §front

```
S_m=\underline B_m^{\mathrm{func}}
```

> If fluxes or barriers can jump, the stock can cross the barrier *without* satisfying equality. 2. **Lower barriers need not be exhaustion thresholds.** The diagnostic distinguishes physical exhaustion ($S_m=0$), functional failure ($S_m=\underline B_m^{\mathrm{func}}$), a resilience or regime-shift threshold, an economically recoverable reserve, and a minimum service-supporting stock.

### D0380 · ratio 0.442 · draft §4.2

```
\mathbb 1^{\top}S^{\top}=0
```

> Every column is a two-compartment transfer under the unit-sum routing constraints, so $\mathbb 1^{\top}S^{\top}=0$ column by column — Theorem 8's conservation, at sight.

### D0669 · ratio 0.416 · draft §9

```
G_0/A_{\mathrm{act},*}=10^3
```

> The "tens of thousands of years" heuristic uses the working flux (at $G_0/A_{\mathrm{act},*}=10^3$), and both scales sit far above the institutional delays of the companion family.

### D0597 · ratio 0.34 · draft §front

```
\mathcal B_{\min}=\mathcal B_{\mathrm{lim}}
```

> *so $\mathbb E[\mathcal T_{\mathrm{fish}}]=\log(\mathcal B_0/\mathcal B_{\min})/(h+\varsigma^2/2)$; as $\varsigma\to0^+$ this converges to the deterministic pure-decay horizon when $h=F$ and $\mathcal B_{\min}=\mathcal B_{\mathrm{lim}}$.*

### D0594 · ratio 0.298 · draft §front

```
\mathcal A^{\sharp}<\mathcal A^{\mathrm{win}}_{\min}
```

> The inverse-Gaussian family has a degenerate boundary limit concentrated at zero, and $\mathrm{IG}(0,0)$ is not an ordinary inverse-Gaussian distribution. **Zero cells report zero relative to the selected observational barrier** — not zero physical uncertainty, and no confirmation of collapse. 2. **Independent physical thresholds.** If an independent physical threshold $\mathcal A^{\sharp}<\mathca

### D0195 · ratio 0.291 · draft §2.1

```
S^{\top}
```

> Read this line in words: *what changes in each box equals the wiring applied to the flows, plus what crosses the boundary, plus a stated disturbance.* Here $S^{\top}$ is the typed stoichiometric (incidence) operator; $y$ collects declared boundary states — environmental or companion variables that live outside the ledger; $\theta$ is the declared constitutive parameter vector; $B u_\partial$ colle

### D0195 · ratio 0.291 · draft §2.1

```
B u_\partial
```

> Read this line in words: *what changes in each box equals the wiring applied to the flows, plus what crosses the boundary, plus a stated disturbance.* Here $S^{\top}$ is the typed stoichiometric (incidence) operator; $y$ collects declared boundary states — environmental or companion variables that live outside the ledger; $\theta$ is the declared constitutive parameter vector; $B u_\partial$ colle


## What this means for the build

The reuse ruling was written as though a reused sentence carried the document's maths with it. It carries the
*draft's* maths, and the draft re-typeset the symbols: `44` of the spans differ from
the deposited article in spelling alone. Two ways out, and the choice is the author's, not the pipeline's:

1. **Normalize the reused sentences' inline maths to the deposited article's spelling** - mechanical, reversible,
   and it changes no word; it is applied only where the two forms differ by wrapper or script position, never where
   a number sits differently.
2. **Send those sentences to the regenerate pile** - the prose comes from the deposit's own wording, so the
   notation arrives correct and the sentence loses whatever the draft's phrasing was chosen for.

Option 1 is what the draft's register argues for; option 2 is what the deposit-as-truth-source argues for. Neither
is applied by this file.


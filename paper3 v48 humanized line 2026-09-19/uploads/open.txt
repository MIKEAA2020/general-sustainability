## 1. LSIT around line 387

There is **no literal “LSIT”** in the attached text. What is there, in the astra section, is:

> `LS_T = 0`  
> `\dot x = S_T v + b`  
> implies `L \dot x = L b`.

So the acronym is almost certainly meant to be **`LS_T`**, not “LSIT.”

Pin it as:

> **`LS_T` = the left null space of the stoichiometric/incidence matrix `S_T`.**  
> Formally: `LS(S_T) = { L : L S_T = 0 }`.

Those `L` vectors are the conservation vectors. If the AI wants a clean acronym, use **`LNS(S_T)`** — “left null space of `S_T`.” Do **not** leave it as “LSIT,” because that string does not appear in the file.

So that open item is closed: tell the AI to replace “LSIT” with “left null space of `S_T`” or `LNS(S_T)`.

---

## 2. Numeric anchor

The file gives **one concrete numeric example**, but only for the delay/deadline calculation:

> `m_0 = 100`  
> `d_0 = 10 units/year`  
> `τ = 2 years`  
> `ρ = 1 unit/year²`  
> `L_max = 10 − 2 − 5 = 3 years`

That is a numeric anchor for the **latest safe intervention time** example.

It is **not** a numeric anchor for the **supportable-output envelope**. The file does not give a number for that. So for the envelope, you still need to supply:

- value
- unit
- what it applies to
- basis/source
- provisional or firm

If the envelope is meant to use the same critical stock as the illustration, you can say:

> “Use the file’s illustration as the anchor: `m_0 = 100`, `d_0 = 10 units/year`, `τ = 2 years`, `ρ = 1 unit/year²`. Treat it as illustrative, not empirical.”

Otherwise, you must give a separate anchor.

---

## 3. Mechanism-design umbrella (with D4 wording)

The file already contains a ready-made wording for this. It is in the opus section, “Upgrade 9. Accounting as mechanism design: manipulation resistance.”

Use this as the umbrella:

> Reframe the failure modes as an **attack surface**: unit substitution, boundary redrawing, reclassification, aggregate offsetting, timescale swapping. Then state what typing actually buys:  
> **Theorem (invariance).** Under typing, interface matching, and declared MDV, the set of reports satisfying all predicates is invariant to boundary redrawing and to reclassification at declared elasticity.

That is the mechanism-design framing. It can be combined with the D4 wording once you provide D4. The file does not contain D4, so that part still needs you.

---

## 4. Identifiability-status field + MDV

Both are already defined in the file, though in different sections.

**Identifiability-status field** — from the sol section:

> `Cert(𝓛, Γ) = (Typed, Balanced, Conservative, Positive, Thermo, Safe, Adequate, Closed) ∈ {⊤, ⊥, ?}^8`  
> The value `?` is essential: “not established” is not equivalent to “false.”

Also from the astra section’s certificate table:

> **Unestablished claims** — Explicit unknowns and excluded interpretations.

So the identifiability-status field should be something like:

> `identifiability_status: established | not established | refuted`

**MDV** — from the opus section, “Upgrade 8”:

> **Minimum detectable violation (MDV).** Every certificate must state the smallest violation it could have detected — the analytical-chemistry detection limit, imported. A barrier certificate without an MDV is uninterpretable.

So one supplementary batch can add both fields to every certificate:

- `identifiability_status`
- `minimum_detectable_violation`

That closes that open item.

---

## What I cannot answer from this file

These are not in the attached text, so I can’t close them honestly:

- **Prop 29’s superlevel-set proof** — the proposition itself is not in the file.
- **Carbon convention in Remark 33** — Remark 33 is not in the file.
- **Companion “eq. (1) and Section 2.4” pointer** — not in the file.
- **`revision/v5/code/` archive upload** — that’s an action, not a question.
- **Def 22’s deficit functional** — not in the file.
- **D4 wording** — not in the file.
- **Non-displacement gate** — needs Def 22, which isn’t here.

If you paste those specific snippets — Def 22, D4, Prop 29, Remark 33, and the companion pointer — I can answer those too.
